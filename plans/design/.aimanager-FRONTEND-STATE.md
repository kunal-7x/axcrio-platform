# AI Manager FRONTEND PAGE — build state

TASK: build app/ai-manager/page.tsx in famit-panel (premium, reuse Signal components).
Edit ONLY this page's own files under app/ai-manager/. NO globals.css/navigation/other pages. NO deploy. NO git.

## CONTRACT (backend, router DEFINED-NOT-MOUNTED — every call 404s today => graceful dormancy is PRIMARY state)
Base = NEXT_PUBLIC_API_BASE || "/api"; auth header `X-Auth: <localStorage famit_token>` (mirror lib/api.ts).
- GET  /ai-manager/status   -> {module,enabled,sip,agent_name,llm_provider,otp_provider,cross_plane,max_pin_attempts,lock_ttl_s, intent_llm{}, otp{}, firewall{}}
- GET  /ai-manager/numbers  -> {numbers:[{number_id,tenant_id,phone,label,role,verify_mode,grants[],verified,status,registered_at,updated_at}]}
- POST /ai-manager/numbers  body{phone,label,role,verify_mode,grants[]} (manager+)
- POST /ai-manager/numbers/{id}/verify  (manager+)
- POST /ai-manager/numbers/{id}/grants  body{grants[]} (admin + step-up)
- POST /ai-manager/numbers/{id}/revoke  (admin + step-up)
- GET  /ai-manager/sessions -> {sessions:[{session_id,caller_id,started_at,ended_at,authed,auth_method,turns[],actions[],outcome,n_actions}]}
KNOWN_GRANTS = campaigns,leads,calls,whatsapp,ads,ads:read,analytics,contacts,billing
ROLES = manager,admin,operator ; VERIFY_MODES = voice_pin,otp ; STATUSES = active,locked,revoked

## DESIGN (verified exist): components Layout,PageHeader,Card,KpiCard,Badge,Tabs,Icon,Button.
globals.css utils (frozen, verified present): kpi*, state-block/glyph/title/sub, meter/meter-fill, pill-*,
data-table, skeleton, rise-in, lift, toast/toast-success/error, input-base, page-head*, signal-glyph, shadow-widget.
ICONS (verified): mobile,lock,chat,send,bell,check-circle,check,clock,info,dashboard,chart,list,cube,plus,
trash,wallet,block,usd-circle,arrow-up-right,dots,reply,help,close.
Auth gate: useMe/canWrite from @/lib/auth (mirror app/whatsapp/page.tsx). No PIN/step-up flow (server-gated).

## TABS: 1 Command Center (status hero + dormancy/coming-soon), 2 Registered Numbers (table+register form),
3 Voice Sessions (PIN-masked list). Treat any non-200 (404 today) as not_configured -> premium coming-soon.

## STATUS: DONE. Verified: `npx tsc --noEmit` EXIT 0 (clean); `next lint` on both files EXIT 0.
## FILES: app/ai-manager/_lib.ts, app/ai-manager/page.tsx
## NOTES: 3 in-page tabs (Command Center / Registered Numbers / Voice Sessions). Dormant-first (404=>premium
## coming-soon). Writes gated by canWrite; revoke gated by isAdmin (server also firewall step-up gated).
## NO edits to globals.css/navigation.tsx/other pages/lib/api.ts. Nav link + build+deploy = the ship step.
