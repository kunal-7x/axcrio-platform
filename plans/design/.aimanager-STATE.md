# aimanager design — STATE

TASK: write execution-ready spec to design/automation-aimanager.md, return summary + cred list.

DECISIONS (locked):
1. Tool-calling = IN-PROCESS adapter registry (not HTTP loopback). Each tool maps to a
   pure function under automation/aimanager/tools/ that the orchestrator imports. caller.py
   wiring is DEFERRED (written as a diff, not applied — matches "final wiring deferred").
   Rationale: decoupled from spine, trivially stubbable, offline-testable, no socket.
2. Offline acceptance = deterministic StubPlanner (no LLM key, no network). Exercises
   loop -> approval gate -> execute against StubTools -> spend guardrail reject -> audit rows.
3. LLM driver = provider-agnostic (like whatsapp.py), DORMANT-until-creds. Default provider
   documented as Claude (claude-opus-4-8, adaptive thinking, manual agentic loop w/ approval
   gates, output_config.format for structured plans) OR Groq (already integrated, cheap).
   No budget_tokens/temperature on Opus 4.8. Returns {status:"not_configured"} when no key.
4. State = own JSONL store under automation/aimanager/ (mirrors audit.py). Reuse audit.record()
   with action names aimanager.plan/approve/reject/execute. NO edits to db/models.py spine.
   Spend caps tie to billing/ledger concepts from credit-ledger-firewall.md.

CONSTRAINT: ad budget = REAL irreversible money to 3rd parties. Highest-risk surface.
   Hard caps + mandatory human approval above threshold + kill-switch + idempotent dry-run.

CRED SETS (verified via WebSearch 2026-06-09):
- Google Ads: developer_token(22ch) + OAuth client_id + client_secret + refresh_token + customer_id(10digit). MFA req for NEW tokens after 2026-04-21 (existing keep working).
- Meta Marketing: app_id + app_secret + system_user_token(non-expiring) + ad_account_id. Perms: ads_management, ads_read, business_management.
- LinkedIn (to verify/cite): client_id + client_secret + access_token + ad_account_urn.

FRAMEWORK: thin native tool-use loop (hand-rolled, matches lean codebase) > CrewAI/LangGraph.
   Pydantic-AI optional as structured-plan validation layer. Cite firecrawl/jetbrains/pydantic.

STATUS: DONE. Doc written + 2 advisor fixes applied (loopback-fallback transport in §3.1 after
verifying campaign.py has no top-level fns + store.py needs init(); atomicity overclaim fixed in
§7.1 with single-writer lock requirement). Returning summary+creds.
