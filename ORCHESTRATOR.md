# ORCHESTRATOR — single source of truth (READ FIRST, always current)

> After ANY compaction / new session: read THIS + `AGENT_LEARNINGS.md` first. This is the bird's-eye ledger — every wave logs its PLAN + OUTPUT + STATUS here, every agent appends learnings/mistakes to `AGENT_LEARNINGS.md`. Never lose agent work. Per-wave detail lives in `memory/build_log/` + `MASTER_BUILD_STATE.md`; resume docs in `CONTINUE_HERE.md`. **The big sequenced backlog (FIXES → Multilingual-voice fix → AIM-Access+PIN → Video Studio → Vault → Workflow/Funnel; Credits/Plans ON-HOLD until founder asks) = `NEXT-BIG-BUILDS.md`** — execute it top-down, one wave at a time, non-stop.

## ✅ 2026-06-14 — RAG W2 VERIFY = DONE (_global telecaller corpus live + all 4 gates PASS)
**PLAN:** Verify the already-deployed W2 corpus on the box: (1) chunk count, (2) tenant read-share, (3) RLS write-lock, (4) idempotency. Update docs. Full earner gate.
**OUTPUT/STATUS = ALL 4 PASS. EARNER GATE PASS.**
- [1] CHUNK COUNT: 120 kb_chunks / 41 kb_sources with tenant_id='_global' — PASS (matches corpus).
- [2] READ-SHARE: tenant-GUC (is_admin=0) FTS search surfaces _global "Bahut mahanga" objection chunk — PASS.
- [3] WRITE-LOCK: tenant-GUC INSERT INTO kb_sources with tenant_id='_global' blocked (ProgrammingError / RLS WITH CHECK) — PASS.
- [4] IDEMPOTENCY: all 41/41 corpus checksums already in DB; re-seed = 0 new rows — PASS.
- AUTH GATE: no-auth→401, legacy-pw→403, GET→405, admin-hmac→200 — all PASS.
- **Earner gate:** agent.py md5 `9150fabe` UNCHANGED · famit-agent PID `2669239` (aim-voice-agent) active · caller /health 200 `{"status":"ok","checks":{"db":{"ok":true},"redis":{"ok":true},"livekit":{"ok":true}}}` · 0 5xx · NO ring.
- RAG-MASTER-PLAN.md + NEXT-BIG-BUILDS.md + WORKFLOW_LEDGER.md updated. Wave log appended.
- **OPEN:** W3 next (grounding cache kb/grounding_cache.py keyed (tenant,campaign,stage,channel,kb_version)) + KB management UI (Sources/Test-answers/Insights panel, Core_2 + frontend-design skill).

## ✅ 2026-06-14 — RAG W1 DEPLOY = DONE (retrieval hardening live — dense-gate + _global UNION + FORCE-RLS)
**PLAN:** Deploy W1 kb/core.py + kb/schema.sql + kb/__init__.py + aim_voice_agent.py (dense=False default + _global UNION predicate + kb_query_log FORCE-RLS) to box; apply DDL via psql; restart aim-voice-agent only; full earner gate.
**OUTPUT/STATUS = ALL PASS. EARNER GATE PASS.**
- Backups: `kb/*.W1bak.20260614-171333`. SCP: core.py(7010a77e) + __init__.py(aa2b7c13) + schema.sql(42c14591) + aim_voice_agent.py(5c3936fa from LIVEBOX) — all box md5s match local W1.
- **8/8 offline probes PASS** on box: T1 dense=False default, T1b retrieve embed guarded, T2 _global predicate, T3 no % wildcard, T4 aim_voice_agent call-site, T5 FORCE-RLS in DDL, T6 WITH CHECK excludes _global, T7 log_query exported.
- **DDL applied (psql -f kb/schema.sql):** kb_sources/kb_documents/kb_chunks — policies updated (DROP+CREATE); kb_query_log NEW table + ENABLE+FORCE-RLS + policy + 2 indexes. No errors.
- **DB FORCE-RLS verified:** all 4 kb_ tables `relforcerowsecurity=t` in `pg_class`.
- aim-voice-agent restarted (new PID `2669239`); active/running.
- **Earner gate:** agent.py `9150fabe` UNCHANGED · famit-agent PID `1477083` NOT restarted · caller /health 200 · NO ring.
- **Flag truth (NOT flipped):** CTX_CACHE (W2) = DORMANT (absent from .env) · INBOUND_PROV_LOCK (Wave A) = DORMANT (absent from .env).
- **OPEN:** W2 next (POST /kb/seed-telecaller + kb/seed_global.py corpus — seeding the _global shared knowledge base). design/RAG-MASTER-PLAN.md W1 row updated to DONE.

## ✅ 2026-06-14 — RAG W0 DEPLOY = DONE (kill-switch live on box, grounding still ON)
**PLAN:** Deploy W0-built `RAG_INJECT_ENABLED` kill-switch to box; set flag=1 (preserve today's RAG grounding); restart aim-voice-agent only; full earner gate.
**OUTPUT/STATUS = ALL PASS. EARNER GATE PASS.**
- SCP: local `8335d4ba` → `/opt/famit-agent/aim_voice_agent.py` (backup `*.W0deploy.bak.20260614-*`). py_compile OK on box venv.
- `.env` `RAG_INJECT_ENABLED=1` added. Kill-switch: set 0 + restart aim-voice-agent → instant RAG disable.
- aim-voice-agent restarted (new PID `2660527`); `starting worker` + all plugins registered + Postgres OK in journal.
- **Earner gate:** agent.py `9150fabe` UNCHANGED · famit-agent PID `1477083` NOT restarted · caller /health 200 · 0 real 5xx · NO ring.
- **Proofs on box:** golden 5/5 byte-identical PASS; flag-gate A (kill→no retrieval) + B (flag-off==no-RAG render, 18938 chars) + C (grounding injects EXACTLY) = 3/3 PASS. `RAG_INJECT_ENABLED=True` confirmed from live module.
- **RECOVERY-STATE.md + RAG-MASTER-PLAN.md updated.** Wave log + WORKFLOW_LEDGER updated.
- **OPEN:** W1 next (core.py dense=gate + _global UNION + RLS clause). CTX_CACHE (W2 feature) + INBOUND_PROV_LOCK default-off are both absent from .env but code defaults are safe (no restart needed).

## ✅ 2026-06-14 — REPO-RECOVERY-AUDIT (P1 drift-map + P2 reconcile + P3 RECOVERY-STATE) = DONE
**PLAN:** Full local↔box consistency check; recover all drift; write authoritative source map + live flag state + open follow-ups; bank rules; no box write, no flag flip, no earner restart.
**OUTPUT/STATUS = CONSISTENT. EARNER GATE PASS throughout.**
- **Drift found (P1):** 3 critical stale local files (aim_voice_agent `a9eefa8c` vs box `018c20a7`; prompt.py `ec5fa971` vs `fb87ea56`; agent.py `1a154ea1` vs `9150fabe`). RAG LIVE+UNGATED (3 sites, 63 chunks, no `RAG_INJECT_ENABLED` flag anywhere). 7-branch sprawl mapped.
- **Reconciled (P2, box→local SCP, zero box write):** aim_voice_agent + LIVEBOX golden + prompt.py + prompt.LIVEBOX + agent.py on-disk + _inbound_ref DEPLOYED ref — all now `018c20a7`/`fb87ea56`/`9150fabe`. Commits `70969dd` + `f025e79` on `fe/unify-run-wavec`, gitleaks 0.
- **RECOVERY-STATE.md written (P3):** `caps/design/RECOVERY-STATE.md` — single authoritative canonical-source-per-file map + true live flag state + 4 open follow-ups (W0 RAG kill-switch, CTX_CACHE/INBOUND_PROV_LOCK .env verify, branch merge, kb/ local tracking).
- **Rules banked:** PLAYBOOK §1 rules 16+17; AGENT_LEARNINGS new entry (box is truth; pull before editing; `.LIVEBOX` golden; one auth copy per file; RAG live+ungated; W0 first).
- **EARNER GATE:** agent.py `9150fabe` / PID `1477083` / /health 200 / 0 5xx / NO ring / NO box write / NO flag flipped.
- **Open follow-ups (RECOVERY-STATE.md §3):** (1) W0 — build `RAG_INJECT_ENABLED` into live aim_voice_agent; (2) verify CTX_CACHE=1 + INBOUND_PROV_LOCK=1 in box .env on next famit-caller restart; (3) merge fe/unify-run-wavec into backend/handoff-name-clean-line; (4) pull kb/ module to local tracking.

> 🎯 **RUN-PLATFORM megaplan SYNTHESIZED (2026-06-14, READ-ONLY) → `design/RUN-PLATFORM-MASTER-PLAN.md`** — 6 designs + 6 red-teams folded into one plan: preview-fix (real cause = `Content-Type: text/plain`, force `audio/mpeg`), REAL pricing (Lean ₹4/Std ₹6/Prem ₹8/min; Vobiz UNVERIFIED, Premium below-COGS), provider-lock (invoked+billed in the LEDGER; the wallet invoice is flat-rate `_charge_call` ignoring vendor — separate deferred fix), 33-item feature-bucket table, crazy Run UI (inline), phased earner-safe roadmap. ✅ **Wave A DONE (2026-06-14)** — env billing fixes + inbound provider-lock + funnels mount (see entry below). ✅ Wave B DONE (prior). → **Wave C NEXT** (crazy Run UI + real cost meter). P0-LEAK DONE+DEPLOYED (no collision). Earner gate = `_golden/verify_golden.py` exit 0 + FRESH box md5 (the `9150fabe` constant is the box value; local=`1a154ea1` — re-baseline, never trust the literal) + PID 1477083 + NO ring.

## ✅ 2026-06-14 — UNIFY PANEL + WAVE C REGRESSION FIX = DONE + DEPLOYED + VERIFIED
**PLAN:** Run page had regressed (BUILD_ID A7YHO lacked Wave C cost-meter/banner/toggle + W4 CRM Memory tab — prior deploy was from a lone backend branch). Unify all FE waves onto `fe/unify-run-wavec` + build locally + deploy to FORTRESS.
**OUTPUT/STATUS = ALL PASS. EARNER GATE PASS.**
- Branch: `fe/unify-run-wavec` commit `fa99acb`. tsc 0 errors, next build 53/53 green, gitleaks 0.
- BUILD_ID before: `A7YHO-5a5p7ZcKqSUrKOo` → after: `TU16Mn1DcJVmxnxr2GVyL`. Backup `.next.UNIFYbak.20260614-101443`.
- VERIFY: /run=200 /crm=200 /workflows=200 /ai-manager/setup=200 /login=200 (loopback + edge). CONFIG-ONLY+Telephony+Memory-tab confirmed in .next chunks.
- EARNER GATE: agent.py md5 `9150fabe…` UNCHANGED · PID 1477083 NOT restarted · caller /health (port 8209) = 200 · NO ring.
- LESSON: PLAYBOOK §1 rule 15 + AGENT_LEARNINGS banked — never deploy from a lone FE branch; unify all waves first.

## ✅ 2026-06-14 — #8 WORKFLOW/FUNNEL EXECUTION = DONE + DEPLOYED + VERIFIED
**PLAN:** Verify + deploy the #8 wave (BE already done per prior report). Verify: (1) human-language node labels on canvas; (2) POST /funnels/{id}/run dispatches to run_job (QUEUED / no ring); (3) tenant-scoped (401 without token). Deploy FE to FORTRESS. Update state files. Earner gate throughout.
**OUTPUT/STATUS = ALL PASS. EARNER GATE PASS.**
- BE: `funnels/endpoints.py` `_mint_run_token` + `/{funnel_id}/run` + `funnels/__init__.py` auto-publish + `workflow/nodes.py` `run_token` field + `interpreter.py` thread-through — all on box `168.144.153.145` with backups `*.RUNTOKbak.20260614-083554`.
- Templates: `['starter_call_run','real_estate_site_visit','clinic_appointment','lead_to_call_nurture']` confirmed live via `/funnels/templates` 200.
- LIVE DISPATCH PROOF: `POST /funnels/fn_d57d62eb1fc0/run` → `{ok:true, run_id:wfr_3a750f6d4908, engine:in_process, status:completed, steps:4}` (10095ms in-process); loopback `/run` = HTTP 400 (empty audience, 0 leads, run_job REACHED and rejected count=0 — no leads to dial). Journal confirmed: POST /funnels/.../run 200 + POST /run 400 on same request cycle. 0 INVITE / 0 ring lines in famit-agent journal.
- SECURITY: `GET /funnels` without token → HTTP 401. `GET /funnels` with token → 200. Funnel tenant_id derived from token (RT-3). Cross-tenant requires a second tenant (DEFERRED to founder's real test).
- FE: commit `0cff177` — `_lib.ts:humanLabel()` + `HUMAN_DEFAULT_LABELS` + `STARTER_CALL_TEMPLATE`; `WfNodeView.tsx:68` `{humanLabel(d)}`; `_editor.tsx` `runStatus` + `templatePickerOpen` + `doRun` + Load-template dropdown + run-status banner. Build green (tsc 0, npm run build green). Deployed to FORTRESS BUILD_ID `A7YHO-5a5p7ZcKqSUrKOo` (loopback :3001 200, panel.famit.in edge 200). FE backup `.next.WFbak.20260614-*`.
- **EARNER GATE FINAL:** agent.py md5 `9150fabe4ff62b4b4470f9a87df346e5` UNCHANGED · famit-agent PID `1477083` ActiveEnter 2026-06-10 19:58:18 NOT restarted · caller `/health` 200 · 0 5xx · NO ring · 0 INVITE in famit-agent journal.
- **FOUNDER RECIPE:** Workflows → Load template → "Starter: call new leads" (or any industry template) → wire your campaign_id into the config → Run workflow → inline banner shows "Run dispatched / completed" + run_id. The backend queues the leads into the existing run pipeline (caller.py run_job). No call fires until real leads with phone numbers are attached.
- **RESIDUAL:** cross-tenant isolation needs a 2nd real tenant for proof; non-empty audience run needs a real campaign wired — both require founder's test. The `funnels` store is still in-memory (FUNNELS_STORE unset) — funnels don't persist across restarts; PG-backed store is a deferred item.
- **NEXT = #9 VIDEO STUDIO** or **VAULT** per NEXT-BIG-BUILDS.md queue.

## 📐 2026-06-14 — VIDEO STUDIO DESIGN SYNTHESIS DONE (READ-ONLY design wave, part of 3-PRODUCTS megaplan: RAG · Video · Vault)
**PLAN:** explore→research→design→red-team→synthesize the FINAL decision-ready Video Studio master plan, folding in all red-team fixes (cost-blowup / completeness / earner-safety / rag-latency / vault-security passes).
**OUTPUT = `design/VIDEO-STUDIO-MASTER-PLAN.md`** (red-team-hardened, ~14 sections + §13b H1-H10 hardening + 10-wave build roadmap).
**HEADLINE:** 3 of the brief's "P0 gaps" are STALE/already-done on disk — do NOT re-do them (would churn correct code): `FEATURE_MEDIA` router already mounted `caller.py:6968`; the engine seam is a 1-LINE fix at `creative/video_studio/engine.py:51` (not :46); presign already 302's at `ai_asset/endpoints.py:246` → NO new `video_presigned_cache` table.
**THE REAL GAPS (designed to 100%):** (1) live-library bridge so generated videos appear in the `ai_asset_*` PG library images already use (the missed integration linchpin), (2) **FFmpeg/Remotion composite tier as the DEFAULT** (~$0 gen-API, Sarvam-TTS, works with ZERO gen-key — the 100× cost lever + the white-space moat), (3) PG+FORCE-RLS schema (`media_type`/video cols on `ai_asset` + `video_jobs` + `video_scripts`), (4) the full video frontend (`AssetMedia <video preload="none">`, Images↔Videos toggle, TierTabs composite/AI/premium), (5) Signal-Loop `ab_group`/`ai_generated` lineage into CAPI.
**RED-TEAM FIXES FOLDED:** ONE coded 1-paid-test choke-point (`submit_gate`, replaces 3 scattered env vars); per-TENANT `VIDEO_DAILY_CAP_USD` (not global); cost-truth = pre-fan-out wallet hold incl. TTS/Whisper/worker-sec (EL is PAID-gated, the "$0.003/clip" headline relabelled "$0 gen-API + metered TTS"); orphaned-hold/lost-render reaper; output-side moderation gate; destination-spec validator; Spaces lifecycle on dead variants + R2 seam; PushNotif/WA alerts; cross-product discipline (only ONE of {RAG,Vault,Video} edits `caller.py` at a time + consolidated registry sort_order + Vault `get_secret` read-seam consumer + new tables in PG backup + integrated soak + `THREE_PRODUCTS_ROLLBACK.md`).
**BUILD ORDER:** U1 seam-fix → U2 PG schema → U3 live-library bridge → U4 mount studio+submit_gate → U5 composite tier → U6 frontend → U7 Signal-Loop → U8 reaper/moderation/lifecycle/alerts → U9 BYO-key/Vault-seam/multilingual/music → U10 soak+rollback. BE on Opus, FE on Sonnet + frontend-design. Composite + manual-upload ship TODAY with zero paid key. **EARNER-SAFE:** video is async by construction → ZERO added to the ~1.1s voice loop; agent.py NEVER touched; all flags default OFF = byte-identical resting; rollback = flags→0 (no deploy).
**STATUS:** DESIGN COMPLETE (READ-ONLY — no box/code mutation this wave). Ready to execute top-down when Video Studio reaches the front of the NEXT-BIG-BUILDS queue.

## 📐 2026-06-14 — VAULT DESIGN SYNTHESIS DONE (READ-ONLY design wave, part of 3-PRODUCTS megaplan: RAG · Video · Vault)
**PLAN:** explore→research→design→red-team (6 passes: vault-backend/vault-frontend/vault-security/earner-safety/cost-blowup/completeness)→synthesize the FINAL decision-ready Vault master plan, folding in every red-team fix.
**OUTPUT = `design/VAULT-MASTER-PLAN.md`** (FINAL, red-team-corrected; supersedes the same-day draft which was full of fiction — §0b lists all 11 corrections).
**HEADLINE:** the original draft would have SHIPPED BROKEN. Net-new pkg `droplet_work/vault/` mounted via the PROVEN `build_router(resolve_tenant,can,need_auth,_forbidden,firewall=_firewall_mod)` + `app.include_router` (NOT the fictional `build_router("vault",…)`). Rides `caller.py` — the SEPARATE FastAPI process; `agent.py` `9150fabe…` is NEVER imported (earner-safety red-team confirmed Vault is genuinely process-isolated; only shared-file risk = the caller.py mount, import-guarded + serialized).
**11 LOAD-BEARING CORRECTIONS (verified file:line):** C2 PIN = salted **sha256, NOT Argon2id** (`firewall.py:95`; Argon2id may wrap the DEK at rest, never the PIN) · C3 step-up was **300s-REPLAYABLE** — no jti store existed → BUILD `vault_used_jti` single-use table + `aud=secret_id` + 60s reveal TTL · C4 `vault_access_log` **FORCE-RLS** (draft left it RLS-exempt = cross-tenant log read) · C5 **real append-only** `REVOKE UPDATE,DELETE` + `BEFORE UPDATE/DELETE` trigger (repo-wide grep = 0 triggers exist; "immutable" was a lie) · C6 **AAD = tenant_id‖secret_id‖version MANDATORY** (ciphertext was copy-paste-swappable across rows) · C7 `api_prefixes:["/vault"]` not `/vault*` (literal `*` never matches `startswith(pr+"/")` → LOCK/suspend silently bypassed) · C8 KEK-0 absent → **503 fail-closed**, never plaintext (the `key_store.py` 0600-plaintext fallback is catastrophic if copied) · C9 reads **`is_admin=False` HARDCODED in `store.py`** + separate `admin_store.py` for the super-admin `is_admin=True` path (the GUC is a master skeleton key) · C10 step-up MINT is unthrottled → add a per-tenant rate-limit · C11 the **read-seam `vault.get_secret(tenant,key_type,scope,is_admin=False)`** consumed by voice connect-window + video worker + llm-router (else Vault is a write-only museum + Video/RAG BYO-key are fiction — the #1 cross-product gap).
**ALSO FOLDED:** §9 reveal/write **trust model** (ai_provider/platform keys: vendor gets NEITHER reveal NOR update-value NOR rotate — only super-admin; FE hides those rows) — resolves the half-gate security theater. §13 **KEK-0 backup/break-glass IN MVP** (loss = total unrecoverable, not deferrable). Dual-read (.env-fallback) migration so no `.env` removal causes an outage. Tenant/secret freeze break-glass. PushNotif/WA alerts for the non-technical founder.
**FE (Sonnet + frontend-design):** 2 routes + 1 drawer, PORT api-keys secret-row verbatim (`page.tsx:268-327`); ONLY registered glyphs (no shield/eye/copy/refresh/download — use `lock`/`clock-1`/text buttons); reveal NOT react-query-cached (useRef wiped); `EntitlementGuard featureKey="vault.secrets"` IS the FE flag.
**ROADMAP:** V0 DDL(5 tables, FORCE-RLS all, append-only trigger)+fail-closed → V1 CRUD+RLS-T3+AAD-probe → V2 reveal+jti-single-use+leak-seal → V3 read-seam+dual-read → V4-V8 FE → DR KEK-0 backup → P2. BE Opus, FE Sonnet. Earner gate per wave (md5/PID/health before+after). **SERIALIZE caller.py vs RAG/Video — only ONE edits caller.py at a time.** Flags default OFF = byte-identical resting; rollback = `VAULT_ENABLED=0` + restart famit-caller ONLY (never famit-agent).
**STATUS:** DESIGN COMPLETE (READ-ONLY — no box/code mutation this wave). Ready to execute top-down when Vault reaches the front of the NEXT-BIG-BUILDS queue (#10, after Video Studio #9).

## ✅ 2026-06-14 — WAVE A DONE (env billing + inbound provider-lock + funnels mount)
**PLAN:** (1) fix 3 billing env vars: `USD_INR=1→95.2`, `EL_RATE_PER_1K_CHARS=1.5→4.76`, add `SARVAM_TTS_RATE_V2_PER_10K=15` + `V3=30`; (2) deploy `vendors/sarvam_meter.py` v2/v3 split; (3) deploy `prompt.py` pure `resolve_providers` leaf + `aim_voice_agent.py` inbound wiring, behind `INBOUND_PROV_LOCK`; (4) flip `INBOUND_PROV_LOCK=1` for aim-voice-agent only (systemd drop-in); (5) verify funnels `build_router` mount + Fix #7 (already live).
**OUTPUT/STATUS = ALL PASS. EARNER GATE PASS.**
- `.env` billing fixes LIVE on box (backup `.env.Abak.20260614-073247`). `sarvam_meter.py` v2/v3 split (commit `9e18231`). `prompt.py` + `aim_voice_agent.py` (commit `ab6777c`, `fae3b64` learnings).
- `INBOUND_PROV_LOCK=1` in `/etc/systemd/system/aim-voice-agent.service.d/vendor-script.conf` (alongside `VENDOR_SCRIPT_INJECT=1`, `CTX_CACHE=1`). New aim-voice-agent PID `2525014` — proc env confirmed: `VENDOR_SCRIPT_INJECT=1`, `CTX_CACHE=1`, `INBOUND_PROV_LOCK=1`. Earner PID `1477083` proc env: 0 occurrences of INBOUND_PROV_LOCK (clean).
- `resolve_providers({})` unit: `{'stt':'sarvam','llm':'groq','tts':'elevenlabs','voice':''}` = PASS (EL default unchanged, flag-off earner byte-identical).
- `_golden/verify_golden.py` = **5/5 OK byte-identical / EARNER GATE PASS**.
- Funnels `build_router` mount: Fix #7 PASS (body tenant_id ignored, all 11 routes 401-without-token, `build_router` is mounted LIVE behind `FEATURE_FUNNELS=1`).
- Funnels store still IN-MEMORY (FUNNELS_STORE unset) — persistent PG-backed funnels + RLS schema = DEFERRED.
- **EARNER GATE FINAL:** agent.py md5 `9150fabe4ff62b4b4470f9a87df346e5` UNCHANGED · famit-agent PID `1477083` ActiveEnter 2026-06-10 19:58:18 NOT restarted · caller `/health` `{"status":"ok","checks":{"db":{"ok":true},"redis":{"ok":true},"livekit":{"ok":true}}}` · 0 5xx · NO ring · aim-voice-agent active, registered worker `AW_5WNMHTwbxf2A`.
- **HONEST NOTE (the 95x Groq correction):** `USD_INR` was `1`; now `95.2` → inbound Groq events rise ~95×. Applies to INBOUND on the current restart; OUTBOUND earner only at the next founder-signed ring-wave (famit-agent was NOT restarted). The lock fixes the COST-LEDGER label, NOT the wallet invoice (flat-rate `_charge_call`, ignoring `vendor` — separate GATED Wave G).
- **ROLLBACK:** revert `.env` to `.env.Abak.*`; remove `INBOUND_PROV_LOCK=1` from drop-in; restore `*.Abak.*` `*.W1DEPLOYbak.*`; `systemctl restart famit-caller aim-voice-agent`.

## 🧬 2026-06-14 — MASTER_DNA_PLAN.md WRITTEN (read-first brain, synthesized from 9 cluster digests)
Assembled `MASTER_DNA_PLAN.md` (repo root) — the comprehensive compaction-proof DNA: THE STORY (vision + strangle-&-evolve + the incidents that wrote the laws) · EVERY SUBSYSTEM A-S (what/why-it-was-born/facts/STATUS) · ✅DONE stories · 🏗️PENDING night-build runlist (P0/P1/P2, ~41 not-done items, each WHAT+WHY+files+earner-safety) · ⛔GATED-on-founder · 📏STANDING RULES + why · 📖READ-ORDER. Reconciled live: P0-LEAK marked DONE+DEPLOYED (not the stale "queued"), RUN-PLATFORM-MASTER-PLAN marked written. Pointers added to TONIGHT-AUTONOMOUS-BUILD.md (top), MEMORY.md (index), here. Counts: ✅ ~25 DONE subsystems/waves · 🏗️ ~41 PENDING items · ⛔ ~16 GATED. READ-ORDER now = MASTER_DNA_PLAN → ORCHESTRATOR → AGENT_LEARNINGS → TONIGHT → NEXT-BIG-BUILDS.

## ✅ 2026-06-14 — VOICE-BRAIN P0-LEAK = DONE + DEPLOYED + VERIFIED (cross-tenant memory + WhatsApp-thread leak CLOSED, inbound + WA, no earner restart)
**PLAN:** the phone-only memory (`memory.py:_path_for`) + WA threads (`caller.py`) lived at unprefixed `{phone}.json` shared across tenants → tenant A could read tenant B for the same phone; unknown inbound WA numbers defaulted to ADMIN_ID (poisoning the admin thread); agent name hardcoded "Riya". Standalone security hotfix, inbound-first, NO earner restart.
**OUTPUT/STATUS = SHIPPED LIVE + ALL-PASS re-verify on the deployed code.** Box `famit@168.144.153.145` `/opt/famit-agent`: `_path_for(phone, tenant_id)`→`{tenant_id}/{phone}.json`; READ prefers the tenant path, else falls back to the legacy flat file ONLY IF its stored `tenant_id` matches THIS tenant OR is empty (claim + migrate-on-read) — a legacy file owned by a DIFFERENT tenant is NEVER returned (**load-bearing leak check `memory.py:110-113`**). Same tenant-checked pattern for WA threads; unknown inbound WA → `_unrouted` (`WA_UNROUTED_TENANT`, `caller.py:245`), never ADMIN_ID. Hardcoded "Riya" replaced by `build_recap(agent_name=…)` from the campaign/tenant value (default-only fallback stays "Riya" for back-compat). Deployed `memory.py` + `caller.py` + `aim_voice_agent.py`; backups `*.LEAKbak.20260614-052257`; restarted **famit-caller + aim-voice-agent ONLY** (🟥 famit-agent NEVER restarted). **LIVE RE-VERIFY (isolated temp MEMORY_DIR, no real data):** T1 tenant A can't read B + A reads own = PASS · T2 legacy-unowned returning-lead loads + migrates into `{tenant}/{phone}.json` = PASS · T2b legacy owned by a DIFFERENT tenant → None = PASS · T4 `build_recap(agent_name="Maya")`→`"Maya: hello there …"`, no "Riya" (default→"Riya") = PASS · WA `_resolve_contact_by_phone(unknown)`→`_unrouted` = PASS. **Deploy integrity:** box `memory.py` md5 `cb70e1d786cd38b723f6b93356b04194` == local mirror; box `caller.py` `992c08ff1a1fd2cb4c998eef09546335` == local; box `aim_voice_agent.py` `5a93096d…` == local. **FULL EARNER GATE before+after PASS:** agent.py md5 `9150fabe4ff62b4b4470f9a87df346e5` UNCHANGED + famit-agent MainPID `1477083` (ActiveEnter 2026-06-10 19:58:18) NEVER restarted + caller `/health` 200 (VPC `10.122.0.4:8310`) + 0 5xx + 0 outbound dispatch/ring. Commit `4db497f` on `backend/handoff-name-clean-line`; state `design/P0-LEAK-STATE.md`. **RESIDUAL:** the OUTBOUND EARNER (`agent.py` / famit-agent PID 1477083) still runs the OLD in-proc `memory.py` + writes legacy flat paths → it fully closes the leak on ITS OWN side at ITS next deploy + ring (founder-signed wave W-OB). Returning-lead memory is NOT lost meanwhile — the tenant-checked fallback still loads the correct tenant's legacy files. **NEXT in epic = W2** (context cache + pooled httpx).

## ✅ 2026-06-14 — VOICE-BRAIN W1 = DONE + DEPLOYED + VERIFIED (dynamic vendor-script→persona + lossless store + Script Studio)
**PLAN:** founder asks A+B on the INBOUND path — (A) a vendor pastes a free-form SCRIPT and the agent ADOPTS the persona/greeting/tone; (B) the ENTIRE vendor context is stored LOSSLESSLY and reaches the live turn at ~0 added per-turn latency (loaded once at connect). Plus a Script Studio UI to author + preview + dry-run. Flag-gated `VENDOR_SCRIPT_INJECT`, earner-safe, inbound-first.
**OUTPUT/STATUS = SHIPPED LIVE + 5/5 VERIFY PASS.** Backend box `famit@168.144.153.145` `/opt/famit-agent`: deployed `caller.py` (29a2f1cc — lossless `fields["raw_script"]` verbatim store + sanitized `script_meta` + atomic JSON writes + derived-projection suppression + Script-Studio `/campaigns/{cid}/prompt-preview` & `/dry-run`), `prompt.py` (a2c5b053 — NEW `build_system_prompt_v2`: base = untouched `build_system_prompt`, splices a fenced `<vendor_script>` persona block ONLY when raw_script present + flag on; injection-guarded), `aim_voice_agent.py` (f169d6e1 — inbound render via v2). Backups `*.W1DEPLOYbak.20260614-045719`. Restarted famit-caller + aim-voice-agent ONLY. Flag `VENDOR_SCRIPT_INJECT=1` set via systemd drop-in `/etc/systemd/system/aim-voice-agent.service.d/vendor-script.conf` (INBOUND worker ONLY — proven: inbound proc env HAS it, earner proc env ABSENT). Script Studio UI deployed to FORTRESS `143.110.247.249` (commit `aae7e0a`, BUILD_ID `Ykm_1fVt267VDkPib8uVg`, 200 loopback + panel.famit.in edge). **VERIFY (no calls):** (1) 9866-char vendor script byte-equal `http==disk` (sha `97839b63`) on BOTH real read paths; (2) dry-run adopts the vendor greeting (Anjali/Skyline/Lakeview; English line→English persona); (3) injection canary NOT echoed/obeyed + forged `</vendor_script>` defanged to fullwidth, legit fence count=1; (4) legacy goldens 5/5 byte-identical (flag off AND on); (5) env-flag drives the inbound v2 splice. **FULL EARNER GATE PASS:** prompt.py golden byte-diff 5/5 IDENTICAL (flag off+on) + agent.py md5 `9150fabe4ff62b4b4470f9a87df346e5` UNCHANGED + famit-agent PID `1477083` NEVER restarted + caller /health 200 + 0 real 5xx + 0 Traceback + NO ring. Test campaign deleted; box temp cleaned. Founder recipe `FOUNDER-SCRIPT-STUDIO.md`; state `design/W1-DEPLOY-STATE.md`. **RESIDUAL:** only a real inbound call to the DID proves the LIVE mic/voice adoption; outbound earner stays flag-OFF pending founder sign-off + a real ring. **NEXT in epic = P0-LEAK** (tenant-prefix phone-only memory/WA leak), then **W2** (context cache + pooled httpx).

## 🧠 2026-06-14 — VOICE-BRAIN MEGAPLAN (CORE HEART) = COMPLETE (READ-ONLY design/research/red-team)
Run `voice-brain-megaplan`. Master doc → `design/VOICE-BRAIN-MASTER-PLAN.md` (vision + 5 subsystems A-E with chosen storage/retrieval/persona/memory + latency budget + cost envelope + blind-spot inventory + phased earner-safe roadmap + read-order). 6 design agents + 5 red-team agents synthesized; binding red-team fixes folded in (agent.py-md5 is a FALSE earner-safety signal → gate on a `prompt.py` golden-render byte-diff; `raw_script` MUST nest in `fields` or it no-ops at both agents; DROP the `[TID:]`-first-token + the prompt-cache-hit gate, no real static prefix exists today; file-mirror stays authoritative+write-first for the earner; durable post-call extraction not fire-and-forget; injection close-tag-escape + canary + trust-tier-gates-earner; 6 RLS breaks incl. WA→`ADMIN_ID` poison). **FIRST 3 WAVES queued (earner-gated, inbound-first):** (1) **P0-LEAK** standalone security hotfix — tenant-prefix the phone-only memory (`memory.py:48`, read by earner `agent.py:466`) + WA (`_wa_thread_path` `:1799`, `_wa_memory_recap` `:1869`) paths, unknown-WA→`_unrouted` not `ADMIN_ID` (`:1838`), legacy read-fallback; (2) **W1** lossless `fields["raw_script"]` verbatim store + fenced dynamic-script persona render (v2) + prompt-preview + dry-run + Script Studio UI, inbound, flag `VENDOR_SCRIPT_INJECT`; (3) **W2** `context_store.py` LRU+Redis+version-stamp cache + deploy pooled `voice_tools.py`, flag `CTX_CACHE`. Each wave: `prompt.py` golden-render byte-diff + md5 `9150fabe…` + PID `1477083` + `/health` 200 + RLS probe + no-context-loss proof (real read paths) + flag-off rollback. NO ring (DID resting).

## 🌙 AUTONOMOUS NIGHT-BUILD MODE (founder asleep 2026-06-14 — laptop ON, full grant, DON'T STOP, DON'T ASK)
DIRECTIVE: build the ENTIRE `NEXT-BIG-BUILDS.md` queue end-to-end, ONE wave at a time (throttle-paced; on a "Server temporarily limiting requests" failure, wait + resume — granular waves resume from cache). Each wave: explore→research→design→build→verify on the real flow, autonomously add the out-of-box features, earner-safe (agent.py md5 9150fabe… untouched), 🟥 NO outbound test-calls (DID is carrier-flagged + RESTING — verify the earner via md5 + famit-agent-not-restarted + /health + 0 5xx ONLY), commit + update ORCHESTRATOR + AGENT_LEARNINGS + NEXT-BIG-BUILDS. ⚙️ NEVER end a turn with ZERO waves running while queue remains — the wave-completion notification is what re-fires the loop; always launch the next wave before yielding. SKIP (flag as founder-blocked, do NOT ask): OB-PROV + earner-LLM-fallback (need agent.py sign-off), ModelScope image (needs Alibaba bind), WhatsApp delivery (needs Meta), Credits/Razorpay (on-hold), Ads (needs OAuth), outbound campaign DIALING (DID carrier-flagged → Vobiz). BUILD everything else autonomously.
NIGHT ORDER: ✅ model-switcher [wufbw0l4y] DONE (6/6 verify) · ✅ perf-diagnosis [a9ab7478] DONE (plan → design/latency-diagnosis-v2-PERF-PLAN.md) → 🚀 PERF OVERHAUL build [wx16gfkps RUNNING] (FIXES-2 folded in) — ✅ UNIT-1 backend pagination LIVE (`dfb663f`) · ✅ UNIT-2 CRM-detail speedup + recordings HEAD-gate LIVE (`40caf3c`) · ✅ UNIT-3 FE react-query cache + skeletons COMMITTED `d42d130` · ✅ UNIT-4 FE infinite-scroll + row virtualization COMMITTED `7068bd7` · ✅ **UNIT-5 FE code-split (`next/dynamic` ssr:false @xyflow editor+minimap + WebGL Aurora loader) + prefetch-on-hover NavLink + FIXES-2b transcript L/R fix — COMMITTED `d48ed46` + 🚀 SINGLE FORTRESS DEPLOY of UNITs 3/4/5 LIVE** (BUILD_ID `g2QcGqqd8YfBKyKVsKkXv`→`0NZEhMOVPoK-uCqp9CQp5`, 200 on / /login /calls /crm /run /ai-manager on loopback + panel.famit.in edge, famit-panel restarted only, backups `*.PERFUIbak.20260613-195958`; 🟥 shipped `@tanstack/react-query`+`query-core` into the box node_modules since the box lacked react-query; earner gate before+after PASS md5 `9150fabe…` unchanged / famit-agent never restarted / caller /health 200, NO ring); · ✅ **UNIT-6 EDGE — FORTRESS nginx gzip on JSON API (`/api/*` 27425B→2746B on the wire, ~90% smaller; `gzip_proxied any;` is the load-bearing directive; brotli NOT in this nginx build → gzip only) + immutable `/_next/static/` cache (`max-age=31536000, immutable`, both server blocks) + analytics 30s-poll visibility-gate — LIVE** (nginx config-only, `nginx -t`→`systemctl reload nginx`, backups `*.PERFbak.20260613-200515`; frontend `app/analytics/page.tsx` BUILD_ID `0NZEhMOVPoK-uCqp9CQp5`→`p6hSTJX9R46-NQdLf8Daw`, FORTRESS deploy backups `*.PERFbak.20260613-201751`, commit `c030ee4`; earner gate before+after PASS md5 `9150fabe…` unchanged / famit-agent never restarted / caller /health 200, NO ring). **→ 🎉 PERF WAVE COMPLETE + UNIT-7 INTEGRATED-VERIFIED 6/6 PASS (units 1-6 ALL LIVE; earner md5 `9150fabe…` unchanged, famit-agent never restarted, gzip 90%, BUILD_ID `p6hSTJX9R46-NQdLf8Daw`).** NEXT → multilingual voice → AIM-access+PIN → funnels-security + workflow execution → video studio → vault → RAG populate+wire → per-person memory inbound → #9 hardening pt2 → ai_asset go-wide → the 6 creative sub-products. (HONEST LIMIT: this runs only while the laptop is ON + the session alive; if it sleeps/closes, a fresh session reads ORCHESTRATOR + NEXT-BIG-BUILDS + AGENT_LEARNINGS and continues.)

## ✅ 2026-06-14 — VOICE-BRAIN W2 = DONE + DEPLOYED + VERIFIED (context cache + pooled httpx, inbound CTX_CACHE=1)
**PLAN:** wire `context_store.py` (LRU+Redis-version-stamp+disk-mtime invalidation, 300s TTL, keyed `(tenant_id, cid)`) into `ai_manager/voice_tools.campaign_fields` so the inbound worker loads campaign context ONCE at connect and serves sub-ms warm reads on turn-2+. Flip `CTX_CACHE=1` for the INBOUND worker only (aim-voice-agent drop-in); keep earner clean (CTX_CACHE not set). Pooled httpx client already deployed. EARNER-SAFE: flag off → `get_campaign_context` returns None → direct HTTP (byte-identical to today).
**OUTPUT/STATUS = SHIPPED + VERIFIED 5 PROBES PASS.** `ai_manager/voice_tools.py` patched: `_fetch_full_campaign` helper + `campaign_fields` wired to `context_store.get_campaign_context` with disk_path fallback (backup `.W2bak.<ts>`). `CTX_CACHE=1` added to `/etc/systemd/system/aim-voice-agent.service.d/vendor-script.conf` (alongside existing `VENDOR_SCRIPT_INJECT=1`); `daemon-reload` + aim-voice-agent restarted ONLY. **5-probe live-box VERIFY PASS (venv `/opt/capsy-agent/.venv`):** P1 COLD 103ms (1 loader call) · P2 WARM 0.164ms (0 loader calls, <5ms PASS) · P3 version-stamp bust `bump_version`→INCR→next get fired loader PASS · P4 CTX_CACHE=0→None/no-loader PASS · P5 `campaign_fields` cold=1 fetch/warm=0 fetch CACHE_HIT=True. Redis :6380 absent → graceful disk-mtime fallback only (self-corrects). aim-voice-agent registered clean: `registered worker agent_name=manager`, 0 tracebacks. **FULL EARNER GATE PASS:** agent.py md5 `9150fabe4ff62b4b4470f9a87df346e5` UNCHANGED + famit-agent PID `1477083` NEVER restarted + caller `/health` `{"status":"ok"}` 200 + all 3 services active + 0 5xx + NO ring. **NEXT = W3** (multi-channel memory PG + durable post-call extraction).

## 2026-06-14 — 🟢 MLV PASS-2 (VERIFY-driven hardening) = the founder's recipe now works in the live-shape smoke
The integrated VERIFY re-run (N=2/case, Hinglish-aware detector, + LIVE-SHAPE scenarios that include the spoken greeting as the assistant's first turn) exposed a residual pass-1 missed: the **mid-call switch (the reported bug) was fixed**, BUT a **cold-open where the caller's first words are English still drew a Hindi reply** — because (a) the reused OUTBOUND earner KNOWLEDGE PACK is big + Hindi-heavy, and (b) the AI's OWN greeting was in Hindi, priming the whole call toward Hindi. **3 additive hardenings (INBOUND only, earner untouched):** (1) mirror rule strengthened to bind the FIRST reply ("your spoken greeting does NOT set the language — the CALLER does"); (2) a **FINAL LANGUAGE LOCK** appended as the last text of the prompt (highest recency) — "reply in the language of the caller's LAST message; the Hindi knowledge pack is reference ONLY, do not let it make you reply in Hindi when the caller spoke English"; (3) **LANGUAGE-NEUTRAL greeting** (`:2664-2682`) — `"Namaste{who}, this is {agent} from {company}. … how can I help you today?"` (English question; the decisive structural fix — the AI's opener no longer pins Hindi, the caller sets the language). **FINAL live-shape smoke (real builder + real Groq llama-4-scout, N=2):** `1` Hindi→Hindi PASS · `3` mid-call Hindi→English switch→English PASS (the reported bug) · `2b` neutral-greeting→caller English→English PASS (founder's recipe) · `2c` neutral-greeting→caller Hindi→Hindi PASS (no regression) · `4` no announcing PASS. (`2` = degenerate no-greeting lone-English shape still Hindi — an ARTIFACT that never occurs live; kept as worst-case probe.) **EARNER GATE held GREEN through every restart** (agent.py md5 `9150fabe…` UNCHANGED, famit-agent ActiveEnter 2026-06-10 19:58:18 never restarted, caller /health 200, 0 REAL 5xx — the `5xx`-regex hits were `304 Not Modified` false positives). Box md5 `3152539f31f6d0073d1a3c8997a1bee9` == repo `_inbound_ref/aim_voice_agent.DEPLOYED.py`. Backups chained `.MLVbak.20260613-205414/-205545/-205925` (+ pass-1 `-020515`). Founder recipe `FOUNDER-MULTILINGUAL-CALL-TEST.md`. Smoke harness `_inbound_ref/_mlv_mirror_smoke.py` (reads keys from .env at runtime, no secrets in repo). ⛔ FINAL ACCEPTANCE still = a REAL inbound call (STT+TTS in the wild).

## 2026-06-14 — 🟢 MLV — MULTILINGUAL ADAPTIVE VOICE (inbound) = SHIPPED + 3/3 smoke PASS
Founder's bug: calls in, speaks Hindi→AI Hindi, but switches to English mid-call→AI **stuck in Hindi**; plus a "Hello/Haan" greeting stutter. **PRIME CAUSE = the language PIN** in `_build_sales_instructions` point 3 (`aim_voice_agent.py:1480-1485`): `"LANGUAGE = CASUAL HINGLISH ... default to easy Hinglish"` sat inside the "HIGHEST PRIORITY OVERRIDES anything below" block, so it beat the adaptive `head` line and anchored Hindi/Hinglish, ignoring a mid-call switch. **3 FIXES (INBOUND-only, additive, earner UNTOUCHED):** (1) replaced the pin with an **ADAPTIVE MIRROR rule** — "MIRROR THE CALLER, EVERY TURN: reply in the SAME language they just used; the MOMENT they switch mid-call, switch WITH them on your very next line; NO default language, NO house style; NEVER announce/explain/ask about the language — just speak it"; (2) **STT was already auto** — `_build_stt:359` = `SARVAM_STT_LANG` default `"unknown"` (Sarvam saarika AUTO-DETECT; NOT set in .env → nothing to unpin; journal proved English "Hello" captured under auto; pinning hi-IN garbles English) → only the docstring clarified; (3) **clean greeting** (`:2645-2655`) — single warm `"Namaste{who}, {agent} from {company} here. ... bataiye, main kaise help karoon?"` (no English→Hindi jolt). Handoff / `session.aclose()` / no-AI-disclosure / no-recording / the bridge all intact. **VERIFY: integrated mirror smoke 3/3 PASS** via the REAL deployed `_build_sales_instructions` + real Groq `GROQ_LLM_MODEL` (llama-4-scout): Hindi→Hindi, switch-to-English→clean English (the exact bug fixed), switch-back→Hindi; no language announcement. **EARNER GATE before+after = PASS** (agent.py md5 `9150fabe4ff62b4b4470f9a87df346e5` UNCHANGED, famit-agent ActiveEnter 2026-06-10 19:58:18 NEVER restarted, caller /health 200, 0 5xx; NO /run, NO ring — DID resting). aim-voice-agent restarted ONLY (re-registered clean, agent_name=manager, 0 tracebacks). Backup `aim_voice_agent.py.MLVbak.20260614-020515`; deployed-ref `_inbound_ref/aim_voice_agent.DEPLOYED.py`; build log `memory/build_log/wave-build-MLV-multilingual-adaptive-voice.md`. ⛔ FINAL ACCEPTANCE = founder's REAL inbound call (speak Hindi → switch to English mid-call). ROLLBACK = restore .MLVbak + restart aim-voice-agent.

## STANDING RULES (in global memory)
1. **Build pipeline (every task):** know issue → EXPLORE the real codebase for the true root cause → deep production-grade research (security/multi-tenant-isolation/scale/low-latency/cost) → design → execute (backend all-green → frontend control-UI → DB → end-to-end test → deploy → verify for real) → fill every missing layer. Think billion-dollar system.
2. **Every backend capability ships with a FRONTEND control UI** (full CRUD + configure + test/preview, real-time, Core_2 kit + frontend-design skill).
3. **A green report ≠ a working product** — only the founder's REAL integrated use (a real call rings / message arrives) is truth. Verify a ring via SIP `180/200`, not the agent-join log.
4. **NEVER touch the earner:** `agent.py` (md5 `9150fabe4ff62b4b4470f9a87df346e5`), trunks `ST_fmtVmNJmpzKa`/`ST_LH8ighJJtHSi`, firewall, SIP. Additive + isolated + earner-regression-gated. ONE box-mutating wave at a time.
5. **Never burn the founder's PAID credits** (free providers; 1 test max).
6. **Compaction-proof:** log plan+output here; agents read/append `AGENT_LEARNINGS.md`.

## 🔴 FOUNDER ACTION ITEMS (only he can clear)
- 🟥 **DID `+918071583488` CARRIER-SPAM-BLOCKED (2026-06-13 ~12:51 UTC)** — "UI says calling, no phone rings" is NOT our code. HARD proof from livekit-sip `signaling_timestamps`: last TRUE ring 12:35:51 UTC (`+917861019021`, RINGING=1807ms ACCEPT=12161ms); from 12:51 UTC every call across 4 numbers = `inviteToTryingMs` only, NO `inviteToRingingMs`, terminal `486/480/603/403` (carrier rejected pre-ring). Earner gate intact (agent.py md5 `9150fabe…` unchanged, famit-agent never restarted), caller.py `ad121cf4` force-fix deployed & dialing, Vobiz ₹479.34 active (not 402), no suppression hit, same trunk+DID on good & bad calls. **ACTION:** (1) stop re-running (each reject deepens the flag); rest the DID. (2) Vobiz support: DID `+918071583488` returning immediate 486/480/603 no-ring since ~12:51 UTC — check throttle/DLT-spam-flag/suspension, rotate to a clean DID if needed. (3) Re-test: once Vobiz clears it, ONE in-window run to a FREE line; confirm `inviteToRingingMs>0` in the SIP log. Detail: `AGENT_LEARNINGS.md` top entry. NOTHING edited this pass.
- **BIND ModelScope ↔ Alibaba Cloud** (image key 401s until then) → unblocks the image pipeline. Guide: `FOUNDER-MODELSCOPE-BIND.md`.
- **Fix Meta WhatsApp account** (payment method 141006 + business verification + subscribe `messages` webhook) → WhatsApp delivery. Guide: `FOUNDER-META-WHATSAPP-FIX.md`. (Backend + panel are ready; Meta accepts but won't DELIVER until this.)
- *(optional)* **Link a payment method on SambaNova** (Developer tier) — free tier = 20/day, too low to be a real LLM fallback.
- ✅ DONE: recharged Vobiz (was ₹0.19 → 402 → no calls; now funded, calls ring).

## ✅ DONE (2026-06-12/13)
- **Handoff NAME + CLEAN caller line** (2026-06-13, re-run after `wimqqngha` net-drop): wired a real `name` field end-to-end (caller.py `_handoff_get/set/add_one` + voice_tools `handoff_list`/`add_handoff` + agent `list_handoff`) — fixed a bug where the spoken name was silently stored into `role` (positional-arg mismatch); caller transfer line now NAMES the dialed person ("Ek second, main aapko <Name> se connect kar rahi hoon.", fallback "apni team") with no phone/reason/AI-disclosure; whisper trimmed ultra-brief; KEPT aclose()/same-room-bridge/disconnect-hangup. Earner before-gate PASS (callID `SCL_Nx4bnnhdj9eS`, agent.py md5 `9150fabe…` unchanged, famit-agent not restarted). Restarted famit-caller+aim-voice-agent only. Build log: HCRB FOLLOW-ON UNIT.
- **Handoff BRIDGE** [wouygzs6v]: no-bridge root cause = beta WarmTransferTask side-room → replaced with DIRECT same-room `create_sip_participant` bridge (proven ring). + Live-calls monitor. 
- **Per-tenant handoff backend** [w208y7sil] + **Handoff Team UI** [w3gb3lmrx]: hold-music while ringing, whisper, fallback ordering/hours/enable, AI-Manager command, per-tenant list CRUD, live "Dialing #N".
- **#9 hardening pt1** [wd7aeby0m]: multi-tenant RLS isolation (42 PASS / 0 cross-bleed) + `Restart=always` + hardened `/health` + Telegram watchdog.
- **WhatsApp**: root cause = Meta WABA payment-block (141006) + not-verified → ACCEPTED-not-DELIVERED. Backend now surfaces Meta's REAL error; panel shows truth (no more "not connected"). run_campaign auto-send fixed to use approved `post_call_followup`.
- **Vobiz low-balance Telegram alert** (watchdog, <₹100).
- **Emergency**: AI-Manager filler-loop ("thoda sa system slow hua hai") = Groq DAILY-quota exhaustion → added Groq→OpenRouter-free FallbackAdapter.
- **Resilience build** [wh51h2pmg, resume wf_832cdc86-295]: smart Groq key-pool (`llm_router/provider_pool.py`, 9 new keys, cooldown-aware least-used, hot-reload) + SambaNova final fallback (Meta-Llama-3.3-70B) + Sarvam rotation + **API Keys page LIVE `panel.famit.in/super-admin/api-keys`** + **run_campaign EXECUTION fixed** (was a strict typed-schema bug → now `ok=True job=… count=5`). Earner untouched.
- **"Signal Aurora" fluid generation UI** [wfgo22p1b] (replaced the cheap loader; /creative + /whatsapp).
- **ModelScope image** RESEARCHED [a0da387569cd4e26d]: key 401s (needs Alibaba bind); plan = `providers/modelscope.py` + 2 render unblockers (SSE token-query auth + presigned URL) + WhatsApp FormData→JSON + default provider → modelscope.
- **Provider/voice switcher** ARCHITECTED: `design/spec-provider-voice-switcher.md` (Phase 1 safe / Phase 2 OB-PROV gated).

## 🎯 HANDOFF + AI-CONVERSATION TARGET SPEC (founder's vision — REBUILD to this)
AI handles the ENTIRE call end-to-end (INBOUND + OUTBOUND): property → objections → booking. Minimal. Casual HINGLISH (not heavy Hindi). NO human by default. NEVER say "this call is being recorded" or "I am an AI assistant". Hand off ONLY when the lead is genuinely VERY HOT or EXPLICITLY asks for a human (AI infers over the conversation — NOT eager/instant). On handoff: ONE short line in the caller's language ("connecting you to my team") → INSTANTLY bridge the human into the SAME SIP room. ⛔ **THEN THE AI MUST FULLY EXIT** — stop listening + stop responding — so ONLY caller+human remain. **#1 BUG TODAY: after handoff the AI keeps speaking OVER the human (both talk).** Human-hangup must end the call gracefully (today it breaks/auto-disconnects). Model on the ORIGINAL outbound `agent.py` WarmTransferTask the founder loved.

## ✅ HANDOFF REBUILD — CONFIRMED WORKING BY THE FOUNDER'S REAL CALL (2026-06-13) [w5qskn8xm]
Founder tested live = "all done, tested, everything fine": AI handles end-to-end, hands off cleanly, **AI exits (no talking over the human)**, clean hangup. The long handoff saga is RESOLVED. Refinement (name-only transfer line + handoff-list name field) is in the wimqqngha wave below.

## ✅ VERIFIED LIVE — FIXES wave (asset preview + CRM transcript chat-view) [FIXUI 2026-06-13]
- **INTEGRATED RE-VERIFY 2026-06-13 (read-only, honest, over real HTTP) = 3/3 PASS.** (1) BUG-1 asset-click image: on the FOUNDER tenant `21d0a13603da` the asset-detail folded (current-version) presigned url GETs **200 image/jpeg 50813B**; admin tenant sample **200 image/jpeg 63436B**; list url signed=True. The detail envelope IS nested `{asset,versions}` as documented; FE `lib/assets.ts getAsset` flattens it + folds the version's presigned url → image renders on click. (2) BUG-2 transcript: `GET /calls/{room}/transcript` for room `famit-916375548830-ad08ff` = **200, 94 turns, direction=outbound, distinct roles {ai,customer}** (ai→LEFT, customer→RIGHT); SAME room on the founder tenant = **404** (tenant-scoped, no cross-tenant leak). Chat-view confirmed token-pure in `app/crm/[id]/page.tsx` (`ChatBubble`: customer `justify-end`+primary-01/12, AI `justify-start`+b-surface2; calm "No transcript for this call" empty state). (3) FORTRESS panel: deployed `.next/BUILD_ID` = `tuuIjqN7fCf_iEL-obLon` (new), famit-panel active PID 248200, `/crm/[id]` in the build manifest, 200 on `/ /login /crm /creative/library` on BOTH loopback:3001 AND panel.famit.in edge. **EARNER GATE PASS: `/opt/famit-agent/agent.py` md5 `9150fabe4ff62b4b4470f9a87df346e5` UNCHANGED, famit-agent MainPID 1477083 / ActiveEnter 2026-06-10 19:58:18 NEVER restarted, caller `/health` 200, famit-aiasset active (binds 10.122.0.4:8310). NO outbound test call placed (DID resting per HARD RULE — verified via md5+process+health only).** Commits `d9daa86` (code) + `6940742` (docs) in history.

- **BUG-1 IMAGE PREVIEW + BUG-2 TRANSCRIPT CHAT-VIEW — ✅ SHIPPED LIVE TO FORTRESS** (FE-only; earner UNTOUCHED, different machine). Commit `d9daa86`; deployed BUILD_ID `4aXNPr1rvAfpK4ku5dNa7`→`tuuIjqN7fCf_iEL-obLon`; 200 on / /login /crm /creative/library /crm/[id] on loopback + panel.famit.in edge (new BUILD_ID served, no stale cache); restart famit-panel ONLY (PID 248200); backups `*.FIXUIbak.20260613-172953`. EARNER GATE before+after PASS (agent.py md5 `9150fabe4ff62b4b4470f9a87df346e5` UNCHANGED, famit-agent PID 1477083 never restarted, caller /health 200).
  - **BUG-1 deeper root cause + REAL FE fix:** the prior FE/backend presign work made the LIST grid fine (list `url`/`thumb_url` ARE presigned, signed=True, GET 200) but the asset-CLICK DETAIL was still broken because live `GET /assets/{id}` returns a NESTED envelope `{asset:{...}, versions:[...]}` AND the `asset` object's own `url`/`thumb_url` are RAW UNSIGNED Spaces URLs (no `X-Amz-Signature` → private bucket 403 → blank), while only the VERSION rows carry the presigned url. The FE `getAsset` returned `res.json()` raw, so `full.headline`/meta were blank (nested under `full.asset`) and the image relied solely on the version. FIX `lib/assets.ts getAsset`: FLATTEN the `{asset,versions}` envelope into a flat `Asset` + OVERRIDE the display `url`/`thumb_url` with the current (else newest) version's PRESIGNED url. Now the detail panel shows the image AND the headline/CTA/meta. (`AssetImage` native `<img>` + `AssetCard`/`AssetDetail` presigned-first were already shipped in `1005ccb`.)
  - **BUG-2:** backend `GET /calls/{call_id}/transcript` was ALREADY live on the box (caller.py:4282, box md5 e802d301 — but the tracked mirror `droplet_work/caller.py` ad121cf4 is BEHIND, lacks the route; box is source of truth). Accepts outbound call id OR room OR inbound session_id; returns `{call_id,direction,phone,name,turns:[{role:"ai"|"customer",text,ts,seq}],total}`, roles pre-normalized (ai→LEFT, customer→RIGHT), tenant-scoped (outbound BOLA 404, inbound RLS). SMOKE: room famit-916375548830-ad08ff → 94 turns, correct sides, 200. FE: `crm/client.ts` `getCallTranscript(callId)` + `CallTranscript`/`TranscriptTurn` types (dormant-safe, role normalize); `app/crm/[id]` makes a `call` timeline row clickable (passes `source_id` = call.id||room) → opens the FULL transcript as a chat in a right slide-over (`CallTranscriptModal`): CUSTOMER bubbles RIGHT (primary-01/12 tint), AI bubbles LEFT (b-surface2), ordered; calm "No transcript for this call" empty state. Token-pure (matches AIM session player convention). Build log `memory/build_log/wave-build-FIXUI-asset-transcript.md`. State `famit-panel/FIXUI_STATE.md`.

## ✅ DONE — APP PERFORMANCE OVERHAUL [wx16gfkps] — ALL 6 UNITS LIVE + INTEGRATED-VERIFIED 2026-06-14
**UNIT-7 INTEGRATED VERIFY = 6/6 PASS** (honest, read-only over real HTTP, NO `/run`, NO ring — DID resting). EARNER GATE before+after PASS: agent.py md5 `9150fabe4ff62b4b4470f9a87df346e5` UNCHANGED, famit-agent MainPID `1477083`/ActiveEnter `2026-06-10 19:58:18` NEVER restarted, all 3 services active, caller `/health`=200, 0 5xx; famit-panel PID `256634`, 0 errors since start (the 7 journal "error" lines are STALE pre-deploy PIDs). MEASURED: U1 `/calls` legacy `{total:264,next:200}` full-rows + paged slim newest-first `next:5`/`null` + `/leads` paged; U2 recordings `playable:true` for real 56-58KB OGGs / `false`+no-url for 0-byte 486-busy, CRM detail 68→41ms repeat-open no `_timeline` leak; U3 react-query staleTime30s wraps app; U4 VirtualRows on calls+leads; U5 `next/dynamic` @xyflow+Aurora + NavLink prefetch + transcript AI-LEFT/customer-RIGHT; U6 `/api/calls` gzip **27425→2746B (90%/10x)** + `_next/static` immutable+Cf-HIT + analytics visibility-gate. Box BUILD_ID `p6hSTJX9R46-NQdLf8Daw`; 200 on `/ /login /calls /crm /run /ai-manager /analytics` loopback+edge. Commits `dfb663f 7562913 40caf3c 0c28c76 d42d130 7068bd7 d48ed46 f6271e1 c030ee4`. Report `memory/build_log/wave-build-perf-overhaul-VERIFY.md`. RESIDUAL (deferred): P7 stretch — selective RSC/streaming on analytics/dashboards (larger refactor; R1-R6 covered). **NEXT NEXT-BIG-BUILDS item = #3 multilingual voice fix.**

## ✅ DONE (2026-06-14) — RUN-PAGE 4-STEP REDESIGN + VOICE-PREVIEW FIX
**Run-page redesign + voice-preview `t=` query-param fix — LIVE on panel.famit.in/run. INTEGRATED VERIFY PASS.** Phase 0 (BE): `/voice-preview` now accepts `?t=<jwt>` so `<audio src>` preview works (no headers). Validate via `_FakeReq` adapter through the SAME `_extract_cred`+`resolve_tenant` path. No-auth/no-t→401, invalid-t→401, valid-t+bogus-id→404 (auth passed). Commit `e275ebc`. Phase 1 (FE): `app/run/page.tsx` single-scroll 7-card wall → 4-step horizontal stepper + sticky right-hand Launch-summary rail. NEW `app/run/_stepper.tsx` (~80 lines, Core_2-composed, role=tablist, aria-current=step, clickable completed, lock-ahead, mobile pill+dots). Steps: (1) Campaign&Audience (4 source modes → Tabs row, progressive disclosure, live audience-count chip) (2) Voice&Providers (reuses `_voice-providers.tsx` verbatim) (3) Pacing&Handoff (4) Review&Launch (Launch + Start-anyway + Live Status polls below once jobId). All state/handlers/buildRunPayload/queue/force/getStatus PRESERVED verbatim. `.step-reveal` staggered CSS reveal in globals.css. Commits `6923b45`+`ba1f63a`. Phase 2 (Deploy): local build EXIT 0 / 16.8 kB /run / gitleaks 0; tarball md5-gated; backup `*.bak-<ts>`; BUILD_ID `p6hSTJX9R46-NQdLf8Daw`→`jcDEy4iclWbxS_zvVpvk0`; loopback / /run /login 200; panel.famit.in edge 200; 0 5xx. Phase 3 (Verify): agent.py md5 `9150fabe…` UNCHANGED, famit-agent active PID 1477083 never restarted, famit-caller :8209 /health 200, FORTRESS BUILD_ID `jcDEy4iclWbxS_zvVpvk0` confirmed live, / /run /login loopback 200, panel.famit.in edge 200, 0 real 5xx (304s = false positives), NO call placed. RESIDUAL: `lib/api.ts:801-803` comment cosmetic only (nginx proxies /api, code correct). OB-PROV = Phase-2 gated (separate wave). Build log: `memory/build_log/wave-build-run-redesign.md`. **FOUNDER RECIPE: open panel.famit.in/run → see 4 horizontal steps (no scroll) → click Step 2 → click ▶ on any voice → audio plays. Launch button always visible in right rail.**

## 🔨 RUNNING NOW
- **(perf wave CLOSED above — units 1-6 LIVE + verified). Original unit ledger kept for resume reference:**
- **PERF OVERHAUL [wx16gfkps]** — UNIT-1 (backend pagination + payload trim + N+1 fix) ✅ SHIPPED LIVE 2026-06-14. caller.py only (agent.py md5 `9150fabe…` UNCHANGED, famit-agent PID 1477083 never restarted, /health 200, 0 errors; famit-caller restarted only; backup `caller.py.PERFbak.20260613-190945`; box+mirror md5 `d96fafc9…`; commit `dfb663f`). **GET /calls** now: `limit/offset/order=desc/slim` + response `{calls,total,offset,limit,next}` (bare `/calls?limit=N` byte-compatible, pagination OPT-IN); paginated callers get SLIM 12-field rows (full record stays on `/calls/{id}`); N+1 transcript read on the `outcome` filter REMOVED (row outcome + one-time memoized fallback). **GET /leads** now: `limit/offset` + `{leads,total,offset,limit,next}` (no-limit legacy = all leads unchanged). SMOKE all PASS over real HTTP. **UNIT-2 (CRM-detail speedup + recordings playable HEAD-gate) ✅ SHIPPED LIVE 2026-06-14** (commit `40caf3c`; caller-only restart; agent.py md5 `9150fabe…` UNCHANGED, PID 1477083 never restarted, /health 200, 0 errors; backups `*.PERFbak.20260614-004718`). **CRM detail**: `rebuild_timeline` was unconditional/open (full PG call-scan + N+1 transcript disk reads) and NO live-append path exists → FRESHNESS-GATED rebuild (`CRM_REBUILD_TTL_S=90`, once/contact/TTL) + route reuses the projected timeline (no 2nd `get_timeline`); live repeat-open **129ms→33ms (~4×)**. **Recordings**: 486-busy/near-empty OGGs played a timer w/ no audio → `recorder.head_object` + `_rec_playable` HEAD-verify (size>=2048 & audio) BEFORE presign; `/contacts/{phone}/recordings` + `/calls/{id}/recording` items now carry `playable:bool`+`size_bytes`, URL only when playable, list adds `with_playable`. FE CONTRACT: render `<audio>` iff `playable===true && recording_presigned_url`. **UNIT-3 (FE react-query cache + skeletons) ✅ COMMITTED `d42d130`** + **UNIT-4 (FE infinite-scroll + @tanstack/react-virtual row virtualization on call-logs+leads) ✅ COMMITTED `7068bd7`** (both frontend-only, built green, deferred to the single deploy). **UNIT-5 (FE code-split + prefetch + transcript L/R fix) ✅ COMMITTED `d48ed46` + 🚀 SINGLE FORTRESS DEPLOY of UNITs 3/4/5 LIVE 2026-06-14:** `next/dynamic(ssr:false)` the @xyflow `WorkflowEditor`+`WorkflowMiniMap` (skeleton fallback) + the WebGL Aurora `GenerationLoader` (3 consumers) so they leave the global bundle; `NavLink` prefetch-on-hover/focus (`router.prefetch`, once-per-mount) for instant clicks; FIXES-2b — call-logs transcript bubbles were keyed on `role==="agent"` but backend normalises to `ai`/`customer` → all bubbles "anonymous" → now `customer|user|caller|lead`→RIGHT (lead-named) / `ai|assistant|agent`→LEFT, mirroring CRM ChatBubble, popup UI kept. (recharts/@tiptap deliberately NOT wrapped — named-import + already route-split / zero live consumers.) tsc+build EXIT 0 (BUILD_ID `0NZEhMOVPoK-uCqp9CQp5`), gitleaks 0. Deploy: build LOCALLY → tar `.next`+`app`+`lib`+`components`+pkg + 🟥 `node_modules/@tanstack/react-query`+`query-core` (box LACKED react-query — UNIT-3's new dep) → scp → md5-gate `53d9a02f…` → backup `*.PERFUIbak.20260613-195958` → atomic swap → chown → restart famit-panel ONLY. 200 + new BUILD_ID on / /login /calls /crm /run /ai-manager on loopback:3001 AND panel.famit.in edge; journal 0 errors. **Earner gate before+after PASS (md5/process/health ONLY, NO ring — DID resting): agent.py md5 `9150fabe4ff62b4b4470f9a87df346e5` UNCHANGED, famit-agent MainPID 1477083/ActiveEnter 2026-06-10 19:58:18 NEVER restarted, caller /health 200.** REMAINING in this wave (optional, low-risk): edge gzip/brotli on FORTRESS nginx `/api/` (R6, FE-box root) + analytics 30s-poll visibility-gate. Contracts + lessons in `AGENT_LEARNINGS.md` + `design/PERF-UNIT2-STATE.md`.
- **Handoff name + clean line + RECORDINGS — GRANULAR re-run** [w7clsa0fz, resume `wf_1ed8af82-3c9`] — prior run [wimqqngha] died on a NETWORK drop (reconcile confirmed NO edits had landed; box byte-identical to baseline = clean). Re-split into 6 short per-unit checkpoints (Handoff name+line ✅ / RecA finalize ✅ / RecB outbound auto-egress ✅ / RecC recordings-api ✅ / Frontend ⬜ / Verify ⬜) so a network drop costs ≤1 unit and resumes from the cache. **REC-C SHIPPED 2026-06-13** (`memory/build_log/wave-build-REC-C-recordings-api.md`): added 2 tenant-scoped read routes to caller.py — `GET /calls/{call_id}/recording` (outbound, BOLA-guarded, cross-tenant→404) + `GET /contacts/{phone}/recordings` (UNIFIED inbound `ai_manager_sessions`/RLS + outbound `CALLS`/tenant-filter, newest-first). Unified ITEM `{call_id,direction,phone,name,campaign_id,started_at,duration_s,status,recording_status,has_recording,recording_presigned_url}`. Reuses `ai_manager.recorder.presign` (proven 200+206) + carries REC-A finalize-on-read self-heal into the inbound list. SMOKE: presigned URL full GET 200 audio/ogg 56916B + range 206 1024B; tenant B→A call 404, B→A phone total:0 (no leak). caller.py md5 e82ccbff→f05fdd2e; agent.py 9150fabe… UNCHANGED, famit-agent PID 1477083 never restarted; backup `caller.py.RECbak.20260613-194141`. NEXT: Frontend (CRM `app/crm/[id]` + AIM player consume these routes). **REC-A SHIPPED 2026-06-13** (`memory/build_log/wave-build-REC-A-finalize.md`): root cause = fire-and-forget `finish()`/`stop()` on room `disconnected` loses race vs worker teardown → rows stuck `recording`/0 (egress always COMPLETED, OGGs full). Fix = `recorder.finalize(egress_id)` (ListEgress, dur ns→s) + finalize-on-READ self-heal in `endpoints.GET /sessions/{id}`; 17 stuck rows healed to `uploaded`+real-dur, presigned URL serves 206 audio/ogg. agent.py untouched (md5 9150fabe…), famit-agent PID 1477083 never restarted. Backups `*.RECbak.20260613-184551`. NEXT: RecB (outbound auto-egress, caller.py `run_job` only). (1) NAME on handoff list + caller line = ONE line naming the person only (no number/reason/AI/blah); (2) recordings for EVERY call (auto-egress, caller.py only — agent.py untouched) + presigned API + CRM lead-profile "Recordings" section/player + retention. Earner-gated; deploy FORTRESS.

### ✅ AIM ACCESS + PIN — DONE 2026-06-14
Backend: `/ai-manager/numbers` CRUD mounted + tenant-scoped (admin→admin numbers; axcrio→hidden by control-layer entitlement); `POST /firewall/pin/change` live (old-PIN verify → new PIN set → 400/401/429 lockout ~4 bad attempts / 15-min TTL). firewall.py `change_pin` additive — existing `check_pin`/`set_pin`/`verify_step_up` byte-identical. Frontend: Setup "Phone numbers" section (table + Remove + "Register first number" CTA) + "Change PIN" card (4/6-digit, mismatch guard, lockout-aware error with admin-reset tip). Build: tsc 0 errors, npm run build green. FORTRESS: panel deployed (BUILD_ID `sTCWP4Jj7t9hcSLHqLxx7`). EARNER GATE: agent.py md5 `9150fabe…` UNCHANGED, famit-agent PID 1477083 NOT restarted, /health 200, 0 5xx, NO ring.
- **Provider/voice P1 build PAUSED** (resumable `wf_4d047a56-724`).

## 📋 QUEUED (build after the handoff test)
- **Recordings build** [research a23379304 DONE — plan ready]: AIM recording infra actually WORKS (presigned URL plays+seeks, PROVEN 206 range). Real "can't play" causes: (a) rows stuck `status='recording'`/`duration=0` + some near-empty greeting-only OGGs (finalize bug); (b) possible browser cross-origin block on sgp1.digitaloceanspaces.com. BIG gap: OUTBOUND calls NOT recorded at all (inbound AIM is). 5 units, ALL SAFE — **agent.py NOT needed** (outbound recording via LiveKit Auto-Egress on `caller.py:2525 run_job` CreateRoomRequest.egress): **A** fix AIM finalize (aim_voice_agent.py+endpoints), **B** record every outbound (caller.py only), **C** recordings API (`/contacts/{phone}/recordings` + `/calls/{id}/recording`, presigned, tenant-scoped, unify inbound+outbound), **D** CRM lead-profile "Recordings" section + player fix (panel `app/crm/[id]`), **E** Spaces lifecycle/retention (+optional CDN/MP3 for Safari).
- **Provider/voice P1 build PAUSED** (resumable `wf_4d047a56-724`).

### ✅ Clean-handoff REBUILD — SHIPPED [w5qskn8xm] (2026-06-13 ~16:33 IST) — INBOUND-only, additive, earner untouched.
New `aim_voice_agent.py` md5 `a7d5e0ad9d50364e04181d37bfc63ba8` (backup `.HCRBbak.20260613-162320`, prev 61f2e0e). All 8 fixes applied: (a) recording greeting append DELETED + inbound "never mention recording" rule; (b) inbound `build_system_prompt(disclose_ai=False)` via a per-call fields override (prompt.py defaults UNTOUCHED — earner keeps disclose_ai=True) + identity override "you are part of the {company} team, never identify as AI/bot" (overrides prompt.py:128/145-146); (c) strong inbound LANGUAGE+STYLE override (casual Hinglish, short, end-to-end, you-close); (d) DE-EAGER handoff — removed "Also call for a HOT ready-to-buy lead" from customer docstring + inbound_note, default = handle+book yourself; (e) **CORE: `session.aclose()` after the bridge + whisper — the soft "do not speak again" return-string is GONE** (was the AI-talks-over-human root cause); (f) `participant_disconnected` handler keyed on `human-handoff-*` → brief goodbye + `_hangup` graceful end; (g) DIRECT same-room `create_sip_participant` bridge KEPT (no WarmTransferTask reintroduced — AST-proven 0 code refs); (h) observability — STT-final + assistant-turn + handoff lifecycle (REQUESTED/Dialing #N/BRIDGED/AI-EXITED/Human-hangup) journal logging. SMOKES: (1) inbound render → no recording / no AI-disclosure (disclose_ai=False proven; outbound default still discloses) / casual Hinglish / end-to-end / de-eager; (2) integrated turn-loop via REAL chain (Groq TPD-exhausted → failed over) → property Q answered (lookup, real price) NO transfer/NO human-offer; explicit "insaan se baat karni hai" → transfer fired ONCE; (3) AST code-path → aclose present, return-string gone, participant_disconnected/human-handoff handler present. Earner gate before+after PASS (agent.py 9150fabe… unchanged, famit-agent PID 1477083 never restarted, outbound to founder ESTABLISHED+RTP=rang). Only famit-caller+aim-voice-agent restarted. ⛔ FINAL ACCEPTANCE = the founder's REAL inbound call to +918071583488 (NOT a smoke).

### ✅ Login-popup FIXED [a6788632] — `caller.py:592 need_auth()` → `Response(status_code=401)` (dropped `WWW-Authenticate: Basic`); browser popup gone, app /login direct, data still 401-gated, earner untouched. Backup `caller.py.LOGINFIXbak.20260613-095615`.

## ✅ DONE (cont. 2026-06-13)
- **"Can't run a campaign from the dashboard" — ROOT-CAUSED + FIXED** (caller.py only; earner UNTOUCHED). NOT the REC-B egress regression (red herring — proven: forced `/run` to `+917861019021` dials with egress armed, CreateRoom OK, no exception, SIP INVITE via `ST_fmtVmNJmpzKa` → 486 Busy = RANG). Vobiz ₹479.5 active, `+917861019021` not suppressed. **REAL CAUSE = TRAI window gate + panel↔backend force mismatch:** outside 09:00-21:00 IST the panel Run returns 202 (idle, by-design), and the panel "Start anyway" button sends `force=true` but the backend `force_window` only read the `now` field → "Start anyway" silently no-op'd (lead stuck `queued`). FIX `caller.py:~3964`: `force_window` now also honours `force` truthy → `bool(str(now).strip()) or str(force).strip().lower() in ("1","true","yes","on")`. PROOF post-fix: force-only `/run` at 21:15 IST → `status:calling` <7s → SIP INVITE → 486 Busy = RANG. Earner gate PASS (agent.py md5 `9150fabe…` UNCHANGED, famit-agent PID `1477083` not restarted, famit-caller only). Backup `caller.py.LPRWINbak.20260613-154448`, box md5 `f05fdd2e…`→`ad121cf4e90d31289c8c691fd754fb1d` (tracked mirror synced). FOUNDER: in-hours press Run; off-hours press Run → "Start anyway" (now actually dials). Both test rings hit 486 Busy = founder's line engaged; one Run with a free line will connect+converse.
- **REC WAVE FINAL VERIFY — 4/4 PASS** (handoff-name + recordings + retention + earner): (1) caller-line `aim_voice_agent.py:740` names person ONLY, `name` round-trips LIVE (add→GET own field), aclose/bridge/hangup intact; (2) outbound auto-egress (fresh `55181bfa77.ogg` landed) + inbound finalize-on-read (uploaded/117s) + unified API `/calls/{id}/recording` & `/contacts/{phone}/recordings` presign → GET 200 + range 206 both directions, tenant-scoped (require_object 404 + RLS), FE live (panel BUILD_ID `4aXNPr1rvAfpK4ku5dNa7`); (3) **RETENTION built this pass** — 2 Spaces lifecycle rules on `capsy-recordings` (`outbound-recordings/`+`aim-recordings/` → expire 90d, Enabled), bucket PRIVATE (unauth 403), presign-on-read only; (4) earner after-gate: outbound `+917861019021` job `cc09c4888b` → livekit-sip 486 Busy/USER_REJECTED = RANG, agent.py md5 `9150fabe…` UNCHANGED, famit-agent PID `1477083` never restarted, 0 5xx, nothing restarted this pass. Box md5s: caller `f05fdd2e…`, aim `37750a77…`, voice_tools `63c3f89b…`, recorder `0d716d19…`, endpoints `740a9aac…`. RESIDUAL = one real founder inbound call for the two-party audible bridge + spoken named line.
- **Handoff "says yes then silence" FIXED** [wtljguc9d]: root cause = announce-don't-act PROMPT bug (Hindi turn: small Groq llama-4-scout SPOKE "kya main transfer karoon?" + fired NO tool → silence). FIX = imperative same-turn instruction in 5 places (call `transfer_to_human` IMMEDIATELY in ANY language; talking≠doing). + Vobiz funded (dial rings 180+200) + FallbackAdapter covers Groq-429. PROVEN: 3/3 smokes fire on Hindi+English; force-fail chain Groq→OpenRouter→SambaNova still fires; earner untouched (agent.py md5 unchanged). Backup `aim_voice_agent.py.HORTbak.20260613-093052` (new md5 61f2e0e…).
- **Lean↔Premium slider** design folded into `design/spec-provider-voice-switcher.md`: 3 tiers (Lean ₹0.75 · Standard ₹1.3 · Premium ₹1.6 /min) + live ₹/min cost-meter + recommender/budgets/A-B/provider-health/favorites; ~90% Phase-1-safe, outbound provider-swap = OB-PROV (gated, founder-approved).

## 2026-06-14 — 🟢 PVS PHASE-1 INTEGRATED VERIFY = 6/6 PASS
Honest end-to-end verify of the shipped PVS Phase-1 (backend + frontend). NO /run, NO ring (DID resting). EARNER GATE md5/process/health only = PASS before+after: agent.py md5 `9150fabe…346e5` UNCHANGED, famit-agent MainPID `1477083` NEVER restarted, /health 200, 0 5xx. (1) /tiers lean0.75/std1.3/prem1.6 + correct triples + ob_prov_pending=true — PASS. (2) FREE preview both: EL 307→GCS clip, Sarvam 200 audio/wav 206KB, zero burn — PASS. (3) Custom-provider CRUD secure: legacy-pw→403, real-admin-JWT add→list→/providers→update→delete clean, key masked — PASS. (4) Persistence: tier/voice_id/providers/avg-min round-trip + tier_resolved snapshot + invalid→lean; voice honored on outbound via agent.py:485 (no edit) — PASS. (5) OB-PROV flagged Phase-2 in /tiers + UI note; agent.py byte-identical — PASS. (6) edge panel.famit.in / /login /run /super-admin/api-keys /crm = 200, BUILD_ID `g2QcGqqd8YfBKyKVsKkXv`. Report: `memory/build_log/wave-build-PVS-phase1-VERIFY.md`. RESIDUAL: cost-meter rate-card sum diverges ~2.5× from headline (UI uses headline for presets by design; re-tune `tts_chars_per_min` ~330-360 for Custom mix). PHASE-2 DECISION FOR FOUNDER = OB-PROV (agent.py edit so the live-call provider swap takes effect; gated on founder sign-off + ring-test when DID un-rested).

## 📋 QUEUE (one box-mutating wave at a time)
1. Handoff fix (running) → then apply the **login-popup one-liner** (`caller.py need_auth()` drops `WWW-Authenticate: Basic` so the browser popup stops; keep 401 → app `/login`). Don't double-edit caller.py while a wave runs.
2. 🖼️ **ModelScope image pipeline (Phase 1)** — AFTER founder binds the key.
3. 🎛️ **Provider/voice switcher Phase 1** (voice + free preview + custom providers + Lean↔Premium slider + cost-meter + UI). Phase 2 OB-PROV (agent.py edit for outbound provider-honor) gated on founder approval + before/after ring-test.
4. 🧠 **VOICE-BRAIN W4 — RAG (HEART)** — design **v2 RED-TEAM-FOLDED + decision-ready 2026-06-14**: `design/RAG-MASTER-PLAN.md` (+ `RAG-INGESTION-PLAN.md` + `RAG-EVAL-SPEC.md`). ⚠️ **LOAD-BEARING CORRECTION (verified LIVE on box this pass — the v1 plan + the brief were WRONG):** RAG grounding is **ALREADY LIVE, UNGATED, and SEEDED** on the inbound earner — NOT "0 rows / un-wired". Running agent = `aim_voice_agent.py` md5 **`018c20a7`** (NOT the local `.DEPLOYED.py` `3152539f`; ALL earner files diverge local↔box → edit the box-pulled `*.LIVEBOX` golden, NEVER the repo, or the deploy silently reverts the live Jun-14 changes), grounding wired at **3 FLAGLESS sites** (`:2532` prefetch, `:1699` pick_campaign, `:1745` lookup); **`grep RAG_INJECT_ENABLED`=0 hits**; `kb_chunks`=**63 rows/3 tenants** (`_global`/admin:49 + 2×7). Earner `agent.py`=`9150fabe…` UNTOUCHED (RAG rides the inbound worker). **So step 1 = RETRO-GATE the live mutation, NOT "build RAG".** Roadmap: **W0** golden-capture `018c20a7` + BUILD `RAG_INJECT_ENABLED` (it does not exist) + prove `grounding=""` byte-identical in CI + 1 real inbound call → **W1** `core.py` per-call-site **`dense=`-gate** (lookup/pick_campaign FTS-only FOREVER so an `EMBED_API_KEY` flip never adds a 40-200ms embed RTT to the reply path — red-team #2) + `_global` UNION under caller's own `is_admin=False` RLS + FORCE-RLS `kb_query_log` → **W2** seed `_global` corpus (canary→promote) → **W3** campaign-save collateral ingest (raw_script-skip) + PII-scrub + soft-delete versioning + per-tenant quota → **W4** W2-cache blob `(tenant,campaign,stage,channel,kb_version)` + meter the +350-tok prompt-tax → **W5** WA grounding → **W6** eval gate (p95-TTFT<150ms + RAGAS-faithfulness≥0.85 + RLS-bleed=0 + no-embed-on-reply) BEFORE any flag flip → **W7** KB-management UI (Sources/Test-retrieval-differentiator/Insights-knowledge-gaps; Sonnet+frontend-design, Core_2). Dense embeddings (OpenAI key, prefetch-only, NOT GPU box, NOT OpenRouter) + DPDP `/kb/erase` + knowledge-gap loop = founder-signed Ph2; outbound precompute = W-OB. Flags `RAG_INJECT_ENABLED`/`RAG_WA_ENABLED`/`KB_INCLUDE_GLOBAL` default 0 (`EMBED_API_KEY` UNSET = FTS-only permanent default). **agent.py NEVER touched; serialize the caller.py mount vs Vault/Video — only ONE of {RAG,Vault,Video} edits caller.py at a time.** Files: aim_voice_agent.py(LIVEBOX) + kb/core.py + caller.py + new kb/seed_global.py + kb/grounding_cache.py + famit-panel app/knowledge/*.

## RESUME RUN-IDs
resilience `wf_832cdc86-295` (done) · handoff-realtime-fix `wf_3dcb8fb6-bf3` (running). Others: `MASTER_BUILD_STATE.md`.
