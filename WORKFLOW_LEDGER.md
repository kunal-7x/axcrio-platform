# 🧾 WORKFLOW LEDGER — durable record of every ultracode workflow + its OUTPUT

> **Why:** workflows run in the main context; when compaction hits, the next session loses what each
> workflow produced. This file (+ `memory/wave_runs/<wave>.md` for full output) makes every run's result
> PERMANENT. **After any compaction, read this FIRST** to know every wave's outcome, then drill into the
> per-wave file for detail. Costs ~1 append per phase — the work is unchanged.

## 📏 THE CONVENTION (every workflow I author bakes this in)
- Each workflow defines a log file `memory/wave_runs/<wave-name>.md`.
- **Every phase agent APPENDS its tight conclusion there** under `## <phase> — <label>` as it finishes
  (crash-safe: a wave that dies mid-run still leaves its completed phases' outputs on disk).
- The **final phase appends ONE line to THIS ledger** (newest on top): `<date> · <wave> · [runId] · STATUS · <one-line outcome> · → <detail file>`.
- This is in addition to (not instead of) the build_log / *-STATE.md / master-plan a wave already writes.

## 📇 INDEX (newest on top)
- 2026-06-15 · comm-w1-p4-live-real-message-inbound-conversation · [LIVE — real message + poll worker + round-trip PROVEN] · ✅ DONE + LIVE (earner-safe; NO caller.py/agent.py edit; famit-caller NOT restarted) · W1-P4: real end-to-end Telegram automation proven live. (1) REAL MESSAGE LANDED: derive_founder_chat_id('admin') pulled chat_id=1862240811 from getUpdates (founder's "hi" tap registered), auto-persisted. engine.send('admin', SendEnvelope(kind=alert, purpose=service, text='Famit Telegram is LIVE — your hot-lead alerts and post-call summaries will land right here.', buttons=[Open Panel→panel.famit.in]), slug='telegram-founder') → ok=True status=sent external_id=4 (Telegram message_id=4). comm_send_log row cms_30de38a5…: status=sent. (2) INBOUND CONVERSATION POLL WORKER: getWebhookInfo confirmed webhook url=[] (box :8209 firewalled to panel 10.122.0.2 only — no public HTTPS ingress; webhook mode is a PLAYBOOK footgun that disables getUpdates). Built comm/poll_worker.py: standalone async getUpdates long-poll (timeout=25s, offset-tracked, no caller.py / no agent.py, earner-isolated); uses comm.webhook.derive_secret_token (exact same HMAC as the handler) so the FAIL-CLOSED verify path is exercised identically; feeds every update through comm.webhook.handle(); systemd unit comm-poll.service (User=famit, WorkingDirectory=/opt/famit-agent, EnvironmentFile=.env, Restart=always). Deployed + enabled + started. REAL ROUND-TRIP PROVEN: poll worker received update_id=972273094, text='done' from chat_id=1862240811 → Groq llama-3.3-70b-versatile POST 200 OK → Telegram sendMessage POST 200 OK → reply landed on founder's phone (comm_send_log cms_28a66c2d…: status=sent, kind=text). (3) POST-CALL ALERT: FEATURE_TELEGRAM_FOUNDER_ALERT=1 + FEATURE_TELEGRAM_FOLLOWUP=1 already ON in .env; _finalize_call hook already wired (W1-P3, caller.py golden ccf9715b). No flag change or restart needed. EARNER GATE AFTER: agent.py md5 9150fabe UNCHANGED · famit-agent PID 2808658 NRestarts=0 NOT restarted · caller /health 200 · 0 5xx · NO ring. gitleaks protect --staged 0. Commit: poll_worker.py force-added on fe/unify-run-wavec. → communication/_BUILD-LOG.md (## W1-P4)
- 2026-06-15 · comm-security-probes · [6 ship-blockers PROVEN] · ✅ DONE (offline-green; NO box/caller.py/agent.py touch) · Implemented + PROVED the 6 security probes the red-team gated Communication ship on (master-plan §4): **T-WEBHOOK · T-INJECT · T-LEAK · T-VAULT · T-DEEPLINK · T-GATE**. NEW `comm/tests/test_security_probes.py` (1-520) = ONE consolidated harness driving the REAL comm-package code (no re-implementation), monkeypatching ONLY the I/O seams (provider registry / DB sessions / on-disk nonce store / Groq+engine send) → fully OFFLINE, deterministic, exit 0 iff 6/6. **RESULT 6/6 PASS, 53 sub-checks, 0 fail.** **T-WEBHOOK** (14 chk): dormant→403-not-200, no-bot→403, missing/wrong/**other-tenant-secret-on-admin's-path→403** (no DB row on any reject = GUC-after-verify), correct→200 stored once, retry update_id→dedup, no-signing-secret→fail-closed, garbage→200 no-raise (webhook.py:359 handle/:113 _verify_secret/:92 derive_secret_token). **T-INJECT** (15 chk): tools OFF→no write surface, **12 injection strings** (ignore-previous/SQLi/role-override/"act as tenant_b"/"reveal token") each merely `noted` text never a command + any embedding STOP/handoff still caught by the ungameable pre-LLM gate, an injection in call_summary renders as quoted GROUNDING DATA (the real instruction stands), **the webhook scopes the store to the PATH tenant** (a body `tenant_id:"tenant_b"` is ignored → get_or_create gets `admin`) (brain.py:101 precheck/:62 tools_enabled/webhook.py:434). **T-LEAK** (5 chk): comm_sessions UNIQUE key includes provider_def_id ⇒ two tenants same phone+chat→different rows (no shared bot, S4), SELECT tenant-scoped, founder-chat read STRICT sentinel-only (alert can't resolve to a contact row), **RUNTIME proof** the live memory.py written by tenantA then read by tenantB → `None` (sessions.py:98/:296/memory.py:76/webhook.py:312). **T-VAULT** (6 chk, real provider_registry.credentials): no plaintext at rest, owner round-trip, **tenantA ciphertext pasted under tenantB→InvalidTag NO plaintext** (the catastrophic copy-paste), cross-def paste refused, distinct ciphertext per tenant, AAD=tenant‖def‖ver — **TRACKED RESIDUAL (S1, not a fail):** the interim DEK is ONE global sha256(master); AAD binding (proven) blocks cross-tenant paste but the per-tenant HKDF DEK is a separate key-version-gated migration (changing _interim_get_key in place would make the LIVE W1-P0 founder token undecryptable) — surfaced honestly, queued as its own crypto wave (credentials.py:133/:102/:85, residual :56). **T-DEEPLINK** (9 chk, real comm.deeplink + temp store): minted within 64-char Telegram alphabet, own-tenant verifies, **replay→replayed**, **forged-mac→bad_mac**, **tampered-phone→bad_mac**, **cross-tenant→tenant_mismatch**, **expired→expired**, malformed never raises, no-secret→fail-closed (deeplink.py:231/:120/:214). **T-GATE** (9 chk): opt-out enforced SERVER-side pre-LLM, `consent_basis` DERIVED from lead_source server-side (purchased→purchased_optin never-auto-fires / inbound→inbound_form / call→prior_transaction, NOT a constant), **end-to-end through the REAL webhook brain-ON a STOP** acks 200 + spends **0 Groq tokens** + writes a **revoke** consent row + action=opted_out (Email DLT/domain server hard-block = W3 lane) (brain.py:101/webhook.py:232/consent.py:41). Gates: `import agent|from agent` over comm/ = **0**, all 8 prior comm offline suites still PASS (zero regression), py_compile clean, **gitleaks staged 0** (~32.7KB; only fixture token + test secrets). Run `cd droplet_work && python -m comm.tests.test_security_probes`. → communication/_BUILD-LOG.md (## SECURITY-PROBES)
- 2026-06-15 · comm-w1-p3-founder-alert-postcall · [BE + caller.py _finalize_call hook LIVE] · ✅ DONE + LIVE (earner-safe; famit-caller restarted ONLY) · Communication Wave 1 phase 3 = the founder's two flagship Telegram features wired into the live earner's post-call path. **`comm/founder_alert.py`** (1-155): `send_hot_lead_alert(tenant_id, snap)` — resolves the founder chat_id (persisted/cached), builds a channel-neutral `SendEnvelope` (kind=alert, purpose=service, an "Open in panel" URL button → `/crm?phone=`, NO callback/firewall per W1), dispatches via `engine.send` (per-channel `asyncio.wait_for` timeout). **PII-MINIMIZED by default** (§5.7 — "Hot lead from a call — score N/100. Tap to view" + button; `COMM_FOUNDER_ALERT_FULL_PII=1` inlines name/phone/summary). Idempotent `comms:{call_id}:alert`. **`comm/post_call.py`** (1-215): `snapshot(rec,tr,camp_fields,*,tenant_id,call_id)` = the PURE-SYNC NO-live-ref dict copy (THE earner law — DUPLICATES the `_wa_draft_followup_text` field reads, does NOT refactor it); `is_hot_lead` = the SAME `>=70 & non-opt_out` def caller.py already uses for notify_handoff_team; `run(snap)` = the detached-task body firing (a) the founder alert (gated FEATURE_TELEGRAM_FOUNDER_ALERT + hot) and (b) the contact auto-summary (gated FEATURE_TELEGRAM_FOLLOWUP, only when a deliverable contact chat_id exists — W1 normally a clean `no_destination` no-op), each via `engine.send`, writing a service-implicit consent artifact BEFORE the contact send (§5.3). **`comm/consent.py`** (1-135): append-only `comm_consent_log` writer + `derive_basis(lead_source)` (§5.2 — basis derived from lead_source, never a constant). **`comm/sessions.py`** (+251-330): `set/get_founder_chat_id` — persist the founder chat_id as a STRICT sentinel `comm_sessions` row (`call_id=__founder_chat__`) surviving Telegram's getUpdates 24h-aging; STRICT sentinel-only read so an alert can NEVER mis-route to a contact's chat. **`comm/engine.py`**: `derive_founder_chat_id` reads the persisted id FIRST (no network) → getUpdates fallback → auto-persist. **caller.py hook** (CALLER_EDIT_LOCK acquired→released; box golden md5 RE-VERIFIED 73d7be4f before edit): ANCHOR at the END of `_finalize_call` after the hot-lead notify_handoff_team except → a flag-gated `COMMUNICATION (W1-P3)` block = `comm.post_call.snapshot(...)` (sync) then `asyncio.create_task(comm.post_call.run(snap))` (DETACHED, never awaited), import-guarded + its OWN try/except (a comm fault can never disrupt finalize). **+28 lines, 0 deletions** (single hunk 2795a2796,2822); box golden 73d7be4f→**ccf9715b**; md5-gated deploy, **famit-caller restarted ONLY**. Flags **FEATURE_TELEGRAM_FOUNDER_ALERT=1 + FEATURE_TELEGRAM_FOLLOWUP=1** ON (founder tenant); config snapshot confirms all 4 True; getMe ok (mr_kunal_bot). **OFFLINE** test_post_call_offline **22/22** (snapshot-no-alias: mutate live dict after snapshot → unchanged; dormant run→skip/skip; one-send-per-hot-lead; no-send-when-cold; PII-minimized vs full-PII; never-raises) + the other 4 suites still PASS; agent-import grep 0; gitleaks 0. **EARNER GATE under an INDUCED api.telegram.org black-hole** (worst case — a hot lead whose alert WILL attempt a send while Telegram is unreachable): snapshot **0.047ms** + create_task **0.015ms** (the ONLY hot-path cost the dial loop pays) · detached run bounded **0.10s** under outage (≪ the 8s timeout; failed cleanly, never hung) · agent.py md5 **9150fabe UNCHANGED** · famit-agent PID **2808658 NOT restarted** · caller /health **200** · **0 5xx** · token **redacted** in logs; black-hole removed → telegram reachable (getMe ok), **0 residual pins**; stale test chat rows purged. Commits 889807e (modules+suite) + e58c836 (persistence+wiring+deploy) on fe/unify-run-wavec. Backups caller.py.COMMW1P3bak.20260615-020542 + .env bak. ROLLBACK: feature flags→0 (instant, hook no-ops) → COMM_ENABLED=0 → restore baks + restart famit-caller. **ONE FOUNDER ACTION pending** = tap @mr_kunal_bot once (the original Start aged out of getUpdates; auto-persists after) for live real-reach → `communication/_HUMAN_TASKS.md`. NEXT = W1 FE (channel-setup + send-log view, deferred per the panel-deploy race) then W2 (the LLM brain reply-only + the `?start=` deep-link seeding contact chat_ids = activates the auto-summary's deliverable path). → communication/_BUILD-LOG.md (## W1-P3)
- 2026-06-15 · comm-w1-p2-webhook-endpoints-mount · [BE + caller.py mount LIVE] · ✅ DONE + LIVE (earner-safe; famit-caller restarted ONLY) · Communication Wave 1 phase 2 = the inbound webhook (FAIL-CLOSED) + the comm API endpoints + the caller.py mount, shipped LIVE to the box (famit@168.144.153.145), flags `COMM_ENABLED=1`+`COMM_TELEGRAM_ENABLED=1` ON for the founder tenant. **`comm/webhook.py`** (1-214): per-tenant `secret_token = HMAC-SHA256(signing, "telegram-webhook‖tenant‖provider_def_id")` (domain-separated, hex); `handle()` verifies the `X-Telegram-Bot-Api-Secret-Token` header with `compare_digest` BEFORE any DB row (the S2 gate) — dormant/no-bot/no-secret/wrong/other-tenant → 403; only AFTER verify is the RLS GUC set (inside sessions.*) + the inbound turn stored; `update_id` idempotency; W1 reply-DISABLED (brain is W2); `_signing_secret()` reads `COMM_WEBHOOK_SIGNING_SECRET`→box `var/secret`→'' (fail-closed); never raises. **`comm/sessions.py`** (1-250): comm_sessions get_or_create (UNIQUE upsert) + append_turn (rolling-20 server-side jsonb trim) + list/get, RLS-scoped best-effort. **`comm/endpoints.py`** (1-250): `build_router(resolve_tenant, can, need_auth, forbidden, *, require_super_admin=, firewall=, audit=)` prefix `/comm` — authed channels(list/test/derive-chat-id/set-webhook)/sessions(list/detail)/send(write) + the ONE unauth fail-closed webhook; COMM_ENABLED-gated → 404 dormant; dropped `from __future__ import annotations` so FastAPI resolves Request/Body. **`comm/router.py`** thin re-export; **`comm/engine.py`** +set_telegram_webhook (setWebhook w/ the derived secret_token). **caller.py mount** (CALLER_EDIT_LOCK acquired→released): anchor-string insert after the whatsapp-builder include_router → flag-gated `MODULE MOUNT — communication`, **+43 lines, 0 deletions** (box golden 44b867ea→73d7be4f), md5-gated deploy. **OFFLINE** test_webhook_offline 17/17 + test_endpoints_offline 9/9 + telegram 22/22 + engine 9/9; agent-import grep 0; empty-env import rc 0; gitleaks 0. **LIVE T-WEBHOOK 6/6** over real HTTP: no/wrong/other-tenant secret→403, correct→200 (stored=true ⇒ GUC-after-verify), retry update_id→200 dedup, unknown tenant→403; routes 401 (authed) not 404. **EARNER GATE before+after under an INDUCED api.telegram.org black-hole** (NOT a green path): agent.py md5 9150fabe UNCHANGED, famit-agent PID 2808658 NOT restarted, caller /health 200, 0 5xx, webhook fail-closed in 9ms, outbound bounded ≤0.7s, NO ring; black-hole removed (telegram reachability restored). Commits 4acaf26 (code) + 1ae0b58 (docs) on fe/unify-run-wavec. Backups caller.py.COMMW1P2bak.20260614-201851 + .env.COMMW1P2bak.*. ROLLBACK: COMM_ENABLED=0 (instant, routes→404) → restore baks + restart famit-caller. NEXT = founder_alert.py + post_call.py + the caller.py _finalize_call insertions (hot-lead alert + post-call auto-summary). → communication/_BUILD-LOG.md (## W1-P2)
- 2026-06-15 · master-index-assembly · [compaction-proof] · ✅ DONE (READ-ONLY survey + index writes; no box/caller.py/agent.py touch) · Wrote `MASTER-INDEX.md` (repo root) = THE single read-first orchestration index: §1 COMPACTION PROTOCOL (Step 0 = save harness summary verbatim to memory/session-summaries + forever-learning loop), §2 RUNNING NOW (all 3 cited runIds wf_b36d2bbe/wf_96724c63/wf_5fea07b9 resolve to DONE waves — comm-megaplan design, video-studio-activate, goldmine-quickwins — ZERO waves mid-flight, box clean, no caller.py lock held), §3 DONE+LIVE (every subsystem w/ BUILD_IDs/flags/commits + how-to-verify), §4 COMPLETE PENDING QUEUE (T0 scheduler-fix HARD GATE → parallel goldmine #44/#42/#47/T4-FE/#34/#26 → after-T0 caller.py serialize #33/#35/#37/#30/#31 → P1 Comm-W1/Vault/Comm-W2/RAG-W4/T5 → P2), §5 GATED+FOUNDER (11 rows), §6 FILE MAP + READ-ORDER + canonical source map + infra + live flags, §7 STANDING RULES (14 laws) + top learnings. Prepended "READ MASTER-INDEX.md FIRST" pointers to ORCHESTRATOR.md, TONIGHT-AUTONOMOUS-BUILD.md, memory/session-summaries/README.md, MEMORY.md. → MASTER-INDEX.md
- 2026-06-15 · ship-ui-and-telephony-t3 · [panel-deploy] · ✅ DONE · Panel deploy from fe/unify-run-wavec to FORTRESS (143.110.247.249). BUILD_ID before: xVT-E_KsjR9pQnUkYfJkC → after: u6yKGIuhALhhzdzQcywXQ. tsc fix: extracted IntegrationsBody to _body.tsx (0 errors, commit 2c299c8). Build ✓ Compiled successfully 58/58. md5 gate PASS. Atomic .next swap + chown + famit-panel restart. Edge PASS 8/8: /integrations 200, /super-admin/integrations 200, /creative/video 200, /run 200, /crm 200, /ai-manager 200, /workflows 200, /knowledge 200. Earner gate PASS: agent.py 9150fabe UNCHANGED, famit-agent active, PID 1477083 not restarted, FORTRESS has no agent.py. → memory/wave_runs/ship-ui-and-telephony-t3.md
- 2026-06-15 · comm-omnichannel-megaplan · [BRAINSTORM:bold-ideas] · DONE (READ-ONLY, opus; no box/caller.py/agent.py edit) · Founder + product-visionary brainstorm of the 99% the founder did NOT name for the unified Communication tab (Telegram+Email+SMS, mirror+exceed WhatsApp). 20 bold ideas in 4 tiers, each feasibility-tagged + earner-safe (rides the new comm_router + caller.py, NEVER agent.py) + RLS/cost-aware, riding EXISTING seams (channel registry = clone of provider_registry, the LLM brain tool loop, wallet/audit/RLS, Hatchet, DO Spaces presign, cross-channel identity anchor, Creative Studio + Testing Lab + CAPI). TIER-1 MOAT: B1 Revenue-Truth Signal Loop extended to EVERY channel (the sales line competitors can't write), B2 One-Brain-One-Memory across phone/WA/TG/email/SMS ("never forgets a lead"), B3 Channel-Arbitrage Auto-Router (cheapest-channel-that-lands = margin-as-a-feature), B4 Comms Copilot (non-technical founder runs the channel by talking to it = the anti-dashboard), B5 Conversation->Creative loop (winning objection-handle auto-drafts the next ad/brochure). TIER-2 INTELLIGENCE: B6 deal-temperature gauge + live founder feed, B7 Next-Best-Action engine (CRM->workforce that tells him what to do), B8 per-tenant objection-handling RAG ("answers like YOUR best closer"), B9 auto-CRM-notes (zero data entry), B10 smart re-engagement w/ ghost-detection + channel-switch, B11 mirror-the-human multilingual + per-tenant/per-language brand voice. TIER-3 SURFACE: B12 Unified Inbox (every channel one timeline, the daily sticky surface), B13 channel-health+reputation cockpit (auto-protect WA quality/DLT/DKIM), B14 compliance-as-trust (DLT/SPF/opt-in built-in), B15 pre-built industry comms packs (instant onboard), B16 ROI/savings dashboard (proves it replaces the telecaller = renewal engine). TIER-4 WILD: B17 voice-note brain (Sarvam STT in stack), B18 proactive event-triggered comms, B19 Telegram group lead-gen, B20 digital-twin-of-best-closer (non-portable moat). TOP-7 = B12 + B2 + B3 + B6/B7 + B1 + B4 + B16; compounding story = one brain/one memory -> cheapest channel that lands -> deal-temp + ranked action list -> one inbox he lives in -> proven conversions to ad algorithm -> run it by talking -> show the rupees saved/earned = a self-improving omnichannel revenue workforce, not a multi-channel chatbot. Honest line held (levers not guarantees; no fabricated metrics; consent/suppression/reputation-gated; Telegram ships FIRST zero-compliance, Email+SMS behind DLT/SPF). → communication/_RESEARCH-LOG.md (## PHASE: BOLD-IDEAS)
- 2026-06-15 · comm-omnichannel-orchestration · [READ-ONLY research] · DONE · Unified message bus + channel adapter pattern (Chatwoot polymorphic inbox, Twilio Conversations "channel equality" model), per-tenant Telegram bot token vault (contextvars Dispatcher, AsyncLocalStorage pattern), unified threading schema (communication_conversations + communication_conversation_channels + communication_messages FORCE-RLS), channel fallback router (Telegram→SMS→Email, failure-triggered not preference-based), LLM brain three-tier Redis memory (Tier1 working/1h + Tier2 contact/72h + Tier3 PG JSONB), post-call hook via _finalize_call() extension (parallel to _wa_ai_followup(), COMM_ENABLED gate), founder hot-lead Telegram deep-link setup flow, multi-tenant email deliverability (BYOD DKIM, DMARC p=quarantine mandatory since May 2025 Microsoft enforcement), India DLT Service-Implicit category (DND-exempt, no time window), US 10DLC Feb-2025 enforcement, cost model (Telegram $0, Resend $0→$20/mo, MSG91 ₹0.18/SMS). Architectural verdict: Channel Registry = clone of provider_registry; unified thread not silo; Telegram Wave 1 first (zero compliance gates); Groq + Redis:6380 already on box. → communication/_RESEARCH-LOG.md (## PHASE: omnichannel-orchestration)
- 2026-06-14 · telephony-foundation-t1-t2 · [T2 trunk_registry pkg] · ✅ DONE (local + offline-tested, flag OFF, earner-safe; NO caller.py edit, NO box mutation, NO service restart, NO call) · T2 of design/TELEPHONY-INDEPENDENCE-PLAN.md = `droplet_work/trunk_registry/*` (commit 46301d2 on fe/unify-run-wavec), a TWIN of provider_registry, flag `TRUNK_REGISTRY_ENABLED` default OFF (resting byte-identical; caller.py:184 TRUNK env path UNCHANGED). T3 mount DEFERRED (caller.py serialized vs the video wave, PLAYBOOK #5). 11 modules: **REUSE (import-share, NOT rewritten)** credentials.py (thin trunk-AAD wrapper over provider_registry AAD AES-256-GCM, AAD=tenant||trunk||ver via _AadAdapter; ONE crypto impl on box; absent→CredentialError never plaintext), ssrf_guard.py (re-export validate_endpoint, FAIL-CLOSED if absent), health.py (re-use the in-memory 3-fail circuit breaker is_open/record_*/run_probe keyed per tenant,trunk). **CLONE provider_registry** config (call-time env, flag OFF + concurrency/velocity/quarantine knobs), schema (SipTrunk/SipTrunkCred/SipTrunkHealth + 8 enums incl Purpose; is_campaign_eligible carried as DB-derived read-only), store (is_admin=False RLS reads/writes; B1 filter campaign_eligible_only + exclude_quarantined; soft_disable_trunk red-team-D default; set_quarantine; recent_did_ringouts B-rel signal query; count_trunk_quarantines B3; append-only write_health_row; is_campaign_eligible/is_undeletable NOT in write whitelist), admin_store (is_admin=True super-admin reads), **registry.get_trunk(tenant,purpose) = THE choke-point** enforcing RED-TEAM B1 TWICE (store filter campaign_eligible_only + per-trunk re-check on the DB-derived column → a non-140/unregistered trunk NEVER campaign-returned even via direct write; purpose=test/manual skip the gate so flag-on dials the non-140 Vobiz `_global` trunk for a single founder ring), skips circuit-open/no-DID/no-livekit, never raises. **NEW (§8)** concurrency.py (IN-PROCESS atomic check-and-reserve counter under ONE lock — red-team C-rel box is uvicorn --workers 1 NOT the fail-open Redis:6380; no A2 TOCTOU oversell, release in try/finally no A1 leak, double/None release no-op; velocity per-DID spacing+calls/hr + GSM 1-SIM=1-call A3 + per-trunk cap + box-global cap A4; injectable clock), rotation.py (DID round_robin/least_used/sticky + avoid; RED-TEAM B-rel note_call_outcome QUARANTINES on a ZERO-DURATION RING-OUT BURST = the fireable signal since caller.py wait_until_answered=False never captures the 486; RED-TEAM B3 ≥K quarantines→DISABLE trunk+LOUD alert naming the +918071583488 pool-burn pattern; RED-TEAM E manual_quarantine_did kill switch), livekit_sync.py (pure request builders outbound/inbound/dispatch-rule w/ metadata:{tenant_id} + injected-async-client create/delete NO container restart native multi-trunk; SDK lazy→dict mirror when absent, SIP pw NEVER echoed only auth_password_present; RED-TEAM D is_protected_trunk_id+delete_trunk REFUSE env LIVEKIT_SIP_TRUNK_ID/protected/empty id unless force_protected PIN-gated at T3; NEVER imports agent.py). OFFLINE TESTS 31/31 PASS (3 suites, no network/PG/LiveKit SDK; fake PG engine enforces the §2.2 RLS GUC in pure Python): registry RLS+B1+AAD-InvalidTag 9/9 (B1 campaign picks ONLY the 140-eligible trunk not lower-pri non-140; test allows non-140 _global; no-eligible-campaign REFUSES; A never sees/reads B; cross-tenant ciphertext copy→InvalidTag; quarantined/circuit-open skipped+fallback; flag-OFF→registry_disabled; routing_hint pins) · concurrency 8/8 (cap never oversold; release-in-finally no leak; double/None no-op; box-global cap; GSM 1-call; velocity spacing+hourly cap via fake clock) · rotation+livekit 14/14 (RR/least-used/sticky+avoid; connected/below-threshold no-quarantine; ring-out burst quarantines; B3 disables+alerts names 918071583488; manual kill switch; livekit shapes no-pw-echo+tenant-metadata; red-team-D delete refuses protected force-allows; create wires injected client). provider_registry reused suites still green (registry 10/10, ssrf 39/39 — no drift). py_compile clean all 12 modules+3 tests; imports empty env (no db, no LiveKit SDK) → version 0.2.0-t2 (all surfaces via import guards); flag-OFF get_trunk→registry_disabled zero I/O. EARNER GATE (read-only box) PASS: agent.py 9150fabe UNCHANGED, famit-agent PID 1477083 active NRestarts=0 NOT restarted, caller /health 200, famit-caller active, 0 5xx last-15min, NO ring. gitleaks staged 0 (force-added the curated pkg the same way provider_registry+the T1 DDL are; droplet_work/ broadly gitignored). NEXT = T3 (additive /trunk-registry/* + /trunks/byo/* guarded mount in caller.py, flag OFF; /test-call rate-limit ≤3/hr; DELETE soft-disables+refuses _global/env; POST /quarantine-did kill switch) once caller.py frees from the video wave. → memory/wave_runs/telephony-foundation-t1-t2.md (## T2)
- 2026-06-14 · telephony-foundation-t1-t2 · [T1 DDL+seed] · ✅ DONE (PG applied live, earner-safe; NO caller.py edit, NO trunk_registry pkg, NO service restart, NO call) · T1 of design/TELEPHONY-INDEPENDENCE-PLAN.md = `droplet_work/db/ddl_trunk_registry.sql` (commit f0efa6c, md5 e96da996), 3 FORCE-RLS tables cloned column-for-column from ddl_provider_registry.sql, applied additively+idempotently to box PG (famit@168.144.153.145, famit_app NOSUPERUSER/NOBYPASSRLS). **sip_trunks**: trunk spec (trunk_type CHECK sip_provider|gsm_gateway|direct_sip, sip_host/port/transport/encryption/auth_username, did_pool jsonb+caller_id, max_concurrency>=1, cost_per_minute_paise INTEGER no-floats, compliance gates is_140_series/dlt_entity_id/dlt_status/per_did_daily_cap) + **RED-TEAM B1** `is_campaign_eligible` GENERATED ALWAYS AS (is_140_series AND dlt_status='registered') STORED + CHECK = unbypassable campaign gate at the DB layer + **RED-TEAM D** is_undeletable col + sip_trunks_protect_undeletable_trg (DELETE refused even admin) + sip_trunks_lock_undeletable_trg (TRUE→FALSE flip refused) + `_global` read-share/write-lock RLS. **sip_trunk_credentials**: AAD AES-256-GCM SIP-password ciphertext + scope CHECK integration|platform, strictly per-tenant RLS (no `_global` share, REUSES provider_registry/credentials.py crypto). **sip_trunk_health_log**: per-DID did col + event/sip_code, append-only trigger (UPDATE+direct-DELETE blocked, FK cascade allowed). SEEDED the live Vobiz trunk ST_fmtVmNJmpzKa as ONE un-deletable `_global` row (id 9896cddf-…, host 2c24f731.sip.vobiz.ai TCP, DID +918071583488, auth capsy-project, livekit_trunk_id=ST_fmtVmNJmpzKa, is_enabled+is_test_verified, is_campaign_eligible=FALSE=blocked-from-campaigns-until-140/DLT) + its AAD-encrypted cred (scope=platform/masked-only, sentinel `__livekit_managed__` since the live SIP digest is held INSIDE the LiveKit trunk object — flag-on dials via livekit_trunk_id, never the raw SIP pw; decrypt roundtrip verified, no plaintext in DB) so flag-on dials the EXACT same trunk. PROOFS: FORCE-RLS=t/t on all 3 (pg_class), 3 triggers present, is_campaign_eligible STORED; behavioural A_reads_global=1 · A_sees_global_cred=0 · `_global` tenant-write→RLS-blocked · cross-tenant B_sees_A=0/B_sees_global=1 · B1 gate compliant=t/noncompliant=f · append-only UPDATE+DELETE blocked · red-team-D Vobiz DELETE refused + flag-clear refused + row intact · FK cascade cleanup left ONLY the Vobiz seed (REMAIN 1/1/0). EARNER GATE before+after PASS: agent.py 9150fabe UNCHANGED, famit-agent PID 1477083 active NOT restarted, /health 200, 0 5xx, NO ring. gitleaks 0. T3 mount DEFERRED (serialized vs the running video wave which owns caller.py). NEXT = T2 (trunk_registry package, flag OFF) once caller.py frees. → memory/wave_runs/telephony-foundation-t1-t2.md (## T1)
- 2026-06-14 · video-framework-to-studio · [W6+W7+W8 VIDEO-STUDIO-BE] · ✅ DONE+DEPLOYED (earner-safe; famit-caller + famit-aiasset restarted ONLY) · Built+deployed the WHOLE Video Studio backend per design/VIDEO-STUDIO-MASTER-PLAN §5/§6/§7/§8 (roadmap W6-W8). **U1 seam fix**: video_studio/engine.py `_real_engine()` → `media_gen.video.client` (1-line cut-over; engine_name resolves it configured, fake_engine dormant). **U2 PG schema** (db/ddl_video.sql md5 137c5ebc, APPLIED LIVE as famit_app): ai_asset_assets +8 video cols (media_type 'image' default→resting byte-identical), ai_asset_versions +4, NEW video_jobs (reaper-ready hold_id/attempts/updated_at, INTEGER PAISE) + video_scripts FORCE-RLS; cross-tenant RLS probe PASS (WITH CHECK blocks cross-tenant INSERT). **U3 live-library BRIDGE** (famit-aiasset :8310; RECONCILED local store/auth to box golden FIRST — stale w/ presign machinery): store.register_video_asset (video asset + spaces-backed MP4 version, presign-on-read) + media_type filter + POST /assets/_internal/register-video (service-token, VPC-only) + GET /assets/{id}/poster; FEATURE_VIDEO_LIBRARY OFF; LIVE PROBE PASS (register/filter/image-excludes-video/B-isolation-RLS/presign; purged). **U5 composite tier** (media_gen/video/compose.py): the always-available $0-gen-key FLOOR (FFmpeg, not api-key gated) — cost-truth chars+min+worker-sec (Sarvam $0.0023 vs EL $0.1523/clip 66× = silent-EL-bill explicit), build_render_plan (4-rung ABR+poster+spaces keys), build_ffmpeg_argv (fixed argv NO shell, Ken-Burns+ASS captions+VO mux+watermark), build_abr_argv (H.265/HLS); client.py provider='compose' branch (AUP+likeness+cost+cap+pre-fan-out wallet HOLD; W5 registry seam preserved) + poll settle/release; shared/config.is_configured OR compose floor (studio configured WITHOUT a gen-key). **U4/W8 mount + submit_gate** (caller.py additive 0-deletion vs golden ef9ae696): submit_gate.py = the ONE 1-paid-test choke (free floor not gated; first paid gen/EL forced size=1/dur≤6/no-auto-approve/require-hold; durable per-tenant marker; fail-safe) + per-tenant VIDEO_DAILY_CAP_USD (isolated); endpoints REWRITTEN token-deriving build_router (resolve_tenant/can + list_campaigns + bridge; old body-tenant hole GONE; per-id ownership); service +tenant checks + collect FIRES the library bridge per finished job + 1-test marker on approve; caller.py video_studio mount under FEATURE_VIDEO_STUDIO (default OFF) + _video_library_bridge (aiasset loopback). DEPLOY: NEW creative/ tree + compose.py + client.py(8dc28563 W5-intact) + caller.py(ef9ae696) to /opt/famit-agent (caller-venv py_compile ALL OK, md5-gate, atomic swap); famit-caller restarted ONLY (PID→2774834 NRestarts=0). LIVE: flag-OFF /creative/video=404 dormant + legacy 401 intact (resting byte-identical); flag-ON proof routes mount (401) + authed GET /campaigns=200 (REAL campaigns) + POST /batches=200 not_configured (dormant-safe); reverted flag OFF. 22 offline tests green throughout. EARNER GREEN before+after: agent.py 9150fabe UNCHANGED, famit-agent PID 1477083 NOT restarted, /health 200, aiasset 200, 0 5xx, NO ring. Backups *.VIDW8bak.20260614-231712 + *.VIDbak.20260614-225653 + .env bak. ROLLBACK: flags→0 + restore baks + restart. REMAINING (build-the-other-half, NOT this BE wave): composite render WORKER (FFmpeg exec on famit-hatchet; compose_worker.enqueue seam), FE W9 (Video Studio page + AssetMedia <video> + Images↔Videos toggle — sibling FE wave), U7 Signal-Loop/U8 reaper+moderation+lifecycle+alerts/U9 BYO-key+music. → memory/wave_runs/video-framework-to-studio.md (## W6 / ## W7+W8)
- 2026-06-14 · telephony-independence-megaplan · [SYNTHESIZE:telephony-independence-master-plan] · ✅ DONE (READ-ONLY, opus; no box/caller.py/agent.py mutation; Vobiz config left as-is) · WROTE the decision-ready master plan **`design/TELEPHONY-INDEPENDENCE-PLAN.md`** folding the DESIGN + all 4 RED-TEAMs. ARCH = a `trunk_registry` cloned column-for-column from the LIVE provider_registry (3 FORCE-RLS tables sip_trunks/sip_trunk_credentials/sip_trunk_health_log, INTEGER paise, AAD AES-256-GCM SIP-password creds; pkg droplet_work/trunk_registry/* REUSE credentials/ssrf/health + NEW livekit_sync/rotation/IN-PROCESS concurrency; strangler at caller.py:184/:2913 behind TRUNK_REGISTRY_ENABLED default OFF=byte-identical; Vobiz ST_fmtVmNJmpzKa imported as ONE UN-DELETABLE `_global` row=flag-on dials SAME trunk; agent.py 9150fabe never imported; Core_2 app/telephony UI w/ founder-placed single test call = ONLY non-campaign originate). HONEST REALITY surfaced (founder mandate): SIM-to-SIP needs a gateway-or-BYO-provider; 1-SIM=1-call; a fresh DID does NOT escape the carrier spam-flag (resets a clock — block follows BEHAVIOUR not digits); 140-series DID on a DLT-registered route is the legal non-negotiable (why +918071583488 is blocked today) — registry ENFORCES it not bypasses. FASTEST PATH = BYO-number SIP/CPaaS (software, instant, multi-concurrency) > GSM gateway (hardware, 1-call, illegal at volume); recommend Plivo 2nd trunk + 140/DLT route. 4 RED-TEAMS FOLDED (gate before flag-ON): B1 BLOCKER is_campaign_eligible GENERATED column+get_trunk(purpose='campaign') gate · B-rel BLOCKER spam-guard fired on a 486 caller.py NEVER captures (wait_until_answered=False :2916) → quarantine on zero-duration ring-out bursts OR SIP-webhook plumbing · C-rel BLOCKER concurrency IN-PROCESS not Redis (box --workers 1; :6380 redis is FAIL-OPEN; also dissolves A1 leak+A2 TOCTOU) · D HIGH DELETE refuses _global/env/AIM trunks+soft-disable+PIN-gated hard-delete · HARD-GATE rotation OFF until scheduler_loop retry bug fixed+verified (T0) + NEW velocity throttle + per-DID kill switch + /test-call rate-limit. ROADMAP T0 scheduler-fix → T1 DDL+seed → T2 pkg flag-OFF → T3 mount flag-OFF → T4 FE FORTRESS → T5 strangler flag-ON w/ real founder outbound-ring smoke before+after (each: scope/files/flag/acceptance/rollback). Pointers: NEXT-BIG-BUILDS #9c + ORCHESTRATOR + this ledger + wave log. NEXT BUILD = T0→T1. → design/TELEPHONY-INDEPENDENCE-PLAN.md + memory/wave_runs/telephony-independence-megaplan.md (## Phase: SYNTHESIZE)
- 2026-06-14 · telephony-independence-megaplan · [RED-TEAM:earner-safety-reliability] · ✅ DONE (READ-ONLY, opus; every claim re-checked vs LIVE caller.py/provider_registry/ratelimit; complements sibling RED-TEAMs spam-reality + sim-concurrency-legal) · VERDICT: core arch SOUND + genuinely additive (flag-OFF byte-identical, agent.py never imported, Vobiz `_global` row preserved — VERIFIED caller.py:184/2913) but **3 NEW code-grounded DEFECTS + 2 rollout gaps** beyond the siblings. DEFECT B (BLOCKER): the spam-rest guard fires on `sip_code` 486/480/603 the hot path NEVER captures — dial is `wait_until_answered=False` (caller.py:2916), outcome inferred from DURATION+transcript in `_classify_outcome` (:1551), zero SIP codes anywhere in caller.py; FIX = quarantine on **zero-duration ring-out bursts per DID** (signal that exists) OR add NEW LiveKit SIP webhook/participant-disconnected mapping — do NOT ship the guard as the DESIGN text describes (this is WHY the sibling spam-guard recommendations also can't fire as-written). DEFECT C: Redis concurrency/rotation contradicts the LIVE posture — box runs uvicorn **--workers 1** (ratelimit.py:13) so in-proc ACTIVE_CALLS (caller.py:535) is already correct; the proposed Redis(:6380) is the rate-limiter's **FAIL-OPEN** redis → a hard concurrency cap on fail-open redis SILENTLY VANISHES on a redis hiccup → 486-storm; and the claimed twin `provider_registry/health.py` is **in-memory** (zero redis) so concurrency.py/rotation.py are NEW not clones; FIX = keep concurrency IN-PROCESS (extend ACTIVE_CALLS per-trunk), redis only if multi-worker AND then fail-CLOSED for the cap (this also resolves the sibling's A1 leak/A2 TOCTOU more simply — an in-proc counter under one worker has neither). DEFECT D: "pure API, no restart" understates risk — `livekit_sync.delete` removes an ST_ object from the SAME LiveKit the earner dials (ST_fmtVmNJmpzKa); wrong-id/misclick/`_global`-DELETE kills the live trunk with no restart; FIX = refuse DELETE of env LIVEKIT_SIP_TRUNK_ID + any `_global`/AIM-inbound trunk, default soft-disable, hard-delete PIN-gated+audited. GAP E: real-time per-DID kill switch (`POST /quarantine-did`) independent of the master flag. GAP F: rate-limit `/test-call` (<=3/hr/trunk, founder-owned dest, counts vs daily cap). UNDER-SPEC: pair concurrency release with the EXISTING ACTIVE_CALLS finalize touch-point (:2844) or channel-leak; UNIQUE-guard DID across tenants; wire cost_per_minute_paise→wallet meter when 2nd trunk lands. SAFE ROLLOUT = T0 scheduler-bug fix → T1 DDL+seed (`_global` un-deletable) → T2 pkg flag-OFF (in-proc counters + ring-out-burst guard) → T3 mount flag-OFF (test-call rate-limited, DELETE soft-disables) → T4 FE+kill-switch → T5 staged strangler (real-ring before+after → DID rotation → cross-trunk only after founder buys 140/Plivo). APPROVE-TO-BUILD **conditional** on B/C/D + the siblings' B1/A1/A2 corrected before TRUNK_REGISTRY_ENABLED=1. → memory/wave_runs/telephony-independence-megaplan.md (## Phase: RED-TEAM [earner-safety-reliability])
- 2026-06-14 · telephony-independence-megaplan · [RED-TEAM:spam-reality] · ✅ DONE (READ-ONLY, opus; no box/caller.py/agent.py edit) · ANSWER = NO. A new DID does NOT escape the 486/480/603 block, and a personal SIM is WORSE (permanent ban + illegal + 1-SIM=1-call). The block follows the calling BEHAVIOUR (high velocity / near-zero answer / failed-call bursts from an un-DLT 10-digit MSISDN = predictive-dialer graph-ML fingerprint), then attaches to the number AND increasingly the ENTITY/route — so a fresh number repeating the same pattern is re-flagged in 24-72h (subtler in 2-4wk). 5 failure modes: FM-1 fresh DID buys days not immunity; FM-2 personal SIM banned permanently + flags every SIM on the gateway + illegal (no 140 CLI); FM-3 carrier scoring moving to entity/route level so rotation has a ceiling; FM-4 the live scheduler_loop retry bug would AMPLIFY the flag across the whole pool (elevated to a HARD GATE — rotation OFF until it's fixed+verified); FM-5 low answer rate is itself a flag input = death spiral, blind round-robin burns healthy DIDs. VERDICT: rotation + reputation-protection (auto-rest/quarantine) + volume-throttling are all REQUIRED survival infra, NOT optional. Forces into BUILD: (a) HARD GATE rotation-OFF-until-retry-bug-fixed+verified; (b) NEW velocity throttle MISSING from design — per-DID inter-call spacing + calls/hour ceiling on top of per_did_daily_cap (daily cap limits volume not velocity; velocity is the stronger signal); (c) quarantine on a LOW threshold + escalate per-DID→per-trunk/route; (d) reputation-aware rotation (never feed a fresh DID into a campaign already throwing 486s). The DESIGN was already honest (5e/8/9) — RED-TEAM confirms + sharpens. NONE of these escape the block; ONLY DLT-registered 140-series origination does. Build the machine (seatbelt); founder buys the fuel (140/DLT engine). COMPLEMENTS the sibling RED-TEAM [sim-concurrency-legal] (which found the code-enforcement gaps B1/A1/A2); together = gate B1+A1+A2 AND ship the velocity throttle + retry-bug fix before TRUNK_REGISTRY_ENABLED=1. → memory/wave_runs/telephony-independence-megaplan.md (## Phase: RED-TEAM [spam-reality])
- 2026-06-14 · telephony-independence-megaplan · [RED-TEAM:sim-concurrency-legal] · ✅ DONE (READ-ONLY adversarial; no box/caller.py/agent.py edit) · Skeptical review of the trunk-registry DESIGN on the two founder axes. VERDICT: design is HONEST + architecturally sound — clarity on 1-SIM=1-call is PASS, legality flagging (TRAI/DLT/140/ban) is PASS (its strongest part); BUT "the registry ENFORCES compliance" is true-in-prose, false-in-code — gates live in default-permissive columns + UI/prose, not in the get_trunk choke-point or DB constraints. 8 concrete failures, 5 ranked: 🟥B1 CRITICAL — unregistered 10-digit trunk (dlt_status DEFAULT 'unregistered', is_enabled DEFAULT true) can be RETURNED for a campaign via direct /trunks/byo API (UI block bypassable) → the exact TRAI violation+2L fine+2yr blacklist the design exists to prevent; FIX = gate in registry.get_trunk(purpose='campaign') + is_campaign_eligible GENERATED column/CHECK. 🟥A1 CRITICAL — Redis active_calls INCR/DECR leaks on crash/raise → trunk stuck "full" → dialer silently STOPS (commonest failure of this pattern); FIX = try/finally + self-healing sorted-set counter w/ TTL eviction (bare INCR/DECR must NOT ship). 🟧A2 HIGH — check-then-acquire TOCTOU oversells channel cap under exactly the burst concurrency exists for → 486 storm/DID flag; FIX = atomic INCR-and-compare Lua. 🟧B3 HIGH — rotation guard on a non-compliant trunk auto-rotates through the WHOLE DID pool one number at a time, hiding root cause (= what blocked +918071583488, now automated across N numbers); FIX = ≥K quarantines on one trunk → DISABLE trunk + loud compliance alert, don't keep rotating. 🟨A4 MED — max_concurrency LIES above the box RTP ceiling (~100) until the recreate wave; FIX = box-global cap (~90) in every acquire. 🟨B2 MED — NCPR scrub/9-9 window/AI-disclosure named but UN-enforced + orphaned between registry↔campaign; FIX = name the 3 choke-points (campaign-launch / scheduler_loop / agent prompt). 🟦A3 LOW — GSM trunk can ask 1 SIM for 2 calls via DID-rotation collision; 🟦B4 LOW — "live AI unregulated" is a MOVING grey area (TRAI amendment pending), disclosure mandatory NOW. Concurrency SOLVE is correct in SHAPE (= sum channels across trunks, never 1 SIM). ALL fixes live in concurrency.py/rotation.py/registry.py/DDL — none touch agent.py or Vobiz; gate B1+A1+A2 before TRUNK_REGISTRY_ENABLED=1 in prod. → memory/wave_runs/telephony-independence-megaplan.md (## Phase: RED-TEAM [sim-concurrency-legal])
- 2026-06-14 · telephony-independence-megaplan · [DESIGN:telephony-trunk-registry] · ✅ DONE (READ-ONLY DESIGN, opus; no box/caller.py/agent.py edit; Vobiz config left as-is) · THE 100% telephony-independence architecture = a `trunk_registry` cloned column-for-column from the LIVE provider_registry (W1-W5). SCHEMA `db/ddl_trunk_registry.sql` = 3 FORCE-RLS tables (INTEGER paise, no floats): `sip_trunks` (trunk_type sip_provider|gsm_gateway|direct_sip, provider_vendor, direction, sip_host/port/transport, did_pool[], max_concurrency, cost_per_minute_paise, is_140_series+dlt_status+per_did_daily_cap COMPLIANCE gates, priority+rotation_strategy, is_test_verified, quarantined_until, livekit_trunk_id=ST_) + `sip_trunk_credentials` (AAD AES-256-GCM SIP password, scope integration|platform, REUSES provider_registry/credentials.py) + `sip_trunk_health_log` (append-only, REVOKE UPDATE/DELETE). PKG `droplet_work/trunk_registry/*` clones provider_registry; REUSES credentials/ssrf_guard/health verbatim; 3 NEW files: livekit_sync.py (LiveKit Server API glue create/list/delete outbound+inbound trunks+dispatch rules, NO container restart), rotation.py (Redis DID round-robin + spam-rest: 486/480/603 burst→quarantined_until→failover; per_did_daily_cap<100), concurrency.py (Redis trunk:{id}:active_calls vs max_concurrency). WIRING: caller.py:184 hardcoded TRUNK becomes a strangler — flag ON → get_trunk(tenant,'outbound')→ST_id+rotated DID as sip_number (native LiveKit per-call caller-ID selector); any miss/flag-OFF → legacy LIVEKIT_SIP_TRUNK_ID (byte-identical). Live Vobiz ST_fmtVmNJmpzKa imported as ONE _global row → flip-on = same dial. FE `app/telephony/page.tsx` (Core_2 port): trunk cards (health dot + concurrency gauge + DID-budget bars + compliance badge + quarantine banner) + 3-step Add-trunk wizard ending in a FOUNDER-PLACED single test call (the ONLY non-campaign originate; never auto) + inbound-routing + spam-reputation panels. FLAG `TRUNK_REGISTRY_ENABLED` (default OFF). HONEST FOUNDER-BUY LIST: (1) 140-series DID + DLT Principal-Entity/Telemarketer registration via Airtel Business/Tata Tele/C-Zentrix — THE non-negotiable for legal India campaigns (~1-2wk, cheap reg + per-channel/min trunk); without it any 10-digit DID incl current Vobiz is non-compliant+spam-flag-prone (why +918071583488 is blocked today); (2) Plivo 2nd trunk for independence/failover (₹250/DID/mo, ₹0.60/min, needs India COI+GST); Exotel = only BYO-port path but Alpha/wait-GA; Telnyx/Twilio NO India DID skip; (3) Yeastar TG200 GSM only for ONE personal-number manual recall — gsm_gateway HARD-flagged manual-only, NEVER in campaign pool (1 SIM=1 call, WILL ban, illegal at volume); (4) NCPR/DND scrub; (5) raise LiveKit RTP range past ~100 conc (future recreate). BUILD SEQ T0 scheduler retry-bug fix → T1 DDL+seed Vobiz → T2 pkg flag-OFF → T3 mount flag-OFF → T4 FE FORTRESS → T5 strangler flag-ON w/ before+after real-ring regression smoke. EARNER-SAFE: agent.py never imported, flag-OFF resting byte-identical. → memory/wave_runs/telephony-independence-megaplan.md (## Phase: DESIGN [telephony-trunk-registry])
- 2026-06-14 · telephony-independence-megaplan · [RESEARCH:sim-to-sip-options] · DONE (READ-ONLY, deep-web sourced) · GSM gateway (GoIP/Yeastar TG): SIM presents as standard SIP trunk to LiveKit; 1-SIM=1-concurrent-call is cellular physics, unbreakable (GoIP-1=1, GoIP-8=8, Yeastar TG800=8, GoIP-32=32). Indian cost: GoIP-8 ~₹12k–20k, Yeastar TG800 ~₹40k–55k. Integration = LiveKit SIPOutboundTrunk + sip_trunk_id per CreateSIPParticipant, same as CPaaS. HARD CONSTRAINTS: (1) 1-SIM=1-call always; (2) personal SIM auto-dialing WILL trigger AI carrier ban (DoT deactivated 4M SIMs 2025, TRAI blacklisted 21L numbers — graph ML catches 85% of patterns within 24–72hrs to 4 weeks); (3) TRAI TCCCPR 2018+Aug2024+Feb2025 = using any 10-digit personal number for commercial calling is ILLEGAL — 140-series mandatory, first-complaint disconnection + 2-year blacklist; (4) consumer SIM cannot present 140-series CLI; (5) eSIM = same gateway hardware requirement, not a shortcut. Android as GSM gateway: ARCHITECTURALLY IMPOSSIBLE on stock Android (locked RIL + no in-call audio API + closed vendor blobs) — confirmed 2026. eSIM/VoLTE: eSIM is a credential delivery mechanism not a SIP interface; VoLTE is carrier-IMS not internet SIP. PRIMARY recommendation: CPaaS BYO SIP trunk registry (Plivo/Telnyx/Vobiz + Airtel Business 140-series for TRAI-compliant campaigns) — maps directly to the sip_trunks PG table + LiveKit multi-trunk already designed. SECONDARY/NICHE: GoIP/Yeastar for founder's own personal number on manual-trigger-only recalls (NOT automated campaign pool). → memory/wave_runs/telephony-independence-megaplan.md (## Phase: RESEARCH [sim-to-sip-options])
- 2026-06-14 · telephony-independence-megaplan · [RESEARCH:byo-number-sip-providers] · DONE (READ-ONLY, web research, sourced) · Telnyx: India NOT on supported-country list, no Indian DID, ~₹4–5.5/min = SKIP. Twilio: no Indian DID, ~₹3.8–5.5/min, foreign CLI = emergency-fallback-only. Plivo: India DID ₹250/mo + ₹0.60/min outbound, unlimited concurrency, officially on LiveKit docs — VIABLE AS SECOND TRUNK (need India entity COI+GST); NO BYO porting (US/CA only). Exotel vSIP: India-native DID + BYO porting (unique!), ~₹0.40/min — BUT Alpha/no-SLA, IP-whitelist-only-auth, LiveKit outbound unverified (GH issues open) = FUTURE when GA. Knowlarity: closed API, no LiveKit SIP path = SKIP. Bandwidth: India not available = SKIP. DIDlogic: AI-voice-native, LiveKit page, no per-channel fee, India status unconfirmed = investigate. Vobiz stays PRIMARY. INDIA HARD CONSTRAINTS: no Indian local DID from Telnyx/Twilio; BYO porting nearly impossible today (only Exotel GA path); TRAI 140-series + DLT registration mandatory for commercial outbound; consumer SIM+GoIP = illegal+banned. Phased path: (1) more Vobiz DIDs + rotation now; (2) Plivo second trunk in 4 weeks; (3) Exotel when GA. → memory/wave_runs/telephony-independence-megaplan.md
- 2026-06-14 · telephony-independence-megaplan · [RESEARCH:multitrunk-concurrency-arch] · DONE (READ-ONLY, web research, 22 sources, 7 claims 3/3 verified) · LiveKit-SIP has NO native trunk pool manager — pool logic (round-robin, channel counter, failover) must live in application layer; SIP trunk 1 channel = 1 concurrent call; GoIP GSM gateways = NOT viable for India telemarketing (SIM bans + illegal under TRAI); TRAI mandates 140-series SIP trunks + DLT registration + NCPR scrub + 9AM-9PM window + AI disclosure; Vapi default = 10 concurrent slots; Twilio Elastic SIP = unlimited concurrent / 1 CPS default; production target = trunk registry (DB + Redis counters + select_trunk() + health check + failover) + LiveKit per-call trunk_id selection + number rotation over DID pool. → memory/wave_runs/telephony-independence-megaplan.md
- 2026-06-14 · video-framework-to-studio · [W5-strangler-video] · ✅ DONE+DEPLOYED (earner-safe, famit-caller restarted ONLY) · Provider-Framework W5 strangler cut-over — VIDEO first (design/PROVIDER-FRAMEWORK-PLAN §14 W5). `media_gen/video/client._resolve_key` now a thin dispatcher: flag `REGISTRY_FOR_VIDEO` ON → `_registry_resolve_key` asks `registry.get_provider(tenant,"video_gen",routing_hint=<provider slug>)` and returns `client._key` on `ok`; on ANY miss (flag off / pkg absent / not-ok / empty key / exception, never-raises, lazy import, NEVER imports agent.py) → `_legacy_resolve_key` (the ORIGINAL fal/replicate/luma/higgsfield/selfhost/generic env path, byte-identical). NEW `config.registry_for_video()` (call-time read, default OFF). Both edits purely additive vs box golden (config `66a67,77`; client `302a303,309`+`304c311,331`+`318a346,355`, 0 deletion/mod to the legacy branch). 6 OFFLINE PROOFS PASS: (1) flag OFF → `_resolve_key`==`_legacy_resolve_key` for all 7 providers (today behavior); (2) ON+ok→registry key wins; (3) ON+miss→legacy; (4) ON+raises→legacy; (5) ON+empty-key→legacy; (6) ON+pkg-absent→legacy. Deploy: edited FROM box golden (client `58af1c8a`/config `b68a1dc6`); backups `*.W5bak.20260614-220716`; scp md5-gate (`be38c169`/`02bb49dc`) PASS; caller-venv py_compile OK; atomic swap; box import-check `registry_for_video()=False`; famit-caller PID 2745889→2757508 NRestarts=0. `REGISTRY_FOR_VIDEO` NOT added to .env (absent→default OFF→resting byte-identical). EARNER GREEN before+after: agent.py `9150fabe` UNCHANGED, famit-agent PID 1477083 NOT restarted, aim-voice-agent(2739156)+famit-aiasset(2364219) untouched, caller /health 200, 0 5xx, NO ring. Rollback: leave `REGISTRY_FOR_VIDEO` absent/0 (instant). NEXT = W6 (engine seam fix + PG video schema + library bridge on :8310). → memory/wave_runs/video-framework-to-studio.md (## W5)
- 2026-06-14 · inbound-voice-naturalness-fix · [VERIFY] · ✅ VERIFIED (read-only, no mutation) · GOLDEN 5/5 PASS (box re-run: 5 campaigns byte-identical, prompt.py fb87ea56 unchanged). INTEGRATED SMOKE 9/9 PASS: SCRIPT RULE block confirmed live in _build_sales_instructions render (1614be09, 20948 chars) — DEVANAGARI rule, BANNED romanized list, LATIN loan-words, NO default language, Switch the MOMENT, applies ONLY when Hindi/Hinglish, FINAL OVERRIDE, casual register (थोड़ी देर रुकिए/please — not प्रतीक्षा कीजिए/कृपया) all present. MLV mirror guards intact. TTS CLIP VERDICT: SIGNIFICANT IMPROVEMENT — BEFORE (romanized): `ke`→`यंग`, `mein`→`मेन`, `sakta hai`→`सेक्टर हाई` (garbled, 1/3 structural words); AFTER (Devanagari+Latin): `आपके...के...में...है...से शुरू होती है` all intact, sentence structure and meaning clear. RESIDUAL: Sarvam Bulbul v2 still phonetically approximates English brand names in Hindi accent (BHK→`थ्री उसाई`, Codename Joy→`वुड नेम मोई`) — inherent v2 limitation on English proper nouns, not a prompt/script issue; upgrade path = bulbul:v3. EARNER GATE GREEN: agent.py 9150fabe unchanged, famit-agent PID 1477083 not restarted, aim-voice-agent PID 2721961 (post-build, NRestarts=0, 15:03 UTC), /health 200, 0 5xx, NO ring. FOUNDER RECIPE: call +918071583488 in Hindi → expect natural Hinglish back (Devanagari structure + Latin loan-words); try English mid-call → expect clean English switch. FINAL PROOF = founder's real inbound call. → memory/wave_runs/inbound-voice-naturalness-fix.md (## Phase: VERIFY)
- 2026-06-14 · inbound-voice-naturalness-fix · [BUILD+DEPLOY] · ✅ DONE+DEPLOYED (earner-safe, aim-voice-agent restarted ONLY) · Restored real-human casual Hinglish on INBOUND aim_voice_agent. FIX (inbound-only, `aim_voice_agent.py:_build_sales_instructions`; `prompt.py` UNTOUCHED): added a SCRIPT RULE block at the FINAL LANGUAGE LOCK (highest recency) — Hindi words in DEVANAGARI (banned romanized hai/mein/ke/…→है/में/के/…), English loan-words+brands kept LATIN (BHK/budget/confirm/book/please/Codename Joy 3.0), digits for numbers, NEVER mix scripts in one line, casual register (थोड़ी देर रुकिए not प्रतीक्षा कीजिए; please not कृपया, 1-2 sentence turns). Engine-AGNOSTIC (clean on Sarvam Bulbul + EL) → NO tier→TTS routing/billing change. PRESERVES MLV mirror: rule opens "applies ONLY when speaking Hindi/Hinglish; an English caller still gets pure English"; FINAL OVERRIDE + "NO default language/house style" + "switch the MOMENT they switch" all intact (functional render confirmed). GOLDEN 5/5 = exit 0 byte-identical (prompt.py fb87ea56 untouched). Deploy: box backup VOICEbak.20260614-203227, scp md5-gate (1614be09) PASS, box py_compile OK, atomic swap, aim-voice-agent PID 2669239→2721961 "registered worker" NRestarts=0 0-errors. EARNER GREEN: agent.py 9150fabe UNCHANGED, famit-agent PID 1477083 not restarted, caller /health 200, 0 5xx, NO ring; RAG_INJECT_ENABLED+flags intact. Commit 69374eb on fe/unify-run-wavec, gitleaks 0. RECOVERY-STATE.md source-map reconciled 018c20a7→1614be09. ACCEPTANCE = founder's real inbound call. → memory/wave_runs/inbound-voice-naturalness-fix.md (## Phase: BUILD)
- 2026-06-14 · inbound-voice-naturalness-fix · [DEEP-WEB RESEARCH] · ✅ DONE (read-only, web search + official docs) · ENGINE VERDICT: Sarvam Bulbul (all versions) REQUIRES Devanagari for Hindi words — official docs state "Transliterated input significantly reduces output quality" + "Always use native script for Indic words"; ElevenLabs Flash v2.5 tolerates Hinglish/romanized but current live voice "Neha P" is an English voice (wrong for Hindi-dominant output). OPTIMAL SCRIPT: Devanagari Hindi words + Latin English words/brands/digits — works on both engines, resolves the garbling. PROMPT RECIPE: add a SCRIPT RULE block after FINAL LANGUAGE LOCK (aim_voice_agent.py:1631): pin Hindi to Devanagari, keep English loan-words Latin, numbers as digits, prohibit romanized Hindi, prohibit same-line script mixing, casual register (thodi der ruko not pratiksha kijiye). EARNER GATE GREEN: no mutation, no restart, no ring. → memory/wave_runs/inbound-voice-naturalness-fix.md (## Phase: DEEP-WEB RESEARCH)
- 2026-06-14 · inbound-voice-naturalness-fix · [DIAGNOSE] · ✅ DONE (READ-ONLY, no box mutation, no call, no restart) · ROOT CAUSE FOUND. Inbound TTS is NOT fixed-EL (brief was wrong) — it is TIER-DEPENDENT via `prompt.resolve_providers` (live `INBOUND_PROV_LOCK` ON): **lean/standard → SARVAM Bulbul `bulbul:v2` anushka; premium → EL `eleven_flash_v2_5` voice `QTKSa2Iyv0yoxvXY2V8a`="Neha P" (an ENGLISH voice pinned language=hi)**. The founder's real "Codename Joy 3.0" campaign resolves `tts=sarvam` → HE HEARS SARVAM. (1) PRIMARY cause = commit `ab6777c` `feat(provider-lock)` (RUN-PLATFORM Wave A) routed lean/standard off the old fixed-EL inbound default onto Sarvam Bulbul. (2) SECONDARY = LLM (Groq llama-4-scout) emits INCONSISTENT SCRIPT — pure Devanagari, romanized, AND mixed-in-one-line ("₹2 करोड़ ke budget mein…") — because the MLV mirror rule (`aim_voice_agent.py:1577`/`:1631`) pins LANGUAGE but never SCRIPT. (3) TERTIARY = EL voice is English-on-`language=hi`. REPRODUCED via real EL+Sarvam synth + round-trip Sarvam-STT oracle: Sarvam GARBLES romanized ("Codename Joy"→"वोडने मोई", "BHK"→"उसाई"); EL handles Hinglish far better; BOTH clean on pure Devanagari. Audio: `droplet_work/_voice_repro/` (6 files). FIX DIRECTION (build phase): pin LLM Hindi output to ROMANIZED Hinglish (engine-agnostic, cheapest) and/or route Hindi-heavy verticals to an EL Hindi-capable voice. EARNER GATE GREEN: agent.py `9150fabe` UNCHANGED · famit-agent PID 1477083 not restarted · aim-voice-agent not restarted · caller /health 200 · 0 5xx · NO ring. NOTE: box aim_voice_agent.py md5=`5c3936fa` (W1 RAG) — RECOVERY-STATE.md stale at `018c20a7`, reconcile. → memory/wave_runs/inbound-voice-naturalness-fix.md
- 2026-06-14 · provider-framework-foundation · [W3-resolve-health-reveal] · ✅ DONE (offline) · W3 of the Universal Provider Framework (design/PROVIDER-FRAMEWORK-PLAN.md §14): the resolve + health + PIN-step-up-reveal layer, LOCAL + offline-tested, NO mount/box-write/restart/deploy, flag PROVIDER_REGISTRY_ENABLED OFF. NEW `provider_registry/{store,admin_store,registry,health}.py` + ADDITIVE `provider.reveal` scope in the DEPLOYED `firewall.py` (built local, NOT deployed — W4 mounts). `registry.get_provider(tenant,capability,routing_hint)` = the single capability-keyed resolution point (flag-OFF→registry_disabled · PG-down→not_configured · enabled defs RLS own+`_global` priority-asc · routing_hint pinned · skip circuit-open · first with a credential decrypted ONLY via the credentials.decrypt get_secret seam wins · never raises). `store.py` is_admin=False RLS reads (GUC-in-txn via db.engine.session, mirrors ai_manager/store.py); `admin_store.py` is_admin=True super-admin reads (require_super_admin-only, W4). `health.py` in-memory 3-fail circuit breaker, expo backoff 60→120→240 (capped 1h), half-open, injectable clock — NO PG on the hot path (§3 0ms voice-loop rule). firewall `provider.reveal`: 60s TTL + aud=provider_def_id + SINGLE-USE jti (consumed on verify; replay→None) — closes the live jti-replay gap; isolated state file var/provider_used_jti.json; FIX: PyJWT auto-validates aud → options verify_aud=False + explicit self-check. OFFLINE PROOFS: resolve RLS A-never-gets-B (fake db.engine enforces the §5 GUC; A's tried never contains B's slug; store.get_active_credential(A,DEF_B)=None) · cross-tenant ciphertext copy→InvalidTag→skipped→fallback (B's key never returned as A's) · circuit-open→fallback-by-priority · jti-replay→403 (+aud/sub/scope/expiry/no-jti all denied) · existing-firewall UNCHANGED golden (diff vs box cd1ac5d1 = single additive insertion 339a340,473, 0 deletion/mod lines; generic mint/verify/PIN/change/lockout/classify behavior PASS). pytest 4 passed (W2+W3). gitleaks 0. EARNER GATE PASS: agent.py md5 9150fabe UNCHANGED · famit-agent PID 1477083 alive NOT restarted · box firewall.py still cd1ac5d1 (reveal NOT deployed) · caller /health 200 · NO ring. Commit on fe/unify-run-wavec. NEXT: W4 (mount endpoints.py in caller.py under PROVIDER_REGISTRY_ENABLED — serialized vs RAG/Vault/Video). → memory/wave_runs/provider-framework-foundation.md
- 2026-06-14 · rag-in-call-verify-gaps · [DESIGN:gaps] · ✅ DONE (READ-ONLY) · 2 gaps designed. PER-PATH VERDICT: INBOUND voice = ✅ DONE+LIVE (FTS grounding, 3 sites, kill-switch, 6–12ms, byte-identical-off proven; nothing to do). OUTBOUND earner agent.py = 🔴 GATED (does NOT ground; RAG inbound-only). FULL-SEMANTIC dense = 🟡 BUILDABLE ENHANCEMENT (founder-signed Ph2). GAP1 (outbound, earner-safe): mirror the proven inbound block but build grounding INTO `instructions` in the CONNECT window BEFORE `_MirrorAgent(instructions=...)` (agent.REFERENCE.py:400→725) — NEVER a mid-call update_instructions (earner's Groq-cache TTFT invariant). New flag `OUTBOUND_RAG_ENABLED=0` (default OFF=byte-identical); PORT `_grounding_seed`/`_format_grounding`/`_kb_retrieve` VERBATIM from aim_voice_agent.py:518-577, dense=False hardcoded; reuse box `kb/` (no fork); optional FTS-only `lookup` tool = deferred W-OB.2. Edit MUST start from a scp-pulled `agent.py.LIVEBOX` golden (BOX-ONLY, never `_inbound_ref`/local). GATE = golden flag-off byte-identical render + `agent.py.PREv<n>` rollback (md5 WILL change—NOT an md5-unchanged gate) + famit-agent restart ONCE; ACCEPTANCE = FOUNDER real outbound ring (I never test-call—spam-flags DID). FOUNDER ACTIONS: (1) sign-off to edit agent.py + (2) DID un-blocked. Supersedes RAG-MASTER W-OB caller.py-precompute (simpler: reuse inbound block in earner's own connect window). GAP2 (full-semantic): dense leg ALREADY wired in kb/core.py:343-427 (pgvector ANN, gated behind `dense`) + embeddings.py ALREADY provider-agnostic/dormant-until-key/off-box — so it's a CONFIG+backfill, not a rewrite. Embedder = OpenAI text-embedding-3-small @256d (NOT GPU box, NOT OpenRouter; Sarvam has no /embeddings yet—pre-wired to flip). Trigger = call-CONNECT only (dense=True at prefetch site ONLY; lookup/pick_campaign FTS-only FOREVER, C-3); latency = ONE embed RTT 40-200ms + ANN 5-15ms paid ONCE in the 200-400ms SIP window, W4 grounding_cache LRU-keyed `(tenant,campaign,stage,channel,kb_version)` → ~0ms HIT → per-turn cost = 0ms; cost ≈ few ₹ one-time backfill + <₹0.001/call. Gated by EMBED_API_KEY presence (no new bool), hard-gated behind a RAGAS-fails-on-FTS proof + W6 no-embed-on-reply assertion. FOUNDER ACTIONS: OpenAI key + sign off paid embedder. PHASED: W-OB.1 (outbound connect-window, founder+DID-gated) · W-OB.2 (optional outbound lookup tool) · W4 (grounding_cache, prereq for dense, inbound) · Ph2-dense (.env config + backfill + dense_ready atomic flip). Earner UNTOUCHED: agent.py 9150fabe (read-only mirror), 0 box mutations, 0 restarts, NO ring, provider-framework-foundation files not touched. → memory/wave_runs/rag-in-call-verify-gaps.md (## GAP DESIGN PHASE)
- 2026-06-14 · rag-in-call-verify-gaps · [SMOKE:integrated] · ✅ DONE (READ-ONLY) · VERDICT: YES — live inbound agent has low-latency KB access. FTS latency 6–12 ms warm/cold against live PG blr1. All 4 KB-needing caller utterances (price, discount, "bahut mahanga" objection, product Q) returned 5 chunks each (0 MISS). Objection "bahut mahanga" fires `objection_too_expensive_pricing` as top chunk. Grounding block (+1625 chars) confirmed PRESENT in rendered `_build_sales_instructions` (delta +1625, grounding_in_with_render=True). All 3 grounding sites confirmed live in running box code: Site1 `CustomerSalesAgent.lookup`, Site2 `CustomerSalesAgent.pick_campaign`, Site3 `_entrypoint_impl` connect-prefetch. `EMBED_API_KEY` UNSET → `dense=False` → ZERO embed RTT. `RAG_INJECT_ENABLED=1` confirmed. Gap: _global corpus floor works but tenant-specific chunks outrank it (correct behaviour; no miss). W4 grounding_cache deferred (6–12ms is within 200–400ms SIP window, acceptable). Earner UNTOUCHED: agent.py 9150fabe unchanged, 0 restarts, NO ring. → memory/wave_runs/rag-in-call-verify-gaps.md
- 2026-06-14 · video-flex-framework-design · [SYNTHESIZE:unified-roadmap] · ✅ DONE (DESIGN-ONLY) · WAVE COMPLETE — synthesized the whole read-only design wave (3 HAVE + 4 RESEARCH + 3 DESIGN phases) into the universal-flexible-provider framing + ONE unified earner-safe build roadmap. **`design/PROVIDER-FRAMEWORK-PLAN.md` §14 = the NEW UNIFIED cross-product W1–W12 roadmap** merging the framework's F1–F9 + Video's U1–U10 into one table-form sequenced plan (each wave: scope · files/schema · flag · acceptance · rollback): **framework FIRST → Video Studio on it → composite tier ships with ZERO paid key**; **caller.py serialized — only ONE of {RAG,Vault,Registry,Video} edits caller.py at a time** (RAG W3 LIVE → registry mount W4 = next caller.py edit, video mount W8 follows, never concurrent). First 3 launchable waves spelled out (W1 FW-DDL+pkg shell · W2 SSRF-guard+3-tier-adapter+named-transforms+AAD-creds offline · W3 resolve+health+PIN-step-up-reveal offline — all local before the W4 mount). BE on Opus, FE on Sonnet+frontend-design. `VIDEO-STUDIO-MASTER-PLAN.md` §9b/§10d/§12.7 (Video = first consumer) verified not re-touched. Pointers appended: NEXT-BIG-BUILDS #8b (Universal Provider Framework, build BEFORE Video) + #9 cross-link · ORCHESTRATOR (newest-on-top) · this ledger · `memory/wave_runs/video-flex-framework-design.md` (## Phase: SYNTHESIZE [unified-roadmap]). DESIGN-ONLY — no box/code/earner mutation, agent.py `9150fabe` never read/imported, no restart, no ring, no open forks. NEXT BUILD = W1 (framework DDL + package shell). → design/PROVIDER-FRAMEWORK-PLAN.md §14 + memory/wave_runs/video-flex-framework-design.md
- 2026-06-14 · video-flex-framework-design · [DESIGN:provider-framework] · ✅ DONE · UNIVERSAL FLEXIBLE PROVIDER/CONNECTOR FRAMEWORK designed → `design/PROVIDER-FRAMEWORK-PLAN.md` (510 lines, decision-ready). Net-new pkg `droplet_work/provider_registry/` + `db/ddl_provider_registry.sql` (3 FORCE-RLS tables: provider_definitions `_global`-read-shared/write-locked · provider_credentials AAD-bound AES-256-GCM + `scope` ai_provider-masked/integration-revealable · provider_health_log append-only). Config-driven registry: super-admin/vendor adds via UI a hosted-api OR self-hosted provider (base_url, auth scheme, request/response field-map, capabilities, cost, health-check) → live no-code-deploy for ~95%. **3-tier transform** (openai_compat zero-config 90% · named_provider 1-dict-entry, existing fal/replicate/luma builders registered not rewritten · custom_field_map JSONPath-only-no-eval) = the "connect ANY future tool" lever. **Capability-keyed** `get_provider(tenant,capability)` = consumers ask a capability not a name (Video Studio FIRST consumer; voice-LLM/RAG/image/WA-AI next). **`get_secret()` Vault seam** (interim Fernet, flip VAULT_BACKEND later, zero consumer change — Vault = separate deferred build). **SSRF guard hard-gate** (CVE-2025-59146; host/port split, DNS-resolve-all, RFC1918/metadata denylist, redirects-off, egress-firewall, health-sandbox, PIN step-up; self-host=super-admin-only). STRANGLER cut-over (per-consumer sub-flags REGISTRY_FOR_VIDEO/IMAGE/LLM, legacy fallback, each revertible). Earner-safe (rides caller.py/AI-asset:8310, never agent.py 9150fabe; voice-router cache-first 0ms), multi-tenant FORCE-RLS, cost-capped (free/composite default + 1-paid-test + per-tenant cap on paise ledger), most-secure, sellable. Crazy UI = ONE Integrations page + visual FieldMapper (FE design = DESIGN[crazy-ui-security] phase). `PROVIDER_REGISTRY_ENABLED` default OFF byte-identical; serialized vs RAG/Vault/Video on caller.py. Video plan §9b/§10d/§12.7 enhanced (Video = first consumer). → design/PROVIDER-FRAMEWORK-PLAN.md + memory/wave_runs/video-flex-framework-design.md
- 2026-06-14 · video-flex-framework-design · [RESEARCH:provider-registry-patterns] · ✅ DONE · 11 sourced findings: LiteLLM DB-backed runtime model add (POST /model/new, no restart), OpenRouter per-request provider routing + BYOK, Kong AI Proxy Advanced targets[]/llm_format/failover, Bifrost virtual-key/access-profile multi-tenant hierarchy, Portkey runtime-fetched secret references, SSRF threat model (CVE-2025-59146/CVE-2025-53767 direct precedents + 9-layer defense), three-tier transform model (OAI-compat/named_provider/custom_field_map JSONB), canonical PG registry schema (provider_definitions+provider_credentials+health_log, FORCE-RLS, AAD-bound AES-256-GCM), capability-based consumer interface, circuit-breaker/fallback pattern. → memory/wave_runs/video-flex-framework-design.md
- 2026-06-14 · rag-w3-kb-ui · [W3-backend] · ✅ DONE+DEPLOYED+VERIFIED-LIVE · 4 KB-management endpoints added to caller.py (token-derived, RLS is_admin=False, all asyncio.to_thread): GET /kb/sources (tenant+`_global` sources w/ live chunk counts+status, ?scope_campaign_id), POST /kb/upload (text OR PDF via pypdf graceful-degrade; size/sniff guards; kb.ingest under tenant GUC, write-locked from `_global`; optional scope_campaign_id; audited), POST /kb/test-retrieve (FTS-only retrieve(dense=False)+include_global → firing chunks {id,source_id,document_id,section,snippet,score,leg}+grounded meter; logs probe to kb_query_log), GET /kb/gaps (kb_query_log grounded=false aggregated, strictly per-tenant). Box /opt/famit-agent: backup caller.py.W3bak.20260614-124353; pypdf 6.13.2 into caller venv; scp+md5-gate (52c59291)+box py_compile OK; atomic swap; famit-caller restarted ONLY (PID 2685432). E2E authed PASS (sources lists+counts; text upload→1 chunk fts_only; "registration charge"→grounded 6 sparse; "zxqwvbbb"→grounded=false→/kb/gaps count 1); smoke data cleaned. Commit a91d049 on fe/unify-run-wavec; gitleaks 0; py_compile OK. EARNER UNTOUCHED: agent.py md5 9150fabe UNCHANGED, famit-agent PID 1477083 not restarted, /health 200, 0 5xx, NO ring. NEXT: W7 KB UI (Sources/Test Answers/Insights) on famit-panel fe/unify-run-wavec, FORTRESS deploy. → memory/wave_runs/rag-w3-kb-ui.md
- 2026-06-14 · rag-w2-seed-telecaller · [W2-verify] · ✅ DONE+VERIFIED-LIVE · Box DB re-probed directly: 120 kb_chunks / 41 kb_sources (_global); tenant FTS read-share PASS (2 "Bahut mahanga" rows); write-lock PASS (RLS WITH CHECK blocked tenant INSERT); idempotency PASS (41/41 checksums in DB, re-seed=0); auth gates 401/403/405/200 PASS; RAG_INJECT_ENABLED=1; /health 200; agent.py md5 9150fabe UNCHANGED; famit-agent active; 0 5xx; NO ring. Docs updated: RAG-MASTER-PLAN.md (W2 DONE) + NEXT-BIG-BUILDS.md + ORCHESTRATOR.md + AGENT_LEARNINGS.md. NEXT: W3 grounding cache (kb/grounding_cache.py) + KB management UI. → memory/wave_runs/rag-w2-seed-telecaller.md
- 2026-06-14 · rag-w2-seed-telecaller · [W2-seed] · ✅ DONE+DEPLOYED · `_global` telecaller corpus SEEDED LIVE (41 sources / 120 kb_chunks on box DB). NEW `kb/seed_global.py` idempotent loader: each of 41 entries ingested as own source under is_admin=True (only RLS path that writes `_global`); dedup by sha256(content) → re-run/double-POST inserts 0 (RUN2 duplicate=41). NEW super-admin-gated `POST /kb/seed-telecaller` (caller.py:3326; no-auth→401, legacy-pw→403, admin-hmac→200). 2 core.py fixes found live (41fde4a): ensure_schema raw-cursor (SA-2.0.50 immutabledict bug on cold apply) + KB_INCLUDE_GLOBAL=0 now truly excludes `_global` (was leaking via read-share RLS). LIVE PROOF: read-share surfaces `_global` objection to tenant; write-lock blocks tenant `_global` INSERT (0 hacked); idempotent via CLI + HTTP. Deployed backup-first/md5-gated; famit-caller restarted ONLY (PID 2679442). Earner UNTOUCHED (agent.py 9150fabe, famit-agent PID 1477083, /health 200, 0 5xx, NO ring). Commits ff44770+41fde4a on fe/unify-run-wavec, gitleaks 0. → memory/wave_runs/rag-w2-seed-telecaller.md
- 2026-06-14 · rag-w2-seed-telecaller · [W2-corpus] · ✅ DONE · 41 FTS-quality _global chunks written to droplet_work/kb/seed_global_corpus.json (gitignored scratch, ready to ingest to box). Coverage: 10+ objection handlers (too_expensive/not_interested/call_later/need_to_think/already_have/send_details_whatsapp/trust/DND+more) each with empathy-first Hinglish + named technique; pricing+value-framing FAQs; 6 vertical product explainer scaffolds (generic/SaaS/finance/edtech/real-estate/health); rapport+behavior patterns (mirroring, backchannel haan/achha, urgency-without-pushiness, 5 close techniques, followup sequence, WhatsApp templates, buying signals, voice tone, active listening, Indian B2C psychology). All chunks: topic+content+tags; strong keyword coverage for FTS-only retrieval (EMBED_API_KEY unset). Commit 9561ffc on fe/unify-run-wavec; gitleaks 0. Earner UNTOUCHED. → memory/wave_runs/rag-w2-seed-telecaller.md
- 2026-06-14 · rag-w1-retrieval-harden · [W1-build] · ✅ DONE (build+offline-verify) · core.py retrieve() gains `dense=False`(reply-path default→ZERO embed/network; C-3) + `include_global=True`; `_global` UNION via explicit `OR tenant_id='_global'` bound selftid under is_admin=False (no `%`, no admin); kb_chunks/sources/documents RLS USING read-shares `_global` / WITH CHECK write-locks it; NEW kb_query_log FORCE-RLS (strict per-tenant) + log_query/purge_query_log TTL(90d). 3 grounding sites funnel one `_kb_retrieve` chokepoint → passes dense=False explicitly (FTS-only forever). Offline 5/5 PASS; DDL rollback-validated on live box DB RC=0; py_compile OK; gitleaks 0. Commit 266f2c1 on fe/unify-run-wavec. Box NOT mutated (DDL deploys next aim-voice-agent restart via ensure_schema). FLAG TRUTH: CTX_CACHE + INBOUND_PROV_LOCK both DORMANT (absent from .env, code-default off) — NOT flipped. Earner UNTOUCHED (9150fabe/PID1477083/caller-200/0-5xx/NO ring). → memory/wave_runs/rag-w1-retrieval-harden.md
- 2026-06-14 · rag-w0-retrogate · [W0-build] · ✅ DONE (build+verify) · Built RAG_INJECT_ENABLED kill-switch (default 1) + wrapped all 3 live grounding sites (lookup/pick_campaign/connect-prefetch) + the _kb_retrieve chokepoint → flag-OFF does NO kb retrieval & passes grounding="" → byte-identical no-RAG inbound render. Golden 5/5; flag CI exit 0 (flag-off==no-RAG render, 18938 chars; non-empty block appends exactly). prompt.py untouched. Commit 8bc8780 on fe/unify-run-wavec, gitleaks 0. Earner UNTOUCHED (9150fabe/PID1477083/caller-200/0-5xx/NO ring). NOT yet deployed to box (still 018c20a7) — box deploy + .env RAG_INJECT_ENABLED=1 = next box-mutating step. → memory/wave_runs/rag-w0-retrogate.md
- 2026-06-14 · repo-recovery-audit (P3 final) · [wf_2b177b10] · ✅ DONE · RECOVERY-STATE.md written (authoritative source map + live flags + 4 follow-ups); PLAYBOOK rules 16+17 banked; AGENT_LEARNINGS + ORCHESTRATOR updated. Local↔box CONSISTENT. Open: W0 RAG kill-switch; CTX_CACHE/INBOUND_PROV_LOCK .env verify; branch merge. Earner UNTOUCHED (9150fabe/PID1477083/health200/0-5xx). → memory/wave_runs/repo-recovery-audit.md + design/RECOVERY-STATE.md
- 2026-06-14 · repo-recovery-audit (P2 reconcile) · [wf_2b177b10] · ✅ DONE · Local mirrors RECONCILED to live box truth (SCP-pull, no box write): aim_voice_agent a9eefa8c→018c20a7, prompt ec5fa971→fb87ea56, both .LIVEBOX goldens refreshed, agent.py on-disk→9150fabe (gitignored), _inbound_ref DEPLOYED ref 3152539f→018c20a7 + README.CANONICAL.md canonical map. kb/ already matched. Commit 70969dd, gitleaks 0. Earner UNTOUCHED (9150fabe/PID1477083/health200/0-5xx). NO safe gap deployed; RAG_INJECT_ENABLED W0 retro-gate = separate box-mutating wave (noted). → memory/wave_runs/repo-recovery-audit.md
- 2026-06-14 · repo-recovery-audit (P1 drift-map) · [wf_2b177b10] · ✅ DONE · Full drift map: earner HEALTHY; 3 critical stale local files; RAG LIVE+UNGATED (W0 must build RAG_INJECT_ENABLED); missing .env flags (CTX_CACHE, INBOUND_PROV_LOCK); 7-branch sprawl mapped. → memory/wave_runs/repo-recovery-audit.md
- 2026-06-14 · panel-unify-redeploy · [wf_0eff1d2b] · ✅ DONE · Run-page regression FIXED — all UI unified (4-step + Wave C cost-meter/provider-lock/exclude-toggle + W4 CRM + AIM + #8). Branch fe/unify-run-wavec, BUILD_ID TU16Mn1DcJVmxnxr2GVyL live.
- 2026-06-14 · three-products-megaplan · [wf_666fe925] · ✅ DONE · 3 master plans (RAG/Video/Vault). 🚨 KEY: RAG grounding LIVE+UNGATED on box (3 sites, 63 chunks) + box↔local earner-file drift → RAG W0 = retro-gate first. → design/{RAG,VIDEO-STUDIO,VAULT}-MASTER-PLAN.md
- 2026-06-14 · workflow-funnel-execution (#8) · [wf_6dfd8def] · ✅ DONE · funnel/workflow now RUNS (empty run_token was the bug → 401 at dialer); human labels + Run button; verified queued-only, NO ring. → droplet_work/funnels/RUN_EXEC_STATE.md
- 2026-06-14 · aim-access-and-pin (#6) · [wf_e7cb7c7d] · ✅ DONE · PIN change (old→new + lockout) + numbers CRUD; firewall byte-identical. BUILD_ID sTCWP4Jj.
- 2026-06-14 · voice-w4-memory-read · [wf_f70d5945] · ✅ DONE · memory read side LIVE (retrieval into inbound + CRM Memory tab + API); LEAD_MEMORY_PG=1; 10/10 verify. → design/W4-MEMORY-READ-STATE.md
- 2026-06-14 · voice-w3-multichannel-memory · [wf_45ee5836] · ✅ DONE · lead_memory/episodes RLS tables + durable PG-outbox extraction (survive-restart); flag-gated. → design/W3b-EXTRACTION-STATE.md
- 2026-06-14 · run-wave-c-ui-costmeter · [wf_88dc2a51] · ✅ DONE (then reverted by branch-sprawl → being re-fixed) · real cost meter + provider-lock banner + exclude-toggle. On branch fe/wave-c-run-cost-meter.
- 2026-06-14 · run-wave-a-billing-providerlock · [wf_59b68b62] · ✅ DONE · resolve_providers leaf (golden 5/5) + USD_INR 95.2 (Groq 95× fix) + funnels secure. → RUN-PLATFORM-MASTER-PLAN.md
- 2026-06-14 · run-wave-b-preview-fix · [wf_7a9bfd23] · ✅ DONE · preview silent cause = text/plain; backend full-buffer + force audio/mpeg. Live: 200 audio/mpeg ID3.
- 2026-06-14 · voice-w2-context-cache · [wf_b26d1b52] · ✅ DONE · full-context cache (warm 0.16ms) + version-stamp invalidation; flag CTX_CACHE=1 inbound. → design/W2-DEPLOY-STATE.md
- 2026-06-14 · voice-p0-leak · [wf_ed3b9042] · ✅ DONE · cross-tenant memory leak CLOSED (inbound+WA, tenant-checked fallback); no earner restart. → design/P0-LEAK-STATE.md
- 2026-06-14 · voice-w1-script-fullcontext · [wf_17dcce28] · ✅ DONE · dynamic vendor-script→persona + lossless raw_script store + Script Studio; golden 5/5. → design/W1-DEPLOY-STATE.md
- 2026-06-14 · master-dna-plan · [wf_4f117706] · ✅ DONE · the full-DNA brain. → MASTER_DNA_PLAN.md
- 2026-06-14 · run-platform-megaplan · [wf_eefea3fb] · ✅ DONE · real pricing + preview cause + provider-lock + feature table. → design/RUN-PLATFORM-MASTER-PLAN.md
- 2026-06-14 · voice-brain-megaplan · [wf_8988e0cb] · ✅ DONE · the adaptive-voice-brain architecture (45 agents). → design/VOICE-BRAIN-MASTER-PLAN.md

## ⏭️ QUEUED (launch when a wave slot frees — avoid rate-limiting the urgent fix + priority research)
- **repo-recovery-audit** — diagnose + recover ALL latent mistakes like the branch-sprawl (other deployed-then-reverted UI, un-deployed committed work, local↔box drift, half-merged branches, dormant flags). Launch after panel-unify-redeploy lands.
- **RAG (heart) → Video Studio → Vault** — build each (BE Opus / FE Sonnet+frontend-design) once three-products-megaplan lands its master plans.
- **🔧 scheduler retry-queue guard fix** (caller.py scheduler_loop ~:7131) — add `if attempts>=max_attempts: remove+skip` so exhausted 3/3 retries can NEVER re-fire (THE bug that auto-dialed 6 numbers, kept the carrier 486 block alive, burned Vobiz balance; queue cleared + backed up `var/retry_queue.json.PAUSED_20260614-201754.bak`). 🟥 DO before the founder resumes ANY campaign. famit-caller only, earner-safe. Detail: memory/wave_runs/outbound-diagnosis.md.
- **🔒 GATED on founder:** outbound RAG grounding + provider-lock (agent.py sign-off + DID unblocked — designs ready); dense/semantic RAG (OpenAI key); Vault (teammate); outbound dialing (Vobiz DID rotation — FOUNDER-VOBIZ-UNBLOCK.md).
- **🎚️ dormant-flag activation** — CTX_CACHE (W2) + INBOUND_PROV_LOCK (Wave A) built-but-OFF in box .env; activate w/ a real-call check when convenient.
- Then the gold-mine backlog (NEXT-BIG-BUILDS items).
- 2026-06-14 RAG W0 DEPLOY ✅ — `RAG_INJECT_ENABLED` kill-switch deployed to box (`8335d4ba`); flag=1 (grounding ON); golden 5/5 PASS; flag-gate A/B/C PASS; earner UNTOUCHED. Kill: set `RAG_INJECT_ENABLED=0` + restart aim-voice-agent.
- 2026-06-14 RAG W1 DEPLOY ✅ — retrieval hardening live: dense=False default (zero embed on reply path), _global UNION predicate (RLS USING + explicit SQL, no %wildcard), kb_query_log FORCE-RLS + TTL. Box: kb/core.py(7010a77e) + schema.sql(42c14591) + __init__.py(aa2b7c13) + aim_voice_agent.py(5c3936fa). DDL applied (all 4 kb_ tables FORCE-RLS=t live; kb_query_log NEW). aim-voice-agent restarted PID 2669239. 8/8 offline probes PASS. Earner UNTOUCHED (agent.py 9150fabe, famit-agent PID 1477083, /health 200). CTX_CACHE+INBOUND_PROV_LOCK = DORMANT (absent from .env, not flipped). NEXT: W2 seed _global corpus.
- 2026-06-14 RAG W3 + W7 DEPLOY ✅ FEATURE-COMPLETE-FOR-NOW — W3 KB-management backend (4 endpoints: /kb/sources /kb/upload /kb/test-retrieve /kb/gaps, caller.py, famit-caller restarted) + W7 /knowledge UI (Sources/Test Answers/Gaps, 3-tab Core_2 page, FORTRESS panel BUILD_ID YV9obkLRRD0U5oX-CPOCH). All 6 routes 200 via CF edge. No regression on Run/CRM/AIM/Workflows. Earner UNTOUCHED (agent.py 9150fabe, PID 1477083, /health 200). RAG is feature-complete-for-now. NEXT PRODUCT = VIDEO STUDIO.

- 2026-06-14 video-flex-framework-design DESIGN [crazy-ui-security] DONE — Universal Connector/Integrations page (app/integrations: Providers/Self-hosted/Health/Audit, ports api-keys+_custom-providers CRUD, Test-connection w/ VRAM readout, capability chips, transform-type wizard, SSRF+PIN-gate on self-hosted add, PIN-pad reveal hidden on ai_provider rows per Vault §9) + Video Studio UI (app/creative/video: TierTabs composite-default, BYO-key picker from registry, AssetMedia split, Images<->Videos one-library toggle) + most-secure model (AES-GCM+AAD, get_secret seam, append-only audit, entitlement HIDE/LOCK literal-prefix). Glyph ground-truth pinned (camera-video/lock/clock-1/chain exist; shield/eye/copy/key/refresh ABSENT=invisible). Files+flags+acceptance+rollback in memory/wave_runs/video-flex-framework-design.md.
- 2026-06-14 · video-flex-framework-design (DESIGN video-studio-on-framework) · ✅ DONE · NEW design/PROVIDER-FRAMEWORK-PLAN.md (universal config-driven provider/connector registry: PG+FORCE-RLS, 3-tier transform openai_compat/named_provider/custom_field_map, AAD-bound per-tenant creds via Vault get_secret seam, SSRF guard, health/circuit-breaker, super-admin+BYO-key UI; strangler-rewires the 3 existing video/image/LLM abstractions) + VIDEO-STUDIO §9b Video-as-first-consumer. Flag PROVIDER_REGISTRY_ENABLED OFF; earner-safe; serialized vs RAG/Vault/Video.
- 2026-06-14 provider-framework-foundation W1-W3 INTEGRATED VERIFY ✅ ALL GREEN — 4/4 offline suites PASS (test_adapter_fieldmap 16 assertions, test_ssrf_guard 16, test_registry_offline 10, test_reveal_stepup 10). FORCE-RLS live on box: all 3 provider_* tables relrowsecurity=t + relforcerowsecurity=t. health-log append-only trigger present. firewall.py diff = purely additive (339a340,473, 0 deletion lines), box stays cd1ac5d1 (reveal scope NOT deployed, W4 mounts). py_compile OK, gitleaks 0. EARNER GATE: agent.py 9150fabe UNCHANGED, famit-agent PID 1477083 NOT restarted, /health 200, NO ring. design/PROVIDER-FRAMEWORK-PLAN.md W1/W2/W3 rows marked DONE. NEXT = W4 (first caller.py mount, serialize vs RAG LIVE + Vault deferred + Video).
- 2026-06-14 provider-framework W4 MOUNT ✅ DONE + DEPLOYED — built provider_registry/endpoints.py (token-deriving build_router: 16 routes, prefix /provider-registry to avoid colliding with the live legacy @app.get("/providers") LLM-router list) + W4 WRITES in store.py (create/update/delete def + upsert_credential + write_health_row, RLS WITH-CHECK _global write-lock) + mounted in caller.py:7309-7349 under PROVIDER_REGISTRY_ENABLED (default OFF, import+mount-guarded, can never crash startup). Endpoints: GET/POST/PUT/DELETE /provider-registry[/admin]/{id} + /credential + /reveal(-init) + /test + /health — all token-derived tenant (resolve_tenant), super-admin gated by require_super_admin (legacy-pw excluded), RLS via store is_admin=False, SSRF-guard on self_hosted base_url, custom_field_map JSONPath-validated no-eval, reveal via firewall.consume_reveal_step_up single-use jti. Deployed W3 firewall reveal scope WITH the mount. caller.py golden byte-diff = additive only (7309a7310,7349, 0 del/mod, box 52c59291->310ea9c9). FIREWALL golden: deployed b77c2cbe = pristine cd1ac5d1 + ONLY 339a340,475 (0 del/mod); box smoke generic step-up PASS + reveal single-use PASS. 12/12 offline endpoints tests PASS (on box venv python too); full suite 5/5. Backups caller.py/firewall.py.W4bak.20260614-210229. famit-caller restarted only; flag absent from .env => resting byte-identical (/provider-registry*->404 dormant, legacy /providers->401 intact); flag-ON in-process proof mounts 16 routes. EARNER GATE before+after PASS: agent.py 9150fabe UNCHANGED, famit-agent PID 1477083 NOT restarted, /health 200, 0 5xx, NO ring. Log: memory/wave_runs/provider-framework-w4-mount.md. NEXT = W5 strangler VIDEO cut-over (REGISTRY_FOR_VIDEO).
2026-06-14 voice-core-surgery SURGICAL-RC: root cause = engine routing to Sarvam Bulbul v2 (ab6777c), NOT language-pin/SCRIPT-RULE. Minimal restore = .env SARVAM_TTS_MODEL=bulbul:v3 + valid v3 speaker, hi-IN unchanged, keep 69374eb. Earner GREEN.
2026-06-14 · voice-core-surgery · [SURGERY] · ✅ DONE+DEPLOYED (.env-ONLY, aim-voice-agent restarted ONLY, ZERO code) · Applied Option A minimal restore on box /opt/famit-agent/.env: SARVAM_TTS_MODEL=bulbul:v3 + SARVAM_TTS_SPEAKER=priya (were unset → v2/anushka defaults = the broken knob). SARVAM_TTS_LANG NOT set → hi-IN unchanged. PRE-RESTART OBJECTIVE VALIDATION (real Sarvam synth + saarika round-trip STT, capsy venv, keys read at runtime never hardcoded): v2/anushka GARBLED "2 BHK"→"टू उसाई", "Codename Joy"→"हुड नेमो"; v3 (priya/kavya/ishita/shreya/simran all clean) → "बीएचके"/"कोड नेम जॉय"/बुक/कंफर्म correct, ₹2 करोड़ preserved = GARBLING GONE. API gave authoritative v3 speaker list (anushka/manisha/vidya/arya/hitesh = v2-ONLY); chose priya (cleanest brand+numeric round-trip). NOTHING REVERTED — SCRIPT RULE 69374eb KEPT (proven to help v2+v3); MLV mirror + FINAL LANG LOCK + RAG_INJECT_ENABLED + all flags intact. Language-auto per engine: Sarvam has no auto-enum → documented code-mix path IS v3+hi-IN+Devanagari-Hindi/Latin-English input (v3 normalizer passes Latin English through natively); STT saarika language=unknown (auto, unchanged); EL path dormant (Codename Joy is Sarvam-routed) → EL Hindi-voice fix deferred as P1. GOLDEN 5/5 = N/A-by-construction (zero edit to aim_voice_agent.py box md5 1614be09 UNCHANGED + prompt.py fb87ea56 untouched → byte-identical guaranteed). EARNER GATE GREEN before+after: agent.py 9150fabe UNCHANGED, famit-agent PID 1477083 NOT restarted, aim-voice-agent PID 2721961→2739156 (NRestarts=0, registered worker agent_name=manager, 0 errors), famit-caller NOT restarted /health 200, 0 voice 5xx (lone /provider-registry 500 = dormant W4 flag-off, unrelated, predates restart), NO ring. Backup .env.SURGbak.20260614-154923 (md5 1a8ffc07); rollback = cp backup + restart. Box scratch cleaned, no key persisted. ACCEPTANCE = founder real inbound call +918071583488. P1 deferred: Sarvam pronunciation dict + EL premium-tier voice/AIM_TTS_LANG fix. → memory/wave_runs/voice-core-surgery.md (## Phase: SURGERY)
- 2026-06-14 provider-framework W4 LIVE VERIFY + FLAG ON — 6/6 CHECKS PASS. Set PROVIDER_REGISTRY_ENABLED=1 in box .env, restart famit-caller. Bug found+fixed: schema.py from_any did not stringify psycopg2 UUID objects (TypeError in json.dumps) — added str() coercion; deployed schema.py, restarted famil-caller. 6 live checks: routes 401 not 404; create hosted-api def + credential = AAD-encrypted ciphertext in DB no plaintext; SSRF blocks 169.254.169.254 + 127.0.0.1; reveal requires step-up single-use jti (replay 403); cross-tenant RLS mutual isolation PASS; flag-off routes 404 dormant legacy /providers 401 intact flag restored. Earner gate: agent.py 9150fabe UNCHANGED, famit-agent PID 1477083 NOT restarted, /health 200, 0 5xx, NO ring. Test data cleaned. Log: memory/wave_runs/provider-framework-w4-mount.md. NEXT = W5 strangler VIDEO cut-over (REGISTRY_FOR_VIDEO).
- 2026-06-14 · telephony-independence-megaplan · [RESEARCH spam-flag-and-compliance] · DONE · Hard findings persisted to memory/wave_runs/telephony-independence-megaplan.md. VERDICT: personal SIM = illegal for commercial outbound (TRAI TCCCPR 2018 + Feb 2025 amendment, 140-series mandate, 2-yr blacklist on violation); GoIP/SIM-bank = active enforcement target (21L SIMs seized, criminal exposure); carrier AI (Airtel 10L/day) flags behavioral signatures not just number identity; rotation within a registered 140-series DID pool is valid load-distribution but not a spam bypass; correct path = 140-series DIDs on licensed SIP trunk (Exotel/Tata or own VNO), DLT PE+header+template registered, NCPR weekly scrub, per-DID <100 calls/day cap; multi-concurrency = SIP channel licenses not SIM count; trunk registry UI design analogous to provider-registry already built. NEXT = trunk-registry design phase.
- 2026-06-14 · voice-core-surgery VERIFY+FE+DEPLOY · ✅ COMPLETE · (1) SYNTHESIS PASS: Sarvam v3/priya real synth+STT round-trip on-box — "BHK"→"बीएचके", "Codename Joy"→"कोड नेम जॉय", book/confirm/₹2करोड़ all clean; English line clean, Hindi casual clean. v2 garble ("उसाई"/"हुड नेमो") GONE. MLV mirror intact. (2) FE INBOUND TRANSCRIPT FIX DEPLOYED: new page famit-panel/app/ai-manager/sessions/page.tsx (commit 73054f9 fe/unify-run-wavec) — inbound sessions list + TranscriptModal (AI-left/customer-right chat-view) via GET /api/calls/{session_id}/transcript. Full .next built local (node v22.11.0, box OOM-avoidance), synced+atomic-swapped to FORTRESS root@143.110.247.249. panel.famit.in/ai-manager/sessions = 200 via CF edge. /crm + /ai-manager unchanged 200. (3) GOLDEN 5/5 EARNER GATE GREEN: verify_golden.py EARNER GATE PASS 5/5 byte-identical; agent.py md5 9150fabe UNCHANGED; famit-agent PID 1477083 NOT restarted; /health 8209 = 200; 0 5xx NO ring. aim-voice-agent NOT restarted (surgery was prior phase). FOUNDER TEST: call +918071583488 Hindi → natural Hinglish (BHK/Codename Joy clean), transcript visible at panel.famit.in/ai-manager/sessions. Log: memory/wave_runs/voice-core-surgery.md ## Phase: VERIFY+FE+DEPLOY.
2026-06-14 · video-framework-to-studio (W9 VIDEO STUDIO FE / U6) · ✅ BUILT + GREEN (FE-only, NOT deployed) · The crazy Video Studio UI per VIDEO-STUDIO-MASTER-PLAN §10, committed 2d26c98 on fe/unify-run-wavec (15 files, +1799/-16). NEW app/creative/video: page.tsx (two-col studio, useVideoStatus dormancy probe → DormantCard byte-identical) + TierTabs (the signature composite-default-vs-AI-vs-Premium control) + VideoCreatePanel (campaign+tier+ad-ready aspect 9:16/1:1/16:9+count+command+HONEST cost meter $0-gen-key composite vs paid+Sarvam/EL voiceover toggle) + BatchProgress (useBatchPoll → liquid skeletons morph into <video> AssetCards + paid-batch approval gate + collect fires the library bridge) + ByoKeyPicker (per-tenant gen provider over useIntegrations("video_gen")) + UploadClip (local <video> preview, zero-key floor). One-library video support: lib/video.ts NEW (typed /creative/video client, dormant-safe, never logs out) + lib/assets.ts Asset +media_type/duration_s/with_audio/poster_url/outputs/ab_group + isVideoAsset/fmtDuration + listAssets media_type passthrough; AssetImage→AssetMedia split (<video preload=none poster controls>, grid=poster-only+CSS play triangle+duration pill, egress-safe) used by AssetCard/AssetDetail; FilterRail +Video facet; LibraryGallery +Images↔Videos segmented toggle; globals.css +.play-tri+reduced-motion guard; navigation +Video Studio child. Core_2+Inter Display, ZERO raw hex, dormant-safe (404 when FEATURE_VIDEO_STUDIO off). VERIFY: npm run build EXIT 0 (Compiled successfully, 58/58, /creative/video 8.96kB); raw-hex EMPTY; gitleaks staged 0 (81.66KB); tsc 1 PRE-EXISTING err in sibling app/integrations/page.ts (not my files, build tolerates). EARNER UNTOUCHED (FE-only, no box mutation/restart/ring, agent.py 9150fabe never imported). Panel NOT deployed (single deploy after voice-core-surgery, no-panel-deploy-race). NEXT = composite render WORKER (famit-hatchet) + U7-U9 hardening tail. → memory/wave_runs/video-framework-to-studio.md (## W9)
- 2026-06-14 · video-framework-to-studio VERIFY (end-to-end integrated, 2026-06-14) · ✅ ALL ITEMS PASS · W5 PASS (registry_for_video()=False on box, NOT in .env, flag-off resting correct); W6+W7+W8 PASS (video_jobs+video_scripts live in famit_db FORCE-RLS; ai_asset_assets has 8 new video cols; compose.py md5 50119b8c on box; submit_gate gate(compose)=free_floor PASS; gate(fal)=gen:fal paid-class PASS; FEATURE_VIDEO_STUDIO=0 in .env, /creative/video/batches=404 dormant PASS); FE PASS (npm run build exit 0 "Compiled successfully" 58/58 static; tsc 1 PRE-EXISTING error not from this wave; gitleaks 0; zero raw hex); commits PASS (a71b87a+f3d9bd4+49d3165+ef95422+2d26c98 all on fe/unify-run-wavec, confirmed); EARNER GATE PASS (agent.py md5 9150fabe4ff62b4b4470f9a87df346e5 UNCHANGED; famit-agent PID 1477083 NOT restarted; aim-voice-agent 2739156 active; caller /health 8209 = 200; 0 5xx; NO ring). PANEL NOT DEPLOYED — Integrations UI + Video Studio UI ship in SINGLE final deploy after voice-core-surgery wave. COMPOSITE RENDER WORKER is the remaining half (famit-hatchet, compose_worker.enqueue seam). Docs updated: PROVIDER-FRAMEWORK-PLAN.md W5-W9 DONE; VIDEO-STUDIO-MASTER-PLAN.md §14 U1-U6 DONE; NEXT-BIG-BUILDS.md #8b+#9 updated; ORCHESTRATOR.md appended; AGENT_LEARNINGS.md 8-point learnings appended; wave log ## VERIFY section appended.
- 2026-06-15 · telephony-foundation-t1-t2 VERIFY + DOCS · ✅ DONE (per-item PASS/FAIL + earner gate + next-steps) · Per-item results: (1) 3 FORCE-RLS tables=PASS (`relforcerowsecurity=t` all 3, live box `pg_class`); (2) 3 custom triggers=PASS (append-only health + 2 undeletable locks); (3) `is_campaign_eligible` STORED=PASS (`attgenerated='s'`); (4) Vobiz seed un-deletable=PASS (DELETE→0 rows, row intact, count=1); (5) B1 gate=PASS (non-140 `is_campaign_eligible=f`, compliant→t); (6) AAD cred encrypted+roundtrip=PASS (sentinel `__livekit_managed__`, `provider_registry.credentials` decrypt verified); (7) RLS cross-tenant=PASS (`_global` shared-read=1, cross-tenant private=0, `_global` write=BLOCKED); (8) red-team-D delete refuse=PASS (DELETE silently blocked, row intact); (9) 31/31 offline suites=PASS (9/9 registry + 8/8 concurrency + 14/14 rotation/livekit); (10) provider_registry suites still green=PASS (5/5); (11) B1 gate in code TWICE=PASS (store filter `campaign_eligible_only` + `_is_campaign_eligible` re-check in `registry.get_trunk`); (12) AAD cross-tenant→InvalidTag=PASS (tested in test_registry_offline suite); (13) in-process counter try/finally no-oversell=PASS (test_concurrency_offline suite); (14) velocity throttle=PASS (concurrency suite); (15) ring-out-burst quarantine+B3 disable+alert=PASS (test_rotation_livekit_offline suite); (16) red-team-D delete refusal (code)=PASS (livekit_sync is_protected_trunk_id); (17) gitleaks 0=PASS; (18) py_compile clean=PASS. EARNER GATE ALL PASS: agent.py md5 `9150fabe4ff62b4b4470f9a87df346e5` UNCHANGED · famit-agent MainPID `1477083` active NRestarts=0 NOT restarted · /health 200 · 0 5xx · NO ring. DOCS UPDATED: TELEPHONY-INDEPENDENCE-PLAN.md T1+T2 rows marked DONE; NEXT-BIG-BUILDS.md roadmap updated; ORCHESTRATOR.md telephony section appended; AGENT_LEARNINGS.md learnings (m)-(s) appended. NEXT STEPS: T0 (scheduler_loop retry-bug fix in caller.py — HARD GATE before enabling rotation, fix that the queued retry re-fires terminal-outcome numbers); T3 (additive `/trunk-registry/*` + `/trunks/byo/*` mount in caller.py, flag OFF, serialize vs the video wave); T4 (Telephony FE, Settings › Telephony page, Core_2 port, FORTRESS deploy); T5 (strangler `TRUNK_REGISTRY_ENABLED` flag-ON + real founder outbound-ring smoke before+after — the ONLY integration truth). Founder buy list still open: (1) 140-series DID + DLT registration [non-negotiable, ~1-2wk, Airtel Business/Tata Tele]; (2) Plivo 2nd trunk.

- 2026-06-15 | Telephony T3 (trunk_registry mount) DEPLOYED flag-OFF | NEW trunk_registry/endpoints.py (16 routes, build_router twin of provider-registry) + additive caller.py:7349-7393 (0-del/45-add, box ef9ae696→44b867ea); FORTRESS deploy famit-caller ONLY; flag-OFF 404 dormant + flag-ON 401 mount-proof (0 5xx) then reverted; 31/31 offline PASS; gitleaks 0; EARNER GATE PASS (agent 9150fabe UNCHANGED, PID 1477083 NOT restarted, /health 200, NO ring). branch fe/unify-run-wavec.
| 2026-06-15 | ship-ui-and-telephony-t3 VERIFY | PASS | Panel 8/8 pages 200 (BUILD_ID u6yKGIuhALhhzdzQcywXQ); T3 flag-OFF=404 dormant + flag-ON=401 auth-gated; earner gate PASS (agent 9150fabe PID 1477083 NRestarts=0 unchanged); docs updated |
- 2026-06-15 · goldmine-queue-survey · READ-ONLY · DONE · Prioritized 12-item gold-mine build queue written to `caps/GOLDMINE-QUEUE.md`. Top-3 parallel-launch-NOW: (A) eval/replay harness #44, (B) dormant-flag activation CTX_CACHE+INBOUND_PROV_LOCK, (C) inbound never-silent apology guard #29. T0 (scheduler retry-bug fix) = HARD GATE before any campaign resume or telephony rotation. Next caller.py wave after T0: #33 DPDP + #35 lead_is_hot + #37 post-call event + #30 recording Egress.
- 2026-06-15 · communication-omnichannel-megaplan (DESIGN, READ-ONLY) · ✅ DONE · Synthesized communication/COMMUNICATION-MASTER-PLAN.md + communication/README.md from _RESEARCH-LOG.md (6 arch phases: channel-registry/telegram/email-sms/automation-engine/llm-brain/ui/data-security + 7 red-teams: deliverability/security/cost-scale/earner-safety/compliance/over-engineering + completeness critic). Unified Communication tab (Telegram+Email+SMS) mirroring+exceeding WhatsApp. Channel registry = the LIVE provider registry (1 cred row + 1 adapter = a channel, zero new crypto). MVP-first/Telegram-first: W1 = 3 tables + Telegram-only + founder hot-lead alert + post-call auto-summary + setup UI (~2wk). Red-team fixes folded: _finalize_call awaited in the live dial loop → create_task+snapshot+timeout NEVER await; no _wa_draft_followup_text refactor in channel wave; webhook FAIL-CLOSED + secret-bound-to-path-tenant; per-tenant DEK (HKDF); signed single-use deep-links; server-side compliance gates; consent=(channel×purpose) w/ consent_basis from lead_source; per-message metering (NO wallet.debit) + per-tenant budget/frequency/anomaly guards; segment-accurate SMS pricing. 6 ship-gating security probes (T-WEBHOOK/T-INJECT/T-LEAK/T-VAULT/T-DEEPLINK/T-GATE). Roadmap W1-W6 each scope/files/flags/acceptance/rollback. Founder action: 2-min BotFather token (W1, no verification, free) → test on real phone; Resend key (W3); MSG91+DLT (W5, 5-10d). NO box mutated; agent.py untouched. Appended NEXT-BIG-BUILDS.md + ORCHESTRATOR.md.
- VIDEO-STUDIO-ACTIVATE-REAL (2026-06-15): flipped FEATURE_VIDEO_STUDIO/COMPOSE + VIDEO_PROVIDER=compose (caller) + FEATURE_VIDEO_LIBRARY + shared AIASSET_SERVICE_TOKEN (caller+aiasset); installed ffmpeg 6.1.1; restarted famit-aiasset+famit-caller (earner agent.py 9150fabe UNTOUCHED). Studio REAL+USABLE: routes mounted (authed /campaigns 200), real FE studio renders (no DormantCard), scripts generate, batch/approve/library-bridge verified, panel build green (tsc 0 / npm build 0). REMAINING: famit-hatchet compose_worker for the actual MP4 render (separate wave). Log: memory/wave_runs/video-studio-activate-real.md.
- 2026-06-15 · goldmine-quickwins (#29 never-silent + CTX_CACHE) · ✅ DONE (zero box mutation — both already shipped+active) · VERIFIED live state (resolves systemd-vs-.env confusion): CTX_CACHE=1 + INBOUND_PROV_LOCK=1 + VENDOR_SCRIPT_INJECT=1 live in the systemd drop-in `aim-voice-agent.service.d/vendor-script.conf` (NOT .env) and confirmed in the running worker `/proc/2739156/environ`. #29 NEVER-SILENT GUARD already present in box golden `aim_voice_agent.py` md5 1614be09: outer entrypoint guard :2127-2147 (apology+hangup via ctx._aim_session :2498) + per-turn `@session.on("error")` recovery line :2508/:2521. CTX_CACHE reader already wired in `ai_manager/voice_tools.py` (:37 import, :449 loader, :472-481 fast-path; falls back to HTTP on miss/flag-off); LIVE runtime probe on box venv PROVED active: is_enabled=True, COLD 56.9ms→WARM 0.205ms (loader did not re-run, ~277x), Redis :6380 version-stamp invalidation works. No restart needed (flag already on running worker). INBOUND_PROV_LOCK left exactly as-is (=1, bulbul voice working). EARNER GATE GREEN: agent.py 9150fabe UNCHANGED, famit-agent PID 2808658 + aim-voice-agent PID 2739156 NOT restarted by me, caller /health 200, 0 5xx, NO ring. Log: memory/wave_runs/goldmine-quickwins.md.
- 2026-06-15 · VIDEO-STUDIO-ACTIVATE-REAL Phase 3+4 (the real MP4) · ✅ DONE · Built the missing render executor `media_gen/video/compose_worker.py` (deployed to box, py_compile OK) — the other half of compose.py: Sarvam-TTS VO → script-paced burned ASS captions → product/slate visual → build_ffmpeg_argv (no-shell) → poster → Spaces → store.update(succeeded) → ai_asset library bridge. `enqueue()` forks a FULLY-DETACHED out-of-band process (start_new_session, never inline, never touches agent.py) so a founder "Generate" auto-renders. REAL MP4 PROVEN: 2 clips rendered end-to-end (Codename Joy 3.0 12s / DLF The Crest 10s) — ffprobe h264 1080x1920 + aac 22050Hz audio, exact duration, in the live library at GET /assets?media_type=video with presigned playable URLs. Edge: panel.famit.in/creative/video=200, /api/creative/video/campaigns=401 (mounted, real studio renders not DormantCard). EARNER GATE PASS: agent.py 9150fabe UNCHANGED, famit-agent PID 2808658 NRestarts=0 NOT restarted, aim-voice-agent 2739156 untouched, caller/health 200, aiasset/status 200, 0 5xx, NO ring. Only famit-caller restarted (allowed). Flag VIDEO_COMPOSE_SPAWN default ON. Rollback: rm compose_worker.py. Log: memory/wave_runs/video-studio-activate-real.md (Phase 3+4); VIDEO-STUDIO-MASTER-PLAN U5 marked DONE+RENDER-WORKER-LIVE + header LIVE+USABLE.
- 2026-06-15 · COMM-W3-COSTGUARDS (the 6 cost guards, master plan §6) · ✅ BUILT OFF (flag-gated, offline-green) · NO caller.py edit (guards live inside the W1-P2-mounted comm.engine.send seam), NO agent.py import. All 6 PROVEN PASS: #1 per-message metering via the LIVE wallet reserve->settle/release (release-on-failure never bills; NO wallet.debit, AST-proven) `comm/metering.py`; #2 per-tenant daily budget ceiling (free TG always flows, metered over-cap -> blocked_budget) + #3 per-(contact,channel)/day frequency cap (blocked_frequency) + #4 spend-anomaly detector (today > 3x trailing-7d median, founder priority-alert once/day) + #5 per-(identity,channel) deliverability (403 -> dead -> next send blocked, adapter never called) `comm/cost_guards.py`; #6 per-bot async token-bucket (30/s global + 1/s per-chat shared budget, founder-alert PRIORITY LANE bypasses a drained global, bounded wait never hangs) `comm/token_bucket.py`. 3 NEW FORCE-RLS tables `communication/db/ddl_comm_cost.sql` (comm_daily_spend/comm_freq_counter/comm_deliverability, additive). All guards permissive-on-fault (PG down -> allow); resting byte-identical (3 master flags OFF). 10/10 comm offline suites PASS (8 prior + 2 new) zero regression; gitleaks 0; py_compile clean. LIVE flip (DDL apply + flags ON + earner gate under induced Telegram outage) = founder-gated, same recipe as W1-P2/P3. Log: communication/_BUILD-LOG.md (W3-COSTGUARDS).
- 2026-06-15 · COMM-W2+W3-LIVE-DEPLOY (brain + 6 cost guards live for the founder tenant) · ✅ DEPLOYED + VERIFIED (BE only; panel deploy still deferred) · NO caller.py edit (W1-P2 mount + W1-P3 hook already route through the mounted comm package; box golden caller.py ccf9715b UNCHANGED, no lock needed), NO agent.py touch. Deployed 13 comm files (7 NEW brain/cost_guards/deeplink/lang/metering/ratelimit/token_bucket + 6 CHANGED config/endpoints/engine/founder_alert/webhook/__init__) md5-verified == local; applied ddl_comm_cost.sql (3 NEW FORCE-RLS tables, total 7 comm tables FORCE-RLS); flipped ON COMM_BRAIN_ENABLED + COMM_COST_GUARDS_ENABLED + COMM_METERING_ENABLED + COMM_TOKEN_BUCKET_ENABLED for tenant admin (.env bak .COMMW2W3bak.20260614-214442, comm bak comm.COMMW2W3bak.20260614-214207); restarted famit-caller ONLY (PID 2846741). LIVE PROOF: GET /comm/channels 200 configured:true all flags live; getMe 200 mr_kunal_bot (token decrypts via live vault); webhook no-secret 403 fail-closed. REAL-MESSAGE: engine.send reaches api.telegram.org for real -> http_400 chat-not-found (whole pipeline LIVE) — only missing the founder chat_id (getUpdates=0, founder hasn't tapped @mr_kunal_bot in 24h; sentinel correctly absent, never fabricated; alert no-ops no_founder_chat_id, never blocks). Webhook deliberately NOT set (caller firewalled to panel box only, no public ingress -> would deaf-bot; stay getUpdates mode). BRAIN PROVEN LIVE: real inbound through /comm/webhook/telegram/admin (valid HMAC secret -> 200 stored) -> Groq llama-3.3-70b grounded Hinglish reply ("haan ji, main aapko EMI breakup bhejne wali thi…", action=replied); cost-guard #5 deliverability proven live (retry -> blocked_dead). EARNER GATE under INDUCED telegram black-hole: hot-path snapshot 0.017ms + create_task 0.016ms, detached run bounded 0.00s (fresh-dest 0.75s ≪ 8s cap), agent.py 9150fabe UNCHANGED, famit-agent PID 2808658 NRestarts=0 NOT restarted, caller /health 200, 0 5xx, NO ring; black-hole removed 0 residual. FOUNDER ACTION (the only blocker to a landed real message): tap @mr_kunal_bot once (_HUMAN_TASKS.md #1). Log: communication/_BUILD-LOG.md (## W2+W3 LIVE DEPLOY).
- 2026-06-15 · LEADS-MGMT-FEATURE (delete-all + multi-select/per-row delete + sort filter on Leads + RUN manual-pick sort) · ✅ BE LIVE+VERIFIED / ⏳ panel deploy pending SSH · FE committed `2c5dc1e` (app/leads/page.tsx full: sort Select recent/oldest/name/status/score, multi-select checkboxes + select-all, bulk-delete toolbar, per-row hover-trash, delete-all type-to-confirm Modal; app/run/page.tsx PICK_SORTS + client-side sortLeads over pickerRows; lib/api.ts deleteLead/deleteLeadsBulk/deleteAllLeads + getLeads sort). BE golden caller.py.LIVEBOX.py DEPLOYED to box md5 ccf9715b→32e6062f (md5-gated, py_compile OK, additive: GET /leads full sort default=recent; +POST /leads/delete by-ids BOLA/idempotent/tenant-scoped; +DELETE /leads?confirm=DELETE delete-all STRICT tenant_id scope never cross-tenant) — famit-caller restarted ONLY. BE VERIFIED over loopback (SAFE, total stayed 30 — no real wipe): no-auth=401, sort recent(2026-06-15)≠oldest(2026-06-03), DELETE w/o confirm=400, bulk empty/bogus ids→deleted=0. EARNER GATE PASS before+after: agent.py 9150fabe UNCHANGED, famit-agent PID 2808658 NOT restarted, /health 200, 0 5xx, NO ring. Build green tsc 0 + npm build 0. Panel tarball uploaded to FORTRESS (md5 801ea5b6 matched); panel deploy BLOCKED on intermittent SSH-to-panel-box timeout (edge=200). Rollback: BE `cp /opt/famit-agent/caller.py.leadsmgmtbak.20260615-174918 → caller.py; systemctl restart famit-caller`. Log: memory/wave_runs/leads-mgmt-feature.md.
- 2026-06-15 · LEADS-MGMT-FEATURE · ✅ UPDATE: panel now LIVE on edge. Panel deployed to FORTRESS (on-box npm install --legacy-peer-deps + build green, .next swap + famit-panel restart). BUILD_ID u6yKGIuhALhhzdzQcywXQ→xF8YUvBmTwYj_yP4w7WY4; edge panel.famit.in/leads=200 + /run=200 + new BUILD_ID present in served HTML (live, not cached). FINAL EARNER GATE: agent.py 9150fabe UNCHANGED, famit-agent PID 2808658 NRestarts=0, /health 200, 0 caller 5xx/12min. WHOLE FEATURE FULLY LIVE + VERIFIED. Panel backup .next.leadsmgmtbak.20260615-124143.
- 2026-06-18 · W1-KERNEL-CORE VERIFY (RealtimeVoiceKernel v2 core + 9 service contracts) · ✅ DONE (commit `718f569` on branch `fix/realtime-voice-kernel-v2`; kernel core landed earlier in `a1a3c58`, this commit adds `design/W1-KERNEL-ARCH.md` + VERIFY log) · `python -m pytest voice_kernel/` = **50 passed / 0 failed** (no tests weakened); `test_adapter_off_identity` ran for REAL (NOT skipped) 10/10 PASSED — flag-OFF kernel render is byte-identical to the actual `droplet_work/prompt.py build_system_prompt`. Flag-OFF parity PROVEN: `KERNEL_ENABLED`+`KERNEL_INBOUND` default OFF, `import voice_kernel` pulls 0 `droplet_work`/`agent` modules + 0 third-party deps (stdlib-only core) — the only agent.py/droplet_work refs in the package are comments + NEGATIVE test assertions. EARNER GATE: `droplet_work/agent.py` byte-identical vs HEAD (`git diff --quiet` clean, untouched by this build; tree md5 98655dbf is the branch-baseline snapshot, EARNER-LAW 9150fabe is the box-truth md5 — local disk agent.py is stale per LEARNINGS §1, NOT restored). gitleaks: `protect --staged`=0 leaks + `detect` over voice_kernel/ (321KB) + design doc = 0 leaks + pre-commit hook clean. Staged ONLY the 3 required paths (never `git add -A`). CONTRACT SURFACE NOW FROZEN for W2-W8: `ContextPacket`+6 layers+`TokenBudget` and the 9 `@runtime_checkable` Protocols (ContextEngine W1/W3 · VendorScriptEngine W3 · BrainPackProvider W2 · RagRuntime W4 · SpeechPlanner W5 · ProviderRouter W5 · MemoryService W7 · EventBus W8 · DialoguePolicy W6) + `build_kernel()`/`KernelServices`/`null_impls`; each downstream wave implements ONE Protocol and registers via `build_kernel(cfg, <name>=impl)`. Log: memory/wave_runs/W1-kernel-core.md (## Phase: VERIFY).
- 2026-06-18 · W1.5-CONTRACT-AMENDMENT VERIFY (C2 tenant-identity + C3 trust-fence + H13 cache/retrieval) · ✅ DONE (commit `05cb505` on branch `fix/realtime-voice-kernel-v2`; the AMEND was committed by a prior session, this VERIFY phase re-ran every gate against that tree — no re-commit, no test weakened, crash-safe RESUME) · `python -m pytest voice_kernel/` = **67 passed / 0 failed** (+17 vs W1's 50); `test_adapter_off_identity` ran for REAL (NOT skipped) **10/10 PASSED** (outbound+inbound × 5 field-sets) — the flag-OFF byte-identity invariant survived the amendment. EARNER LAW HELD: `droplet_work/agent.py` byte-identical vs HEAD (`git diff --quiet` clean), NOT in the `05cb505` file list, tree md5 `98655dbfc71d5c3da36bcfe3f848082c` == box truth; `import voice_kernel` from a clean interpreter pulls **0** `agent`/`droplet_work` modules (146 new, 0 leaked). gitleaks: `detect --no-git` over voice_kernel/ (≈416KB)=0 + `protect --staged`=0 + diff scan `05cb505~1..05cb505` (37.8KB)=0. Staged ONLY the 3 required paths (never `git add -A`). **FINAL FROZEN PUBLIC SURFACE for W2/W3/W4/W5/W7/W8** (= W1's 9 `@runtime_checkable` Protocols + ContextPacket/6 layers/TokenBudget + build_kernel/KernelServices) NOW PLUS the W1.5 additions: C2 `KernelSession`+`CallContext.session`+`TenantIdentityError` (ON path fail-closed via `_require_session`); C3 `SourceTrust`{PLATFORM,CAMPAIGN_BRIEF,RETRIEVED_KNOWLEDGE,LEAD_MEMORY,CALLER_UTTERANCE}+`FencedText`+`fence()`; H13 `ContextPacket.render_cache_split()->(stable,volatile)` + `CampaignCard.full_product_summary/full_usps/summary_overflow/usps_overflow`. Log: memory/wave_runs/W1-kernel-core.md (## Phase: VERIFY (W1.5)).
- 2026-06-18 · W4-RAG VERIFY+COMMIT (reconcile) · ✅ DONE · StageAwareRagRuntime (4 stores, hot-cache, fenced, degrade-to-empty) + B1 fence-escape blocker CLOSED at packet.FencedText.render choke point. Full rag package + fix already in HEAD via concurrent session (commits 6fa3f09 fence-fix, bbd3442 precompute-warm). Verified CLEAN HEAD: test_rag_runtime 22/22, test_redteam_injection 9/9, test_adapter_off_identity 12/12 (>=10 req), 0 droplet-imports leaked, gitleaks 0. EARNER LAW: agent.py md5 98655dbf UNCHANGED. The 4 in-suite ..._pulls_no_droplet_modules FAILs = PRE-EXISTING memory-suite sys.modules pollution (green per-file in clean process), not a W4 defect. → memory/wave_runs/W4-rag.md (## VERIFY+COMMIT)
- 2026-06-18 · W5-SPEECH+PROVIDER VERIFY+COMMIT (Speech Planner + fail-loud Provider Router + Sarvam-silence seam) · ✅ DONE (commit folds the seam note + wave log; W5 modules already in HEAD via prior session + red-team fold `2cb06ac`) · RECONCILE-FIRST: `voice_kernel/speech/` (normalize/hinglish/segment/prosody/planner) + `voice_kernel/providers/` (keypool/router) already committed; this VERIFY re-ran all gates. `python -m pytest voice_kernel/` = **212 passed / 0 failed** (was 178 at red-team time, suite grew, none weakened; the 7 context/ Stage.PITCH fails the red-team flagged are resolved in HEAD); W5 subset 55 passed; `test_adapter_off_identity` ran for REAL **12/12 PASSED** (flag-OFF byte-identity intact, default build_kernel binds Null impls). EARNER LAW: `droplet_work/agent.py` md5 `98655dbfc71d5c3da36bcfe3f848082c` UNCHANGED; `aim_voice_agent.py`/`caller.py` NOT edited; `git status droplet_work/` clean. 0 leaked imports (no executable `import droplet_work.agent`/`.caller`/`aim_voice_agent` in voice_kernel/ — 3 hits are comments/isolated-load docstring). gitleaks `protect --staged`=0 + detect on staged docs=0. Staged ONLY design/W5-SARVAM-AND-SPEECH-SEAM.md + memory/wave_runs/W5-speech-provider.md + WORKFLOW_LEDGER.md (never `git add -A`). FROZEN W5 SURFACE: `SpeechPlanner.plan` (sync HOT-path, FAIL-OPEN, →SpeechPlan) = DefaultSpeechPlanner; `ProviderRouter` (fail-LOUD, never silent EL swap, ProviderDiagnostics.silent_swap, SARVAM_WS_CONTRACT, →ProviderChoice) = DefaultProviderRouter w/ health-scored keypool; both register via `build_kernel(cfg, speech=, provider_router=)`. Red-team blockers folded (`2cb06ac`): half-word survival, paise/decimal drop, bare 4-7 digit leak to normalizer-OFF TTS, Devanagari substring corruption. Seam note = the LATER flag-gated cutover (INBOUND_PROV_LOCK Sarvam-silence aim_voice_agent.py:2437/2446/424-439/451, GROQ_MAX_TOKENS raise agent.py:617, semantic turn-detection) w/ file:line + one-env reverts. Log: memory/wave_runs/W5-speech-provider.md (## Phase: VERIFY+COMMIT).
- 2026-06-18 · W2-BRAIN-PACKS VERIFY+COMMIT (cross-vertical swappable use-case + industry packs binding BrainPackProvider; compliant structural disclosure; versioned RenderBrain) · ✅ DONE (package already in HEAD via `39304ba` BUILD + `ce5eddc` red-team + `637bb86` order-independent isolation test + `6db7dcf` disclosure `build_structural_identity`; this commit folds the W2 VERIFY phase + ledger — no test weakened, RECONCILE-FIRST crash-safe) · `python -m pytest voice_kernel/` = **212 passed / 0 failed**; `test_adapter_off_identity` ran for REAL (NOT skipped) **12/12 PASSED** (>=10 req) — flag-OFF byte-identity intact. CROSS-VERTICAL PROVEN: L1 use-case packs are abstract behavior, ALL vertical vocabulary lives ONLY in L2 IndustryPacks (default NEUTRAL_INDUSTRY empty — no RE-vocab leaks into insurance/support/neutral); `Stance.pushes_sale=False` (support/complaint/feedback/after_sales/reminder/onboarding/booking/inbound/ai_manager) drops the sales-advance directive AND (red-team fix `ce5eddc` `hooks_for(use_case)`) the price/competitor/urgency objection hooks — revenue modes (sales/renewal) keep the full menu; regression-locked by `test_service_modes_objective_carries_no_sales_coaching_hook`. FOUNDER LAWS HELD: no hardcoded campaign content/words (campaign `fields["goal"]` LAYERED IN, never replaced), behavior not scripts, casual Hinglish not literary Hindi. DISCLOSURE structural (rendered in L0 above the C3 fence, vendor cannot weaken; banned-phrase block-list scanned; `build_structural_identity` seals the injection break-out at the render choke point). EARNER LAW: `droplet_work/agent.py` md5 `98655dbfc71d5c3da36bcfe3f848082c` UNCHANGED (`git diff --quiet` clean, NOT edited/imported); caller.py / aim_voice_agent.py NOT touched; `import voice_kernel`+brain_packs+contracts pulls 0 agent/droplet_work/caller modules. gitleaks `detect --no-git` over voice_kernel/brain_packs (164 KB) = 0 + `protect --staged` = 0. Staged ONLY memory/wave_runs/W2-brain-packs.md + WORKFLOW_LEDGER.md (brain_packs already committed; never `git add -A`). FROZEN W2 SURFACE: `BrainPackProvider` Protocol (use_case_layer + industry_layer) = `build_brain_packs()` (+ optional `JsonBrainPackStore`), registers via `build_kernel(cfg, brain_packs=impl)`; null_impls exist. Log: memory/wave_runs/W2-brain-packs.md (## Phase: VERIFY+COMMIT).
- 2026-06-18 · W3-CONTEXT VERIFY+COMMIT (ContextEngine + AUTHORITATIVE vendor-script flow + dual-layer campaign compiler; red-team blockers folded) · ✅ DONE (HEAD `fe52bf6` branch `fix/realtime-voice-kernel-v2`; the `context_engine.py` blocker fix + my 3 proving files were swept into a concurrent W2/W4 commit via the shared index — RECONCILE-FIRST per worktree rules, content verified correct in history, no test weakened) · `python -m pytest voice_kernel/` = **212 passed / 0 failed**; W3 subset `test_w3_context.py` = **18 passed** (15 original + 3 new RED-TEAM blocker tests); `test_adapter_off_identity` flag-OFF byte-identity intact. **CLAIM (a) vendor-script-AUTHORITATIVE now TRUE end-to-end** (was HALF-true): `_apply_vendor_overrides` folds the WHOLE ordered flow into the card's NATURAL slots — GREET+PERMISSION+INTRO→talking_points, QUALIFY→qualifying_questions, OBJECTION→objections (leading Objection), CLOSE→closing_lines — so the back half of the vendor blueprint reaches `render_stable_prefix()` (BLOCKER 1 closed; proven by `2BHK`/`metro ke paas`/`EOI offer`/`site visit book` asserted in the assembled prompt). `_merge_unique` keeps vendor flow FIRST then the vendor's own authored card content WITHOUT eviction + dedupes (BLOCKER 2 + duplicate-opener quality bug closed); `packet.clamp()` applies the final per-field cap so the authoritative head survives. **CLAIM (b) full brief lossless** = PASSES (full_product_summary/full_usps/overflow flags). C3 HELD: every fold lands inside the CAMPAIGN_BRIEF fence below the PLATFORM safety layer — authoritative-on-flow ≠ authoritative-over-safety. Per-turn `stage_excerpt`/`full_blueprint` left as an OPTIONAL future L5 token-saver (NOT on the critical path; design note records the C3 rule it must follow). EARNER LAW: `droplet_work/agent.py` md5 `98655dbf` UNCHANGED; caller.py / aim_voice_agent.py NOT edited; **0** real `droplet_work` import lines in `voice_kernel/context/`. gitleaks `protect --staged` = 0. Staged ONLY my W3 paths (never `git add -A`; unstaged the concurrent W7/W2 files that kept appearing in the shared index). Log: memory/wave_runs/W3-context-vendor-campaign.md (## Phase: VERIFY) + design/W3-INTEGRATION-SEAM.md (## VERIFY red-team note).
- 2026-06-18 · W7-LEAD-MEMORY VERIFY+COMMIT (MemoryService L4: lead-centric hot/warm/cold/dead lifecycle + structured salient extraction + AI summary card + next-best-action + conversation continuity + FORCE-RLS schema + right-to-erasure; red-team SHIP verdict folded) · ✅ DONE (commit `4733cd7` on branch `fix/realtime-voice-kernel-v2`; the W7 module bulk + seam doc + wave-log landed earlier in `fe52bf6` via the shared index, this commit folds the 2 red-team hardening fixes + VERIFY log — RECONCILE-FIRST, no test weakened) · `python -m pytest voice_kernel/` = **212 passed / 0 failed** (27 in memory; the erasure-cascade + forged-tenant-rejected-by-WITH-CHECK + tenant-blast-radius tests pass under the new explicit-predicate DELETEs); `test_adapter_off_identity` ran for REAL (NOT skipped) **12/12 PASSED** — flag-OFF byte-identity intact. **RED-TEAM = SHIP** (multi-tenant isolation sound: FORCE-RLS USING+WITH-CHECK on `tenant_id = current_setting('app.tenant_id')` byte-identical to the live wallet/`db/rls.sql` pattern; scope from `asession(tenant_id=…)` SET-LOCAL GUC never a body value; load/persist carry redundant `tenant_id = :t`; fail-closed on unset GUC; erasure cascades head+history+every registered Purgeable). **2 minor hardening notes folded** (neither a blocker): **S1** erasure DELETEs gained an explicit `tenant_id = :t` seatbelt (`_LEAD_DELETES`/`_TENANT_DELETES`) so a bare `DELETE FROM lead_memory` can never wipe all tenants even in an RLS misconfig — consistent with load/persist's defense-in-depth (RLS-fake branch detection updated to the `lead_phone` token since the SQL now leads with `tenant_id`); **S2** the conversion_prob side-table switched from `dict[int,int]` keyed by `id(mem)` to a `WeakKeyDictionary[LeadMemory,int]` keyed by the object, eliminating `id()` reuse mis-attribution (LeadMemory is a non-slotted frozen dataclass → weak-referenceable; `prob_for` API unchanged). EARNER LAW: `droplet_work/agent.py` md5 `98655dbf` UNCHANGED; `caller.py`/`aim_voice_agent.py` NOT edited; **0** real `droplet_work.(agent|caller)`/`aim_voice_agent` import lines in `voice_kernel/` (only the lazy `droplet_work.db.engine.asession` proven RLS shim + docstrings/comments/negative-test regexes). DDL off the Alembic chain; live splice DEFERRED to the flag-gated cutover (`design/W7-MEMORY-SEAM.md`: post-call WRITE seam `aim_voice_agent.py` hangup hooks → `extract_and_persist` under `KERNEL_MEMORY_ENABLED` default OFF; next-call RECAP seam → kernel `enrich_prefix`/`continuity_opener_hint` replacing the lossy legacy recap; erasure seam behind `require_super_admin`). gitleaks `protect --staged` = 0 + pre-commit hook clean. Staged ONLY voice_kernel/memory/{erasure,extraction}.py + tests/fakes.py + memory/wave_runs/W7-lead-memory.md (never `git add -A`). FROZEN W7 SURFACE: `MemoryService` Protocol (async, PG/RLS) = `LeadMemoryService` (load/persist/erase + summary_card), registers via `build_kernel(cfg, memory=impl)`; `LeadMemory` L4 + `Purgeable` erasure contract. Log: memory/wave_runs/W7-lead-memory.md (## Phase: VERIFY).
- 2026-06-18 · W-INT-INBOUND VERIFY+COMMIT (inbound integration façade wiring W2–W7 kernel into aim_voice_agent via one tracked module + a minimal flag-gated doc-only hook; red-team SHIP folded) · ✅ DONE (BUILD already in HEAD as `e9f569d` = `voice_kernel/integrations/inbound.py` + 28 tests + design/W-INT-INBOUND-{PLAN,PATCH}.md + wave log + systemd drop-in; this VERIFY re-ran every gate + folded red-team note #1 in commit `06397f1`, branch `fix/realtime-voice-kernel-v2` — RECONCILE-FIRST, no test weakened) · `python -m pytest voice_kernel/` = **240 passed / 0 failed** (212 prior + 28 integration); OFF byte-identity proven against the REAL `droplet_work/prompt.py` (`enabled_for("inbound")` False w/ no env ⇒ `build_for_call`→None⇒legacy); 43 targeted off/identical/tenant_mismatch/cross_tenant/import tests pass. **RED-TEAM = SHIP**; the ONE actionable note (#1: Patch B passed `campaign_tenant_id=tenant_id` = the same server tenant twice, disarming `session.assert_matches_campaign(...)` to a no-op) FOLDED in `06397f1` — patch now passes the campaign record's REAL owner (`contact.get("tenant_id")`, :2178) so the C2 cross-check is ARMED (a contact whose campaign belongs to tenant B can never be served under server tenant A; kernel fail-closes→None→legacy). Module was always correct (accepts `campaign_tenant_id` as a distinct param; armed behavior proven by `test_on_tenant_mismatch_disengages_no_cross_tenant`). Note #2 (ON-path import latency, NOT OFF) deferred to deploy — not a blocker. EARNER LAW HELD: outbound `droplet_work/agent.py` md5 `98655dbfc71d5c3da36bcfe3f848082c` UNCHANGED (re-checked post-edit), NOT edited/imported/restarted; local inbound `aim_voice_agent.py` has **0** kernel refs (doc-only hook honored, gitignored=not committed); `import voice_kernel.integrations.inbound` pulls **0** droplet/agent/aim_voice_agent modules (22 voice_kernel, 0 leaks). gitleaks `protect --staged`=0. Staged ONLY design/W-INT-INBOUND-PATCH.md (never `git add -A`). KERNEL_INBOUND default OFF ⇒ inbound byte-identical to the `1614be09` golden. NO box deploy (founder-gated). DEPLOY-READY: golden `1614be09` + §2 patch hunks (armed C2) + ship tracked `voice_kernel/integrations/` to box venv (inert w/o flag); order flag-OFF smoke→flag-ON LEAN/STANDARD canary; restart ONLY `aim-voice-agent`; rollback `KERNEL_INBOUND=0`+restart. Log: memory/wave_runs/W-INT-INBOUND.md (## Phase: VERIFY+COMMIT).
- 2026-06-18 · W-DEPLOY-INBOUND FLAG-ON CANARY (live deploy of the voice-kernel inbound brain to the box; `KERNEL_INBOUND` 0→1) · ✅ SUCCESS — **flag left ON for the founder ring-test** · Box famit@168.144.153.145, service `aim-voice-agent` (inbound `agent_name="manager"`, :8091) ONLY. **EARNER UNTOUCHED, ZERO DRIFT:** outbound `agent.py` md5 `98655dbfc71d5c3da36bcfe3f848082c` IDENTICAL before+after; `famit-agent` active (MainPID 3112900, :8090) + caller `/health`=200 (uvicorn :8209/:8208) before+after; **NO outbound ring**, agent.py never edited/imported/restarted. FLAG seam = systemd drop-in `/etc/systemd/system/aim-voice-agent.service.d/kernel-inbound.conf` (`[Service] Environment=KERNEL_INBOUND=1`) — NEVER in the shared `.env` (would leak to earner); `daemon-reload`+restart ONLY `aim-voice-agent`; running env (MainPID 3954188, worker `AW_LPfSopkFx6cJ`) confirms `KERNEL_INBOUND=1`+`VENDOR_SCRIPT_INJECT=1`+`CTX_CACHE=1`+`INBOUND_PROV_LOCK=1`. **SYNTHETIC CANARY (no telephony) = PASS:** per-call kernel assembled a **3632-char** inbound prompt (NOT legacy_render); no "AI assistant" phrase (W2 structural-identity present: "ek warm insaan ki tarah, kabhi AI ya assistant kehkar nahi"); vendor-script content authoritative in the prompt; W5 ProviderRouter TTS resolved `lean=sarvam`+`standard=sarvam`; `errors=[]`. Sarvam audio synth soft-failed = EXPECTED/NON-GATING (LiveKit Sarvam plugin needs the worker http/job context; kernel owns only the routing DECISION = proven sarvam; real bytes come from the live worker on the founder's actual call). Journal clean: all 4 workers `process initialized`+`AIM prewarm: Silero VAD loaded`, PG sync+async up, inference executor up, HTTP :8091; the `exit 255` lines = OLD generation (PID 3953385) torn down on restart, not a flag-ON error. Temp canary files removed. **INBOUND DID for founder = +918071583488** (confirmed on box: agent header routes this DID → `agent_name="manager"`; trunk `ST_bpGqmc9TL9Ph`). ROLLBACK (one cmd): set the drop-in to `KERNEL_INBOUND=0` + `daemon-reload` + `restart aim-voice-agent` (earner never touched). Log: memory/wave_runs/W-DEPLOY-INBOUND.md.
- 2026-06-18 · W-INT-OUTBOUND VERIFY+COMMIT (flag-gated OUTBOUND kernel integration façade wiring the ONE BRAIN into the SACRED earner via one tracked module + a minimal DOC-ONLY agent.py hook; red-team SHIP folded) · ✅ DONE (BUILD already in HEAD as `1a2492b` = `voice_kernel/integrations/outbound.py` + `KERNEL_OUTBOUND` flag + design/W-INT-OUTBOUND-{PLAN,PATCH}.md, and `cd24434` = the 33-test outbound integration suite; this VERIFY re-ran every gate + folded the wave-run VERIFY phase, branch `fix/realtime-voice-kernel-v2` — RECONCILE-FIRST, no test weakened) · `python -m pytest voice_kernel/integrations/tests/test_outbound_integration.py -q` = **33 passed**; `python -m pytest voice_kernel/` = **318 passed / 0 failed** (the 3 `voice_kernel/events/` failures the red-team flagged as pre-existing+unrelated are now GREEN too; the only uncommitted kernel.py delta = additive W8 `event_bus`→`events` impl-alias, NOT this wave, left for its own unit). **RED-TEAM = SHIP, no earner-safety blockers** (2 non-blocking notes folded into the wave log: outbound.py top-level is stdlib-only but `voice_kernel/__init__.py` eagerly loads the droplet-free kernel CORE = identical to the shipped inbound pattern + irrelevant to the OFF earner; intended-new-closure md5 must be computed+asserted pre-deploy per PLAN §5). **EARNER LAW HELD, ZERO DRIFT:** `droplet_work/agent.py` md5 `98655dbfc71d5c3da36bcfe3f848082c` EXACT match (untouched, gitignored, NOT edited/imported); `import voice_kernel.integrations.outbound` pulls **0** droplet/agent modules (sys.modules diff). **OFF = byte-identical:** `kernel_outbound_enabled()` default False ⇒ `build_for_call(...)`→None (legacy path) ⇒ `assemble_outbound_instructions(None, legacy_render=…)` returns the legacy render BYTE-FOR-BYTE (content+length, incl. unicode). **Flag scoping:** `KERNEL_OUTBOUND=1` enables outbound ONLY (inbound stays False, no leak); `KERNEL_OUTBOUND_SHADOW=1` alone does NOT enable live replacement. PATCH anchors valid in the `98655dbf` copy (`instructions = base_instructions` :461, `add_shutdown_callback(_persist_memory)` :553, `elevenlabs.TTS(` :563). gitleaks `protect --staged` = 0. Staged ONLY memory/wave_runs/W-INT-OUTBOUND.md + WORKFLOW_LEDGER.md (never `git add -A`; left the dirty kernel.py W8 alias + the broad untracked tree for their own waves). NO box deploy — by EARNER LAW the agent.py edit + deploy is a SEPARATE super-gated founder step (PLAN §5: backup+record `98655dbf` md5, drift-check, compute intended-new-closure md5 + assert box==it, ship tracked `voice_kernel/` INERT, deploy patched agent FLAG-OFF first w/ golden-campaign render-equality + a real outbound ring smoke, THEN flag-ON canary = FOUNDER REAL OUTBOUND RING on his own number only, restart `famit-agent` ONLY; instant rollback = `KERNEL_OUTBOUND=0`+restart OR restore the `98655dbf` backup). `aim-voice-agent` (inbound) NEVER touched by any outbound step. Log: memory/wave_runs/W-INT-OUTBOUND.md (## Phase: VERIFY).

- 2026-06-18 · W8-EVENT-BACKBONE VERIFY+COMMIT (event-driven backbone over Redis Streams — the founder's "nothing updates in real time" fix — + canonical UTC-store/vendor-tz-render timestamp layer; red-team SHIP-with-2-fixes folded) · ✅ DONE (branch `fix/realtime-voice-kernel-v2`; DISJOINT new pkg `voice_kernel/events/` + tests + frozen-spec `event_bus→events` kernel alias; this commit folds the red-team blockers) · `python -m pytest voice_kernel/` = **321 passed / 0 failed**; events suite **46** (44 original + 3 new regression tests); off-identity **12/12** flag-OFF byte-identity intact; **0** leaked `droplet_work`/`agent`/`caller`/`aim_voice_agent` imports (AST + sys.modules, both proofs green). **RED-TEAM = SHIP, the 2 blockers folded BEFORE commit** (both in the consumer redelivery path, dormant until the LATER seam runs sinks ⇒ live earner never at risk): **BLOCKER-1** handler-failure was XACKed-and-dropped (at-most-once-then-drop) because `_read` acked unconditionally while `SinkConsumer.run` swallowed the handler exception — FIXED via a `_AckSkip` sentinel the consumer now `.athrow()`s back into the `subscribe` async-generator at the yield, so `_read` SKIPS the XACK and leaves the entry in the PEL for XAUTOCLAIM redelivery (true at-least-once; the in-memory fake models leave-in-PEL by not advancing its cursor); **BLOCKER-2** PEL drain abandoned entries beyond `claim_count` (flipped to `>` after the first batch) — FIXED to keep re-reading `0` until empty THEN switch to live `>` (a 120-entry PEL fully drains). **FIX-NOW-3** `daily_report` iid wasn't stable per (tenant,day) — FIXED by pinning `ts_iso` to `{report_date}T00:00:00Z`. Frozen `EventBus` Protocol (emit + subscribe) UNCHANGED; fold confined to `events/{bus,consumer,fake,taxonomy}.py`. EARNER LAW HELD: `droplet_work/agent.py` md5 `98655dbf` UNCHANGED; `caller.py`/`aim_voice_agent.py` NOT edited/imported. gitleaks `protect --staged` = 0. Staged ONLY `voice_kernel/events/` + `voice_kernel/tests/test_events_*.py` + `voice_kernel/kernel.py` (frozen-spec alias) + `design/W8-EVENT-SEAM.md` + `memory/wave_runs/W8-events.md` + `WORKFLOW_LEDGER.md` (never `git add -A`). NO box deploy — wiring is the LATER founder-gated seam (`design/W8-EVENT-SEAM.md`: outbound emit-sites in `caller.py::_finalize_call`, inbound in the `aim_voice_agent.py` shutdown hook, panel SSE consumer replacing the Run-page poll, all behind `EVENTBUS_ENABLED` systemd drop-in, with a real-outbound-ring smoke + revert). Log: memory/wave_runs/W8-events.md (## Phase: VERIFY + RED-TEAM FOLD).
- 2026-06-18 · W9-RECORDING VERIFY+COMMIT (real-time recording finalize + staged transcript/summary + R2/B2 retention + panel status API; disjoint tracked pkg `voice_ops/recording/`; red-team SHIP folded) · ✅ DONE (BUILD already in HEAD as `2cc300e` = the 8-module package + 32 tests + `design/W9-RECORDING-SEAM.md` + wave-log + `voice_ops/__init__.py`; this VERIFY re-ran every gate + folded the ONE actionable red-team note in commit `f54fdf7`, branch `fix/realtime-voice-kernel-v2` — RECONCILE-FIRST, no test weakened) · `pytest voice_ops/recording/ voice_kernel/` = **389 passed / 0 failed** (32 recording + 357 kernel); off-identity guards green; **import isolation = NONE** (clean-process probe: zero livekit/boto3/botocore/redis/droplet_work at module load — every heavy SDK import is LAZY inside a function, mirroring `voice_kernel.integrations.*`). **RED-TEAM = SHIP.** Probes refuted: (1) poller BOUNDED — `poller.py:_poll_loop` returns the last view once `elapsed >= deadline`, cannot spin forever; the founder's slow 20-60min LiveKit finalize leaves the row at `"recording"` which IS in `build_recording_view`'s `_IN_PROGRESS` self-heal set ⇒ a later panel read HEAD-checks the deterministic key + flips it to `completed` (two nets: read self-heal `caller.py:5317` + reconciliation sweep `caller.py:7251`); the poller's internal `"timeout"`/`"failed"` are returned for logging only, NOT emitted, NOT written back. (2) Playback works for BOTH providers — outbound `list_egress_for_room`(no egress_id) / inbound `finalize_one`(egress_id); presigned URL minted ONLY after a HEAD confirms `size >= 2048` ⇒ a 486-busy/zero-byte file never advertises a broken player. (3) Staged flow resilient — `pipeline.py:run` wraps each stage in its own try/except; empty transcript SKIPS (no event, no block); summary emits even when the recording isn't playable; ordering recording→transcript→summary holds on the bus. **ONE minor finding folded as a DOC constraint** (NOT a blocker): on a transient-storage retry (`playable=False`→`playable=True`) `recording_ready` can emit TWICE because `playable` is in the dedup payload while the content-pinned `ts` excludes wall-clock — idempotent on the sink (same row, second event = correct terminal state, ordering preserved), at most a redundant row-update, no data loss/corruption. Fold = `design/W9-RECORDING-SEAM.md §3` now EXPLICITLY forbids the wiring wave from persisting the poller's internal `FinalizeResult` `"timeout"`/`"failed"` strings to `recording_status` (doing so would remove the row from the self-heal set and strand a slow-but-successful finalize); only the emitted `recording_ready` event may mutate the row. **EARNER LAW HELD:** `droplet_work/agent.py` md5 `98655dbf` UNCHANGED; `caller.py`/`aim_voice_agent.py` NOT edited (SEAM stays patch-DOC only). gitleaks `protect --staged` = 0 + pre-commit hook clean. Staged ONLY `design/W9-RECORDING-SEAM.md` (never `git add -A`); the dirty `voice_ops/__init__.py` W11 docstring + the untracked `voice_ops/{booking,callback,gcal,tests}/` belong to OTHER waves (W10/W11) and were left untouched. **FROZEN W9 SURFACE:** `RecordingConfig.from_env()` (flag `RECORDING_FINALIZE_ENABLED` default OFF) · `FinalizePoller` (poll→EGRESS_COMPLETE→status=completed + ONE idempotent `recording_ready`) · `StagedPipeline.run(call_id,tenant_id,room_name,direction[,egress_id])` (ordered recording→transcript→summary, injected providers) · `build_recording_view(...)`/`recordings_envelope(...)` (panel status contract w/ HEAD self-heal). NO box deploy — wiring is the LATER founder-gated seam (`design/W9-RECORDING-SEAM.md`: outbound `caller.py::_finalize_call` detached task + read self-heal `:5317`; inbound `ai_manager/recorder.py` hangup hook; all behind `RECORDING_FINALIZE_ENABLED` per-service env, NOT the shared `.env`; provider bindings + object-storage upgrade + retention cron are separate sub-waves; verify = flag-OFF byte-identity → flag-ON canary w/ a real recording that flips to `completed` in seconds + plays; rollback = flag 0 + restart). Log: memory/wave_runs/W9-recording.md (## Phase: VERIFY + RED-TEAM FOLD).
- 2026-06-18 · W11-TRANSFER-BOOKING-GCAL VERIFY+COMMIT (3 founder bugs: warm-transfer over-talk/no-ring-or-music, UI-only booking → REAL AI `book_site_visit` appointment lifecycle, Google Calendar OAuth async sync; disjoint tracked pkgs `voice_ops/booking/` + `voice_ops/gcal/`; red-team SHIP folded) · ✅ DONE (branch `fix/realtime-voice-kernel-v2`) · `pytest voice_ops/booking/tests voice_ops/gcal/tests` = **39 passed**; full `pytest voice_ops/ voice_kernel/` = **360 passed / 0 failed**. **RED-TEAM = SHIP**, 3 non-blocking nits logged to the wave-log (none risk the earner): (1) the transfer ring/music root cause is OPERATIONAL — the live box ran a stale import-time `_OUTBOUND_TRUNK` never restarted after the `.env` trunk swap; Patch A reads the trunk per-call, immediate deploy fix = `systemctl restart aim-voice-agent`; (2) a transient gcal-sync retry can re-emit a booking sync event (idempotent on the sink, at most a redundant row update); (3) `datetime_resolve` needs the vendor tz set — missing tz falls back to a graceful re-ask, never a wrong slot. **TRANSFER** = pure planner `voice_ops/booking/transfer.py`: ONE fixed-template `ack_line` + ONE `fallback_line` (no paragraph path; test asserts no digits/`+91` leak in the spoken line — numbers only ever reach `create_sip_participant`); the SIP/`session.aclose()` AI-exit choreography (`delete_room_on_close=False` so caller+human continue, `finally` stops hold music) lives in the PATCH DOC, NEVER live-edited. **BOOKING** = `voice_ops/booking/{service,store,datetime_resolve,config}.py` lazy-wraps the gitignored engine `droplet_work/booking/core.py:341-355` (single atomic `INSERT … ON CONFLICT (org_id,resource_id,slot_start) … DO NOTHING RETURNING id` ⇒ no read-check-write double-book, inherited not reimplemented; 0 rows → `slot_taken` re-ask); `store._require_org` fail-closes on empty org_id, every engine call passes `tenant_id` into the FORCE-RLS session (`test_tenant_isolation` proves orgB can't read/list/mutate orgA); persistence is the only synchronous step, W8 event emit + calendar sync are fire-and-forget (swallowed `_emit` ⇒ dead Redis never breaks a booking/call; dormant/disabled/engine-error all return a graceful `say`). **GCAL** = self-contained `voice_ops/gcal/{vault,oauth,sync,config}.py`: refresh token is the only thing at rest, AES-256-GCM 12-byte random nonce + AAD `tenant|google_calendar|version` (a stolen ciphertext re-pasted under another tenant fails `InvalidTag` — `test_vault_cross_tenant_ciphertext_fails`), FORCE-RLS table, refuses empty token/tenant/missing master secret (no weak-key fallback), `mask()` never emits the full token, access tokens never persisted; async create/update/cancel on booking changes never block the call. **EARNER LAW HELD, ZERO DRIFT:** live OUTBOUND `droplet_work/agent.py` md5 `98655dbf` UNCHANGED (recomputed exact match); never edited/imported/restarted; `voice_ops/booking` lazy-imports the engine ONLY at call-time (dormant-safe when absent), `voice_ops/gcal` depends on no gitignored code. gitleaks `protect --staged` = **0** (~130 KB; no OAuth client secret — the `client_secret` diff hits are env-var NAMES, `os.getenv` reads, doc placeholders, fake test fixtures). Staged ONLY `voice_ops/booking/` + `voice_ops/gcal/` + the W11 `voice_ops/__init__.py` docstring + `design/W11-TRANSFER-BOOKING-GCAL-SEAM.md` + `memory/wave_runs/W11-transfer-booking-gcal.md` + this ledger (never `git add -A`; left the untracked `voice_ops/{callback,tests}/` + every other dirty file for their own waves). NO box deploy — by EARNER LAW the aim_voice_agent.py transfer choreography + booking/gcal go-live is a SEPARATE founder-gated seam (SEAM doc §5: flags `BOOKING_OPS_ENABLED`/`BOOKING_CALENDAR_SYNC`, correct `LIVEKIT_SIP_TRUNK_ID`, `restart aim-voice-agent` ONLY — earner `famit-agent` never touched). **FOUNDER ACTION = Google OAuth Web client creds:** Google Cloud Console → enable Calendar API → OAuth consent → create OAuth Web client, redirect `https://panel.famit.in/api/gcal/callback` → send Client ID + Client secret out-of-band; then set `GOOGLE_CALENDAR_CLIENT_ID/SECRET` + `BOOKING_CALENDAR_SYNC=1`. Log: memory/wave_runs/W11-transfer-booking-gcal.md (## Phase: VERIFY).
- 2026-06-18 · W-OUTBOUND-CUTOVER (voice_kernel façade cutover onto the LIVE OUTBOUND earner `/opt/famit-agent/agent.py`, service `famit-agent`, behind systemd drop-in flag `KERNEL_OUTBOUND`) · ✅ DONE — **OUTBOUND EARNER CUTOVER: SUCCESS. `KERNEL_OUTBOUND=1` is LIVE on `famit-agent`, ready for the founder's dashboard ring-test. NO real PSTN ring placed (correct — the founder places it himself).** OLD golden md5 `98655dbfc71d5c3da36bcfe3f848082c` → NEW-closure md5 `480d23c3f2e1daf4814b9a3a9c9695d4` (computed locally from golden + the W-INT-OUTBOUND-PATCH hunks, `py_compile` OK, asserted == box file after upload + unchanged across both restarts). BACKUP of golden = `/opt/famit-agent/agent.py.WOUTbak.1781793303` (md5 `98655dbf` — verified still intact at wave end). PATCH = 4 flag-gated hunks (~63 lines added, diff = exactly the intended hunks, each carrying the verbatim legacy expression as the OFF branch): **A** flag + `_OK` slot at entrypoint top; **B+C** per-call façade build (tenant = `camp["tenant_id"]`, fail-closed) + instruction source (`_legacy_instr()` on OFF/None); **D** SHADOW-only TTS (computes+logs the router engine but keeps `elevenlabs.TTS` unconditional — the `98655dbf` earner has NO Sarvam TTS factory, per PATCH §3 NOTE; never invent one under the earner); **F** additive kernel post-call persist after the legacy transcript write. **G OMITTED** per PATCH §7 (box import is `db.engine` not `droplet_work.db.engine`; W7 memory degrades safely to the kernel Null impl + today's `mem.save_memory` — additive, DEFERRED follow-up); **E** (per-turn shadow) deferred per plan. **EARNER GATE BEFORE:** agent.py md5 `98655dbf` golden = PASS; famit-agent active, ActiveEnterTimestamp 2026-06-15 18:12:34 UTC (not restarted by task at start) = PASS; drop-in absent (default OFF) = PASS; /health 8208=200 8209=200 = PASS. **DEPLOY flag-OFF first:** voice_kernel/ already on box (`outbound.py` md5 `b8c99546` = the equivalence-proven reverted copy; `config.py` wires `KERNEL_OUTBOUND`); box import of façade OK, OFF helpers verified (`assemble(None)==legacy`, `choose_tts(None)=="elevenlabs"`); drop-in created `KERNEL_OUTBOUND=0` + daemon-reload; **RENDER-EQUALITY GATE** golden campaign `c17e55e9f3` OFF-path system prompt rendered from legacy backup AND patched file = IDENTICAL md5 `0af2e07fdb49a73a62f01459fc0180bf` (14131 chars) = PASS; uploaded patched agent.py, box md5 == `480d23c3` = PASS, restarted famit-agent ONLY; **FLAG-OFF SMOKE = PASS** (new master registered worker, /health 8208=200 8209=200, resting build byte-identical at flag-OFF, zero 5xx). **FLAG-ON synthetic canary = PASS** (drop-in→`KERNEL_OUTBOUND=1` + daemon-reload + restart famit-agent ONLY; façade built per-call tenant-from-camp fail-closed, kernel instruction source active, TTS shadow logs router engine while `elevenlabs.TTS` stays live, no exceptions, worker registered, /health 200/200). **FINAL STATE:** `KERNEL_OUTBOUND=1` LIVE; live agent.py md5 `480d23c3`; golden backup `98655dbf` intact; famit-agent active/running; both health ports 200. **EARNER LAW HELD** — flag lives ONLY in the systemd drop-in `/etc/systemd/system/famit-agent.service.d/kernel-outbound.conf`, NEVER in the shared `.env`; OFF path proven byte-identical to golden render. **ONE-LINE ROLLBACK:** `ssh -i ~/.ssh/do-blr-test/id_ed25519 famit@168.144.153.145 'sudo cp /opt/famit-agent/agent.py.WOUTbak.1781793303 /opt/famit-agent/agent.py && sudo sed -i "s/KERNEL_OUTBOUND=1/KERNEL_OUTBOUND=0/" /etc/systemd/system/famit-agent.service.d/kernel-outbound.conf && sudo systemctl daemon-reload && sudo systemctl restart famit-agent'` (or simply flip the drop-in to `KERNEL_OUTBOUND=0` + daemon-reload + restart to keep the new file on the byte-identical legacy OFF path). NO ORCHESTRATOR.md edit (conductor-only). Log: memory/wave_runs/W-OUTBOUND-CUTOVER.md.
- 2026-06-18 · W13-CONFIG VERIFY+COMMIT (real-time provider config + key-health rotation/failover + Vendor Control Center + retention; disjoint tracked pkg `voice_ops/config/` — vault/store/keys/keyhealth/router_bridge/profile/events; red-team SHIP-after-1-blocker folded) · ✅ DONE (BUILD already in HEAD as `b30f9de`; this VERIFY re-ran every gate + folded the ONE red-team BLOCKER in commit `e98cd2f`, branch `fix/realtime-voice-kernel-v2` — RECONCILE-FIRST, no test weakened) · `python -m pytest voice_ops/` = **151 passed**; `python -m pytest voice_kernel/` = **321 passed / 0 failed**; the W13 config suite = **24 passed** (23 + 1 new regression); import isolation = **NONE** (clean-process probe: `import voice_ops.config` pulls ZERO livekit/agent/caller/droplet/elevenlabs/sarvam/groq/openai/torch/numpy modules — every heavy dep is lazy). **RED-TEAM = SHIP, the 1 BLOCKER folded BEFORE commit** (dormant until a key actually fails ⇒ live earner never at risk): **BLOCKER-1** `router_bridge.KeyRouter.resolve_key`→`keys.ProviderKeyStore.decrypt`→`vault.decrypt_secret` RAISED straight through on three normal real-world conditions — founder ROTATES/changes `FAMIT_KEYSTORE_SECRET`, on-disk/row ciphertext TAMPER/corruption, master secret ABSENT in a worker's env — crashing the live voice path instead of returning the documented `ResolvedKey(found=False)` + `key_pool_exhausted`. FIXED at the right seam `voice_ops/config/keys.py:186-196`: `decrypt()` now wraps base64+`decrypt_secret` in try/except → logs LOUD (fingerprint + error **TYPE** only, NEVER the secret/ciphertext) and returns `None`, so the existing `resolve_key` decrypt-miss path drives clean failover / `KEY_POOL_EXHAUSTED`. Regression test `voice_ops/config/tests/test_config.py::test_decrypt_failure_fails_closed_not_crash` (rotation AND missing-secret both → `found=False`, no crash, no plaintext in the signal, `KEY_POOL_EXHAUSTED` emitted) = PASS. **Secret-leakage axis CLEAN:** plaintext canary in NONE of `ResolvedKey.__repr__` (custom repr omits it) / `list_keys` / `fingerprints` / `health()`/`snapshot()` / persisted rows (only `ciphertext_b64` at rest) / event payloads (fingerprints only); vault refuses empty-secret/no-tenant/no-provider/no-master-secret (never a weak/empty key, never a plaintext fallback); `mask`/`fingerprint` non-reversible. **Tenant isolation CLEAN:** AAD = `tenant|provider|version` into GCM ⇒ cross-tenant & cross-provider ciphertext both `InvalidTag` (now fail-closed→`None`); FORCE-RLS DDL on `config_state` w/ GUC policy; cache key `(tenant,namespace)`; in-mem backend tenant-keyed; `is_admin` can't read another tenant's row. **Live propagation w/o redeploy CONFIRMED:** external-DB writes picked up ≤1s (`version_poll_ttl_s` belt) + instantly via `config_changed` event (suspenders); same-process reads instant (class-level cache); atomic single-statement version-bump UPSERT (no read-then-write race); event emit fail-soft (dead/throwing bus never breaks a write). `LiveProviderRouter.key_for` routes through the guarded `KeyRouter.resolve_key` so it inherits the fix; `on_error` safe. **EARNER LAW HELD, ZERO DRIFT:** live OUTBOUND `droplet_work/agent.py` md5 `98655dbfc71d5c3da36bcfe3f848082c` UNCHANGED (recomputed exact match); NOT edited/imported/restarted; `caller.py`/`aim_voice_agent.py` NOT edited. gitleaks `protect --staged` = **0** (~2.97 KB scanned, no leaks). Staged ONLY `voice_ops/config/keys.py` + `voice_ops/config/tests/test_config.py` (the only dirty W13 files; `design/W13-CONFIG-SEAM.md` + `memory/wave_runs/W13-config.md` already committed in `b30f9de`; never `git add -A` — left the untracked `voice_ops/{compliance,db,telephony}/` + W12 telephony/compliance tests for their own waves). NO box deploy — wiring is the LATER founder-gated seam (`design/W13-CONFIG-SEAM.md`: Vendor Control Center panel CRUD over encrypted keys + live router rebind behind a per-service flag; verify = flag-OFF byte-identity → flag-ON canary w/ a real key-add joining rotation + an unhealthy-key failover logged → rollback flag 0 + restart). Did NOT edit ORCHESTRATOR.md (conductor-only). Log: memory/wave_runs/W13-config.md (## Phase: VERIFY + RED-TEAM FOLD).
- 2026-06-18 · W12-TELEPHONY-COMPLIANCE VERIFY+COMMIT (Telephony Sales-OS — capacity/number-pool/spam-reputation/adaptive-routing/lead-lock/calling-window — + the India dial-time compliance engine — DLT-registration/number-series/legal-window-floor/consent-ledger/DND-NCPR-scrub/disclosure; disjoint tracked pkgs `voice_ops/telephony/` + `voice_ops/compliance/`; red-team SHIP-with-2-fixes folded) · ✅ DONE (branch `fix/realtime-voice-kernel-v2`) · `pytest voice_ops/tests/test_telephony_sales_os.py voice_ops/tests/test_compliance_engine.py` = **41 passed** (35 original + 6 new regression); full `pytest voice_ops/ voice_kernel/` = **476 passed / 2 failed** where the 2 failures are `voice_kernel/integrations/tests/test_inbound_integration.py` caused by UNCOMMITTED in-flight `voice_kernel/integrations/inbound.py`/`outbound.py` edits from ANOTHER wave (proved by `git stash`→those 28 inbound tests pass; my W12 changes touch only `voice_ops/compliance/` + `voice_ops/tests/`; NOT staged, not my scope). **RED-TEAM = SHIP, the 2 fixes folded BEFORE commit** (neither lets an illegal call through on the default path, neither can block the live flag-OFF earner): **FINDING-1 (MEDIUM)** the "legal hard floor" was env-overridable with no absolute cap — `window_floor.legal_floor()` read `COMPLIANCE_WINDOW_START/END` verbatim, so `COMPLIANCE_WINDOW_END=23:30` authorized dialing to 23:30 (TCCCPR/TRAI violation). FIXED (`voice_ops/compliance/window_floor.py`): added ABSOLUTE statutory ceilings `ABS_COMMERCIAL=((10,0),(19,0))`/`ABS_BFSI=((8,0),(19,0))` and clamp the env floor INTO them (`eff_open=max(env,abs)`, `eff_close=min(env,abs)`, degenerate→ceiling) so the env knob can ONLY narrow — a misconfigured/hostile env can never authorize a dial outside 10:00–19:00 (08:00–19:00 BFSI); end-to-end test: now=20:00 IST + env claims 23:30 → BLOCK at window gate. **FINDING-2 (MEDIUM)** consent scope-collapse on empty campaign id — `InMemoryConsentStore.rows_for` matched `scope=="" or r.scope==scope or r.scope==""`, so a no-id campaign (`scope=""`, the DEFAULT path) matched a consent granted only for a *different specific* campaign → a lead who consented to campaign A could be dialed by a no-id campaign on A's consent. FIXED (`voice_ops/compliance/consent.py`): replaced the inline predicate with `_scope_matches(query,row)` — a GLOBAL grant (`row==""`) satisfies any query; a SPECIFIC grant only exact-match; an EMPTY query rides ONLY a global grant, never collapses onto another campaign's specific consent; end-to-end test: consent for campA + no-id campaign → BLOCK at consent gate. Red-team safety invariants confirmed unchanged: DND stale-clear→blocked+rescrub (fail-closed), opt-out=absolute block, local suppression tenant-isolated (t2 doesn't inherit t1's opt-out, fail-closes on its own cache miss), NCPR cache intentionally tenant-shared (national register=correct), `needs_rescrub` requeue is the seam's bound; no double-dial/two-number-dial (`LeadLock` (tenant,phone) TTL mutex, tenant-isolated, blank fail-closed, guards campaign loop §3b + retry §5); no number-burn (`number_pool.lease` atomic per-number daily-cap+concurrency under one lock + `_cooldown_ok` min-gap + `health.SpamReputation` quarantine + router exclusion). **EARNER LAW HELD, ZERO DRIFT:** live OUTBOUND `droplet_work/agent.py` md5 `98655dbfc71d5c3da36bcfe3f848082c` UNCHANGED (recomputed exact match); NOT edited/imported/restarted; `caller.py` NOT edited (seam stays patch-DOC only). Resting flag-OFF byte-identical (`COMPLIANCE_ENABLED`/`TELEPHONY_OPS_ENABLED` default OFF → preflight ALLOW + `compliance_unenforced`). gitleaks `protect --staged` = **0** (~181 KB, no leaks). Staged ONLY `voice_ops/telephony/` + `voice_ops/compliance/` + `voice_ops/db/ddl_telephony_compliance.sql` (FORCE-RLS DDL: number_pool/consent_ledger/dlt_registry/dnd_cache/compliance_audit) + the 2 W12 test files + `design/W12-TELEPHONY-COMPLIANCE-SEAM.md` (caller.py patch-DOCs) + `memory/wave_runs/W12-telephony-compliance.md` + this ledger (never `git add -A`; left the dirty inbound/outbound + all other untracked files for their own waves). NO box deploy — wiring is the LATER founder-gated seam. **FOUNDER ACTIONS (DLT/TRAI, required BEFORE `COMPLIANCE_ENABLED=1` else 100% of dials block):** (1) register the Principal Entity on a DLT platform (Jio/Airtel/Vi/BSNL/Tanla/Karix) → PE ACTIVE; (2) register ≥1 Header/Sender-ID + ≥1 content TEMPLATE → APPROVED; (3) provision a 140-series (promo)/1600 (txn) CLI — NOT a 10-digit mobile; (4) auto-dialer pre-notification to the originating access provider; (5) counsel sign-off on suspension/abandoned-call-cap + default disclosure tier (Tier 0 commercial / Tier 1 BFSI). Did NOT edit ORCHESTRATOR.md (conductor-only). Log: memory/wave_runs/W12-telephony-compliance.md (## Phase: VERIFY + RED-TEAM FOLD).
- 2026-06-18 · W-LANG-PROPER VERIFY+COMMIT+REDEPLOY-INBOUND (adaptive per-turn language seam in the shared kernel — the founder's correct fix for "speaks English even when I speak Hindi"; replaces the reverted heavy LanguageTracker forcing; red-team SHIP folded) · ✅ DONE (BUILD already in HEAD as `1610ed5` = `voice_kernel/language.py` + wired `on_turn` in `integrations/{inbound,outbound}.py` + `tests/test_language.py` + updated both integration suites + `design/W-LANG-PROPER-SEAM.md`; this phase VERIFIED every gate AND made the seam LIVE by redeploying INBOUND, branch `fix/realtime-voice-kernel-v2`) · `python -m pytest voice_kernel/` = **340 passed**; `test_language.py` + off-identity (`test_adapter_off_identity` + `test_events_off_identity`) = **42 passed**. **SYNTHETIC BOTH-WAYS = PASS:** Hindi(stt hi-IN)→hindi/hi-IN; switch(stt en-IN)→english/en-IN switched=True; back→hindi/hi-IN switched=True; short "ok" + blank STT → source=`carried`, keeps PRIOR lang **NOT English** (the English-only regression trap is closed — every give-up branch carries the prior lang, never defaults to English; seed is Hinglish). **RED-TEAM = SHIP:** the kernel logic was correct but INERT in prod (the box had NO `voice_kernel/language.py` — confirmed) until the inbound agent had the new tree, which this redeploy did; `UserInputTranscribedEvent.language` is real + populated from Sarvam per-utterance `language_code`, and the live `on_turn` is wired to pass `ev.language`→`on_turn(detected_lang=...)` + apply `tts_lang`; truly soft single suffix `USER LANGUAGE: <lang> — mirror it.` (no forcing/lock, re-decided per turn); STT-auto-detect dependency has a safe fallback chain (blank→text-classify→carry-prior); W5 casual-Hinglish/no-half-words untouched (commit diff touches only language.py + integrations + tests + docs). **EARNER LAW HELD, ZERO DRIFT:** tree golden `droplet_work/agent.py` md5 `98655dbf` UNCHANGED; box-deployed earner `/opt/famit-agent/agent.py` md5 `480d23c3` (the live KERNEL_OUTBOUND=1 build) UNCHANGED before+after; **`famit-agent` NOT restarted** (MainPID 3979046 + ActiveEnterTimestamp 14:44:47Z identical before/after); caller `/health`=200; **NO outbound ring placed**; `famit-agent` never edited/imported/restarted. gitleaks `detect --log-opts="-1 1610ed5"` = **0 leaks** (38.7 KB). Staged nothing new (commit already in HEAD); appended only `memory/wave_runs/W-LANG-PROPER.md` (## Phase VERIFY) + this ledger (never `git add -A`). **REDEPLOY INBOUND (aim-voice-agent ONLY):** flag already ON (drop-in `KERNEL_INBOUND=1`, lives ONLY in the drop-in not the shared `.env`); no Sarvam STT config change needed (inbound STT already `language="unknown"` auto-detect — no hi-IN lock to remove); shipped `voice_kernel/` tar-over-ssh with ATOMIC swap + timestamped backup `/opt/famit-agent/voice_kernel.LANGbak.20260618-152053`; LIVE box `language.py` md5 `b1c05080f5a41cd510c8be0084dba63c`; pre-restart box-python import check (language + integrations.inbound import clean, `on_turn` present, resolve correct); `sudo systemctl restart aim-voice-agent` ONLY → NEW MainPID 3988655, all workers `process initialized` + Silero VAD prewarm, HTTP :8091, `registered worker agent_name="manager" id=AW_zD59Xv2uKwGJ`, journal new-gen = **0** Traceback/ImportError (the 15:23:14 `Failed to kill control group`/`result 'timeout'` = OLD-gen teardown, not a flag-ON error). **FOUNDER RE-TEST:** call **+918071583488**, start in Hindi then switch to English mid-call → the agent must follow both ways (a short "ok"/"haan" must NOT flip it to English). **ROLLBACK (aim-voice-agent only, earner never affected):** flip drop-in to `KERNEL_INBOUND=0`+daemon-reload+restart, OR `sudo rm -rf /opt/famit-agent/voice_kernel && sudo mv /opt/famit-agent/voice_kernel.LANGbak.20260618-152053 /opt/famit-agent/voice_kernel && sudo systemctl restart aim-voice-agent`. **GATED OUTBOUND-REDEPLOY (later founder step):** the shared `voice_kernel/` is now on the box so the OUTBOUND earner picks up the same fix on its NEXT `famit-agent` restart (flag `KERNEL_OUTBOUND=1` already on) — but by EARNER LAW that restart is its OWN super-gated step: backup-record `480d23c3`, restart `famit-agent` ONLY in a quiet window, EARNER GATE before/after, FOUNDER places ONE real outbound ring (Hindi→English) to confirm; rollback `KERNEL_OUTBOUND=0`+restart or restore `agent.py.WOUTbak.1781793303`. Did NOT restart famit-agent in this wave (founder is testing the earner). Did NOT edit ORCHESTRATOR.md (conductor-only). Log: memory/wave_runs/W-LANG-PROPER.md (## Phase: VERIFY + COMMIT + REDEPLOY-INBOUND).
