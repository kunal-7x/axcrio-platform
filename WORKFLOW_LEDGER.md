# 🧾 WORKFLOW LEDGER — durable record of every ultracode workflow + its OUTPUT

> **Why:** workflows run in the main context; when compaction hits, the next session loses what each
> workflow produced. This file (+ `memory/wave_runs/<wave>.md` for full output) makes every run's result
> PERMANENT. **After any compaction, read this FIRST** to know every wave's outcome, then drill into the
> per-wave file for detail. Costs ~1 append per phase — the work is unchanged.

## 📏 THE CONVENTION (every workflow I author bakes this in)
- Each workflow defines a log file `memory/wave_runs/<wave-name>.md`.
- **Every phase agent APPENDS its tight conclusion there** under `## <phase> — <label>` as it finishes
  (crash-safe: a wave that dies mid-run still leaves its completed phases' outputs on disk).
- The **final phase appends ONE line to THIS ledger** (newest on top): `<date> · <wave> · [runId] · STATUS · <one-line outcome> · → <detail file>`.
- This is in addition to (not instead of) the build_log / *-STATE.md / master-plan a wave already writes.

## 📇 INDEX (newest on top)
- 2026-06-14 · inbound-voice-naturalness-fix · [VERIFY] · ✅ VERIFIED (read-only, no mutation) · GOLDEN 5/5 PASS (box re-run: 5 campaigns byte-identical, prompt.py fb87ea56 unchanged). INTEGRATED SMOKE 9/9 PASS: SCRIPT RULE block confirmed live in _build_sales_instructions render (1614be09, 20948 chars) — DEVANAGARI rule, BANNED romanized list, LATIN loan-words, NO default language, Switch the MOMENT, applies ONLY when Hindi/Hinglish, FINAL OVERRIDE, casual register (थोड़ी देर रुकिए/please — not प्रतीक्षा कीजिए/कृपया) all present. MLV mirror guards intact. TTS CLIP VERDICT: SIGNIFICANT IMPROVEMENT — BEFORE (romanized): `ke`→`यंग`, `mein`→`मेन`, `sakta hai`→`सेक्टर हाई` (garbled, 1/3 structural words); AFTER (Devanagari+Latin): `आपके...के...में...है...से शुरू होती है` all intact, sentence structure and meaning clear. RESIDUAL: Sarvam Bulbul v2 still phonetically approximates English brand names in Hindi accent (BHK→`थ्री उसाई`, Codename Joy→`वुड नेम मोई`) — inherent v2 limitation on English proper nouns, not a prompt/script issue; upgrade path = bulbul:v3. EARNER GATE GREEN: agent.py 9150fabe unchanged, famit-agent PID 1477083 not restarted, aim-voice-agent PID 2721961 (post-build, NRestarts=0, 15:03 UTC), /health 200, 0 5xx, NO ring. FOUNDER RECIPE: call +918071583488 in Hindi → expect natural Hinglish back (Devanagari structure + Latin loan-words); try English mid-call → expect clean English switch. FINAL PROOF = founder's real inbound call. → memory/wave_runs/inbound-voice-naturalness-fix.md (## Phase: VERIFY)
- 2026-06-14 · inbound-voice-naturalness-fix · [BUILD+DEPLOY] · ✅ DONE+DEPLOYED (earner-safe, aim-voice-agent restarted ONLY) · Restored real-human casual Hinglish on INBOUND aim_voice_agent. FIX (inbound-only, `aim_voice_agent.py:_build_sales_instructions`; `prompt.py` UNTOUCHED): added a SCRIPT RULE block at the FINAL LANGUAGE LOCK (highest recency) — Hindi words in DEVANAGARI (banned romanized hai/mein/ke/…→है/में/के/…), English loan-words+brands kept LATIN (BHK/budget/confirm/book/please/Codename Joy 3.0), digits for numbers, NEVER mix scripts in one line, casual register (थोड़ी देर रुकिए not प्रतीक्षा कीजिए; please not कृपया, 1-2 sentence turns). Engine-AGNOSTIC (clean on Sarvam Bulbul + EL) → NO tier→TTS routing/billing change. PRESERVES MLV mirror: rule opens "applies ONLY when speaking Hindi/Hinglish; an English caller still gets pure English"; FINAL OVERRIDE + "NO default language/house style" + "switch the MOMENT they switch" all intact (functional render confirmed). GOLDEN 5/5 = exit 0 byte-identical (prompt.py fb87ea56 untouched). Deploy: box backup VOICEbak.20260614-203227, scp md5-gate (1614be09) PASS, box py_compile OK, atomic swap, aim-voice-agent PID 2669239→2721961 "registered worker" NRestarts=0 0-errors. EARNER GREEN: agent.py 9150fabe UNCHANGED, famit-agent PID 1477083 not restarted, caller /health 200, 0 5xx, NO ring; RAG_INJECT_ENABLED+flags intact. Commit 69374eb on fe/unify-run-wavec, gitleaks 0. RECOVERY-STATE.md source-map reconciled 018c20a7→1614be09. ACCEPTANCE = founder's real inbound call. → memory/wave_runs/inbound-voice-naturalness-fix.md (## Phase: BUILD)
- 2026-06-14 · inbound-voice-naturalness-fix · [DEEP-WEB RESEARCH] · ✅ DONE (read-only, web search + official docs) · ENGINE VERDICT: Sarvam Bulbul (all versions) REQUIRES Devanagari for Hindi words — official docs state "Transliterated input significantly reduces output quality" + "Always use native script for Indic words"; ElevenLabs Flash v2.5 tolerates Hinglish/romanized but current live voice "Neha P" is an English voice (wrong for Hindi-dominant output). OPTIMAL SCRIPT: Devanagari Hindi words + Latin English words/brands/digits — works on both engines, resolves the garbling. PROMPT RECIPE: add a SCRIPT RULE block after FINAL LANGUAGE LOCK (aim_voice_agent.py:1631): pin Hindi to Devanagari, keep English loan-words Latin, numbers as digits, prohibit romanized Hindi, prohibit same-line script mixing, casual register (thodi der ruko not pratiksha kijiye). EARNER GATE GREEN: no mutation, no restart, no ring. → memory/wave_runs/inbound-voice-naturalness-fix.md (## Phase: DEEP-WEB RESEARCH)
- 2026-06-14 · inbound-voice-naturalness-fix · [DIAGNOSE] · ✅ DONE (READ-ONLY, no box mutation, no call, no restart) · ROOT CAUSE FOUND. Inbound TTS is NOT fixed-EL (brief was wrong) — it is TIER-DEPENDENT via `prompt.resolve_providers` (live `INBOUND_PROV_LOCK` ON): **lean/standard → SARVAM Bulbul `bulbul:v2` anushka; premium → EL `eleven_flash_v2_5` voice `QTKSa2Iyv0yoxvXY2V8a`="Neha P" (an ENGLISH voice pinned language=hi)**. The founder's real "Codename Joy 3.0" campaign resolves `tts=sarvam` → HE HEARS SARVAM. (1) PRIMARY cause = commit `ab6777c` `feat(provider-lock)` (RUN-PLATFORM Wave A) routed lean/standard off the old fixed-EL inbound default onto Sarvam Bulbul. (2) SECONDARY = LLM (Groq llama-4-scout) emits INCONSISTENT SCRIPT — pure Devanagari, romanized, AND mixed-in-one-line ("₹2 करोड़ ke budget mein…") — because the MLV mirror rule (`aim_voice_agent.py:1577`/`:1631`) pins LANGUAGE but never SCRIPT. (3) TERTIARY = EL voice is English-on-`language=hi`. REPRODUCED via real EL+Sarvam synth + round-trip Sarvam-STT oracle: Sarvam GARBLES romanized ("Codename Joy"→"वोडने मोई", "BHK"→"उसाई"); EL handles Hinglish far better; BOTH clean on pure Devanagari. Audio: `droplet_work/_voice_repro/` (6 files). FIX DIRECTION (build phase): pin LLM Hindi output to ROMANIZED Hinglish (engine-agnostic, cheapest) and/or route Hindi-heavy verticals to an EL Hindi-capable voice. EARNER GATE GREEN: agent.py `9150fabe` UNCHANGED · famit-agent PID 1477083 not restarted · aim-voice-agent not restarted · caller /health 200 · 0 5xx · NO ring. NOTE: box aim_voice_agent.py md5=`5c3936fa` (W1 RAG) — RECOVERY-STATE.md stale at `018c20a7`, reconcile. → memory/wave_runs/inbound-voice-naturalness-fix.md
- 2026-06-14 · provider-framework-foundation · [W3-resolve-health-reveal] · ✅ DONE (offline) · W3 of the Universal Provider Framework (design/PROVIDER-FRAMEWORK-PLAN.md §14): the resolve + health + PIN-step-up-reveal layer, LOCAL + offline-tested, NO mount/box-write/restart/deploy, flag PROVIDER_REGISTRY_ENABLED OFF. NEW `provider_registry/{store,admin_store,registry,health}.py` + ADDITIVE `provider.reveal` scope in the DEPLOYED `firewall.py` (built local, NOT deployed — W4 mounts). `registry.get_provider(tenant,capability,routing_hint)` = the single capability-keyed resolution point (flag-OFF→registry_disabled · PG-down→not_configured · enabled defs RLS own+`_global` priority-asc · routing_hint pinned · skip circuit-open · first with a credential decrypted ONLY via the credentials.decrypt get_secret seam wins · never raises). `store.py` is_admin=False RLS reads (GUC-in-txn via db.engine.session, mirrors ai_manager/store.py); `admin_store.py` is_admin=True super-admin reads (require_super_admin-only, W4). `health.py` in-memory 3-fail circuit breaker, expo backoff 60→120→240 (capped 1h), half-open, injectable clock — NO PG on the hot path (§3 0ms voice-loop rule). firewall `provider.reveal`: 60s TTL + aud=provider_def_id + SINGLE-USE jti (consumed on verify; replay→None) — closes the live jti-replay gap; isolated state file var/provider_used_jti.json; FIX: PyJWT auto-validates aud → options verify_aud=False + explicit self-check. OFFLINE PROOFS: resolve RLS A-never-gets-B (fake db.engine enforces the §5 GUC; A's tried never contains B's slug; store.get_active_credential(A,DEF_B)=None) · cross-tenant ciphertext copy→InvalidTag→skipped→fallback (B's key never returned as A's) · circuit-open→fallback-by-priority · jti-replay→403 (+aud/sub/scope/expiry/no-jti all denied) · existing-firewall UNCHANGED golden (diff vs box cd1ac5d1 = single additive insertion 339a340,473, 0 deletion/mod lines; generic mint/verify/PIN/change/lockout/classify behavior PASS). pytest 4 passed (W2+W3). gitleaks 0. EARNER GATE PASS: agent.py md5 9150fabe UNCHANGED · famit-agent PID 1477083 alive NOT restarted · box firewall.py still cd1ac5d1 (reveal NOT deployed) · caller /health 200 · NO ring. Commit on fe/unify-run-wavec. NEXT: W4 (mount endpoints.py in caller.py under PROVIDER_REGISTRY_ENABLED — serialized vs RAG/Vault/Video). → memory/wave_runs/provider-framework-foundation.md
- 2026-06-14 · rag-in-call-verify-gaps · [DESIGN:gaps] · ✅ DONE (READ-ONLY) · 2 gaps designed. PER-PATH VERDICT: INBOUND voice = ✅ DONE+LIVE (FTS grounding, 3 sites, kill-switch, 6–12ms, byte-identical-off proven; nothing to do). OUTBOUND earner agent.py = 🔴 GATED (does NOT ground; RAG inbound-only). FULL-SEMANTIC dense = 🟡 BUILDABLE ENHANCEMENT (founder-signed Ph2). GAP1 (outbound, earner-safe): mirror the proven inbound block but build grounding INTO `instructions` in the CONNECT window BEFORE `_MirrorAgent(instructions=...)` (agent.REFERENCE.py:400→725) — NEVER a mid-call update_instructions (earner's Groq-cache TTFT invariant). New flag `OUTBOUND_RAG_ENABLED=0` (default OFF=byte-identical); PORT `_grounding_seed`/`_format_grounding`/`_kb_retrieve` VERBATIM from aim_voice_agent.py:518-577, dense=False hardcoded; reuse box `kb/` (no fork); optional FTS-only `lookup` tool = deferred W-OB.2. Edit MUST start from a scp-pulled `agent.py.LIVEBOX` golden (BOX-ONLY, never `_inbound_ref`/local). GATE = golden flag-off byte-identical render + `agent.py.PREv<n>` rollback (md5 WILL change—NOT an md5-unchanged gate) + famit-agent restart ONCE; ACCEPTANCE = FOUNDER real outbound ring (I never test-call—spam-flags DID). FOUNDER ACTIONS: (1) sign-off to edit agent.py + (2) DID un-blocked. Supersedes RAG-MASTER W-OB caller.py-precompute (simpler: reuse inbound block in earner's own connect window). GAP2 (full-semantic): dense leg ALREADY wired in kb/core.py:343-427 (pgvector ANN, gated behind `dense`) + embeddings.py ALREADY provider-agnostic/dormant-until-key/off-box — so it's a CONFIG+backfill, not a rewrite. Embedder = OpenAI text-embedding-3-small @256d (NOT GPU box, NOT OpenRouter; Sarvam has no /embeddings yet—pre-wired to flip). Trigger = call-CONNECT only (dense=True at prefetch site ONLY; lookup/pick_campaign FTS-only FOREVER, C-3); latency = ONE embed RTT 40-200ms + ANN 5-15ms paid ONCE in the 200-400ms SIP window, W4 grounding_cache LRU-keyed `(tenant,campaign,stage,channel,kb_version)` → ~0ms HIT → per-turn cost = 0ms; cost ≈ few ₹ one-time backfill + <₹0.001/call. Gated by EMBED_API_KEY presence (no new bool), hard-gated behind a RAGAS-fails-on-FTS proof + W6 no-embed-on-reply assertion. FOUNDER ACTIONS: OpenAI key + sign off paid embedder. PHASED: W-OB.1 (outbound connect-window, founder+DID-gated) · W-OB.2 (optional outbound lookup tool) · W4 (grounding_cache, prereq for dense, inbound) · Ph2-dense (.env config + backfill + dense_ready atomic flip). Earner UNTOUCHED: agent.py 9150fabe (read-only mirror), 0 box mutations, 0 restarts, NO ring, provider-framework-foundation files not touched. → memory/wave_runs/rag-in-call-verify-gaps.md (## GAP DESIGN PHASE)
- 2026-06-14 · rag-in-call-verify-gaps · [SMOKE:integrated] · ✅ DONE (READ-ONLY) · VERDICT: YES — live inbound agent has low-latency KB access. FTS latency 6–12 ms warm/cold against live PG blr1. All 4 KB-needing caller utterances (price, discount, "bahut mahanga" objection, product Q) returned 5 chunks each (0 MISS). Objection "bahut mahanga" fires `objection_too_expensive_pricing` as top chunk. Grounding block (+1625 chars) confirmed PRESENT in rendered `_build_sales_instructions` (delta +1625, grounding_in_with_render=True). All 3 grounding sites confirmed live in running box code: Site1 `CustomerSalesAgent.lookup`, Site2 `CustomerSalesAgent.pick_campaign`, Site3 `_entrypoint_impl` connect-prefetch. `EMBED_API_KEY` UNSET → `dense=False` → ZERO embed RTT. `RAG_INJECT_ENABLED=1` confirmed. Gap: _global corpus floor works but tenant-specific chunks outrank it (correct behaviour; no miss). W4 grounding_cache deferred (6–12ms is within 200–400ms SIP window, acceptable). Earner UNTOUCHED: agent.py 9150fabe unchanged, 0 restarts, NO ring. → memory/wave_runs/rag-in-call-verify-gaps.md
- 2026-06-14 · video-flex-framework-design · [SYNTHESIZE:unified-roadmap] · ✅ DONE (DESIGN-ONLY) · WAVE COMPLETE — synthesized the whole read-only design wave (3 HAVE + 4 RESEARCH + 3 DESIGN phases) into the universal-flexible-provider framing + ONE unified earner-safe build roadmap. **`design/PROVIDER-FRAMEWORK-PLAN.md` §14 = the NEW UNIFIED cross-product W1–W12 roadmap** merging the framework's F1–F9 + Video's U1–U10 into one table-form sequenced plan (each wave: scope · files/schema · flag · acceptance · rollback): **framework FIRST → Video Studio on it → composite tier ships with ZERO paid key**; **caller.py serialized — only ONE of {RAG,Vault,Registry,Video} edits caller.py at a time** (RAG W3 LIVE → registry mount W4 = next caller.py edit, video mount W8 follows, never concurrent). First 3 launchable waves spelled out (W1 FW-DDL+pkg shell · W2 SSRF-guard+3-tier-adapter+named-transforms+AAD-creds offline · W3 resolve+health+PIN-step-up-reveal offline — all local before the W4 mount). BE on Opus, FE on Sonnet+frontend-design. `VIDEO-STUDIO-MASTER-PLAN.md` §9b/§10d/§12.7 (Video = first consumer) verified not re-touched. Pointers appended: NEXT-BIG-BUILDS #8b (Universal Provider Framework, build BEFORE Video) + #9 cross-link · ORCHESTRATOR (newest-on-top) · this ledger · `memory/wave_runs/video-flex-framework-design.md` (## Phase: SYNTHESIZE [unified-roadmap]). DESIGN-ONLY — no box/code/earner mutation, agent.py `9150fabe` never read/imported, no restart, no ring, no open forks. NEXT BUILD = W1 (framework DDL + package shell). → design/PROVIDER-FRAMEWORK-PLAN.md §14 + memory/wave_runs/video-flex-framework-design.md
- 2026-06-14 · video-flex-framework-design · [DESIGN:provider-framework] · ✅ DONE · UNIVERSAL FLEXIBLE PROVIDER/CONNECTOR FRAMEWORK designed → `design/PROVIDER-FRAMEWORK-PLAN.md` (510 lines, decision-ready). Net-new pkg `droplet_work/provider_registry/` + `db/ddl_provider_registry.sql` (3 FORCE-RLS tables: provider_definitions `_global`-read-shared/write-locked · provider_credentials AAD-bound AES-256-GCM + `scope` ai_provider-masked/integration-revealable · provider_health_log append-only). Config-driven registry: super-admin/vendor adds via UI a hosted-api OR self-hosted provider (base_url, auth scheme, request/response field-map, capabilities, cost, health-check) → live no-code-deploy for ~95%. **3-tier transform** (openai_compat zero-config 90% · named_provider 1-dict-entry, existing fal/replicate/luma builders registered not rewritten · custom_field_map JSONPath-only-no-eval) = the "connect ANY future tool" lever. **Capability-keyed** `get_provider(tenant,capability)` = consumers ask a capability not a name (Video Studio FIRST consumer; voice-LLM/RAG/image/WA-AI next). **`get_secret()` Vault seam** (interim Fernet, flip VAULT_BACKEND later, zero consumer change — Vault = separate deferred build). **SSRF guard hard-gate** (CVE-2025-59146; host/port split, DNS-resolve-all, RFC1918/metadata denylist, redirects-off, egress-firewall, health-sandbox, PIN step-up; self-host=super-admin-only). STRANGLER cut-over (per-consumer sub-flags REGISTRY_FOR_VIDEO/IMAGE/LLM, legacy fallback, each revertible). Earner-safe (rides caller.py/AI-asset:8310, never agent.py 9150fabe; voice-router cache-first 0ms), multi-tenant FORCE-RLS, cost-capped (free/composite default + 1-paid-test + per-tenant cap on paise ledger), most-secure, sellable. Crazy UI = ONE Integrations page + visual FieldMapper (FE design = DESIGN[crazy-ui-security] phase). `PROVIDER_REGISTRY_ENABLED` default OFF byte-identical; serialized vs RAG/Vault/Video on caller.py. Video plan §9b/§10d/§12.7 enhanced (Video = first consumer). → design/PROVIDER-FRAMEWORK-PLAN.md + memory/wave_runs/video-flex-framework-design.md
- 2026-06-14 · video-flex-framework-design · [RESEARCH:provider-registry-patterns] · ✅ DONE · 11 sourced findings: LiteLLM DB-backed runtime model add (POST /model/new, no restart), OpenRouter per-request provider routing + BYOK, Kong AI Proxy Advanced targets[]/llm_format/failover, Bifrost virtual-key/access-profile multi-tenant hierarchy, Portkey runtime-fetched secret references, SSRF threat model (CVE-2025-59146/CVE-2025-53767 direct precedents + 9-layer defense), three-tier transform model (OAI-compat/named_provider/custom_field_map JSONB), canonical PG registry schema (provider_definitions+provider_credentials+health_log, FORCE-RLS, AAD-bound AES-256-GCM), capability-based consumer interface, circuit-breaker/fallback pattern. → memory/wave_runs/video-flex-framework-design.md
- 2026-06-14 · rag-w3-kb-ui · [W3-backend] · ✅ DONE+DEPLOYED+VERIFIED-LIVE · 4 KB-management endpoints added to caller.py (token-derived, RLS is_admin=False, all asyncio.to_thread): GET /kb/sources (tenant+`_global` sources w/ live chunk counts+status, ?scope_campaign_id), POST /kb/upload (text OR PDF via pypdf graceful-degrade; size/sniff guards; kb.ingest under tenant GUC, write-locked from `_global`; optional scope_campaign_id; audited), POST /kb/test-retrieve (FTS-only retrieve(dense=False)+include_global → firing chunks {id,source_id,document_id,section,snippet,score,leg}+grounded meter; logs probe to kb_query_log), GET /kb/gaps (kb_query_log grounded=false aggregated, strictly per-tenant). Box /opt/famit-agent: backup caller.py.W3bak.20260614-124353; pypdf 6.13.2 into caller venv; scp+md5-gate (52c59291)+box py_compile OK; atomic swap; famit-caller restarted ONLY (PID 2685432). E2E authed PASS (sources lists+counts; text upload→1 chunk fts_only; "registration charge"→grounded 6 sparse; "zxqwvbbb"→grounded=false→/kb/gaps count 1); smoke data cleaned. Commit a91d049 on fe/unify-run-wavec; gitleaks 0; py_compile OK. EARNER UNTOUCHED: agent.py md5 9150fabe UNCHANGED, famit-agent PID 1477083 not restarted, /health 200, 0 5xx, NO ring. NEXT: W7 KB UI (Sources/Test Answers/Insights) on famit-panel fe/unify-run-wavec, FORTRESS deploy. → memory/wave_runs/rag-w3-kb-ui.md
- 2026-06-14 · rag-w2-seed-telecaller · [W2-verify] · ✅ DONE+VERIFIED-LIVE · Box DB re-probed directly: 120 kb_chunks / 41 kb_sources (_global); tenant FTS read-share PASS (2 "Bahut mahanga" rows); write-lock PASS (RLS WITH CHECK blocked tenant INSERT); idempotency PASS (41/41 checksums in DB, re-seed=0); auth gates 401/403/405/200 PASS; RAG_INJECT_ENABLED=1; /health 200; agent.py md5 9150fabe UNCHANGED; famit-agent active; 0 5xx; NO ring. Docs updated: RAG-MASTER-PLAN.md (W2 DONE) + NEXT-BIG-BUILDS.md + ORCHESTRATOR.md + AGENT_LEARNINGS.md. NEXT: W3 grounding cache (kb/grounding_cache.py) + KB management UI. → memory/wave_runs/rag-w2-seed-telecaller.md
- 2026-06-14 · rag-w2-seed-telecaller · [W2-seed] · ✅ DONE+DEPLOYED · `_global` telecaller corpus SEEDED LIVE (41 sources / 120 kb_chunks on box DB). NEW `kb/seed_global.py` idempotent loader: each of 41 entries ingested as own source under is_admin=True (only RLS path that writes `_global`); dedup by sha256(content) → re-run/double-POST inserts 0 (RUN2 duplicate=41). NEW super-admin-gated `POST /kb/seed-telecaller` (caller.py:3326; no-auth→401, legacy-pw→403, admin-hmac→200). 2 core.py fixes found live (41fde4a): ensure_schema raw-cursor (SA-2.0.50 immutabledict bug on cold apply) + KB_INCLUDE_GLOBAL=0 now truly excludes `_global` (was leaking via read-share RLS). LIVE PROOF: read-share surfaces `_global` objection to tenant; write-lock blocks tenant `_global` INSERT (0 hacked); idempotent via CLI + HTTP. Deployed backup-first/md5-gated; famit-caller restarted ONLY (PID 2679442). Earner UNTOUCHED (agent.py 9150fabe, famit-agent PID 1477083, /health 200, 0 5xx, NO ring). Commits ff44770+41fde4a on fe/unify-run-wavec, gitleaks 0. → memory/wave_runs/rag-w2-seed-telecaller.md
- 2026-06-14 · rag-w2-seed-telecaller · [W2-corpus] · ✅ DONE · 41 FTS-quality _global chunks written to droplet_work/kb/seed_global_corpus.json (gitignored scratch, ready to ingest to box). Coverage: 10+ objection handlers (too_expensive/not_interested/call_later/need_to_think/already_have/send_details_whatsapp/trust/DND+more) each with empathy-first Hinglish + named technique; pricing+value-framing FAQs; 6 vertical product explainer scaffolds (generic/SaaS/finance/edtech/real-estate/health); rapport+behavior patterns (mirroring, backchannel haan/achha, urgency-without-pushiness, 5 close techniques, followup sequence, WhatsApp templates, buying signals, voice tone, active listening, Indian B2C psychology). All chunks: topic+content+tags; strong keyword coverage for FTS-only retrieval (EMBED_API_KEY unset). Commit 9561ffc on fe/unify-run-wavec; gitleaks 0. Earner UNTOUCHED. → memory/wave_runs/rag-w2-seed-telecaller.md
- 2026-06-14 · rag-w1-retrieval-harden · [W1-build] · ✅ DONE (build+offline-verify) · core.py retrieve() gains `dense=False`(reply-path default→ZERO embed/network; C-3) + `include_global=True`; `_global` UNION via explicit `OR tenant_id='_global'` bound selftid under is_admin=False (no `%`, no admin); kb_chunks/sources/documents RLS USING read-shares `_global` / WITH CHECK write-locks it; NEW kb_query_log FORCE-RLS (strict per-tenant) + log_query/purge_query_log TTL(90d). 3 grounding sites funnel one `_kb_retrieve` chokepoint → passes dense=False explicitly (FTS-only forever). Offline 5/5 PASS; DDL rollback-validated on live box DB RC=0; py_compile OK; gitleaks 0. Commit 266f2c1 on fe/unify-run-wavec. Box NOT mutated (DDL deploys next aim-voice-agent restart via ensure_schema). FLAG TRUTH: CTX_CACHE + INBOUND_PROV_LOCK both DORMANT (absent from .env, code-default off) — NOT flipped. Earner UNTOUCHED (9150fabe/PID1477083/caller-200/0-5xx/NO ring). → memory/wave_runs/rag-w1-retrieval-harden.md
- 2026-06-14 · rag-w0-retrogate · [W0-build] · ✅ DONE (build+verify) · Built RAG_INJECT_ENABLED kill-switch (default 1) + wrapped all 3 live grounding sites (lookup/pick_campaign/connect-prefetch) + the _kb_retrieve chokepoint → flag-OFF does NO kb retrieval & passes grounding="" → byte-identical no-RAG inbound render. Golden 5/5; flag CI exit 0 (flag-off==no-RAG render, 18938 chars; non-empty block appends exactly). prompt.py untouched. Commit 8bc8780 on fe/unify-run-wavec, gitleaks 0. Earner UNTOUCHED (9150fabe/PID1477083/caller-200/0-5xx/NO ring). NOT yet deployed to box (still 018c20a7) — box deploy + .env RAG_INJECT_ENABLED=1 = next box-mutating step. → memory/wave_runs/rag-w0-retrogate.md
- 2026-06-14 · repo-recovery-audit (P3 final) · [wf_2b177b10] · ✅ DONE · RECOVERY-STATE.md written (authoritative source map + live flags + 4 follow-ups); PLAYBOOK rules 16+17 banked; AGENT_LEARNINGS + ORCHESTRATOR updated. Local↔box CONSISTENT. Open: W0 RAG kill-switch; CTX_CACHE/INBOUND_PROV_LOCK .env verify; branch merge. Earner UNTOUCHED (9150fabe/PID1477083/health200/0-5xx). → memory/wave_runs/repo-recovery-audit.md + design/RECOVERY-STATE.md
- 2026-06-14 · repo-recovery-audit (P2 reconcile) · [wf_2b177b10] · ✅ DONE · Local mirrors RECONCILED to live box truth (SCP-pull, no box write): aim_voice_agent a9eefa8c→018c20a7, prompt ec5fa971→fb87ea56, both .LIVEBOX goldens refreshed, agent.py on-disk→9150fabe (gitignored), _inbound_ref DEPLOYED ref 3152539f→018c20a7 + README.CANONICAL.md canonical map. kb/ already matched. Commit 70969dd, gitleaks 0. Earner UNTOUCHED (9150fabe/PID1477083/health200/0-5xx). NO safe gap deployed; RAG_INJECT_ENABLED W0 retro-gate = separate box-mutating wave (noted). → memory/wave_runs/repo-recovery-audit.md
- 2026-06-14 · repo-recovery-audit (P1 drift-map) · [wf_2b177b10] · ✅ DONE · Full drift map: earner HEALTHY; 3 critical stale local files; RAG LIVE+UNGATED (W0 must build RAG_INJECT_ENABLED); missing .env flags (CTX_CACHE, INBOUND_PROV_LOCK); 7-branch sprawl mapped. → memory/wave_runs/repo-recovery-audit.md
- 2026-06-14 · panel-unify-redeploy · [wf_0eff1d2b] · ✅ DONE · Run-page regression FIXED — all UI unified (4-step + Wave C cost-meter/provider-lock/exclude-toggle + W4 CRM + AIM + #8). Branch fe/unify-run-wavec, BUILD_ID TU16Mn1DcJVmxnxr2GVyL live.
- 2026-06-14 · three-products-megaplan · [wf_666fe925] · ✅ DONE · 3 master plans (RAG/Video/Vault). 🚨 KEY: RAG grounding LIVE+UNGATED on box (3 sites, 63 chunks) + box↔local earner-file drift → RAG W0 = retro-gate first. → design/{RAG,VIDEO-STUDIO,VAULT}-MASTER-PLAN.md
- 2026-06-14 · workflow-funnel-execution (#8) · [wf_6dfd8def] · ✅ DONE · funnel/workflow now RUNS (empty run_token was the bug → 401 at dialer); human labels + Run button; verified queued-only, NO ring. → droplet_work/funnels/RUN_EXEC_STATE.md
- 2026-06-14 · aim-access-and-pin (#6) · [wf_e7cb7c7d] · ✅ DONE · PIN change (old→new + lockout) + numbers CRUD; firewall byte-identical. BUILD_ID sTCWP4Jj.
- 2026-06-14 · voice-w4-memory-read · [wf_f70d5945] · ✅ DONE · memory read side LIVE (retrieval into inbound + CRM Memory tab + API); LEAD_MEMORY_PG=1; 10/10 verify. → design/W4-MEMORY-READ-STATE.md
- 2026-06-14 · voice-w3-multichannel-memory · [wf_45ee5836] · ✅ DONE · lead_memory/episodes RLS tables + durable PG-outbox extraction (survive-restart); flag-gated. → design/W3b-EXTRACTION-STATE.md
- 2026-06-14 · run-wave-c-ui-costmeter · [wf_88dc2a51] · ✅ DONE (then reverted by branch-sprawl → being re-fixed) · real cost meter + provider-lock banner + exclude-toggle. On branch fe/wave-c-run-cost-meter.
- 2026-06-14 · run-wave-a-billing-providerlock · [wf_59b68b62] · ✅ DONE · resolve_providers leaf (golden 5/5) + USD_INR 95.2 (Groq 95× fix) + funnels secure. → RUN-PLATFORM-MASTER-PLAN.md
- 2026-06-14 · run-wave-b-preview-fix · [wf_7a9bfd23] · ✅ DONE · preview silent cause = text/plain; backend full-buffer + force audio/mpeg. Live: 200 audio/mpeg ID3.
- 2026-06-14 · voice-w2-context-cache · [wf_b26d1b52] · ✅ DONE · full-context cache (warm 0.16ms) + version-stamp invalidation; flag CTX_CACHE=1 inbound. → design/W2-DEPLOY-STATE.md
- 2026-06-14 · voice-p0-leak · [wf_ed3b9042] · ✅ DONE · cross-tenant memory leak CLOSED (inbound+WA, tenant-checked fallback); no earner restart. → design/P0-LEAK-STATE.md
- 2026-06-14 · voice-w1-script-fullcontext · [wf_17dcce28] · ✅ DONE · dynamic vendor-script→persona + lossless raw_script store + Script Studio; golden 5/5. → design/W1-DEPLOY-STATE.md
- 2026-06-14 · master-dna-plan · [wf_4f117706] · ✅ DONE · the full-DNA brain. → MASTER_DNA_PLAN.md
- 2026-06-14 · run-platform-megaplan · [wf_eefea3fb] · ✅ DONE · real pricing + preview cause + provider-lock + feature table. → design/RUN-PLATFORM-MASTER-PLAN.md
- 2026-06-14 · voice-brain-megaplan · [wf_8988e0cb] · ✅ DONE · the adaptive-voice-brain architecture (45 agents). → design/VOICE-BRAIN-MASTER-PLAN.md

## ⏭️ QUEUED (launch when a wave slot frees — avoid rate-limiting the urgent fix + priority research)
- **repo-recovery-audit** — diagnose + recover ALL latent mistakes like the branch-sprawl (other deployed-then-reverted UI, un-deployed committed work, local↔box drift, half-merged branches, dormant flags). Launch after panel-unify-redeploy lands.
- **RAG (heart) → Video Studio → Vault** — build each (BE Opus / FE Sonnet+frontend-design) once three-products-megaplan lands its master plans.
- **🔧 scheduler retry-queue guard fix** (caller.py scheduler_loop ~:7131) — add `if attempts>=max_attempts: remove+skip` so exhausted 3/3 retries can NEVER re-fire (THE bug that auto-dialed 6 numbers, kept the carrier 486 block alive, burned Vobiz balance; queue cleared + backed up `var/retry_queue.json.PAUSED_20260614-201754.bak`). 🟥 DO before the founder resumes ANY campaign. famit-caller only, earner-safe. Detail: memory/wave_runs/outbound-diagnosis.md.
- **🔒 GATED on founder:** outbound RAG grounding + provider-lock (agent.py sign-off + DID unblocked — designs ready); dense/semantic RAG (OpenAI key); Vault (teammate); outbound dialing (Vobiz DID rotation — FOUNDER-VOBIZ-UNBLOCK.md).
- **🎚️ dormant-flag activation** — CTX_CACHE (W2) + INBOUND_PROV_LOCK (Wave A) built-but-OFF in box .env; activate w/ a real-call check when convenient.
- Then the gold-mine backlog (NEXT-BIG-BUILDS items).
- 2026-06-14 RAG W0 DEPLOY ✅ — `RAG_INJECT_ENABLED` kill-switch deployed to box (`8335d4ba`); flag=1 (grounding ON); golden 5/5 PASS; flag-gate A/B/C PASS; earner UNTOUCHED. Kill: set `RAG_INJECT_ENABLED=0` + restart aim-voice-agent.
- 2026-06-14 RAG W1 DEPLOY ✅ — retrieval hardening live: dense=False default (zero embed on reply path), _global UNION predicate (RLS USING + explicit SQL, no %wildcard), kb_query_log FORCE-RLS + TTL. Box: kb/core.py(7010a77e) + schema.sql(42c14591) + __init__.py(aa2b7c13) + aim_voice_agent.py(5c3936fa). DDL applied (all 4 kb_ tables FORCE-RLS=t live; kb_query_log NEW). aim-voice-agent restarted PID 2669239. 8/8 offline probes PASS. Earner UNTOUCHED (agent.py 9150fabe, famit-agent PID 1477083, /health 200). CTX_CACHE+INBOUND_PROV_LOCK = DORMANT (absent from .env, not flipped). NEXT: W2 seed _global corpus.
- 2026-06-14 RAG W3 + W7 DEPLOY ✅ FEATURE-COMPLETE-FOR-NOW — W3 KB-management backend (4 endpoints: /kb/sources /kb/upload /kb/test-retrieve /kb/gaps, caller.py, famit-caller restarted) + W7 /knowledge UI (Sources/Test Answers/Gaps, 3-tab Core_2 page, FORTRESS panel BUILD_ID YV9obkLRRD0U5oX-CPOCH). All 6 routes 200 via CF edge. No regression on Run/CRM/AIM/Workflows. Earner UNTOUCHED (agent.py 9150fabe, PID 1477083, /health 200). RAG is feature-complete-for-now. NEXT PRODUCT = VIDEO STUDIO.

- 2026-06-14 video-flex-framework-design DESIGN [crazy-ui-security] DONE — Universal Connector/Integrations page (app/integrations: Providers/Self-hosted/Health/Audit, ports api-keys+_custom-providers CRUD, Test-connection w/ VRAM readout, capability chips, transform-type wizard, SSRF+PIN-gate on self-hosted add, PIN-pad reveal hidden on ai_provider rows per Vault §9) + Video Studio UI (app/creative/video: TierTabs composite-default, BYO-key picker from registry, AssetMedia split, Images<->Videos one-library toggle) + most-secure model (AES-GCM+AAD, get_secret seam, append-only audit, entitlement HIDE/LOCK literal-prefix). Glyph ground-truth pinned (camera-video/lock/clock-1/chain exist; shield/eye/copy/key/refresh ABSENT=invisible). Files+flags+acceptance+rollback in memory/wave_runs/video-flex-framework-design.md.
- 2026-06-14 · video-flex-framework-design (DESIGN video-studio-on-framework) · ✅ DONE · NEW design/PROVIDER-FRAMEWORK-PLAN.md (universal config-driven provider/connector registry: PG+FORCE-RLS, 3-tier transform openai_compat/named_provider/custom_field_map, AAD-bound per-tenant creds via Vault get_secret seam, SSRF guard, health/circuit-breaker, super-admin+BYO-key UI; strangler-rewires the 3 existing video/image/LLM abstractions) + VIDEO-STUDIO §9b Video-as-first-consumer. Flag PROVIDER_REGISTRY_ENABLED OFF; earner-safe; serialized vs RAG/Vault/Video.
- 2026-06-14 provider-framework-foundation W1-W3 INTEGRATED VERIFY ✅ ALL GREEN — 4/4 offline suites PASS (test_adapter_fieldmap 16 assertions, test_ssrf_guard 16, test_registry_offline 10, test_reveal_stepup 10). FORCE-RLS live on box: all 3 provider_* tables relrowsecurity=t + relforcerowsecurity=t. health-log append-only trigger present. firewall.py diff = purely additive (339a340,473, 0 deletion lines), box stays cd1ac5d1 (reveal scope NOT deployed, W4 mounts). py_compile OK, gitleaks 0. EARNER GATE: agent.py 9150fabe UNCHANGED, famit-agent PID 1477083 NOT restarted, /health 200, NO ring. design/PROVIDER-FRAMEWORK-PLAN.md W1/W2/W3 rows marked DONE. NEXT = W4 (first caller.py mount, serialize vs RAG LIVE + Vault deferred + Video).
- 2026-06-14 provider-framework W4 MOUNT ✅ DONE + DEPLOYED — built provider_registry/endpoints.py (token-deriving build_router: 16 routes, prefix /provider-registry to avoid colliding with the live legacy @app.get("/providers") LLM-router list) + W4 WRITES in store.py (create/update/delete def + upsert_credential + write_health_row, RLS WITH-CHECK _global write-lock) + mounted in caller.py:7309-7349 under PROVIDER_REGISTRY_ENABLED (default OFF, import+mount-guarded, can never crash startup). Endpoints: GET/POST/PUT/DELETE /provider-registry[/admin]/{id} + /credential + /reveal(-init) + /test + /health — all token-derived tenant (resolve_tenant), super-admin gated by require_super_admin (legacy-pw excluded), RLS via store is_admin=False, SSRF-guard on self_hosted base_url, custom_field_map JSONPath-validated no-eval, reveal via firewall.consume_reveal_step_up single-use jti. Deployed W3 firewall reveal scope WITH the mount. caller.py golden byte-diff = additive only (7309a7310,7349, 0 del/mod, box 52c59291->310ea9c9). FIREWALL golden: deployed b77c2cbe = pristine cd1ac5d1 + ONLY 339a340,475 (0 del/mod); box smoke generic step-up PASS + reveal single-use PASS. 12/12 offline endpoints tests PASS (on box venv python too); full suite 5/5. Backups caller.py/firewall.py.W4bak.20260614-210229. famit-caller restarted only; flag absent from .env => resting byte-identical (/provider-registry*->404 dormant, legacy /providers->401 intact); flag-ON in-process proof mounts 16 routes. EARNER GATE before+after PASS: agent.py 9150fabe UNCHANGED, famit-agent PID 1477083 NOT restarted, /health 200, 0 5xx, NO ring. Log: memory/wave_runs/provider-framework-w4-mount.md. NEXT = W5 strangler VIDEO cut-over (REGISTRY_FOR_VIDEO).
2026-06-14 voice-core-surgery SURGICAL-RC: root cause = engine routing to Sarvam Bulbul v2 (ab6777c), NOT language-pin/SCRIPT-RULE. Minimal restore = .env SARVAM_TTS_MODEL=bulbul:v3 + valid v3 speaker, hi-IN unchanged, keep 69374eb. Earner GREEN.
2026-06-14 · voice-core-surgery · [SURGERY] · ✅ DONE+DEPLOYED (.env-ONLY, aim-voice-agent restarted ONLY, ZERO code) · Applied Option A minimal restore on box /opt/famit-agent/.env: SARVAM_TTS_MODEL=bulbul:v3 + SARVAM_TTS_SPEAKER=priya (were unset → v2/anushka defaults = the broken knob). SARVAM_TTS_LANG NOT set → hi-IN unchanged. PRE-RESTART OBJECTIVE VALIDATION (real Sarvam synth + saarika round-trip STT, capsy venv, keys read at runtime never hardcoded): v2/anushka GARBLED "2 BHK"→"टू उसाई", "Codename Joy"→"हुड नेमो"; v3 (priya/kavya/ishita/shreya/simran all clean) → "बीएचके"/"कोड नेम जॉय"/बुक/कंफर्म correct, ₹2 करोड़ preserved = GARBLING GONE. API gave authoritative v3 speaker list (anushka/manisha/vidya/arya/hitesh = v2-ONLY); chose priya (cleanest brand+numeric round-trip). NOTHING REVERTED — SCRIPT RULE 69374eb KEPT (proven to help v2+v3); MLV mirror + FINAL LANG LOCK + RAG_INJECT_ENABLED + all flags intact. Language-auto per engine: Sarvam has no auto-enum → documented code-mix path IS v3+hi-IN+Devanagari-Hindi/Latin-English input (v3 normalizer passes Latin English through natively); STT saarika language=unknown (auto, unchanged); EL path dormant (Codename Joy is Sarvam-routed) → EL Hindi-voice fix deferred as P1. GOLDEN 5/5 = N/A-by-construction (zero edit to aim_voice_agent.py box md5 1614be09 UNCHANGED + prompt.py fb87ea56 untouched → byte-identical guaranteed). EARNER GATE GREEN before+after: agent.py 9150fabe UNCHANGED, famit-agent PID 1477083 NOT restarted, aim-voice-agent PID 2721961→2739156 (NRestarts=0, registered worker agent_name=manager, 0 errors), famit-caller NOT restarted /health 200, 0 voice 5xx (lone /provider-registry 500 = dormant W4 flag-off, unrelated, predates restart), NO ring. Backup .env.SURGbak.20260614-154923 (md5 1a8ffc07); rollback = cp backup + restart. Box scratch cleaned, no key persisted. ACCEPTANCE = founder real inbound call +918071583488. P1 deferred: Sarvam pronunciation dict + EL premium-tier voice/AIM_TTS_LANG fix. → memory/wave_runs/voice-core-surgery.md (## Phase: SURGERY)
