# Wave 2 Features — Final Build + Deploy (frontend)

Date: 2026-06-10
Box: root@143.110.247.249:/opt/famit-panel (famit-panel-2, hardened)
Branch (local): feat/premium-ui

## What shipped
Final build + FORTRESS deploy of the Wave-2 feature set:
- **/run** — Run-Campaign upgrade: multi-card UI + CSV/XLSX audience upload + filters
  (`app/run/page.tsx` 18.3 kB, `app/run/_lib/{audience,types}.ts`). Live HTML shows
  Audience + upload markers.
- **/workflows** — React-Flow (@xyflow/react ^12.11.0) node canvas workflow builder
  (`app/workflows/page.tsx` 75 kB, `_canvas.tsx`, `_editor.tsx`, `_nodes/{NodeInspector,WfNodeView}.tsx`,
  `_lib.ts`). Live HTML shows react-flow / xyflow / Trigger / Canvas markers.
- Module pages overhauled (Core_2 ports): /crm (+/crm/[id]), /forms (+/forms/[id]),
  /funnels, /booking, /ads, /ai-manager, /payments, /support, /campaigns, /leads.

## STEP 1 — Build (local)
- `npm install --legacy-peer-deps` → up to date (@xyflow/react already pinned in package.json).
- `npm run build` → **BUILD_EXIT=0**, no type/build errors. 33 routes generated. No fixes needed.

## STEP 2 — FORTRESS deploy
- Tar excl node_modules/.next/.git (also excl stale famit-panel.tgz + build.log) → 22M
  `/tmp/famit-panel-20260610-194109.tgz` → scp to box /tmp.
- **Backup FIRST:** `cp -a /opt/famit-panel /opt/famit-panel.bak.20260610-194109` (933M, incl node_modules).
- Stage-extract to /tmp/fp-stage (419 files) → `rsync -a --delete --exclude node_modules
  --exclude .next --exclude .env.local` into /opt/famit-panel (preserves box node_modules + env;
  .next regenerated by build). `.env.local` local==remote (`NEXT_PUBLIC_API_BASE=/api`), so excluding it is safe.
- `chown -R deployuser:deployuser /opt/famit-panel`.
- As **deployuser**: `npm install --legacy-peer-deps` then `npm run build` → **BUILD_EXIT=0** on box.
- `systemctl restart famit-panel` → active, "Ready in 1016ms". Service binds 127.0.0.1:3001 as deployuser (hardened unit).

## STEP 3 — Verify (all 200)
localhost:3001 — all 13 target routes = 200 (incl / /run /workflows).
public https://panel.famit.in — all 13 = 200:
/ /run /workflows /crm /forms /funnels /booking /ads /ai-manager /payments /support /campaigns /leads.
(One transient curl `000` on /payments was a network timeout; 3/3 retries = 200. App served it 200 on localhost throughout.)
Feature presence confirmed in live HTML: /run = Audience+upload; /workflows = react-flow/xyflow/Trigger/Canvas.

## STEP 4 — Rollback path (if ever needed)
```
systemctl stop famit-panel
rm -rf /opt/famit-panel && mv /opt/famit-panel.bak.20260610-194109 /opt/famit-panel
chown -R deployuser:deployuser /opt/famit-panel
systemctl start famit-panel
```
Backup retained on box: `/opt/famit-panel.bak.20260610-194109` (plus older .bak.20260610-183441, .153428).

## Notes / learnings
- @xyflow/react was already installed in node_modules; npm install was a no-op pull. Build is the gate.
- Deploy used rsync overlay (not full dir replace) to keep the box's prebuilt node_modules → fast incremental install. node_modules/.next/.env.local explicitly preserved.
- SSH key C:\Users\kunal\.ssh\do-blr-test\id_ed25519, no resets hit this run.
