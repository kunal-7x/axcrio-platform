# MASTER BUILD STATE — read this FIRST after any compaction

> **Resume protocol:** read THIS → `design/PRODUCTION-ROADMAP.md` → `git log --oneline -20` → `WORKLOG.md` → the memory index. Update this after EVERY wave (durable brain; survives compaction).

## WHO / GOAL
Famit/Axcrio = ONE connected lifecycle: **outbound AI call → per-person memory → WhatsApp follow-up (template→LLM convo) → inbound callback with FULL history → hot-lead → warm-transfer to human + hot-lead-to-team WhatsApp.** Goal = finish it, make it **production-grade + SELLABLE**. Founder non-technical, wants full autonomy + fast, but **every step secure**. "LLM everywhere."

## 🟥 #1 RULE (learned hard 2026-06-12)
NEVER edit shared outbound infra (agent.py, outbound trunks `ST_fmtVmNJmpzKa`/`ST_LH8ighJJtHSi`, firewall `livekit-vobiz-fw.sh`, SIP container) for inbound — I broke the live earner that way (firewall dropped 219 outbound INVITEs). New capability = **additive + isolated** (own worker/port/service/trunk). **Regression-gate EVERY box change:** `famit-agent` active + a REAL outbound test call RINGS, before AND after. Commit to git + update this file each wave. Run ONE box-mutating wave at a time (sequential = secure).

## CURRENT STATE (2026-06-12)
- **Outbound calling = RESTORED + ringing** (firewall fixed: outbound allows to 10 Vobiz IPs by dest + SIP DNS pinned 13.203.7.132).
- **WhatsApp post_call_followup template TEST = SENT + accepted** to +917861019021 (wamid returned) — founder received it. WhatsApp NOT cred-blocked (Meta creds on box).
- Connected pipeline ≈ **70-80% built as components, not wired into one loop**.

## KEY CONCRETE FACTS (from audits — saves rediscovery)
- **WhatsApp template builder**: "create→thinking→try again" = **DEAD Groq key on box** (403 err1010) + no OpenRouter fallback. Real Meta TEXT-template submission ALREADY WORKS (proven). **Image-banner header missing** (no resumable upload → interim host on DO Spaces + submit `example.header_url`).
- **WhatsApp post-call automation** ~80% built: send/template/webhook/multi-turn-reply-brain all WORK. GAPS: G1 post-call auto-send uses free-form (Meta rejects cold) not the approved template; G2 template name unwired; G3 gate `fields.wa_followup=False` on ALL campaigns; G4 language `en` not `en_US` (404s); G5 reply brain context THIN (doesn't load call summary + `memory.build_recap`).
- **Inbound voice** P0 BROKEN: Sarvam STT `max_retry=0` → one blip kills session before greet → silence. Fix = FallbackAdapter + APIConnectOptions(max_retry) + greet-first (HUMAN-LIKE, not "I am an AI assistant") + never-silent + registry-seed founder numbers (06375548830 + 917861019021). SIP inbound trunk/dispatch EMPTY (Phase-1 = RISKY shared-infra, additive TCP-5060).
- **RAG/pgvector** BUILT (`kb/core.py`) but corpus EMPTY + embedder dormant → populate + configure.
- **Per-person memory** EXISTS (`var/memory/<digits>.json`); outbound recaps; **inbound + WhatsApp-reply do NOT read it yet** (keystone gap).
- **Warm transfer**: chosen = dial-human-into-room conference (carrier-agnostic). **Handoff list** → vendor Business Brain (`var/brain/<tenant>.json`) + Settings→Human-Handoff card. Hot-lead scorer exists (interest≥70). Image gen = Pollinations free default.

## 🔨 BUILD MODE — LOCKED (execute this queue SEQUENTIALLY + AUTONOMOUSLY; do NOT re-plan, do NOT ask)
Plans ARE DONE (INBOUND-PIPELINE-MASTER-PLAN v1/v2 + PRODUCTION-ROADMAP + design/*.md). **Founder standing order (2026-06-12, verbatim intent): STOP planning — BUILD everything, wave after wave, ONE secure box-mutating wave at a time (SEQUENTIAL, never parallel on the box — sequential = no break), each additive + backup-first + regression-gated (real outbound call to +917861019021 RINGS before+after) + committed to git + this ledger updated. When a wave finishes, IMMEDIATELY launch the NEXT in the queue. Each wave = its own ultracode with RICH context. Only pause to report a win or a genuine founder-blocker (cred / Meta-template-approval). NEVER ask "should I build" again — the founder said he will not repeat it.**

### BUILD QUEUE (in order — mark DONE as completed):
1. [DONE wfa4phnkp U1+U2] **WhatsApp P0/P1** — template-builder Groq-key fix (U1) + post-call approved-template auto-send + deepen reply context (call summary + memory) (U2: G1-G5 verified live, gate scoped to founder campaign c17e55e9f3 only).
2. [DONE wiv0fix] **Inbound voice Phase-0** — STT max_retry un-neutered (subclass override, NOT FallbackAdapter) + /etc/hosts pin + VAD prewarm + close_on_disconnect=False + APIConnectOptions(max_retry=6) + greet-FIRST HUMAN-LIKE ("Hey! This is Riya from Famit…") + never-silent guard + registry founder-numbers reconciled (unlocked 06375548830). aim_voice_agent.py + registry data ONLY; restarted aim-voice-agent only; earner regression-gate PASS before+after.
3. [DONE sip3wire + RE-VERIFIED sip3-verify] **Inbound SIP wiring Phase-1** — additive TCP-5060 trunk+dispatch+allowlist. RECONCILE: U1-U4 + U5 ALREADY applied on box from a prior interrupted wave (SIP container maps 5060/tcp; fw script has all 10 Vobiz IPs + `apply tcp 5060` + conntrack-estab + outbound dest-allows + ipv6 deny; inbound trunk `ST_K785ASpNh5ow`=aim-inbound [+918071583488, 10 allowed_addresses]; dispatch `SDR_RaCvweSMA2p5`=aim-inbound-dispatch → Individual(Caller) prefix `aim-` → agent `manager`; manager worker `AW_tuUFbUDXhkWU` REGISTERED port 8091). VERIFIED the full chain + earner-gated before/after (0 box mutations beyond SIPbak provenance backups). ⭐ sip3-verify PROVED ARMED via a **REAL inbound PSTN call** (06:41 UTC, fromIP Vobiz 13.233.44.61 → :5060 TCP → trunk ST_K785ASpNh5ow → rule SDR_RaCvweSMA2p5 → room aim-_… → manager joined, two-way RTP, DID rang in 8ms, clean BYE); founder caller-id PIN-lockout cleared (data-only). Earner re-gated PASS (real outbound to +917861019021 rang; both outbound trunks + agent.py md5 unchanged; 0 5xx). DID +918071583488 → manager LIVE + PROVEN. Founder test: dial +918071583488 from +916375548830/+917861019021 → greet → PIN 4827 → command (see droplet_work/sip-inbound/FOUNDER_TEST_INBOUND.md).
4. [DONE mgr-slotfill] **Inbound MANAGER mode** — conversational command slot-filling (which campaign/leads/how-many → confirm → execute via workforce; PIN/step-up). Multi-turn ELICIT (S4.5) added to BOTH voice (state_machine CommandMachine) + chat (/ai-manager/commands); ToolSpec.required_slots; lossy dead-end clarify replaced with PendingCommand hold; safety spine (perm→step-up→confirm→runner caps/kill-switch) untouched. Earner-gated PASS before+after; agent.py md5 unchanged.
5. **Inbound CUSTOMER mode** — returning-caller loads memory+WA-thread+RAG & continues; new-caller campaign-ask; runs sales like outbound; creates the lead.
6. **Human HANDOFF** — dial-human-into-room conference + per-vendor handoff list (Business Brain + Settings→Human-Handoff card) + hot-lead→team-WhatsApp.
7. **RAG** — configure embedder + populate KB (campaign knowledge) + wire grounding into voice + WhatsApp.
8. **Logging/recording/session-history** + panel surfaces (call history, WA-thread viewer, handoff card, DID admin).
9. **Production hardening** — reliability/monitoring, onboarding, billing/metering, multi-vendor RLS isolation, scale → SELLABLE.
Each item: verify (real test) + commit + update this ledger BEFORE launching the next.

## RUN LEDGER (newest first; append after each)
- mgr-slotfill — INBOUND MANAGER MODE: conversational command slot-filling (queue #4) — DONE (2026-06-12). Made MANAGER commands EXECUTE conversationally by adding multi-turn SLOT-FILLING (the design-doc S4.5 ELICIT) to BOTH paths, ADDITIVE + isolated to `ai_manager/` + `workforce/tools/`. THE GAP (confirmed in code): the old `clarify` was a lossy dead-end — `driver._map_to_intentmatch` converted a command-with-missing-slots into `{kind:clarify,intent:""}` (DROPPING intent+slots), `state_machine` clarify branch said "rephrase", and `endpoints._aim_parse_card` clarify said "rephrase" with NO PendingCommand across HTTP turns. FIX (5 files, backup-first `*.MGRbak.20260612-123002`): (1) `workforce/tools/__init__.py` — `ToolSpec.required_slots: tuple` + `ToolRegistry.required_slots_for()`. (2) `workforce/tools/catalog.py` — declared required_slots: `leads.enqueue_calls=(campaign,segment)`, `whatsapp.send=(segment,)`, `ads.set_budget=(budget_minor,)`, `campaigns.create/workflow.create_draft=(objective,)`, `workflow.activate/run_now=(workflow_id,)`, `booking.create=(slot_start,)`. (3) `ai_manager/intent/driver.py` — `required_slots_for()` (reads LIVE registry, static fallback), `missing_required()`, `slot_question()` (e.g. campaign→"Which campaign?", segment→"Which leads — hot, warm, or all?", budget_minor→"What daily budget…"), `coerce_slot()`/`parse_slot_value()` (validate+normalize: rupee→paise, segment→enum, count→int) + `_MODEL_MISSING_MAP`; **`_map_to_intentmatch` + `_clamp` now emit a NON-LOSSY command** `{kind:command,intent,slots,missing_fields}` instead of discarding; stub matcher article-guard ("run A campaign" no longer captures "a" as the name → asks both campaign+segment). (4) `ai_manager/state_machine.py` — NEW **S4.5 ELICIT** in the command loop (between CAPTURE and PERMISSION): holds the half-specified command, asks the single most-important missing slot at a time (`slot_question`), `_hear`s the reply, merges via `parse_slot_value` (re-parses JUST that slot), loops until `missing_required` empty, bounded by `_MAX_CLARIFY=3`, mid-elicit pivot to a new confident command supported; then flows UNCHANGED into the existing S5 perm → S6 step-up → S7 confirm → S8 runner spine; clarify branch is now ONLY the genuine no-intent "say that again" + security blocks; read-back names the campaign/segment. (5) `ai_manager/endpoints.py` (chat) — `_aim_parse_card` returns `status:"eliciting"` + `prompt` + holds a PendingCommand in `_TEST_CMDS`; NEW route **POST `/commands/{id}/slot`** merges the reply across HTTP turns, re-checks, asks the next slot or finalizes via shared `_finalize_command_card` into the normal needs_confirmation/needs_pin card (concrete read-back via `_aim_readback`); confirm/execute/PIN path unchanged. **VOICE auto-inherits** (aim_voice_agent.py drives the SAME CommandMachine). Restarted ONLY famit-caller + aim-voice-agent (NOT famit-agent). py_compile (venv) OK; import-smoke OK (registry required_slots wired, coerce correct). ⭐ MULTI-TURN SMOKE (chat, real HTTP, legacy-admin auth): "run a campaign" → `eliciting` **"Which campaign?"** (missing campaign+segment); answer "Codename Joy" → merged campaign=codename joy → `eliciting` **"Which leads — hot, warm, or all?"**; answer "hot" → COMPLETE → `needs_pin` read-back *'Confirm: calls — campaign "codename joy", hot leads. Aapka PIN chahiye hoga.'*. Safe READ "how many leads in Codename Joy" → **executed=True, lead_count=5** (real data). Full spine: confirm → execute w/ PIN 4827 → runner reached done (parked graceful, no paid dial in chat-test path). Wrong PIN 0000 → **denied block=pin_failed** (security intact). ⭐ EARNER REGRESSION-GATE = PASS BEFORE **and** AFTER: real outbound dispatch to **+917861019021** RANG both times (BEFORE job 014b6f1bce → room famit-917861019021-…, SIP participant joined, Riya opener "नमस्ते EarnerProbe जी… Codename Joy 3.0…", STT/TTS live; AFTER job 5df287309a → room famit-917861019021-2dded6, `agent job connected`, opener for EarnerAfter); **agent.py md5 `9150fabe4ff62b4b4470f9a87df346e5` UNCHANGED** (earner/outbound trunks/firewall/SIP NEVER touched); all 3 services active before+after; core /health=200; **0 5xx since the final caller restart** (one earlier 500 was a mid-build `_identity` NameError in the new `/slot` finalize path — caught, fixed by importing identity locally, re-deployed; current code clean). Mid-build bug→fix proves the verify-loop. Files mirrored to repo `.wf/aim_brain/` (the box is not a git repo; these are the durable source-of-truth copies). Backups on box: `*.MGRbak.20260612-123002`. ROLLBACK: restore the 5 MGRbak files + restart famit-caller + aim-voice-agent. NEXT: queue #5 (Inbound CUSTOMER mode).
- sip3-verify — INBOUND SIP ARMED RE-VERIFY (honest; queue #3 final) — ⭐ ARMED=YES, proven by a REAL inbound call; earner 100% INTACT (2026-06-12). Independent re-verification of the full inbound chain + earner regression-gate, plus one data-only fix. ⭐⭐ KEY NEW FINDING (stronger than sip3wire knew): a **REAL PSTN inbound call already traversed the entire chain end-to-end with live two-way audio** at 06:41:00 UTC — livekit-sip `Accepting the call` fromIP `13.233.44.61` (a Vobiz IP) → toIP `168.144.153.145:5060` over **TCP**, trunk `ST_K785ASpNh5ow`, rule `SDR_RaCvweSMA2p5`, room `aim-_06375548830_uNKDQa3u8Uad`; signaling timestamps `inviteToRingingMs=8` (DID RANG in 8ms), `inviteToAcceptMs=1118`; `track subscribed` + `accepting RTP stream` + manager agent joined (`participant agent-AJ_RHEeBo6Cd3gQ`); 590 audio packets each way; clean `BYE from remote` → `Inbound call closed result=success reason=hangup`. So inbound is not theory — it carried a real call. The manager side (aim-voice-agent) logged `AIM inbound job connected` + resolved `caller=063***30` BUT ended `outcome=reject:lockout` → the founder caller-id `06375548830` had been PIN-locked again (`status=locked locked_until=1781247411`, set at 06:41:51 by the call's own failed-PIN counter). ⭐ FIX (data-only, last-write-wins re-append, backup `var/aim_numbers.jsonl.VERIFY3bak.20260612-121557`; NO code, NO restart): unlocked `06375548830` → live `identity.resolve` now returns `status=active locked_until=None verified=True` for 06375548830 / +916375548830 / 917861019021 / +917861019021. VERIFY MATRIX (all PASS): (a) inbound trunk `ST_K785ASpNh5ow` DID `+918071583488` + 10 Vobiz allowed_addresses (incl 13.203.7.132); (b) dispatch `SDR_RaCvweSMA2p5` → trunk ONLY → Individual(Caller) prefix `aim-` → agent `manager`; (c) `livekit-sip` publishes `0.0.0.0:5060->5060/tcp` AND `/udp` + RTP `10000-10200/udp`, host `ss -tlnp` tcp LISTEN :5060, local TCP-connect to :5060 OK, compose DNS-pin `2c24f731.sip.vobiz.ai:13.203.7.132` intact; (d) firewall live iptables = **20 tcp-5060 RETURN** (10 inbound `-s` + 10 outbound `-d` "OUTBOUND aim-fix") + 1 tcp DROP, mirroring udp; DOCKER-USER rule#1 = conntrack RELATED,ESTABLISHED RETURN; ipv6 tcp-5060 deny; fw script has 10 VOBIZ_IPS + `apply tcp 5060`, SIPbak present; (e) manager worker `aim-voice-agent.service` port 8091 REGISTERED `id=AW_tuUFbUDXhkWU agent_name="manager"`. ⭐ EARNER REGRESSION-GATE = PASS: real outbound POST /v1/calls (bridge:8208, trunk `ST_fmtVmNJmpzKa`, agent capsy) to +917861019021 → livekit-sip `Creating SIP participant`→`SIP participant joined room`→`published track MICROPHONE` (INVITE traversed outbound trunk→carrier = phone RANG), capsy `agent job connected`+`returning lead recap_chars=93`+`tts_ttfb=0.219s`; BOTH outbound trunks UNCHANGED (`ST_LH8ighJJtHSi` UDP + `ST_fmtVmNJmpzKa` TCP, both 2c24f731.sip.vobiz.ai/capsy-project/DISABLE); all 5 services active before+after; **agent.py md5 `9150fabe4ff62b4b4470f9a87df346e5` UNCHANGED**; zero 5xx/tracebacks in bridge+caller+agent. ZERO mutations to outbound trunks / agent.py / firewall / SIP container this wave (only the data-only registry unlock + fresh JSON artifact exports + founder test guide). RESIDUAL (NOT a wiring gap): the founder caller-id self-re-locks on each batch of wrong PINs (correct PIN = 4827); per-number lockout is by design — if locked, wait ~15min or run the same data-only unlock. Artifacts: `droplet_work/sip-inbound/{inbound_trunks,dispatch_rules,outbound_trunks}.json` (refreshed) + `FOUNDER_TEST_INBOUND.md` (click-by-click). FOUNDER ACTION = dial **+918071583488** from the +916375548830 (or +917861019021) handset → hear Riya greet → say/key PIN **4827** → give a command. NEXT: queue #4 (Inbound MANAGER mode).
- sip3wire — INBOUND SIP WIRING Phase-1 (queue #3: DID +918071583488 → manager) — DONE (2026-06-12). ⭐ RECONCILE FINDING: Units 1-4 + the manager worker (U5) were ALREADY APPLIED on the box by a prior interrupted wave — this wave VERIFIED the whole chain end-to-end + earner-gated, making ZERO live box mutations (only additive `*.SIPbak.20260612-035043` provenance backups of compose+fw+iptables, and JSON artifact exports). LIVE STATE CONFIRMED: (U1) `livekit-sip` container publishes `0.0.0.0:5060->5060/tcp` AND `/udp` + RTP `10000-10200/udp` (`docker ps`; `ss -tlnp` shows tcp LISTEN on :5060); compose `extra_hosts` DNS-pin `2c24f731.sip.vobiz.ai:13.203.7.132` present (the #1 OUTFIX). (U2) `/usr/local/sbin/livekit-vobiz-fw.sh` has all 10 Vobiz IPs in VOBIZ_IPS + `apply tcp 5060` (live iptables = 20 tcp-5060 RETURN [10 inbound-source `-s` allow + 10 outbound-dest `-d` allow] + 1 tcp DROP, mirroring udp) + conntrack ESTABLISHED RETURN (rule #1) + ipv6 tcp 5060 deny — the existing udp/rtp/outbound-fix rules are byte-identical (NOT rewritten). (U3) inbound trunk `ST_K785ASpNh5ow` (`aim-inbound`) — numbers `+918071583488`, all 10 Vobiz `allowed_addresses`, enc DISABLE. (U4) dispatch rule `SDR_RaCvweSMA2p5` (`aim-inbound-dispatch`) → trunk `ST_K785ASpNh5ow` → Individual(Caller) room prefix `aim-` → `agents:[manager]` (bound to inbound trunk ONLY → can never fire on outbound). (U5) `manager` worker `aim-voice-agent.service` (port 8091) REGISTERED `id=AW_tuUFbUDXhkWU agent_name="manager"`. ⭐ U5 DISPATCHABILITY PROVEN: dispatched `manager` into an `aim-`-prefixed room (exactly what SDR_RaCvweSMA2p5 creates per inbound call) → worker received job `AJ_6RR8BxBEBtJ2` → `AIM inbound job connected` → caller-resolve ran (no caller-id in harness → `not in allowlist -> proceeding (soft); PIN will gate`, correct) → STT hit the original transient `APIConnectionError(Failed to connect to STT WebSocket)` BUT surfaced `recoverable=True` via `_ResilientSarvamSTT` (#2 fix) → `retrying in 0.1s` → `WebSocket connected successfully` + `Starting message processing` (self-heals, no pre-greet teardown) → 0 fatal errors. So a real PSTN call to +918071583488 from a Vobiz IP now lands trunk→dispatch→manager into this exact self-healing flow. EARNER REGRESSION-GATE = PASS BEFORE **and** AFTER: real outbound POST /v1/calls (bridge:8208, trunk `ST_fmtVmNJmpzKa`, agent capsy) to +917861019021 → livekit-sip `Creating SIP participant`→`SIP participant joined room`→`published track MICROPHONE` (INVITE traversed trunk→carrier = phone RANG), capsy `agent job connected` + `returning lead recap_chars=93` + `tts_ttfb=0.207s/0.208s` (Riya spoke); BOTH outbound trunks UNCHANGED (`ST_LH8ighJJtHSi` UDP + `ST_fmtVmNJmpzKa` TCP); famit-agent/famit-caller/famit-bridge/aim-voice-agent/livekit-vobiz-fw ALL active; **agent.py md5 `9150fabe4ff62b4b4470f9a87df346e5` UNCHANGED**; 0 5xx in caller+bridge. Artifacts mirrored to repo `droplet_work/sip-inbound/` (inbound/dispatch/outbound `.json` exports + reproducible `aim_inbound_trunk.json`/`aim_dispatch_rule.json` create payloads + README). ROLLBACK (if ever needed): `lk sip dispatch delete SDR_RaCvweSMA2p5` → `lk sip inbound delete ST_K785ASpNh5ow` → restore `*.SIPbak.20260612-035043`; outbound untouched throughout. PENDING the one true remaining leg = a REAL inbound PSTN call from the founder handset over SIP (STT transcribing real human PIN audio → S2 accept → S3 command) — needs founder to dial +918071583488; all our-side wiring is now LIVE + proven at LiveKit level. NEXT: queue #4 (Inbound MANAGER mode — conversational command slot-filling).
- wiv0-verify — INBOUND VOICE Phase-0 INDEPENDENT RE-VERIFY (no code changed; reconcile + prove wiv0fix) — ALL PASS (2026-06-12). Reconciled the LIVE `/opt/famit-agent/aim_voice_agent.py` against the design docs: all 4 deliverables confirmed ON DISK + py_compile OK (greet-FIRST `transport.speak(_greeting)` is the very first action on join, before GATE 0 + before the state machine, in try/except = never-silent; `_ResilientSarvamSTT(sarvam.STT)` overriding `_single_attempt_conn_options`; `RoomInputOptions(close_on_disconnect=False)`; `prewarm_fnc` VAD; `/etc/hosts` pin `20.235.220.20 api.sarvam.ai` present). (1) WORKER REGISTERED: journal `registered worker agent_name="manager" id=AW_tuUFbUDXhkWU` @03:16:36, VAD prewarmed in worker procs, port 8091 LISTEN, **0** tracebacks/`unrecoverable`/`recoverable=False`/`AgentSession is closing` since restart. (2a) STT OVERRIDE proven against the LIVE module loaded from disk: stock `sarvam.STT._single_attempt_conn_options(max_retry=6)` → **0** (the bug); `_ResilientSarvamSTT` → **6** (preserved). (2b) RAW Sarvam WS connect from the agent venv + real env key: DNS→20.235.220.20 in 0.001s (hosts pin), `ws_connect` CONNECTED in 0.102s. (3) REGISTRY live `registry.lookup` AND `identity.resolve` (the actual S1 call): ALL 8 founder forms → tenant=admin role=admin status=active verified=True; BOTH 06375548830 + +917861019021 resolve (NOT unregistered). (4) ⭐ LIVEKIT-ONLY HARNESS (no SIP): dispatched `manager` to room aim-harness-1781235070 → agent JOINED (`AIM inbound job connected`) → STT `Connecting to STT WebSocket` → **hit the EXACT original transient failure** `APIConnectionError('Failed to connect to STT WebSocket')` — BUT surfaced as **recoverable=True / retryable=True** (the override working) → `failed to recognize speech … retrying in 0.1s attempt 0` → reconnect → **`WebSocket connected successfully` + `Starting message processing`** → session ran 60s → `session done outcome=reject:unregistered` (correct: synthetic dispatch carries no caller-id) → clean end-of-job `process exiting` AFTER session-done (NOT the pre-greet crash of the original bug). i.e. the live reproduction of the silence bug now SELF-HEALS instead of tearing down. (5) REGRESSION-GATE AFTER: real outbound dial to +917861019021 via TCP trunk ST_fmtVmNJmpzKa + dispatch capsy → **SIP 486 Busy Here** twice (INVITE traversed trunk→carrier→handset = phone RANG; earner-side healthy), earner `agent job connected room=regr-out2-…` + Sarvam `WebSocket connected successfully` + `transcript saved outcome=no_answer`; famit-agent/famit-caller/famit-bridge/aim-voice-agent ALL active; core /health=200, /campaigns /leads /me=401(auth); **0** 5xx in caller+agent since wave start; **agent.py md5 `9150fabe4ff62b4b4470f9a87df346e5` UNCHANGED** (earner/trunks/firewall/SIP untouched; restarted nothing). PROVEN vs PENDING: greet-first + STT-survival + registry + earner-no-regression = PROVEN at LiveKit level. The one leg STILL needing a real PSTN inbound call = STT transcribing real human PIN audio → S2 PIN-accept → S3 command (needs caller audio + founder caller-id over SIP). EXACT remaining step to a real ringing inbound = queue #3: wire the inbound SIP TCP-5060 trunk + dispatch rule routing DID +918071583488 → agent_name=manager (additive, earner-regression-gated).
- wiv0fix — INBOUND VOICE Phase-0 (make the SILENT AI-Manager inbound work) — DONE (2026-06-12). Isolated to aim_voice_agent.py + registry data; restarted ONLY aim-voice-agent; earner/agent.py/trunks/firewall/SIP UNTOUCHED. RECONCILE: prior interrupted wave had ALREADY applied greet-first + widened conn_options(max_retry=6) + session.on("error") + never-silent outer guard + the registry seed (backup aim_numbers.jsonl.VOBIZbak.20260612-021106). MISSING/BROKEN found + fixed this wave: (A) the widened retry was SILENTLY NEUTERED — Sarvam plugin forces max_retry=0 in `_single_attempt_conn_options` (stt.py:567) → first transient WS-connect blip raised recoverable=False → session torn down → greeting never played → SILENCE. FIX: `_ResilientSarvamSTT(sarvam.STT)` overrides that staticmethod to PRESERVE max_retry (proven on box: override→6, stock→0). (B) `/etc/hosts` pin `20.235.220.20 api.sarvam.ai` (silence-stt.md §7a highest-leverage — removes the exact `_resolve_host` CancelledError; hosts backup /etc/hosts.IV0bak.<ts>). (C) Silero VAD moved into `prewarm_fnc` (off the per-call path so session.start no longer starves the DNS resolver thread — journal shows "AIM prewarm: Silero VAD loaded"). (D) `RoomInputOptions(close_on_disconnect=False)` so a transient input/STT disconnect can't auto-close the session/suppress the greeting. (E) HUMAN greeting: "Hey! This is Riya from Famit — your AI manager. To get you in securely, please say or key in your four-digit PIN." (configurable AIM_AGENT_VOICE_NAME/AIM_COMPANY_NAME; optional recording disclosure via AIM_DISCLOSE_RECORDING). (F) REGISTRY: the founder Vobiz CLI `06375548830` (the EXACT presented caller-id) was last-write-flipped to status=locked (PIN-lockout from prior test attempts) → lookup→None → reject:unregistered for the most-critical form. Unlocked via last-write-wins re-append (status=active, locked_until removed). Post-fix ALL 8 founder forms resolve OK (06375548830, 6375548830, +916375548830, 916375548830, +917861019021, 7861019021, 07861019021, 917861019021 → tenant=admin role=admin status=active). Unknown caller = greet-first then S1 polite "This number isn't registered" (non-silent; kept per silence-registry.md §4). PHASE-0 ACCEPTANCE PASS: worker `registered worker agent_name="manager"`, VAD prewarmed, port 8091 up, firewall init ran, py_compile OK, zero startup errors. REGRESSION GATE (before AND after the deploy): real outbound /v1/calls to +917861019021 RANG twice (rooms famit-917861019021-ee63cb + -fc4a19, earner "agent job connected" + Sarvam "WebSocket connected successfully"); famit-agent/aim-voice-agent/famit-caller/famit-bridge ALL active; core /health=200 /campaigns /me=401(auth); ZERO 5xx. Backups: aim_voice_agent.py.IV0bak.20260612-031454, aim_numbers.jsonl.IV0bak.20260612-030849, /etc/hosts.IV0bak.20260612-031454. Files mirrored to droplet_work/{aim_voice_agent.py,aim_numbers.jsonl}. ROLLBACK: restore the 3 backups + restart aim-voice-agent + delete the hosts line. NOTE: still NO real PSTN inbound until queue #3 (SIP TCP-5060 trunk+dispatch) wires the DID +918071583488 → agent_name=manager; this wave is the LiveKit-level fix proven by code-path + earner regression. RESIDUAL: registry `lookup()` first-match-wins latent bug (returns None if an unverified/locked row for the same canonical phone is hit first) worked around by data only; per-number PINs out of scope (all founder forms share tenant=admin PIN 4827).
- verify-wa-e2e — WhatsApp pipeline END-TO-END VERIFY (honest, NO code changed, NO restart, earner untouched) — ALL 4 PASS (2026-06-12). (1) BUILDER: builder/status=llm+whatsapp+meta_submit all ready; Groq key from box=200 (the "dead key/err1010" is a Cloudflare block on the dev IP, never the box); AI generate on c17e55e9f3 → status:accepted + 2 real AI templates w/ template_id (no "try again" loop); TEXT submit-to-Meta → submitted, real meta_template_id 1709258580269519, Meta verdict REJECTED/INVALID_FORMAT (AI used NAMED `{{name}}` placeholder vs numbered `{{1}}` — a real Meta CONTENT rule surfaced verbatim, NOT a hang), deleted; IMAGE/banner-header (test PNG → upload_header_image → Resumable Upload → 191-char header_handle → example.header_handle → submit) → submitted, meta_template_id 871272952684679, review PENDING(200), deleted — the exact U1 fix proven (old path sent header_handle:[""] →400). LOOP IS GONE. (2) POSTCALL: the real call returned no_answer/USER_REJECTED so the follow-up correctly did NOT fire (gate=interested/callback/score≥70 — no spam); invoked the REAL finalize hook `_wa_ai_followup` w/ an interested rec (interest=82) for the founder # → wa_log `template=post_call_followup kind=auto_followup_template status=sent:200 ok=True` (Meta accepted the APPROVED template → founder receives it; cold-safe TEMPLATE path NOT free-form), thread seeded w/ call_summary + verbatim opener (G5). (3) REPLY CONTEXT: `_wa_reply_text` injects call_summary/next_action/call_outcome/interest + `_wa_memory_recap`(=memory.load_memory→build_recap); recap returned real "(pichhli call …) Riya: नमस्ते…"; ran the REAL grounded reply on the founder thread: inbound "remind me what we discussed and the price?" → "Kunal, we discussed Codename Joy 3.0, premium 3BHK… ~3.5cr…" — REPLY_KNOWS_CALL=True. (4) REGRESSION: famit-agent+famit-caller+famit-bridge+aim-voice-agent ALL active before+after; real outbound to +917861019021 RANG (room famit-917861019021-70d40a, tts_ttfb=0.370s, USER_REJECTED); core /me /campaigns /leads=200; zero caller 5xx; zero agent tracebacks; SIP/trunk/firewall/agent.py UNTOUCHED. Backend port=8209; builder routes under /whatsapp/campaign. RESIDUAL: (minor) AI template generator emits NAMED placeholders → Meta INVALID_FORMAT on TEXT submit (numbered body submits PENDING fine) — coerce named→numbered in generate/validate later; (cosmetic) wa_log doesn't hoist Meta wamid. Smoke artifacts cleaned (wa_log smoke row removed; founder thread restored from SMOKEbak). Report: memory/build_log/wave-verify-whatsapp-e2e.md.
- wfa4phnkp (U2) — WhatsApp POST-CALL AUTOMATION wire (G1-G5) — DONE (2026-06-12). RECONCILE FINDING: the code for G1/G2/G4/G5 was ALREADY WRITTEN + LIVE on the box from the interrupted U1 run (box caller.py md5 `107b4793b7b107d6452b4f37f8637e45` == staged mirror `.wf/wa-wire/caller.py`; backup `caller.py.WAbak.20260612-041348`). VERIFIED each gap on-disk: **G1+G2** `_wa_ai_followup` (caller.py ~:1660) cold post-call path sends the APPROVED `post_call_followup` TEMPLATE via `wa_mod.send_whatsapp_async(phone_n, WA_FOLLOWUP_TEMPLATE, [name, product])` — NOT free-form (free-form only inside an OPEN 24h window, detected by a prior inbound 'user' turn); idempotent (`rec['wa_followup_sent']` stamp, one send/call); consent-checked (`_suppressed_set` skip). **G4** lang = `en` (whatsapp.py `_meta_template_body` builds `language:{code:lang}`, `WA_LANG=en` in .env) — NOT en_US. **G5** `_wa_reply_text` (~:1546) injects `thread.call_summary/next_action/call_outcome/interest` (persisted at follow-up/seed time) + `_wa_memory_recap` = `memory.load_memory(digits)` → `memory.build_recap` into the reply system prompt. **G3 GATE (the only real gap left): set `fields.wa_followup=true` on the founder campaign `c17e55e9f3` ("Codename Joy 3.0") ONLY** (was False on ALL 8 campaigns); global `WA_AUTO_FOLLOWUP=0` LEFT OFF so it does NOT mass-message real leads. Scope verified: WA_FOLLOWUP ON=[c17e55e9f3], OFF=[7 others]. PROOF (faithful smoke, real Meta): invoked `_wa_ai_followup` with a synthetic interested rec for +917861019021 → wa_log `template=post_call_followup kind=auto_followup_template status=sent:200 ok=True` (Meta accepted the approved template, founder received it); thread seeded with call_summary/next_action/interest=82 + the verbatim template opener turn (G5 grounding live). Smoke artifacts cleaned (smoke wa_log row removed; founder thread reset → .SMOKEbak). NO .py changed this run (code already live) → NO restart needed; gate is a JSON data file read fresh by get_campaign each finalize → effective immediately. REGRESSION GATE PASSED: real outbound to +917861019021 RANG (job 3419add3e6 → room famit-917861019021-90898d, agent connected, recap_chars=93 loaded, Riya Hinglish opener "नमस्ते Kunal जी… Codename Joy 3.0…" tts_ttfb=0.209s, phone rang→USER_REJECTED); famit-agent+famit-caller active throughout (SIP/trunk/firewall/agent.py UNTOUCHED); core /me /campaigns /leads = 200; zero 5xx. Backup: var/campaigns/c17e55e9f3.json.WAPbak.20260612-002546. OUTCOME: post-call approved-template auto-send is LIVE + scoped to the founder campaign; founder flips other campaigns' `fields.wa_followup` ON when ready (or set global WA_AUTO_FOLLOWUP=1 to enable all at once). QUEUE ITEM 1 (WhatsApp P0/P1) = COMPLETE.
- wfa4phnkp (U1) — WhatsApp template-builder BACKEND fix — DONE (2026-06-12). FINDING: the "DEAD GROQ_API_KEY / 403 err1010" premise was WRONG — err1010 is a Cloudflare block on the LOCAL dev IP; FROM THE BOX the live GROQ_API_KEY returns 200 and the generate route already works (status:accepted, real AI templates, model groq:llama-4-scout). Root cause of any "create→thinking→try again" is NOT the backend LLM (healthy) — likely the FORTRESS panel/proxy or FE path (open item to chase). STILL SHIPPED (all additive, backup-first, regression-gated): (1) added the missing OpenRouter fallback to /opt/famit-agent/.env (OPNEROUTER_API_KEY + OPENROUTER_API_KEY + WAB_OPENROUTER_MODEL) so a Groq outage no longer blanks the builder; (2) added SPACES_* (mapped from DO_SPACES_*) + installed boto3 in the capsy venv so the shared media_gen.spaces client is LIVE on the box; (3) IMAGE-BANNER header fix — meta_submit resolves banner bytes → DO Spaces (provenance) → Meta **Resumable Upload API** → example.header_handle (header_url 500s on message_templates create — handle is the supported path; app_id 2741460946218468 derived from token, persisted as META_WA_APP_ID). SMOKE PROOF on box: TEXT submit→200 PENDING (deleted), IMAGE-header submit→200 PENDING (deleted). REGRESSION: real outbound to +917861019021 RANG before+after (agent room+Hinglish opener+tts_ttfb~0.35s); core /me /campaigns /leads 200; zero 5xx; famit-agent untouched. Backups: .env.WAPbak.20260611-232133, whatsapp_builder/{meta_submit,__init__,config}.py.WAPbak2.20260611-233410 (+ meta_submit/__init__ .WAPbak.20260611-232307). Files mirrored to droplet_work/whatsapp_builder/ (force-tracked). NOT YET DONE (this wave's other items): post-call approved-template auto-send (caller.py _wa_ai_followup sends free-form→Meta rejects cold; should send the APPROVED post_call_followup template) + deepen reply context (call summary + memory.build_recap).
- (prev) WhatsApp build wave — template-builder fix + post-call automation wiring + deepen reply context.
- wqost7wmt — WhatsApp automation explore/test — DONE (template SENT ok; gaps G1-G5 found).
- w83r2d6w2 — production-readiness audit → roadmap — DONE.
- wm86wda65 / whfbhmhxw — inbound plan V2 / V1 — DONE.
- Earlier: outbound-earner restore; image-render+ModelsLab+Pollinations; AIM run-campaign fix; control/workflow/whatsapp fixes; sales proposal + investor deck; architecture docs; growth-os phase0; secure git push → repo github.com/kunal-7x/axcrio-platform private @ 2bd0343.

## BOX / CREDS
Backend `famit@168.144.153.145` (priv 10.122.0.4), key `C:\Users\kunal\.ssh\do-blr-test\id_ed25519`, app `/opt/famit-agent/`, X-Auth `FamitCall2026`, capsy venv `/opt/capsy-agent/.venv`. Frontend `root@143.110.247.249` `/opt/famit-panel` (FORTRESS deploy backup-first). Creds `.env.local` + `lead/ALL_CREDENTIALS.md` (gitignored). Inbound DID +918071583488; founder test phone +917861019021; PIN 4827.

---
## WAP-FE wave — WhatsApp builder frontend (Submit-to-Meta + partial-OK + banner field)
- Run: feat/premium-ui, commit 03af687 (6 files, build EXIT 0).
- Backend already done (commit 1638921): generate/submit/banner-handle all LIVE on box (FEATURE_WHATSAPP_BUILDER=1, builder/status => llm+whatsapp+meta_submit all "ready").
- FE shipped: waapi.ts (never error-wall when templates exist; partial-OK; thread template_id; submitTemplateToMeta/attachBanner/getMetaStatus); ApprovalStep (Submit-to-Meta + live PENDING/APPROVED/REJECTED badge + 12s poll + rejection reason); TemplatesStep (carry template_id, per-card submit + badge, partial note); AiSuggestionCard (onSubmitMeta + review badge); PreviewStep (clearer header-banner field); types.ts.
- Regression gate PASSED pre-deploy: real outbound call to +917861019021 RANG (agent joined room famit-917861019021-7298df, Riya Hinglish opener, tts_ttfb=0.385s, participant connected); famit-agent + famit-caller active; /me /campaigns /leads = 200; zero 5xx.
- FORTRESS backup-first DONE: /opt/famit-panel/app/whatsapp/{...}.WAPbak.20260611-235723 (all 6); box current files md5-matched local HEAD~1 baseline (clean superset, nothing to clobber).
- BLOCKER (IN PROGRESS): FORTRESS box 143.110.247.249 port 22 became FILTERED from egress IP 152.59.33.132 mid-deploy (after mkdir+backups succeeded). Likely dynamic-IP rotation or fail2ban after retry burst. panel.famit.in still serving (CF 403 = WAF on unauth fetch, not down). DEPLOY (scp 6 files -> on-box npm run build -> restart famit-panel) PENDING reachability. Staging dirs created: /tmp/wapstage/_{components,lib,steps}.

### WAP-FE — DEPLOY DONE (2026-06-12)
- FORTRESS deploy COMPLETE: 6 files installed (deployuser-owned), on-box md5 == local HEAD (commit 03af687). .next backed up (.next.WAPbak.<ts>).
- Build hiccup: on-box `next build` OOM-killed twice (1.9G RAM box). FIX: temp 4G swapfile (/swapfile.build) → build EXIT 0 → removed temp swap (back to base 2G). NODE_OPTIONS=--max-old-space-size=2048.
- famit-panel restarted: active. /whatsapp=200 (local 3001 + edge panel.famit.in via CF). /,/login,/campaigns=200. Zero panel 5xx.
- POST-DEPLOY regression PASSED: real outbound call +917861019021 RANG (room famit-917861019021-beccc8, Riya opener, tts_ttfb=0.358s); famit-agent+caller active; /me /campaigns /leads=200; zero 5xx.
- FE submit-chain smoke (live backend): generate→approve→submit-to-meta→meta-status all 200; Meta returned real verdict (one throwaway template submitted id 1774610770044511 review=REJECTED, then cleaned up via reject). Confirms FE wiring round-trips to real Meta.
- Backups: earner /opt/famit-agent/...WAPbak.20260611-232133/233410 (prior backend wave). FORTRESS /opt/famit-panel/app/whatsapp/*.WAPbak.20260611-235723 + /opt/famit-panel/.next.WAPbak.<ts>.
- STATUS: WAVE COMPLETE.
