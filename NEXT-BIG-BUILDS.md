> 🎯 **RUN-PLATFORM MASTER PLAN (2026-06-14) → `design/RUN-PLATFORM-MASTER-PLAN.md`** — ✅ **WAVE A DONE (2026-06-14)**: env billing fixes (USD_INR=95.2, EL=4.76, Sarvam v2/v3 split) + inbound provider-lock (INBOUND_PROV_LOCK=1 in aim-voice-agent drop-in, PID `2525014` proc env confirmed) + funnels mount (already live, Fix #7 PASS). Golden 5/5 exit 0; earner gate PASS (agent.py `9150fabe…` UNCHANGED, famit-agent PID `1477083` NOT restarted, /health 200, 0 5xx, NO ring). Commits `9e18231`+`ab6777c`. ✅ **WAVE B DONE** (prior deploy — voice preview fix shipped). ✅ **WAVE C DONE (2026-06-14)**: crazy Run UI (4-step stepper + exclude-called toggle + provider-lock banner + CostBreakdown w/ Telephony) + W4 CRM Memory tab unified + deployed. Branch `fe/unify-run-wavec` commit `fa99acb`. BUILD_ID `TU16Mn1DcJVmxnxr2GVyL`. LIVE on panel.famit.in. Earner gate PASS. → **WAVE D NEXT** (performance / recordings / RAG / next in queue). Honest: the lock fixes the COST-LEDGER label; the wallet invoice is still flat-rate `_charge_call` (GATED Wave G).

# NEXT-BIG-BUILDS — COMPLETE MASTER QUEUE (post-audit 2026-06-13)

> Single sequenced backlog of EVERYTHING planned-but-unbuilt across the whole project (reconciled vs the LIVE box by audit a7b7853). Build TOP-DOWN, one box-mutating wave at a time (shared caller.py + panel + FORTRESS deploy + the server rate-limits heavy parallel load). READ-ONLY research may parallelize; BUILDS are sequential. EVERY wave: explore→research→design→build (backend→frontend→DB→AI→security)→verify on the founder's REAL flow, **autonomously add the out-of-box / sellable / differentiating features he didn't name** (global-memory mandate), earner-gated (agent.py md5 `9150fabe4ff62b4b4470f9a87df346e5` untouched), append AGENT_LEARNINGS + update ORCHESTRATOR.

## 🧠 VOICE-BRAIN EPIC (CORE HEART) — megaplan COMPLETE → `design/VOICE-BRAIN-MASTER-PLAN.md` (read first)
The adaptive-human-voice-brain epic (5 needs A-E: dynamic vendor script→persona, lossless full-context store <50ms, real-human Hinglish + telecaller-KB, multi-channel call+WhatsApp memory, blind-spot sweep) is fully designed + red-teamed. Build inbound-first, earner-safe, ONE box-mutating wave at a time. ✅ **W1 DONE + DEPLOYED + VERIFIED (2026-06-14)** — dynamic vendor-script→persona + lossless `fields["raw_script"]` store + Script Studio UI. SHIPPED: caller.py + prompt.py v2 + aim_voice_agent.py to box `168.144.153.145` (restart famit-caller + aim-voice-agent only); Script Studio UI to FORTRESS (BUILD_ID `Ykm_1fVt267VDkPib8uVg`); flag `VENDOR_SCRIPT_INJECT=1` via systemd drop-in on **aim-voice-agent ONLY** (earner env clean). 5/5 verify PASS (lossless byte-equal http==disk sha `97839b63`; dry-run adopts vendor greeting; canary not echoed + `</vendor_script>` defanged; legacy goldens 5/5 byte-identical; env-flag drives inbound splice). FULL EARNER GATE PASS (golden 5/5 byte-identical flag off+on, agent.py `9150fabe…` unchanged, famit-agent PID 1477083 never restarted, /health 200, 0 5xx, NO ring). Founder recipe `FOUNDER-SCRIPT-STUDIO.md`; state `design/W1-DEPLOY-STATE.md`. RESIDUAL: only a real inbound call to the DID proves the LIVE mic/voice; outbound earner stays flag-OFF pending sign-off+ring. ✅ **P0-LEAK DONE + DEPLOYED + VERIFIED (2026-06-14)** — closed the cross-tenant memory + WhatsApp-thread leak for the INBOUND + WhatsApp paths. `memory.py:_path_for(phone,tenant_id)`→`{tenant}/{phone}.json` with a TENANT-CHECKED legacy fallback + migrate-on-read (a legacy file owned by a DIFFERENT tenant is NOT returned — load-bearing check `memory.py:110-113`); WA threads tenant-scoped; unknown inbound WA→`_unrouted` (`caller.py:245`, never ADMIN_ID); hardcoded "Riya" replaced by per-campaign/tenant agent name (`build_recap(agent_name=…)`, default-only fallback "Riya"). Deployed caller.py + memory.py + aim_voice_agent.py to box `168.144.153.145` (backups `*.LEAKbak.20260614-052257`); restarted famit-caller + aim-voice-agent ONLY. **Live re-verify on the deployed code: T1/T2/T2b/T4 + WA-`_unrouted` all PASS; box md5 == local mirror (memory.py `cb70e1d7…`, caller.py `992c08ff…`).** EARNER GATE before+after PASS (agent.py `9150fabe…` UNCHANGED, famit-agent PID 1477083 NOT restarted, /health 200, 0 5xx, NO ring). Commit `4db497f`; state `design/P0-LEAK-STATE.md`. **RESIDUAL: the OUTBOUND EARNER (agent.py/PID 1477083) still runs the OLD in-proc memory.py + writes legacy flat paths — it fully closes on ITS next deploy+ring (founder-signed W-OB). Returning-lead memory is NOT lost meanwhile (the tenant-checked fallback still loads the correct tenant's legacy files).** ✅ **W2 DONE + DEPLOYED + VERIFIED (2026-06-14)** — `context_store.py` LRU+Redis-version-stamp+disk-mtime cache wired into `ai_manager/voice_tools.campaign_fields`; `CTX_CACHE=1` drop-in on aim-voice-agent ONLY (earner env clean). 5-probe live-box VERIFY PASS: P1 COLD 103ms · P2 WARM 0.164ms (< 5ms) · P3 bust immediate · P4 CTX_CACHE=0→None · P5 campaign_fields cold=1 fetch/warm=0 fetch CACHE_HIT=True. EARNER GATE PASS (agent.py `9150fabe…` UNCHANGED, famit-agent PID `1477083` NOT restarted, /health 200, 0 5xx, NO ring). **NEXT in the epic = W3** (multi-channel memory PG + durable post-call extraction).

## P0 — NOW / NEXT (core, founder-reported, quick)
0. ✅ **🚀 APP PERFORMANCE OVERHAUL — DONE + INTEGRATED-VERIFIED 6/6 PASS 2026-06-14** [founder's #1 pain]. All 6 units LIVE + UNIT-7 honest integrated re-verify over real HTTP (earner gate before+after PASS — md5 `9150fabe…` unchanged, famit-agent PID 1477083 never restarted, /health 200, 0 5xx, NO ring). MEASURED: `/calls`+`/leads` paginate (`{total,next}`); recordings `playable` flag (real 56-58KB→play, 0-byte 486-busy→"preparing"); CRM detail 68→41ms repeat-open; react-query cache (tab-back instant, staleTime 30s); VirtualRows on calls+leads; code-split @xyflow+Aurora + NavLink prefetch; transcript AI-LEFT/customer-RIGHT; edge gzip `/api/calls` **27425→2746B (90%/10x)** + `_next/static` immutable+Cf-HIT + analytics visibility-gate. BUILD_ID `p6hSTJX9R46-NQdLf8Daw`; 200 on / /login /calls /crm /run /ai-manager /analytics loopback+edge. Report `memory/build_log/wave-build-perf-overhaul-VERIFY.md`. RESIDUAL (deferred): P7 stretch RSC/streaming on analytics/dashboards. **→ NEXT = item 3 multilingual voice.** _(original diagnosis + 7-phase plan retained below)_ —
   **🚀 APP PERFORMANCE OVERHAUL** [founder's #1 pain — ✅ DIAGNOSIS DONE → `design/latency-diagnosis-v2-PERF-PLAN.md`]. RANKED root causes (evidence): R1 NO client cache (no SWR/react-query → every tab re-fetches from zero = THE 10-20s) · R2 all 53 pages `'use client'`, 0× code-split/SSR → blank until JS+data · R3 lists load all (`getLeads` no limit lib/api.ts:487; `/calls?limit=200` hard cap no offset/total caller.py:4193) · R4 backend reads whole `calls.json` to RAM + O(N) scan + N+1 transcript reads (caller.py:1062/1283/4206) · R5 CRM detail = full timeline rebuild + 3 serial DB hits/open (caller.py:3056) · R6 nginx `/api/` no gzip/brotli/cache + 30s poll no visibility-gate. 7-PHASE FIX (impact-first): (1) React-Query cache + Suspense skeletons [panel-only, tab-back INSTANT] (2) server keyset-pagination `/calls`+`/leads` + virtualized lists (3) code-split heavy deps (@xyflow/@tiptap/recharts/Aurora) + prefetch-on-hover (4) CRM detail rebuild-on-write + parallel reads (5) backend calls store indexed + payload trim (drop N+1) (6) edge gzip/brotli + cache `_next/*` + gate the poll (7) stretch RSC streaming. SAFE (panel + caller.py/ai_asset; NEVER agent.py). **TOP PRIORITY.** PROGRESS: ✅ UNIT-1 backend `/calls`+`/leads` pagination+slim+N+1-fix LIVE (`dfb663f`); ✅ UNIT-2 CRM-detail speedup + recordings HEAD-gate LIVE (`40caf3c`); ✅ UNIT-3 FE react-query cache + skeletons (`d42d130`); ✅ UNIT-4 FE infinite-scroll + row virtualization on call-logs+leads (`7068bd7`); ✅ **UNIT-5 FE code-split (@xyflow editor+minimap + Aurora loader via `next/dynamic` ssr:false) + prefetch-on-hover NavLink + FIXES-2b transcript L/R fix (`d48ed46`) + 🚀 SINGLE FORTRESS DEPLOY of UNITs 3/4/5 LIVE** (BUILD_ID `0NZEhMOVPoK-uCqp9CQp5`, 200 on / /login /calls /crm /run /ai-manager on loopback + panel.famit.in edge, famit-panel restarted only, earner gate PASS md5 `9150fabe…` unchanged / never restarted / caller /health 200, NO ring). ✅ **UNIT-6 EDGE LIVE (phase 6) — FORTRESS nginx gzip on JSON API (`/api/*` 27425B→2746B on the wire, ~90%/10× smaller; `gzip_proxied any;` load-bearing; brotli NOT in nginx build → gzip only) + immutable `/_next/static/` cache (`max-age=31536000, immutable`) + analytics 30s-poll visibility-gate** (nginx reload not restart, backups `*.PERFbak.20260613-200515`; FE BUILD_ID `p6hSTJX9R46-NQdLf8Daw`, commit `c030ee4`; earner gate PASS md5 `9150fabe…` unchanged / never restarted / caller /health 200, NO ring). **→ 🎉 PERF WAVE COMPLETE — all 6 units LIVE. NEXT = item 3 multilingual voice.**
0b. **FIXES-2** (folded into the perf wave): ✅ (a) recordings HEAD-gate SHIPPED in UNIT-2 (`40caf3c`, `playable:bool` + URL only when verified non-empty/audio); ✅ (b) CALL-LOGS transcript L/R SHIPPED in UNIT-5 (`d48ed46`, ai→LEFT / customer-lead→RIGHT, deployed LIVE). Original notes: (a) CRM recordings TIMER-but-NO-AUDIO — NOT a missing URL; OUTBOUND auto-egress items whose OGG is near-empty/486-busy (duration shows, bytes don't decode) → backend HEAD-verify object non-zero before presigning outbound items, render `<audio>` only when verified. (b) CALL-LOGS transcript "anonymous" — `calls/page.tsx:283` keys on `role==="agent"` but backend normalizes to `ai`/`customer` → never matches → all-right same-avatar; fix: ai/assistant/agent→LEFT("AI"), customer/user/caller/lead→RIGHT, keep the window, mirror CRM ChatBubble. (✅ deploy key verified working — SSH_OK famit-livekit.)
1. **Campaign-run fix** [agent a06ee231 RUNNING] — can't run campaigns; prime suspect = my recordings auto-egress blocking `run_job` dial (make recording best-effort, never block the call); + window/Vobiz/suppression. Proof = a real ring.
2. ~~**FIXES wave**~~ ✅ **DONE + VERIFIED LIVE 2026-06-13** — (a) image preview/asset-click-empty FIXED (asset-detail folds the current-version PRESIGNED url; live GET 200 image/jpeg ~50–63KB on founder+admin tenants); (b) CRM transcript chat-view SHIPPED (customer RIGHT / AI LEFT; `GET /calls/{room}/transcript` → 200/94 turns/roles {ai,customer}, tenant-scoped 404 cross-tenant). Deployed FORTRESS BUILD_ID `tuuIjqN7fCf_iEL-obLon`; commits `d9daa86`+`6940742`; earner untouched (agent.py md5 `9150fabe…` unchanged, famit-agent never restarted). 3/3 PASS.
3. ✅ **Multilingual adaptive voice + greeting glitch — DONE (2026-06-14, MLV wave, 2 passes).** Root cause = the "CASUAL HINGLISH … default to easy Hinglish" PIN in `_build_sales_instructions:1480-1485` (inside the HIGHEST-PRIORITY override) anchored Hindi. Replaced with an ADAPTIVE "MIRROR THE CALLER, EVERY TURN — switch WITH them mid-call, no default, never announce the language" rule; STT was already auto (`SARVAM_STT_LANG` default `unknown` = Sarvam auto-detect, not pinned); cleaned the greeting. INBOUND only, earner UNTOUCHED. **PASS-2 (VERIFY-driven):** the integrated re-run with LIVE-SHAPE scenarios (greeting-as-assistant-turn) caught that a COLD-OPEN English caller still drew Hindi (big Hindi knowledge-pack + a Hindi greeting priming the call). Fixed with (a) first-reply mirror binding, (b) a FINAL LANGUAGE LOCK appended last in the prompt (highest recency), and (c) the decisive **language-NEUTRAL greeting** `"Namaste{who}, this is {agent} from {company}. … how can I help you today?"` so the AI's own opener no longer pins Hindi. **FINAL live-shape smoke = PASS** (real Groq llama-4-scout, N=2): Hindi→Hindi · mid-call Hindi→English switch→English (the reported bug) · neutral-greeting→English caller→English (founder's recipe) · neutral-greeting→Hindi caller→Hindi (no regression) · no announcing. Earner gate before+after PASS (agent.py md5 `9150fabe…` unchanged, famit-agent not restarted, /health 200, 0 real 5xx). Box md5 `3152539f…` == `_inbound_ref/aim_voice_agent.DEPLOYED.py`. Backups `.MLVbak.20260614-020515 / 20260613-205414 / -205545 / -205925`. Founder recipe `FOUNDER-MULTILINGUAL-CALL-TEST.md`; build log `memory/build_log/wave-build-MLV-multilingual-adaptive-voice.md`; smoke `_inbound_ref/_mlv_mirror_smoke.py`. FINAL ACCEPTANCE = founder's real inbound call (English↔Hindi switch; STT+TTS in the wild). (OUTBOUND `agent.py` mirror still GATED — separate founder-approved item.)
4. **Two-party inbound handoff** — PROVEN by parts; needs ONE real founder inbound call to confirm (60-sec test).

## ✅ RUN-PAGE 4-STEP REDESIGN + VOICE-PREVIEW FIX — DONE (2026-06-14)
- **Voice-preview `t=` fix:** `GET /voice-preview` now accepts `?t=<jwt>` so `<audio src>` works. Commits `e275ebc` (BE) + deployed. No-auth→401, invalid-t→401, valid-t+bogus-id→404.
- **Run-page 4-step stepper + sticky Launch-summary rail:** pure presentational re-housing (all state/handlers verbatim), new `app/run/_stepper.tsx` (Core_2, role=tablist, progressive-disclosure Tabs for audience source, mobile pill+dots). Commits `6923b45`+`ba1f63a`. Deployed BUILD_ID `jcDEy4iclWbxS_zvVpvk0`. Integrated verify PASS. Build log: `memory/build_log/wave-build-run-redesign.md`.

## P1 — big builds (sequential, back-to-back)
5. **Model/Voice switcher Phase-1** [PAUSED, resume `wf_4d047a56-724`] — Lean/Standard/Premium slider + ₹/min cost-meter + free voice preview + custom-provider CRUD + recommender/health/favorites (Run-page; caller.py /voices,/voice-preview,/providers,/tiers; ~90% SAFE). **Founder is owed this — build next after P0.**
6. ✅ **AIM Access + PIN — DONE (2026-06-14).** Backend: `/ai-manager/numbers` CRUD mounted + tenant-scoped (admin sees admin numbers only; axcrio hidden by control-layer entitlement, correct); `POST /firewall/pin/change` live (old-PIN verify → set new → 400/401/429+lockout after ~4 bad attempts, 15-min TTL). firewall.py `change_pin` additive (existing `check_pin`/`set_pin`/`verify_step_up` byte-identical, proven). Frontend: Setup tab gains "Phone numbers" section (table: phone/label/role badge/verify-mode/status/added + Remove confirm + "Register first number" CTA) + "Change PIN" card (old/new/confirm 4-or-6-digit, mismatch guard, lockout-aware warning with "Wait 15 min / ask admin" copy, clear-on-success). Build green (tsc 0 errors, npm run build green). FORTRESS deployed (panel box BUILD_ID `sTCWP4Jj7t9hcSLHqLxx7`). EARNER GATE: agent.py md5 `9150fabe4ff62b4b4470f9a87df346e5` UNCHANGED, famit-agent PID 1477083 NOT restarted, /health 200, 0 5xx, NO ring.
7. **🔒 Funnels/Media MOUNT-BLOCKER security fix** — `funnel_wiring`/`media_gen` read tenant FROM BODY (cross-tenant hole) → build a token-deriving `build_router` BEFORE serving. Do this with/before the Workflow wave. (P1-SECURITY.)
8. ✅ **Workflow/Funnel execution — DONE + DEPLOYED + VERIFIED (2026-06-14).** Human-language node labels (not raw JSON); Load template dropdown (Starter: call new leads + 4 industry templates); inline Run status banner below toolbar; `POST /funnels/{funnel_id}/run` dispatches to run_job via `_mint_run_token` from the authenticated tenant (RT-3, never body); auto-publishes draft before run; `POST /run` loopback = 200 (8ms, empty-audience 0-lead job, run_job REACHED, NO ring). FORTRESS deployed BUILD_ID `A7YHO-5a5p7ZcKqSUrKOo` (loopback + panel.famit.in edge 200). EARNER GATE: agent.py md5 `9150fabe4ff62b4b4470f9a87df346e5` UNCHANGED · famit-agent PID 1477083 ActiveEnter 2026-06-10 19:58:18 NOT restarted · /health 200 · 0 5xx · NO ring · NO INVITE in famit-agent logs. BE commit `0cff177`; FE deployed from same commit. Backups on box: `*.RUNTOKbak.20260614-083554` (funnels + workflow). FE `.next.WFbak.20260614-*`. RESIDUAL: cross-tenant isolation probe requires a second real tenant; run_job with a NON-EMPTY audience requires a real campaign wired into the template — both require the founder's test. Founder recipe: Workflows → Load template → wire your campaign → Run workflow → banner shows "Run dispatched" + run_id.
9. **VIDEO STUDIO** — sub-page prompt→video→preview→assets (provider abstraction, `media_gen/video/*`); "add your API key"; manual video upload; library **Images↔Videos toggle**; attach video+image/banner to WhatsApp + Ads. Dormant until a founder video key (→ Vault). **📐 FULL DESIGN (red-team-hardened, decision-ready) = `design/VIDEO-STUDIO-MASTER-PLAN.md`.** HEADLINE: 3 of the brief's "P0 gaps" are STALE — already done on disk (`FEATURE_MEDIA` router mounted `caller.py:6968`, seam fix is 1 line at `engine.py:51`, presign already 302's at `ai_asset/endpoints.py:246` — NO new table). REAL gaps: (1) live-library bridge so videos appear in the `ai_asset_*` PG library images already use (the missed integration), (2) FFmpeg/Remotion **composite tier as the DEFAULT** (~$0 gen-API, Sarvam-TTS, works with NO gen-key — the 100× cost lever + the moat), (3) PG+FORCE-RLS schema, (4) the video frontend (`<video preload="none">`, Images↔Videos toggle, TierTabs), (5) Signal-Loop `ab_group`/`ai_generated` lineage. Red-team fixes folded: ONE coded 1-paid-test choke-point (`submit_gate`), per-tenant daily cap, cost-truth pre-fan-out wallet hold (EL TTS is paid-gated, NOT "$0.003 free"), orphaned-hold/lost-render reaper, output-side moderation gate, destination-spec validator, storage lifecycle + R2 seam, alerts, cross-product caller.py/registry serialization (only ONE of {RAG,Vault,Video} edits caller.py at a time). Build order **U1 seam-fix → U2 PG schema → U3 live-library bridge → U4 mount studio+submit_gate → U5 composite tier → U6 frontend → U7 Signal-Loop → U8 reaper/moderation/lifecycle → U9 BYO-key/Vault-seam/multilingual/music → U10 soak+rollback**. BE on Opus, FE on Sonnet + frontend-design. Composite + manual-upload ship TODAY with zero paid key. Earner-safe (async, agent.py untouched, all flags default OFF = byte-identical resting).
10. **VAULT** — PIN-gated per-vendor encrypted secret store (API/private keys, numbers); super-admin always-on, vendor HIDDEN-by-default + super-admin toggle; flexible for future self-hosted models; reuse control HIDE/LOCK + firewall PIN + key-store. **📐 FINAL DESIGN (red-team-corrected, decision-ready) = `design/VAULT-MASTER-PLAN.md` (2026-06-14).** Net-new pkg `droplet_work/vault/` mounted via the PROVEN `build_router(resolve_tenant,can,need_auth,_forbidden,firewall)` + `include_router` (NOT the fictional `build_router("vault",…)`); rides `caller.py` (separate process — `agent.py` `9150fabe…` NEVER imported, earner-safety red-team confirmed). AES-256-GCM **AAD-bound** envelope (KEK-0 env → per-tenant KEK-1 → per-secret DEK), **FORCE-RLS on ALL 5 tables incl. the access log**, reads default **`is_admin=False`** (separate `admin_store.py` for the super-admin `is_admin=True` path). 11 red-team corrections folded (the draft was full of fiction): C2 PIN=**sha256 not Argon2id** · C3 **`vault_used_jti` single-use** table (step-up was 300s-replayable) · C5 **real append-only** (`REVOKE UPDATE,DELETE`+trigger — no trigger existed anywhere) · C6 AAD mandatory · C7 `api_prefixes:["/vault"]` not `/vault*` (wildcard silently bypassed LOCK/suspend) · C8 KEK-0-absent→**503 fail-closed**, never plaintext · C11 the **read-seam `vault.get_secret`** (else Vault is write-only + Video/RAG BYO-key are fiction). §9 reveal/write **trust model** (ai_provider keys: vendor gets NEITHER reveal NOR update NOR rotate). §13 **KEK-0 backup/break-glass in MVP** (total-loss class). 10-wave earner-safe roadmap (V0 DDL+fail-closed → V1 CRUD+RLS+AAD → V2 reveal+jti+leak-seal → V3 read-seam+dual-read → V4-V8 FE → DR backup → P2). BE Opus, FE Sonnet+frontend-design. **Serialize the `caller.py` mount vs RAG/Video — only ONE edits caller.py at a time.** All flags default OFF = byte-identical resting.
11. **🛡️ Earner own LLM fallback** [GATED-on-agent.py, founder sign-off] — the earner shares ONE Groq org TPD pool with AIM test-burn → can starve the LIVE earner (real risk, flagged in 3 ledgers, never built). Give agent.py its own FallbackAdapter or a 2nd Groq org + gate AIM test volume. **Highest-value forgotten SAFETY item.**
12. **RAG (HEART) — `design/RAG-MASTER-PLAN.md` v2 (red-team-folded, decision-ready, 2026-06-14).** ⚠️ **GROUND-TRUTH CORRECTION (verified on the LIVE box this pass):** RAG grounding is NOT "empty + un-wired" — it is **ALREADY LIVE, UNGATED, and SEEDED** on the inbound earner. The running agent is `aim_voice_agent.py` md5 **`018c20a7`** (NOT the local `.DEPLOYED.py` `3152539f`/`a9eefa8c`; every earner file diverges local↔box) with grounding wired at **3 flagless sites** (connect-prefetch `:2532`, `pick_campaign` `:1699`, `lookup` `:1745`); **`grep RAG_INJECT_ENABLED` = 0 hits**; `kb_chunks` = **63 rows / 3 tenants** (`admin`/`_global`:49 + 2 tenants ×7). So the v1 "default-0, byte-identical-off, corpus-empty" safety story was **fiction**. The earner `agent.py` = `9150fabe…` is UNTOUCHED (RAG rides the inbound worker). **Real scope = retro-gate the live mutation, NOT "build RAG".** Roadmap **W0 RETRO-GATE** (golden-capture `018c20a7`, BUILD the `RAG_INJECT_ENABLED` kill switch the design wrongly assumed, prove `grounding=""` byte-identical in CI, verify on 1 real inbound call) → **W1** `core.py` per-call-site **`dense=`-gate** (so no `EMBED_API_KEY` flip ever puts a 40-200ms embed RTT on the `lookup`/`pick_campaign` reply path — red-team #2) + `_global` UNION under the caller's own `is_admin=False` RLS + FORCE-RLS `kb_query_log` → **W2** seed `_global` telecaller corpus (canary→promote) → **W3** campaign-save collateral ingest (raw_script-skip) + PII-scrub + soft-delete versioning + per-tenant quota → **W4** W2-cache the blob keyed `(tenant,campaign,stage,channel,kb_version)` + meter the +350-tok prompt-tax → **W5** WA grounding → **W6** eval gate (p95-TTFT<150ms + RAGAS-faithfulness≥0.85 + RLS-bleed=0 + no-embed-on-reply) BEFORE any flag flip → **W7** KB-management UI (Sources/Test-retrieval-differentiator/Insights-knowledge-gaps; Sonnet+frontend-design, Core_2). Dense embeddings (OpenAI key, prefetch-only, NOT a GPU box, NOT OpenRouter), DPDP `/kb/erase`, knowledge-gap loop = **founder-signed Phase-2**. Serialize the `caller.py` mount vs Vault/Video — only ONE edits caller.py at a time. Companions: `RAG-INGESTION-PLAN.md` + `RAG-EVAL-SPEC.md`. BE Opus, FE Sonnet. (Supersedes the stale "self-hosted BGE-M3 + load + wire" framing.)
13. **Per-person memory inbound + WA-reply** [keystone gap] — memory is read for OUTBOUND recaps only; load `var/memory/<digits>.json` + call summary into the INBOUND agent + the WhatsApp reply-brain (deepen G5 context too).
14. **#9 Hardening PART 2** — onboarding flow, billing/metering at scale, reliability/monitoring → SELLABLE (pt1 RLS+watchdog DONE).
15. **ai_asset go-wide** — `/api/assets/raw` stream (`version.local_path` unset, one-line map) + per-tenant ON/OFF gate (flag is global) + write the cross-module PG `events` leg. (Plus FE nginx `/api/assets/` proxy → `10.122.0.4:8310` — needs FE-box `143.110.247.249` root access.)

## P2 — forgotten / spec'd, lower urgency
16. **6 un-built Creative Studio sub-products** (specs exist, only image-banner shipped): **Brochure/Catalog · Creative-Batch (all-sizes) · Ads-Engine · Landing-Page Builder · 3D-Model · A/B Testing-Lab** (`design/creative-*.md`).
17. **Control-Layer C10 — AI Copilot entitlement gate** (probe T18) — the one un-shipped control unit.
18. **AIM step-up → runner approval-row wiring** — spend commands (e.g. ads.set_budget) silently re-park; wire AIM step-up → workforce approval/resume.
19. **WhatsApp residuals** — reply-brain deep context (call summary + recap); `hot_lead_alert` cold-team template (Meta approval); banner-in-builder (image-header upload live).
20. **Switcher P2** — A/B voice-tier test, hard budget auto-pause, per-call ₹ in call log, spend sparkline.
21. **OB-PROV** (outbound provider honor) — agent.py reads `fields.{stt,llm,tts}_provider`/tier; GATED (default-identical + ring-gate + founder sign-off).
22. **growth-os** — flagship ads monorepo SCAFFOLD-ONLY; `@growth-os/events` codegen build FAILS (Payload-suffix/dup-type bug) — fix that first; 6-container stack never booted (infra).
23. **Logto OIDC** wired into caller.py (deployed on hatchet box, not wired); control C2 Phase-2 admin-org binding.
24. **AIM dedicated-service 39-unit items** (superseded by in-process; per-user Argon2id PIN, strict-JSON NLU, PolicyEngine, CostGuard, compliance window/DLT gate, + 8 dashboard pages) — extract only if DO droplet limit raised. **P3.**
25. **media_gen monolith retirement** (superseded by ai_asset) — P3 cleanup.

## ⏸️ ON-HOLD (build ONLY when founder specifically asks)
- **Credit / Billing / Plans** (balance icon→Credits page, spend breakdown, Razorpay-ready, PLANS tiers) — wallet.py exists; dropped from auto-sequence 2026-06-13.

## 🔴 FOUNDER ACTIONS (unblock whole pipelines)
- **BIND ModelScope↔Alibaba Cloud** → image gen (`FOUNDER-MODELSCOPE-BIND.md`). · **Fix Meta WhatsApp** payment+verify+webhook → delivery (`FOUNDER-META-WHATSAPP-FIX.md`). · Video-gen API key → Vault. · Razorpay (when Credits un-holds). · Ads OAuth (Meta/Google). · SambaNova Developer tier. · FE-box root for the nginx /api/assets proxy.

## 🪙 NET-NEW from gold-mine sweep (2026-06-14 — full detail in `MASTER_PLAN.md` §GOLD-MINE SWEEP)
> Deep read-only sweep of all docs + code-marker grep + LIVE-box flag/route verification. NET-NEW only (de-duped vs the queue above). ALL earner-safe (agent.py untouched). P-keys: P0 quick · P1 big-clear · P2 lower · ⛔ gated.
26. **ADS ENGINE (dormant)** — built router `ads_engine/endpoints.py` + FE `app/ads/page.tsx` exist but `FEATURE_ADS` OFF (`/ads/*`→404 on box); flip-on the propose/approve flow [P1 SAFE], live spend ⛔ Ads OAuth.
27. **MEDIA-GEN router (dormant)** — `media_gen/router.py` `FEATURE_MEDIA` OFF (`/media/*`→404); needs body-tenant→token `build_router` fix then flag-on; fold VIDEO path into Video Studio [P2].
28. **PAYMENTS (dormant)** — `payments/router.py` + FE `app/payments/page.tsx` shipped, `FEATURE_PAYMENTS` OFF (`/payments/*`→404); needs `wire()`+keys ⛔ Razorpay.
29. **Inbound never-silent apology guard** — try/except in inbound entrypoint, speak a line on any error (no dead air) [P0 SAFE, inbound only].
30. **Inbound recording Egress** — inbound audio→DO Spaces presigned `recording_url` (outbound already does this; inbound doesn't) [P1 SAFE].
31. **Inbound spend metering** — meter inbound minutes/handoff/template/RAG into wallet (only outbound metered today) [P1 SAFE].
32. **Compliant recording posture** — consent line + PIN-span pause + region/retention [consent line P1 SAFE; rest ⛔ policy].
33. **DPDP delete-my-data endpoint** — per-person purge + purpose-limit docs (legal exposure) [P1 SAFE].
34. **Inbound business analytics dashboard** — containment/booking/transfer/hot/sentiment/language-mix per call [P1 SAFE, FE+queries].
35. **Mid-call `lead_is_hot` LLM tool** — buy-intent detect mid-call → mark hot live [P1 SAFE, inbound].
36. **Warm-transfer no-answer fallback ladder** — ring next by strategy + voicemail-detect + logged callback [P1 SAFE].
37. **Post-call workflow event emission** — emit `call.completed` into workflow DSL [P1 SAFE, pairs w/ Workflow wave].
38. **Customer-mode "sales-in" inbound worker (Mode A)** — inbound SALES brain over voice_core (`sales_flow.py`), distinct from AIM command brain [P1 SAFE].
39. **Knowledge-gap + objection learning loop** — mine transcripts→draft KB→curator→re-ingest (self-improving) [P2, after RAG].
40. **External CRM sync adapters** — Salesforce/HubSpot/Zoho push+webhook [⛔ founder API keys].
41. **Ads flywheel promote-winner-to-ad** — top creative-test template→auto Meta/Google ad [⛔ ads creds, pairs w/ #26].
42. **LiveKit semantic turn-detector** — add `turn-detector` plugin w/ Silero VAD (Hindi ~99.4% endpointing) [P1 SAFE, inbound first].
43. **Structured per-stage flow layer** — greet/pitch/qualify/close LiveKit agents w/ handoffs [P2, inbound first].
44. **Eval/replay harness + per-call QA score** — persona scenarios + LLM-judge to gate every voice change + score real calls (`design/eval-harness.md`) [P1 SAFE, offline]. **Highest leverage — makes voice changes provable.**
45. **In-memory grounding bank** — per-campaign objection/fact sheet in FAISS/prompt (lighter than full RAG) [P2, overlaps RAG].
46. **LoRA/QLoRA fine-tune on telecaller transcripts** [⛔ DEFERRED — after eval harness + self-host latency decision].
47. **Inbound voice warm-cache + pooled HTTP client** — Redis hot-cache + reused clients to STT/LLM/TTS (cuts inbound first-token) [P1 SAFE]; + endpointing tune [P2].
48. **growth-os Integration Hub** — 12-op connector lifecycle + Origin bridge; `connections` table exists, zero module mounted [⛔ Phase-0 contracts-first].
49. **growth-os Billing money-path + Gateway OIDC + Kafka SSE** — consumption stub only, no ledger-debit write; dev-JWT stub; in-memory SSE only [⛔ Phase 1/3 infra].
50. **WhatsApp builder residuals (fine)** — WB-2 `status=partial` fallback cards [P0]; WB-3 banner via Spaces `header_url` [P1]; WB-4 Submit-to-Meta + webhook [⛔ Meta].
