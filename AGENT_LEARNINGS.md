- 2026-06-13 — ✅ FIXUI WAVE — ASSET-DETAIL PREVIEW (deeper root cause) + CRM CALL TRANSCRIPT CHAT-VIEW — SHIPPED LIVE TO FORTRESS (FRONTEND-only; earner UNTOUCHED — the FORTRESS panel box `143.110.247.249` has no agent dir, the earner box `168.144.153.145` never touched). Commit `d9daa86` (4 FE files: `lib/assets.ts`, `app/crm/client.ts`, `app/crm/[id]/page.tsx`, `famit-panel/FIXUI_STATE.md`). **🟥 BUG-1 — THE PRIOR FIX WAS HALF-DONE (LIST fixed, DETAIL still blank on click):** the documented presign work (`store.public_dict` + `endpoints.get_asset` fold + `1005ccb` FE) made the gallery GRID render (live LIST `/assets` items DO carry presigned `url`+`thumb_url`, `X-Amz-Signature` present → GET 200), but clicking an asset STILL showed a blank/empty detail because of TWO things only visible by hitting the LIVE endpoint with a real tenant token: (1) `GET /assets/{id}` returns a **NESTED envelope `{asset:{...}, versions:[...]}`**, not a flat Asset — the FE `getAsset` returned `res.json()` RAW, so `full.headline`/`full.cta`/all meta were `undefined` (nested under `full.asset.*`) and the panel showed blanks; (2) the `asset` object's OWN `url`/`thumb_url` are RAW UNSIGNED path-style Spaces URLs (`https://sgp1.digitaloceanspaces.com/capsy-recordings/...`, NO `X-Amz-Signature`) → private bucket → **403** → blank image; ONLY the VERSION rows (`versions[0].url`) carry the signature (so the prior endpoint "fold" apparently folded the UNSIGNED asset url, or the path-style strip branch failed to presign it). **FIX (`lib/assets.ts getAsset`, additive, FE-only):** FLATTEN the `{asset,versions}` envelope into a flat `Asset` (`base={...data.asset}`, `base.versions=data.versions`) AND OVERRIDE `base.url`/`base.thumb_url` with the current (else newest) version's PRESIGNED url. Now the detail renders the signed image via the native `<img>` (`AssetImage`) AND headline/CTA/meta populate. 🟥 **LESSON (overturns the prior "fold version url server-side" as SUFFICIENT):** a blank asset-DETAIL is rarely "presign not shipped" — it's that `/assets/{id}` returns a NESTED `{asset,versions}` envelope and the asset's OWN url is the UNSIGNED canonical url while only the VERSION carries the signature. ALWAYS hit LIVE `/assets/{id}` with a real tenant hmac token (`tid.hmac(tid,SECRET)`; asset svc binds VPC `10.122.0.4:8310`, NOT loopback `127.0.0.1`), check `top_keys==['asset','versions']` (nested) + grep each url for `X-Amz-Signature`, and FLATTEN+OVERRIDE on the FE so the detail shows image AND meta. **BUG-2 — CRM CALL TRANSCRIPT CHAT-VIEW:** backend `GET /calls/{call_id}/transcript` was ALREADY LIVE on the box (caller.py:4282, box md5 `e802d301…`) — 🟥 but the TRACKED MIRROR `droplet_work/caller.py` (`ad121cf4…`) is BEHIND and LACKS the route (grep 0); the BOX is source of truth (a prior wave deployed it without syncing the mirror — resync the mirror later). It accepts an outbound call id OR room OR an inbound `ai_manager_sessions` session_id, returns `{call_id,direction,phone,name,turns:[{role:"ai"|"customer",text,ts,seq}],total}` roles PRE-NORMALIZED (`ai/assistant/agent`→"ai" LEFT, `user/customer/caller`→"customer" RIGHT), tenant-scoped (outbound `require_object` BOLA→404, inbound RLS by vendor_id). LIVE SMOKE: room `famit-916375548830-ad08ff` → 94 turns, sides correct, 200. **KEY WIRING:** a CRM timeline `call` row carries `source="calls"` + `source_id = call.id||room` (crm/core.py:580) = exactly what the route accepts → pass `row.source_id` as `call_id`. FE: `crm/client.ts` `getCallTranscript(callId)` + `CallTranscript`/`TranscriptTurn` types (dormant-safe: 404/501/5xx/offline→empty transcript, never an error wall); `app/crm/[id]/page.tsx` makes a `call` row a clickable `<button>` (hover "View transcript") → opens `CallTranscriptModal` (Core_2 `Modal isSlidePanel` right slide-over) = full ordered chat: CUSTOMER bubbles RIGHT (`bg-primary-01/12`), AI bubbles LEFT (`bg-b-surface2 ring-1 ring-s-subtle`), labelled, calm "No transcript for this call" empty + footer legend; token-pure, matches the AIM session-player convention. DEPLOY (proven recipe): local `tsc --noEmit` EXIT 0 + `npm run build` EXIT 0 (BUILD_ID `tuuIjqN7fCf_iEL-obLon`, `/crm/[id]` 8.46kB ↑7.09); `tar` `.next`+`app`+`lib` (59MB, md5 `0ccd08fb…`); scp to FORTRESS root@143.110.247.249 (key `do-blr-test/id_ed25519`); md5-gate local==box; ONE SSH session = backup-first `*.FIXUIbak.20260613-172953` → extract `_fixui_stage` → grep-verify new code in stage → atomic `mv` swap → `chown -R deployuser:deployuser` → `systemctl restart famit-panel` ONLY (active PID 248200). PROOF: 200 on `/ /login /crm /creative/library /crm/[id]` on BOTH loopback:3001 AND `panel.famit.in` edge; served HTML carries the NEW BUILD_ID `tuuIjqN7fCf_iEL-obLon` (no stale cache). No swap needed (prebuilt artifact; box had 1432MB avail + 28G disk free). EARNER GATE before+after PASS: agent.py md5 `9150fabe4ff62b4b4470f9a87df346e5` UNCHANGED, famit-agent MainPID `1477083`/ActiveEnter `2026-06-10 19:58:18` NEVER restarted, caller `/health`=200 (NO `/run`, NO ring test per the HARD RULE). 🟥 GOTCHA (NEW): a tight `stat`-every-6s SSH poll-loop to watch SCP progress against the hardened FORTRESS box trips sshd/fail2ban rate-limiting → `ConnectTimeout` (the SCP itself had already succeeded — box md5 matched local). DON'T poll a transfer with a tight SSH loop; do the whole deploy in ONE SSH session and verify with a couple of sparse single connections. Build log `memory/build_log/wave-build-FIXUI-asset-transcript.md`; state `famit-panel/FIXUI_STATE.md`.
- 2026-06-13 — ✅ IMAGE PREVIEW / ASSET-CLICK BLANK — ROOT-CAUSED + FIXED (AI Asset svc only; earner UNTOUCHED — separate box). Box famit@168.144.153.145, svc `famit-aiasset` (`/opt/famit-aiasset`, venv `.venv`, bind `10.122.0.4:8310`, src `/opt/famit-aiasset/ai_asset/`). 🟥 **THE PROMPT'S PREMISE WAS HALF-STALE:** the documented fix (presign in `store.public_dict()`) was ALREADY shipped + live (store.py mtime 2026-06-11) and WORKING — the LIST endpoint `/assets` already returns a presigned `url` (verified: presigned GET → **200 image/jpeg 63436B**, raw unsigned → **403**, confirming the private bucket). So the gallery GRID was fine. **THE REAL REMAINING BUG = the ASSET-CLICK detail preview.** `GET /assets/{id}` returned `{asset, versions[]}`: the `versions[]` were presigned, but the `asset` object has **NO `url`/`thumb_url` at all** (the asset table stores no image — the bytes live on the version row). The FE `AssetDetail.tsx:97` computed `src = a.thumb_url || a.url || assetRawUrl(id, current_version_id)` → both empty → fell through to the **X-Auth-gated `/raw` proxy**, and an `<img src>` cannot send X-Auth → `/raw` **401** → blank preview on click (PROVEN: `/raw` no-auth=401, with-auth=200). 🟥 **SECONDARY GOTCHA (path-style URL):** the stored Spaces URL is PATH-style `https://sgp1.digitaloceanspaces.com/capsy-recordings/<key>` but `_spaces_base()`/`public_base()` returns VIRTUAL-HOSTED `https://capsy-recordings.sgp1.digitaloceanspaces.com` — so `_spaces_key_for`'s `url.startswith(base)` strip branch FAILS for path-style; key recovery only works via the `local_path`/`storage=spaces` branch (not load-bearing for THIS fix since versions presign fine via that branch, but note it if extending presign to path-style urls). **FIX (1 backend file + 1 FE file, both additive):** (1) `ai_asset/endpoints.py get_asset` now FOLDS the current (else newest) version's already-presigned `url`/`thumb_url` ONTO the asset object before returning, when the asset has none — so the asset-detail API is self-sufficient and the FE never hits `/raw`. Backup `endpoints.py.FIXbak.20260613-162358` (md5 fb93296a→dadadbe6). (2) `famit-panel/app/creative/_components/AssetDetail.tsx:97` `src` now also tries `currentVersion?.thumb_url || currentVersion?.url` (from `a.versions[]`, already presigned) BEFORE the `/raw` fallback — belt-and-suspenders so even an un-deployed-backend panel renders. ⭐ **ASSET-DETAIL API SHAPE for the FE:** `GET /api/assets/{id}` → `{asset:{id, headline, kind, platform, size, angle, cta, language, status, current_version_id, score, metrics, tags, meta, created_at, **url(presigned ~24h GET), thumb_url(presigned)**}, versions:[{id, version_no, **url(presigned), thumb_url(presigned)**, storage:"spaces", model, cost_minor, created_at, is_current}]}`. List `GET /api/assets?<facets>` → `{assets:[{...,url(presigned),thumb_url(presigned),storage}], total, limit, offset}`. Auth = X-Auth tenant hmac token `tenant.hmac(tenant,SECRET)` (admin secret `FamitCall2026` alone is NOT a tenant → 401 `unauthenticated`); mint via `hmac.new(open('/opt/famit-agent/var/secret').read().strip().encode(), tid.encode(), sha256).hexdigest()`. DEPLOY: backup-first → py_compile on `/opt/famit-aiasset/.venv` OK → `systemctl restart famit-aiasset` ONLY → active, `/status`=200, 0 errors. SMOKE PASS: detail `asset.url` presigned=True, GETs **200 image/jpeg 63436B** (was empty→/raw 401→blank). EARNER GATE before+after PASS: agent.py md5 `9150fabe4ff62b4b4470f9a87df346e5` UNCHANGED, famit-agent MainPID `1477083`/ActiveEnter 2026-06-10 NEVER restarted, caller `/health`=200. 🟥 LESSON: when a "presign the image" prompt arrives, FIRST hit the LIVE list+detail endpoints with a real tenant token and HTTP-GET the returned url (200 vs 403) — the presign may already be shipped for the LIST but NOT the DETAIL asset object. The asset RECORD often has no image url (image lives on the version row); fold the current version's presigned url onto the asset so the detail preview never falls back to an X-Auth-gated `/raw` proxy that an `<img src>` can't authenticate. Commit `1005ccb` (FE). Box backend deploy-by-copy (droplet_work not git-tracked). ⛔ NEXT: deploy `AssetDetail.tsx` to FORTRESS panel + BUG-2 transcript chat-view.
- 2026-06-13 — 🟥🟥 HARD RULE / METHODOLOGY CHANGE (highest priority, all future waves): **NEVER place repeated real outbound TEST CALLS to verify the earner.** Our per-wave "earner gate = real /run to +917861019021 RINGS" hammered the DID `+918071583488` with DOZENS of calls in one day → it tripped the Indian carrier's anti-telemarketing SPAM block (every INVITE now rejected pre-ring, no `180`). The test-call methodology itself helped break the live earner. → Verify "earner untouched" via **agent.py md5 `9150fabe4ff62b4b4470f9a87df346e5` UNCHANGED + famit-agent process (MainPID/ActiveEnter) NOT restarted + /health 200 + 0 5xx ONLY** — NO `/run`, NO ring test. A real outbound test is the FOUNDER's job (his clean test on a free line), never an automated per-wave gate. When editing/launching any `.wf` wave, DROP the "real outbound RINGS" gate and use the md5/process gate. Let the DID REST; founder clears/rotates it via Vobiz.
- 2026-06-13 — 🟥 "UI SAYS CALLING, NO PHONE RINGS" — ROOT CAUSE = **CARRIER-SIDE SPAM BLOCK ON THE DID `+918071583488`** (NOT ours; NOTHING edited; earner UNTOUCHED). Box famit@168.144.153.145, time 21:24 IST. **HARD EVIDENCE from the founder's ACTUAL recent runs (livekit-sip Docker logs), NOT a forced test:** the box logs `inviteTo{Trying,Ringing,Accept}Ms` per call — this is the DEFINITIVE ring-proof. In the founder's last ~35 min, **9 outbound calls across FOUR DIFFERENT numbers** (`+917861019021`, `+916375548830`, `+918839352959`, `+916376980812`) ALL got `inviteToTryingMs:~14` (100 Trying) but **`RINGING=None` + `ACCEPT=None`** → the phones NEVER rang. Terminal codes = a MIX of `486 Busy Here` / `480` / `603 Decline` / `403` with NO preceding `180 Ringing` (e.g. failed call `SCL_PvSaFeXVCe24`→`+916376980812`: `signaling_timestamps` had ONLY `inviteToTryingMs:14`, then `SIP invite failed: sip status: 486: Busy Here`). 486 across FOUR distinct numbers at once ≠ "user busy" — it is the carrier rejecting the INVITE before ring. **THE TRANSITION IS EXACT:** earlier today the SAME trunk `ST_fmtVmNJmpzKa` + SAME DID `+918071583488` RANG AND ANSWERED real conversations — `inviteToRingingMs` appears 28× + `inviteToAcceptMs` 22× over 6h, incl `+917861019021` answered at 10:13/10:14/10:15/10:26/10:44/11:04/11:20/11:33/11:34/12:01 UTC. **LAST TRUE RING = `SCL_5Ld3x5EKUtKW`→`+917861019021` at 12:35:51 UTC (18:05 IST), RINGING=1807ms ACCEPT=12161ms (rang+12s of audio).** From **12:51 UTC onward EVERY call = trying-only, no ring** (12:51/13:28/13:30/13:48/13:49/13:51/13:59/14:43/14:47/14:57/15:35/15:43/15:45/15:50/15:51/15:52). So between 12:35 and 12:51 UTC the DID flipped from ringing→blocked. **OUR SIDE IS CLEAN (all suspects CLEARED with evidence):** (a) earner gate intact — agent.py md5 `9150fabe4ff62b4b4470f9a87df346e5` UNCHANGED, famit-agent MainPID `1477083`/ActiveEnter 2026-06-10 NEVER restarted; (b) caller.py md5 `ad121cf4…` = the force_window fix IS deployed and run_job IS dialing — SIP INVITEs demonstrably leave the box for every lead (the force/window bug is FIXED, not the cause); (c) Vobiz **₹479.34 INR, status:configured/active** (`vendors.vobiz.get_balance()` live) — NOT drained, NOT 402 (zero 402/balance errors in the caller journal — the codes are carrier 4xx/6xx from DOWNSTREAM of Vobiz); (d) leads ARE dialable — `var/suppression.json` has only 3 unrelated entries, NONE of the founder's 4 dialed numbers; (e) SAME trunk+DID on both the good and bad calls (no trunk swap). **VERDICT (HONEST):** the founder's recent campaign calls got an **IMMEDIATE 4xx/6xx with NO `180 Ringing` → the phones NEVER rang.** It is NOT the window, NOT force, NOT Vobiz balance, NOT suppression, NOT run_job, NOT agent.py. It is a **carrier/DID-level block** — the DID `+918071583488` made dozens of repeated outbound calls today and tripped the Indian carrier's anti-telemarketing/spam throttle (or a Vobiz-side routing flap), which now rejects every INVITE pre-ring. 🟥 **NOTHING WAS EDITED** (no caller.py/agent.py/trunk/firewall/SIP change; no service restarted) — there is no code bug to fix this time; the fix is carrier/Vobiz-side + a cooldown. **FOUNDER ACTION (only he/Vobiz can clear):** (1) STOP re-running campaigns for now — each rejected INVITE deepens the spam-flag; let the DID rest a few hours. (2) Contact **Vobiz support**: report that DID `+918071583488` started returning immediate `486/480/603` with no ring from ~12:51 UTC 2026-06-13 (last good call 12:35:51 UTC) — ask them to (a) check the DID/trunk isn't suspended/throttled for telemarketing-spam, (b) check it isn't flagged on India's DLT/anti-spam list, (c) if needed rotate to a clean DID. (3) For an immediate clean test: once Vobiz confirms the DID is clear, run ONE campaign to a FREE (non-busy) line during 09:00-21:00 IST and we re-read the SIP log for `inviteToRingingMs` > 0 — only that proves the fix. ⭐ **KEY LESSON (overturns a prior wrong reading):** `inviteToRingingMs`/`inviteToAcceptMs` ARE present in this box's livekit-sip signaling_timestamps — USE THEM as the ring oracle. A `486` WITH `inviteToRingingMs` present = rang-then-busy (real); a `486` with ONLY `inviteToTryingMs` (no ring field) = carrier rejected pre-ring, phone NEVER rang. The same DID can ring fine for hours then get spam-blocked mid-day — diff the failed calls against earlier GOOD calls (same trunk/DID) and find the exact timestamp the `RINGING` field stopped appearing; that pinpoints a carrier flip, not a code regression.
- 2026-06-13 — ✅ "CAN'T RUN A CAMPAIGN FROM THE DASHBOARD" — ROOT-CAUSED + FIXED (caller.py only; earner UNTOUCHED). Box famit@168.144.153.145 `/opt/famit-agent/caller.py`, venv `/opt/capsy-agent/.venv`, caller **port 8209**, X-Auth `FamitCall2026`. **PRIME SUSPECT (REC-B auto-egress regression) = CLEARED:** a REAL forced `/run` to `+917861019021` (`now=1`) at 21:13 IST → CreateRoom SUCCEEDED **with egress armed** (`AIM_RECORDING_ENABLED=1` + all 4 Spaces creds present, row `recording_status:recording`), no run_job exception, no egress error in the journal → SIP INVITE via the sacred earner trunk `ST_fmtVmNJmpzKa` (`+918071583488`→`+917861019021`, sipCallID `T4413lKmslyKdOI9KucpcCANFfH`, `inviteToTryingMs:14`) → **486 Busy Here** = carrier RANG. Egress does NOT block the dial (it's already wrapped: `_build_outbound_egress` NEVER raises, returns `(None,"","")` on any error; CreateRoom accepts the egress fine). Other suspects cleared: Vobiz **₹479.5 available/active** (live `api.vobiz.ai/.../balance/INR`); `+917861019021` NOT in `var/suppression.json`; both `famit-caller`+`famit-agent` active. **ACTUAL ROOT CAUSE = the TRAI calling-window gate + a panel↔backend FORCE MISMATCH.** It was 21:13 IST, past the default `09:00-21:00` window. The panel's normal Run sends NO `now` field → `/run` returns **202 `queued_out_of_window`** → `run_job` idles (`paused_reason:out_of_window`), dial suppressed BY DESIGN (correct). BUT the panel's **"Start anyway"** button sends `force=true` (not `now`), and the backend's `force_window` was computed ONLY from `bool(str(now).strip())` — so `force=true` flipped the HTTP code to 200 ("Started anyway!") but `run_job` STILL idled and NEVER dialed (proven: force-only `/run` → lead stuck `status:queued` after 6s). So the founder's only outside-window override silently no-ops. **FIX (file:line `caller.py:~3964`, in `POST /run` job build):** `"force_window": bool(str(now).strip())` → `"force_window": bool(str(now).strip()) or str(force).strip().lower() in ("1","true","yes","on")` — now the panel "Start anyway" (and AIM `now`) both genuinely bypass the window-idle. Additive, earner-isolated (NO agent.py/trunk/firewall/SIP touch). Backup `caller.py.LPRWINbak.20260613-154448`; md5 `f05fdd2e…`→`ad121cf4e90d31289c8c691fd754fb1d` (tracked mirror should be synced). py_compile OK on box venv; `sudo systemctl restart famit-caller` ONLY → active, `/health`=200, 0 5xx. **PROOF post-fix:** force-only `/run` (mimics "Start anyway", NO `now`) at 21:15 IST → lead flips to `status:calling` in <7s → SIP INVITE trunk `ST_fmtVmNJmpzKa` sipCallID `wGfjbvYTJQD0GhUxKs65VLXfRSz` → **486 Busy Here** = RANG (founder's line busy/declined both attempts; carrier reachability proven). **EARNER GATE PASS:** agent.py md5 `9150fabe4ff62b4b4470f9a87df346e5` UNCHANGED (before+after), famit-agent MainPID `1477083` NEVER restarted, only famit-caller restarted, 0 5xx, 0 Traceback. 🟥 **LESSONS:** (1) recording must NEVER block the dial — it doesn't (egress is try/except-wrapped + dormant-safe), this regression was a RED HERRING; the dial path is sound. (2) A panel "override" button is worthless if its flag isn't wired to the backend toggle that actually gates the behaviour — `force` (UI) was decoupled from `force_window` (engine); always trace the override flag end-to-end. (3) After 21:00 IST the dashboard CANNOT dial unless "Start anyway" is used — that compliance gate is correct and intended; only the broken override was the bug. ⭐ **FOUNDER RECIPE:** during 09:00-21:00 IST just press **Run** and it dials. Outside those hours, press **Run**, then click **"Start anyway"** on the yellow banner — it now actually dials immediately. **RESIDUAL:** both rings hit `486 Busy` because the founder's line was engaged — a free line will connect+converse; recommend one more Run during the window with the phone free to hear the full call.
- 2026-06-13 — ✅ REC WAVE FINAL INTEGRATED VERIFY (handoff-name + recordings + retention + earner) — 4/4 PASS (independent, honest, live box + Spaces + panel edge; READ-ONLY except ONE /run trigger + ONE Spaces lifecycle PUT — NO code edit, NO service restart this pass). **(1) HANDOFF caller-line PASS:** deployed `aim_voice_agent.py:740` = `Ek second, main aapko {_dial_who} se connect kar rahi hoon.`, `_dial_who` = dialed `name`→`role`→`apni team` (lines 732-737); names the person and NOTHING else. `name` round-trips LIVE: POST `/brain/handoff/add {"name":"VerifyRajesh","phone":"+919999000111"}` → GET `/brain/handoff` returns `"name":"VerifyRajesh"` in its OWN field (role empty); cleaned up via `DELETE /brain/handoff/remove?phone=%2B919999000111` → founder's original 2-entry list (`+917861019021`,`+916375548830`) restored. `session.aclose()`+`create_sip_participant` bridge+`participant_disconnected` hangup intact (18 grep refs). **(2) RECORDINGS PASS:** outbound auto-egress fresh call `55181bfa77` → Spaces `outbound-recordings/2026/06/13/55181bfa77.ogg` 57235B (landed 14:44:04) + `recording_key`/`recording_status:recording` on row; inbound finalize-on-read `vs_dee5eeef4141` → session JSON `recording_status:uploaded`/`recording_duration_s:117` + presigned-url field; `GET /calls/61a6bfeada/recording` → 361-char presign → full GET **200** audio/ogg 56916B + `Range:bytes=0-1023` **206** 1024B; inbound presign → **200** 1.6MB + **206**; `GET /contacts/+918949906361/recordings` → 8 calls newest-first `with_recording:1` both directions (pre-REC-B rows correctly `has_recording:false`). Tenant scope in code: `/calls/{id}/recording` `require_object(t,rec,not_found=True)`→404 (caller.py:4165), `/contacts/{phone}/recordings` `calls_for(t)` + RLS `_inbound_rec_items(t["tenant_id"],…)` (caller.py:4171). FE LIVE on panel (BUILD_ID `4aXNPr1rvAfpK4ku5dNa7`, famit-panel active): CRM Recordings card + `<audio>` + Download (12 refs `app/crm/[id]/page.tsx`), handoff name field (2 refs `_handoff.tsx`), `getContactRecordings` (`crm/client.ts`). **(3) RETENTION — was the ONLY UNBUILT item; BUILT THIS PASS + PASS:** `get_bucket_lifecycle_configuration(capsy-recordings)` returned `NoSuchLifecycleConfiguration` (none existed). Created via boto3 on box (Spaces creds in `/opt/famit-agent/.env`: `SPACES_KEY/SECRET/BUCKET=capsy-recordings/REGION=sgp1/ENDPOINT`): 2 rules, Status=Enabled — `{ID:expire-outbound-recordings, Prefix:"outbound-recordings/", Expiration.Days:90}` + `{ID:expire-inbound-aim-recordings, Prefix:"aim-recordings/", Expiration.Days:90}` (read back confirmed). Bucket PRIVATE: unauthenticated GET → **403** on BOTH `https://sgp1.digitaloceanspaces.com/capsy-recordings/...` AND `https://capsy-recordings.sgp1.digitaloceanspaces.com/...`. Access = presign-on-read ONLY. NO code/earner touched (Spaces config only). To change the window: edit `Days` on the lifecycle rule. 🟥 NOTE: DO Spaces `get_bucket_acl` returns `NotImplemented` — don't rely on bucket-ACL reads to prove private; prove it with an unauthenticated GET (403) instead. **(4) EARNER AFTER-GATE PASS:** in-window outbound `+917861019021` (job `cc09c4888b`, conc 1, `now=1` → `count:1 suppressed:0`) → docker `livekit-sip` SIP INVITE via trunk `ST_fmtVmNJmpzKa`, `+918071583488`→`+917861019021`, sipCallID `XY5QFVaxg3O8G8CNaen0L9iqrcc`, `inviteToTryingMs:14` → **486 Busy Here / USER_REJECTED** = carrier RANG (real ring); earner agent `agent-AJ_FAv4EKXw25mD` joined room `famit-917861019021-d5b728`. agent.py md5 `9150fabe4ff62b4b4470f9a87df346e5` UNCHANGED (before+after); famit-agent MainPID `1477083` / ActiveEnter `2026-06-10 19:58:18 UTC` NEVER restarted; famit-agent/famit-caller/aim-voice-agent all active; `/health`=200; 0 5xx; 0 Traceback. Box md5s at verify: caller `f05fdd2e35c3227e4a0aa7284c292f92`, aim `37750a77e65f1b35168209cbcfb87a66`, voice_tools `63c3f89b0b9edce2072a63a374a63554`, recorder `0d716d190b9f10b5d242e014dec1de89`, endpoints `740a9aaccb4689d7d957fa0d7cf6d32d`. **⭐ FOUNDER RECIPE (dead-simple, 2 things to try):** (A) HANDOFF NAME — open the panel → **AI Manager → Handoff Team → Add**, type the person's **Name** (e.g. "Rajesh") + their phone, Save. Or on a live call tell the AI "add Rajesh, plus-nine-one… to my handoff team." Then on a real inbound call ask "mujhe insaan se baat karni hai" → the AI will say **"Ek second, main aapko Rajesh se connect kar rahi hoon"** (it speaks the NAME, nothing else) and ring that person. (B) RECORDINGS — open the panel → **CRM → click any lead** → scroll the left side to the **Recordings** card → press **play** on any call (inbound or outbound) to listen, or **Download** to save the audio file. New leads' calls record automatically; older calls made before recording was turned on simply show no audio. **RESIDUAL:** caller-line + recordings proven over API/SIP/Spaces; the two-party AUDIBLE bridge + the spoken named line still need ONE real founder inbound call to fully accept (everything upstream proven). 90-day retention = sane default; tell me a different number and I'll change the `Days`. Build log: `memory/build_log/wave-build-REC-C-recordings-api.md` (FINAL INTEGRATED VERIFY section); ledgers `CONTINUE_HERE.md`/`MASTER_BUILD_STATE.md`/`ORCHESTRATOR.md` updated.
- 2026-06-13 — ✅ REC-UI FRONTEND DEPLOY (Handoff `name` field + CRM lead Recordings player) — SHIPPED LIVE to FORTRESS (FRONTEND-only; earner UNTOUCHED — the FORTRESS panel box `143.110.247.249` has NO agent dir, and the earner box `168.144.153.145` was never touched this unit). 🟥 RE-RUN RECONCILE (the `wimqqngha` wave died on a network-drop): the UI CODE was already COMPLETE + COMMITTED — `68bbc63 feat(ui): handoff Name field + CRM lead Recordings player` (Part 1 `app/ai-manager/_handoff.tsx` + `lib/api.ts` HandoffMember.name wired through type/normalizer/`addHandoffMember` FormData/`saveHandoffOrder`; Part 2 unit D `app/crm/client.ts` `getContactRecordings(phone)` dormant-safe + `app/crm/[id]/page.tsx` `RecordingsCard`/`RecordingRow` native `<audio controls preload=none>`+Download mirroring the AIM player; D3 = AIM player needed NO change, the stuck-playback was the REC-A backend finalize bug). What was UNDONE: the DEPLOY. VERIFIED ON THE BOX (don't trust the prompt): live panel was at the OLD LPR `BUILD_ID WwBfbgcnCuH-Rzi9--YvE`, box `_handoff.tsx` had `grep -c 'EMPTY_FORM = { name'`=0 and box `crm/client.ts` had `getContactRecordings`=0, and NO `*.RECUIbak.*` existed → the frontend agent committed-but-never-shipped. ⭐ DEPLOY RECIPE (build-LOCALLY, ship artifacts — the box has only ~1.4GB free so an on-box `next build` risks OOM): (1) local `npx tsc --noEmit` EXIT 0 + `npm run build` EXIT 0 (both green; `/crm/[id]` 7.09kB, `/run` 12.4kB). (2) `tar -czf` the built `.next` + changed `app` + `lib` (56M). (3) backup-first on box: `cp -a` `.next`/`app`/`lib` → `*.RECUIbak.<ts>` (NOTE: `.next.RECUIbak.*` is a DOTFILE — won't show in a plain `*.RECUIbak.*` glob, list it explicitly). (4) 🟥 GOTCHA — SCP of the 56M tarball got TRUNCATED the first time (box 43,084,800 B vs local 58,478,042 B; likely the backgrounded call + a 120s extract timeout cut the stream) → tar died "gzip: unexpected end of file". FIX = re-`scp` in the FOREGROUND with a generous timeout and ALWAYS `md5sum` local==box BEFORE extracting (caught it cleanly; the live `.next` was still the old one, so no downtime). (5) extract to `_stage`, `grep -c` the new code is present in stage, read `_stage/.next/BUILD_ID`. (6) atomic swap: `mv` old `.next`/`app`/`lib`→`.old`, `mv` staged into place, `chown -R deployuser:deployuser` (live files are deployuser-owned even though you SSH as root). (7) `systemctl restart famit-panel` ONLY → active PID 239673, new `BUILD_ID 4aXNPr1rvAfpK4ku5dNa7`. (8) PROOF: 200 on BOTH loopback:3001 AND the `panel.famit.in` Cloudflare edge for `/ /login /crm /crm/[id] /ai-manager /ai-manager/sessions/[id]`, and the served HTML carried the NEW BUILD_ID on both (not cached). (9) cleanup `.old` + tarball, KEEP the 3 RECUIbak dirs. UX SHIPPED: the handoff add-contact modal now has a REQUIRED Name field (hint "The AI names this person to the caller on handoff — connecting you to Rajesh"), roster rows lead with the name (phone secondary w/ mobile icon); the CRM lead profile left-rail has a Recordings card listing inbound+outbound calls newest-first, each with an inline seekable player (preload=none → OGG only fetches on play, HTTP-range seek) + per-row Download of the presigned URL, with calm "no recordings"/"preparing" empty states (dormant-safe — `getContactRecordings` resolves any 404/offline/5xx to `[]`, never an error wall). 🟥 LESSON: when a wave dies mid-run, the code units may already be committed — `git log --oneline` + `git show --stat <sha>` the wave's commits FIRST; if the code shipped, the ONLY undone work is often the DEPLOY (a new deploy mints a new BUILD_ID + a new `*UIbak` — their ABSENCE on the box proves it never shipped). And for a >50M artifact push, md5-gate the transfer before extract — a truncated SCP fails silently as a tar EOF. State `famit-panel/REC_UI_STATE.md`. NEXT: founder acceptance — open a CRM lead with calls (recordings play+download) + add a named person to the Handoff Team and confirm the AI speaks the name on a real handoff (backend `name` wiring proven over API in the `b06ef17` unit).
- 2026-06-13 — ✅ REC-C UNIFIED RECORDINGS API (tenant-scoped) — SHIPPED (wave `handoff-name-and-recordings` REC-C; additive READ-only, famit-caller only; `agent.py`/earner/trunks/firewall/SIP UNTOUCHED). Box famit@168.144.153.145 `/opt/famit-agent/caller.py`; backup `caller.py.RECbak.20260613-194141`; md5 `e82ccbff…`→`f05fdd2e35c3227e4a0aa7284c292f92` (tracked mirror `droplet_work/caller.py` updated to match). venv `/opt/capsy-agent/.venv`, caller **port 8209**, X-Auth `FamitCall2026`. ⭐ **TWO new read routes (inserted right after `call_detail` / `@app.get("/calls/{call_id}")`):** (1) `GET /calls/{call_id}/recording` — outbound recording for ONE call; looks up `CALLS` by id, `require_object` BOLA guard (cross-tenant id → **404**, existence not revealed) → `{recording: ITEM}`. (2) `GET /contacts/{phone}/recordings` — ALL recordings for one lead UNIFIED across BOTH directions (newest-first): OUTBOUND from the REC-B JSON `CALLS` store (`calls_for(t)` tenant filter, matched on `norm(phone)`) **+** INBOUND from the REC-A `ai_manager_sessions` PG table (RLS-scoped `store.list_sessions(tid,channel="voice")` filtered on `caller_phone`, then `store.get_session`) → `{phone, recordings:[ITEM...], total, with_recording}`. **UNIFIED ITEM SHAPE** = `{call_id, direction("inbound"|"outbound"), phone, name, campaign_id, started_at, duration_s, status, recording_status, has_recording(bool), recording_presigned_url}`. ⭐ **PRESIGN REUSE (not a re-impl):** new helper `_rec_presign(bucket,key,expires_s=3600)` lazy-imports `ai_manager.recorder.presign` — the SAME proven sigv4 + path-style minter that powers the AIM player (serves 200 + 206 range) — and returns `""` on any error (panel shows "recorded, link unavailable" not a broken player). Helper `_outbound_rec_item(rec)` hardcodes `direction:"outbound"` (the row's own `direction` field is None on outbound) and treats `recording_key` as the authoritative handle (auto-egress returns no id). Helper `_inbound_rec_items(tid,phone_n)` carries the **REC-A finalize-on-read self-heal**: a session row with an `egress_id` that isn't terminal (`status not in uploaded/failed/disabled` OR `duration_s<=0`) gets reconciled via `recorder.finalize()` (`ListEgress` → true terminal status + ns→s duration) and persisted via `set_recording` BEFORE presigning. All helpers **NEVER raise** → degrade to `has_recording=false`/empty url, never a 500. Tenant pinned from the TOKEN (`resolve_tenant`) exactly like `/contacts`, NEVER a body/param; the inbound branch runs off the event loop (`asyncio.to_thread`) since `store`/`finalize` are sync. DEPLOY: backup-first → edit live copy → scp `/tmp/caller.recc.py` (md5 match) → box-venv `py_compile` OK + AST verify (5 fns + 2 routes present) → `cp` in place → `sudo systemctl restart famit-caller` ONLY → `is-active`+`/health`=200, 0 Traceback. EARNER GATE before+after PASS: agent.py md5 `9150fabe4ff62b4b4470f9a87df346e5` UNCHANGED, famit-agent MainPID `1477083`/ActiveEnter `2026-06-10 19:58:18` NEVER restarted; only famit-caller restarted. SMOKE (real HTTP): `GET /calls/61a6bfeada/recording` (admin) → ITEM w/ 360-char presigned URL → full GET **200** `audio/ogg` **56916 B**, `Range: bytes=0-1023` → **206** `audio/ogg` **1024 B** (Spaces `head_object` confirmed the object 56916 B audio/ogg); `GET /contacts/+918949906361/recordings` → 8 calls newest-first `with_recording:1` (REC-B-armed presigns; pre-REC-B rows `has_recording:false`/url ""); **TENANT ISO** (token minted via `_make_token` on box SECRET): tenant B `21d0a13603da` GET tenant-A call `/calls/61a6bfeada/recording` → **404**, B's token valid (`/calls`→200), B GET `/contacts/+918949906361/recordings` (A-owned phone) → **total:0** (no leak). 🟥 LESSON: a UNIFIED recordings API across inbound (PG/RLS `ai_manager_sessions`) + outbound (JSON `CALLS`) is just a SHAPE-MERGE over the two existing tenant-scoped stores + ONE shared presign helper — DON'T re-implement sigv4, reuse `ai_manager.recorder.presign` (already serves 200+206). Outbound BOLA = `require_object` returns 404 (don't reveal existence); inbound isolation is already FORCE-RLS in `store._query`. Carry the REC-A finalize-on-read into the list endpoint so a lead's stuck inbound rows self-heal their duration/status the moment the panel opens the lead. Build log: `memory/build_log/wave-build-REC-C-recordings-api.md`; state `caps/droplet_work/REC_C_STATE.md`. ⛔ NEXT (same wave, UNBUILT): CRM (`app/crm/[id]`) + AIM player UI to consume these routes; handoff-name e2e.
- 2026-06-13 — ⚠️ REC-B RE-RUN RECONCILE (post network-drop): the granular re-run prompt's premise was STALE — it claimed "NO edits landed, box at baseline caller.py md5 ce9718a7…". VERIFIED ON DISK instead of trusting it: box `/opt/famit-agent/caller.py` md5 = `e82ccbffa01824e23c3a8b9b3a89472b` = the **post-REC-B** state (helpers `_outbound_recording_enabled`/`_outbound_recording_key`/`_build_outbound_egress` at :170/:177/:185, `CreateRoomRequest(...egress=_egress)` at :2596, `recording_key` stored at :2615); tracked mirror `droplet_work/caller.py` md5 matches; agent.py md5 `9150fabe…` UNCHANGED; famit-agent MainPID `1477083`/ActiveEnter 2026-06-10 NEVER restarted; all 3 services active + `/health`=200; Spaces `outbound-recordings/2026/06/13/` still holds the 2 proof OGGs (`8ac35e10d9.ogg` 60436B, `7856586d3a.ogg` 56131B) + 2 egress manifests. CONCLUSION: REC-B is already shipped + live + proven — re-applying would risk a needless regression on the live earner box, so NO edit was made. LESSON: a "network-drop killed the agent, nothing landed" prompt can be wrong; ALWAYS md5 the box + check the deployed wiring + Spaces objects BEFORE re-doing a unit — the dead agent may have completed AND verified before the socket dropped.
- 2026-06-13 — ✅ REC-B RECORD EVERY OUTBOUND CALL (server-side auto-egress) — SHIPPED (wave `w7clsa0fz` REC-B; additive, OUTBOUND-recording only; `agent.py`/earner/trunks/firewall/SIP/`prompt.py`-defaults UNTOUCHED). Box famit@168.144.153.145 `/opt/famit-agent/caller.py`; backup `caller.py.RECbak.20260613-185529`; md5 `874d27e6…`→`e82ccbffa01824e23c3a8b9b3a89472b` (tracked mirror `droplet_work/caller.py` updated to match). venv `/opt/capsy-agent/.venv`, caller **port 8209**, X-Auth `FamitCall2026`. ⭐ **THE WIRING:** every outbound campaign call now records server-side by attaching **AUTO-EGRESS to the room at create time** — `caller.py run_job` (~:2528) now passes `CreateRoomRequest(name=room, …, egress=_egress)` where `_egress = api.RoomEgress(room=api.RoomCompositeEgressRequest(audio_only=True, file_outputs=[api.EncodedFileOutput(EncodedFileType.OGG, filepath=key, s3=api.S3Upload(…, force_path_style=True))]))`. LiveKit's egress worker does the capture+upload — **the agent process (`agent.py`) never touches it** (that's why agent.py stays byte-identical). New helpers after the LK config block (~:159): `_outbound_recording_enabled()` (arms ONLY when `AIM_RECORDING_ENABLED` truthy AND the `AIM_SPACES_*` creds complete — DORMANT-UNTIL-CREDS, and it **REUSES the exact same already-configured inbound Spaces creds** → bucket `capsy-recordings`, region `sgp1`), `_outbound_recording_key(call_id)` → deterministic `outbound-recordings/YYYY/MM/DD/<call_id>.ogg`, `_build_outbound_egress(call_id)` → `(egress|None, key, bucket)` and **NEVER raises** (disabled/error → `(None,"","")` so a paid outbound call ALWAYS dials, recorded or not = earner-safety). The `call_id` is now chosen BEFORE `create_room` so the **call-row `id` == the `<call_id>` embedded in the object key** (the row IS the recording's address). `record_call`'s `rec` dict gains 3 ADDITIVE keys — `recording_key`, `recording_bucket`, `recording_status` (`"recording"` armed / `"disabled"` dormant); CALLS store is a free-form dict (JSON, or PG stores the dict as-is) → NO schema migration. 🟥 `egress_id` is NOT stored at create time on purpose — **auto-egress returns NO id at room-create**; the AUTHORITATIVE handle is the deterministic `recording_key` (object lands there) and a later REC-C reconcile can `ListEgress(room_name)` to fill duration/status exactly like REC-A's read-side self-heal. PROOF (two real in-window outbound calls, 18:58 IST, campaign `c17e55e9f3`→+917861019021 conc1 `now=1`): run#1 job `6e5075f615`→row `8ac35e10d9`→SIP callID `SCL_vWxZbnWSArf4` trunk `ST_fmtVmNJmpzKa` +918071583488→+917861019021 (carrier reached: `486 Busy`/reason busy = REAL ring) → egress `EG_CkpWN2775XQY` **status=3 EGRESS_COMPLETE** filename `outbound-recordings/2026/06/13/8ac35e10d9.ogg` **size 60436**; run#2 job `9ca79fd03c`→row `7856586d3a`→object `…/7856586d3a.ogg` **56131 bytes**. Spaces `capsy-recordings` prefix `outbound-recordings/2026/06/13/` = 4 objects (2 OGG + 2 egress-manifest JSON); `HEAD …/8ac35e10d9.ogg`→60436 bytes ContentType **audio/ogg**, presigned GET minted OK; BOTH call rows carry `recording_key`=the exact landed object + `recording_status="recording"`. DEPLOY: local py_compile OK → scp `/tmp/caller.recb.py` (md5 match) → box-venv py_compile OK → egress-object validated against live `livekit.api`+box `.env` (`enabled=True`, audio-only OGG, key prefix correct, bucket+sgp1 endpoint) → cp in place → `systemctl restart famit-caller` ONLY → `/health`=200, 0 Traceback/5xx. EARNER GATE before+after PASS: agent.py md5 `9150fabe4ff62b4b4470f9a87df346e5` UNCHANGED, famit-agent MainPID `1477083`/ActiveEnter 2026-06-10 NEVER restarted; only famit-caller restarted; aim-voice-agent + famit-agent active+untouched. 🟥 LESSON: to record an outbound call server-side WITHOUT touching the voice earner, attach **auto-egress on `CreateRoomRequest.egress`** at room-create in `run_job` — it runs in LiveKit's egress worker, completely independent of the agent process, so `agent.py` stays byte-identical (the egress completes + the OGG lands even when the human declines the ring — proven by a `486 Busy` call still producing a 60KB object). Choose the `call_id` BEFORE `create_room` so the call row id == the deterministic object key, and DON'T expect an `egress_id` back at create time (auto-egress returns none) — store the deterministic `recording_key` as the authoritative handle and reconcile id/duration/status later via `ListEgress`. Build log: `memory/build_log/wave-build-REC-B-outbound-egress.md`. ⛔ NEXT (same wave, UNBUILT): REC-C unified recordings API (list + `/calls/{id}/recording` presign that reconciles via `ListEgress`), CRM/AIM player UI for outbound recordings.

- 2026-06-13 — ✅ AIM RECORDING FINALIZE FIX — SHIPPED (wave `w7clsa0fz` REC-A; additive, INBOUND-recording only; `agent.py`/earner/trunks/firewall/SIP/`prompt.py`-defaults UNTOUCHED). Box famit@168.144.153.145 `/opt/famit-agent`; backups `recorder.py.RECbak.20260613-184551` + `endpoints.py.RECbak.20260613-184551`. 🟥 **ROOT CAUSE (PROVEN, not guessed):** the egress ALWAYS completes and the full OGG ALWAYS lands in Spaces (verified via LiveKit `ListEgress`: `EG_W9hFFGao8RLj` status=3 EGRESS_COMPLETE dur=117173963610**ns**=117s size=1.6MB; `EG_pmG3A2BfSiNE`=102s/1.4MB). The bug is the finalize **WRITE**: `aim_voice_agent.py` `_SessionLogger.finish()` (→`recorder.stop()`→`store.set_recording(status='uploaded',duration_s)`) is dispatched **fire-and-forget** via `asyncio.run_coroutine_threadsafe(_slog.finish(...), _loop)` on the room `"disconnected"` event (aim_voice_agent.py:2606-2612) → that off-loop PG write almost always LOSES THE RACE vs LiveKit worker-process teardown on SIP hangup → the `uploaded`+duration UPDATE never lands → row stuck `recording`/`0` (only 1/15 ever won the race). FIX (2 files, famit-caller/aim-voice-agent only; agent.py untouched): (1) `ai_manager/recorder.py` new `finalize(egress_id)->{complete,status,duration_s,key,size}` — calls `ListEgress`, maps EgressStatus (3 COMPLETE→`uploaded`, 4/5 FAILED/ABORTED→`failed`, else `recording`), `file_results[0].duration` **ns→s**, real key from `filename`; NEVER raises (LiveKit 404 "egress does not exist" → `complete=False`, row LEFT AS-IS); (2) `ai_manager/endpoints.py` `GET /ai-manager/sessions/{id}` **finalize-on-READ self-heal**: if row has `recording_egress_id` but not terminal (`status not in uploaded/failed/disabled` OR `duration_s<=0`) → `recorder.finalize()`; if complete, `store.set_recording(status='uploaded',key,duration_s)` + reflect on response, THEN presign. The read-side reconcile is AUTHORITATIVE and does NOT depend on the fragile teardown — agent-side stop() leg left UNCHANGED. PROOF: stuck `vs_dee5eeef4141` → one `GET /sessions/{id}` → DB re-queried = `uploaded`/`117` (persisted), presigned URL serves OGG `curl -r 0-1048575`→**HTTP 206** `audio/ogg` 1048576 bytes; fresh `vs_07c19d8f8b0b`→`uploaded`/`102`; batch-heal via reads = **17 rows now uploaded** (dur 11–229s); lone remaining `recording` row's egress 404s in LiveKit (aged out) → correctly untouched (no false uploaded). EARNER GATE PASS: agent.py md5 `9150fabe4ff62b4b4470f9a87df346e5` UNCHANGED, famit-agent PID `1477083` NEVER restarted; both restarted services active, 0 Traceback, aim worker re-registered `agent_name=manager` `AW_c8iRik2Qkexz`. 🟥 LESSON: a fire-and-forget `run_coroutine_threadsafe` finalize on a LiveKit room `disconnected`/shutdown event will routinely lose to worker-process teardown — DON'T rely on the hangup leg for durable state; make the truth **reconcilable on READ** from the authoritative source (`ListEgress`), which also self-heals every historical stuck row. And remember LiveKit egress `duration` is NANOSECONDS. caller on **port 8209**, X-Auth `FamitCall2026`. Build log: `memory/build_log/wave-build-REC-A-finalize.md`. ⛔ NEXT UNITS (same wave, UNBUILT): REC-B outbound auto-egress (caller.py `run_job` CreateRoomRequest, agent.py untouched), REC-C unified recordings API, CRM/AIM player UI.

- 2026-06-13 — ✅ HANDOFF NAME + CLEAN CALLER LINE — SHIPPED (granular re-run after the `wimqqngha` network-drop; INBOUND/handoff-list only, additive; `agent.py`/trunks/firewall/SIP/`prompt.py`-defaults UNTOUCHED). Box famit@168.144.153.145 `/opt/famit-agent`; backups `*.RECbak.20260613-125259` for caller.py/aim_voice_agent.py/ai_manager/voice_tools.py. 🟥 **THE BUG FOUND:** the AIM `add_handoff` voice tool passes the spoken `name` POSITIONALLY as the 3rd arg into `_vt.add_handoff(tenant, phone, name, prio, whatsapp)`, but `voice_tools.add_handoff`'s 3rd param was `role=` — so every spoken name was being silently stored into `role`, and there was NO `name` field anywhere in the handoff data model. FIX (3 layers, all additive): (1) `caller.py` `_handoff_get`/`_handoff_set`/`_handoff_add_one` now read+store `"name"`; (2) `ai_manager/voice_tools.py` `handoff_list` returns `name`, and `add_handoff` signature reordered to `(tenant_id, phone, name="", priority=0, whatsapp="", hours="", enabled=True, role="")` so the agent's positional call now lands `name`→name (role kept as a trailing kw for the `/brain/handoff/add` HTTP path); (3) `aim_voice_agent.py` `list_handoff` speaks `name` (fallback `role`). E2E PROVEN over live API: POST `/brain/handoff/add {"name":"Rajesh",...}` → GET `/brain/handoff` returns `"name":"Rajesh"`; pre-existing entries show `"name":""` (backward-compat); test entry then restored to the founder's original 2-entry list. **CLEAN CALLER LINE** in `_do_warm_transfer`: OLD `"Ek minute, main aapko hamari team se connect kar rahi hoon, line par baney rahiye…"` → NEW `f"Ek second, main aapko {_dial_who} se connect kar rahi hoon."` where `_dial_who` = `dialable[0].name` (then `.role`, else `"apni team"`) — NAMES the dialed person, NOTHING else (no phone/reason/AI-disclosure). The dialed person = `dialable[0]` (first eligible, priority order) which is computed at line 727 BEFORE the say at 731, so it's available. **HUMAN-WHISPER trimmed** (`_transfer_whisper`): the shared-SIP-room line the caller ALSO hears no longer dumps phone/reason/summary — now just `f"{name} aapse baat karna chahte hain — aap baat kar sakte hain."` (name here = the CALLER's `_caller_name`); reason/phone/summary stay in the signature/chat_ctx but are NOT spoken. KEPT untouched: `session.aclose()` AI-exit, the DIRECT same-room `create_sip_participant` bridge, the `participant_disconnected` `human-handoff-*` hangup handler. EARNER BEFORE-GATE PASS: agent.py md5 `9150fabe4ff62b4b4470f9a87df346e5` UNCHANGED, famit-agent MainPID 1477083 / ActiveEnter 2026-06-10 19:58:18 NEVER restarted; in-window 18:21 IST `/run c17e55e9f3`→+917861019021 conc1 → `count:1 suppressed:0` job `a8949c33e9` → livekit `API SIP.CreateSIPParticipant status:"200"` callID `SCL_Nx4bnnhdj9eS` trunk `ST_fmtVmNJmpzKa` +918071583488→+917861019021, earner agent `capsy` joined room+published audio, phone leg `486 Busy Here`/`USER_REJECTED` (RANG+declined = real carrier ring), NO 402/5xx. 🟥 GOTCHA: the prior gate had auto-added `+917861019021` to `var/suppression.json` reason `opt_out_call` (a TEST call at 12:35 misclassified) → my first `/run` came back `suppressed`; cleared it via `DELETE /suppression/%2B917861019021` (URL-encode the `+`) then re-ran clean. caller is on **port 8209** (not 8000), X-Auth `FamitCall2026`; the SIP dial originates in `caller.py:2528 run_job` (create_room+create_dispatch agent_name=capsy+create_sip_participant) so the ring proof is in livekit-server/sip Docker logs + `var/calls.json` (status `done`+`sip_call_id`), NOT famit-bridge/famit-agent journals. DEPLOY: local+box-venv py_compile OK ×3 → moved in place → restarted ONLY famit-caller+aim-voice-agent (active; caller /health=200; aim re-registered `agent_name:manager` AW_8dsnRLJBzwCo; 0 Traceback/Import/Syntax) → famit-agent NOT restarted (MainPID unchanged). New md5: caller `874d27e6…`, aim `37750a77…`, voice_tools `63c3f89b…`. ⛔ REAL acceptance = the founder's REAL inbound call: ask the AI to "add <Name> +91… to my handoff team", then trigger a handoff → the caller should hear ONE clean line naming that person. LESSON: when a voice tool maps a spoken arg POSITIONALLY into a backend fn, verify the backend signature's positional order — a silently-misnamed param (name→role) compiles + "works" but stores the wrong field forever. Build log: `memory/build_log/wave-build-HCRB-inbound-clean-handoff.md` (FOLLOW-ON UNIT section).

- 2026-06-13 — ✅ NETWORK-DROP MID-WAVE RECONCILE (wave `wimqqngha` handoff-name-and-recordings died on an API ConnectionRefused after ~90 tool calls; READ-ONLY reconcile, NOTHING edited/restored, earner UNTOUCHED). PROTOCOL that resolved it cleanly: (1) BOX HEALTH — all 3 services `active` (famit-caller restarted 10:54 UTC, aim-voice-agent 10:55, famit-agent earner NEVER restarted MainPID 1477083 since 2026-06-10); BOTH `caller.py` + `aim_voice_agent.py` `py_compile` OK on `/opt/capsy-agent/.venv`; zero Traceback/ImportError/SyntaxError post-restart; aim worker re-registered clean `agent_name:manager` (served real inbound 11:19 + 11:57 UTC). (2) 🟥 KEY FINDING — the dead backend agent created its `*.RECbak.20260613-171500` backups (its FIRST step) but DIED before ANY edit persisted: live `caller.py` (md5 `ce9718a7…`, 6242 lines) is **byte-identical** to `caller.py.RECbak.20260613-171500`, and live `aim_voice_agent.py` (md5 `a7d5e0ad…` = the confirmed-good HCRB handoff) is **byte-identical** to `aim_voice_agent.py.RECbak.20260613-171500`. So the box was ALREADY at the exact pre-wave known-good state → NO RESTORE NEEDED (restoring would have been a no-op write). NONE of the 5 build units landed: no handoff `name` enhancement beyond the pre-existing HCRB `add_handoff(phone,name,priority)`, no transfer-line change, no AIM finalize fix, no run_job auto-egress, **no recordings API (`GET /sessions`/recording route is NOT mounted; the only presigned-URL code is pre-existing WhatsApp doc-send at caller.py:1931/3493)**. (3) EARNER GATE PASS — agent.py md5 `9150fabe4ff62b4b4470f9a87df346e5` UNCHANGED, in-window (18:04 IST) Vobiz funded ₹480.19, real `/run` (campaign `c17e55e9f3`, leads form-field `+917861019021`, conc 1) → `count:1` job `0d81f54d18` → livekit-sip callID `SCL_5Ld3x5EKUtKW` = `Outbound SIP call established`+`accepting RTP stream`+agent↔phone `track subscribed`, NO 402 (RANG+CONNECTED); 0 5xx. (4) PANEL HEALTHY + UNTOUCHED — FORTRESS `famit-panel` active since 09:04 UTC (BEFORE the wave's 09:56 backend edit window), BUILD_ID `WwBfbgcnCuH-Rzi9--YvE` (the prior LPR build — no new `RECUIbak` exists → frontend agent NEVER deployed), `/`+`/login`+`/crm`+`/ai-manager` = 200 on BOTH loopback:3001 AND public Cloudflare, zero panel errors in the wave window. 🟥 LESSON: when a wave dies mid-run on a NETWORK error (not a code bug), reconcile by diffing each live file against its OWN `*.RECbak.<ts>` (the agent's pre-edit snapshot) — if `diff -q` says identical, the agent backed-up-but-died-before-writing and the box is ALREADY known-good (DON'T restore — that's a needless write to the live earner box). Confirm the GATE (compile + service active + earner md5 + real in-window ring with `inviteToRingingMs`/`established`+RTP, NOT a participant line) and the panel BUILD_ID (a new deploy mints a new BUILD_ID + a new `*UIbak`; their ABSENCE proves the frontend agent never shipped). RE-RUN: the whole `handoff-name-and-recordings` build (all 5 units) is UNBUILT and safe to re-launch from this clean baseline.

- 2026-06-13 — ✅ HCRB INBOUND CLEAN-HANDOFF + END-TO-END BEHAVIOR REWRITE — SHIPPED (INBOUND-only, additive; `agent.py`/trunks/firewall/SIP/`prompt.py`-defaults UNTOUCHED). Box famit@168.144.153.145, `/opt/famit-agent/aim_voice_agent.py` md5 `61f2e0e…` → `a7d5e0ad9d50364e04181d37bfc63ba8` (backup `.HCRBbak.20260613-162320`); venv `/opt/capsy-agent/.venv`. ⭐ **THE CORE FIX (fix e) = the documented AI-talks-over-human root cause:** `_do_warm_transfer` used to end a successful bridge with a SOFT return-STRING ("…now STOP talking completely…do not speak again") — but that leaves the `AgentSession` ALIVE (still consuming STT/LLM/TTS), so the AI keeps reacting OVER the live human. FIX = after the human SIP participant answers + the ONE short whisper plays out, **`await session.aclose()`** (the AgentSession ref is reachable as `getattr(context,"session",None)`); the room + caller + bridged human all PERSIST and the room-disconnect shutdown hooks (memory/lead persist, `_slog.finish`, `_live_remove`) still run — only the AI's media pipeline stops. DELETED the soft return-string; bridge now returns the minimal token `"handed_off"` (the LLM never reads it anyway — session is closed). Mute-alone is NOT enough (session stays alive); `aclose` is the real exit. **THE OTHER 7 FIXES:** (a) deleted the greeting "this call may be recorded for quality" append (`:2515-2516`, was behind `AIM_DISCLOSE_RECORDING` default-off; now gone entirely) + inbound rule "never mention recording/monitored/quality". (b) AI-disclosure OFF for inbound by calling `prompt.build_system_prompt` with a PER-CALL fields COPY `{...,"disclose_ai":False}` (does NOT mutate the caller dict; **prompt.py defaults stay `disclose_ai=True` so the OUTBOUND earner that reuses `build_system_prompt` is byte-identical**) → the disclosure clause (`prompt.py:274-284`, opener `:352-356`) is not injected; + an inbound IDENTITY override "you are {agent} from the {company} team, NEVER say you are an AI/bot/assistant; if asked, stay in character" that OVERRIDES `prompt.py:128`+`:145-146` admit-AI lines (those live in the SHARED brain, so override in the inbound builder, do NOT edit prompt.py). (c) strong inbound LANGUAGE+STYLE override (casual Hinglish, NOT heavy Devanagari; short 1-2-sentence turns; YOU close the deal end-to-end — pitch→objections→booking). (d) DE-EAGER handoff: REMOVED "Also call it for a clearly HOT, ready-to-buy lead" from the customer `transfer_to_human` docstring (`:1646`) AND the customer `inbound_note` (`:1415`); default = handle it + book yourself, never offer/jump to a human; fire ONLY on EXPLICIT ask OR genuinely-very-hot-AND-can't-close — kept the imperative same-turn ACT-don't-announce trigger (the HORT fix) so it still fires reliably WHEN warranted. (manager docstring `:1214` + prompt bullet `:520` were already de-eager from HORT; added "DEFAULT = handle it yourself".) (f) HUMAN-HANGUP: `ctx.room.on("participant_disconnected")` keyed STRICTLY on the `human-handoff-*` identity (set at the `create_sip_participant` call `:819`) → brief goodbye to the caller (best-effort; AI usually already aclose'd) → `_hangup(ctx,room_name)` (`:2534`) so the caller call ends gracefully instead of hanging. (g) KEPT the DIRECT same-room `create_sip_participant(room_name=<caller room>)` bridge, trunk `ST_fmtVmNJmpzKa` — AST-proven ZERO `WarmTransferTask` code references (only comment/docstring prose); no side-room regression. (h) OBSERVABILITY (the diagnosis found zero transcript logging + empty turns table): universal journal logging — STT FINAL transcript via `@session.on("user_input_transcribed")`, each assistant/user turn via `conversation_item_added`, + the full handoff lifecycle (REQUESTED → Dialing #N → BRIDGED → AI-EXITED → Human-hangup); PINs scrubbed (`\d{4,}`→`****`) before any log; existing `_slog` PG transcript writes kept. 🟥 **GOTCHA — STALE LOCAL COPY:** `caps/droplet_work/aim_voice_agent.py` (md5 5daff97, 2393 lines) was PRE-LPR/STALE; the LIVE box file (61f2e0e, 2555 lines) is the source of truth and matched the diagnosis line numbers. ALWAYS `scp` the live box file down, edit THAT, py_compile on the box venv, push back — never edit the local scratch copy. GOTCHA — `entrypoint` is a thin wrapper that calls `_entrypoint_impl`; the room-event handlers live in `_entrypoint_impl` (1990-2659), so AST-scoped checks must target `_entrypoint_impl`, not `entrypoint`. DEPLOY: backup → scp to /tmp → `py_compile` on `/opt/capsy-agent/.venv` (OK) → cp into place → restart famit-caller + aim-voice-agent ONLY; worker re-registered clean (`agent_name:manager` AW_iRXGWULzZBC3, 0 Traceback/ImportError); caller `/health`=200. SMOKES (minimal burn): (1) inbound render → no recording / no AI-disclosure (disclose_ai=False PROVEN: outbound default STILL discloses, inbound does NOT; prompt.py source default still `disclose_ai=True`) / casual Hinglish / end-to-end / de-eager; (2) **integrated turn-loop through the REAL `_aim_llm` chain** (built exactly as `_entrypoint_impl` does; ran WHILE Groq TPD was exhausted at 496046/500000 so it provably FAILED OVER to SambaNova/OpenRouter): T1 "Is project mein 3 BHK ka price kya hai?" → fired `lookup`, answered the real price "lagbhag एक करोड़ बत्तीस लाख se start hoti hai" in Hinglish, did NOT fire `transfer_to_human`, did NOT offer a human; T2 "Mujhe kisi insaan se baat karni hai" → fired `transfer_to_human` ONCE (reason "caller explicitly asked for a human"); `_do_warm_transfer` STUBBED so no real founder dial; (3) AST code-path → `session.aclose()` present after bridge, return-string GONE, returns "handed_off", AI-EXITED logged, `participant_disconnected`+`human-handoff-` handler + `_end_after_human_left`→`_hangup` present, same-room bridge kept, 0 WarmTransferTask code refs, STT-final+turn+REQUESTED journal logs present. EARNER GATE before+after PASS: agent.py md5 `9150fabe4ff62b4b4470f9a87df346e5` UNCHANGED, famit-agent MainPID 1477083 / ActiveEnter 2026-06-10 19:58:18 NEVER restarted, Vobiz ₹486 funded; BEFORE in-window /run `c17e55e9f3`→+917861019021 callID `SCL_LgpZwqq5FjYn` = `Outbound SIP call established`+`accepting RTP stream`+`track subscribed`+famit-agent `memory saved turns=1,3,5,7` (RANG+ANSWERED+multi-turn); AFTER callID `SCL_Lbu6UsfcebY6` = `established`+`accepting RTP stream` (RANG+connected); only famit-caller+aim-voice-agent restarted. ⛔ **THE ONLY REAL ACCEPTANCE = the founder's REAL inbound call to the DID +918071583488** (NOT a smoke) — smokes prove the code path, not the lived call. Founder test: call in, ask a property question (AI answers + steers to booking, NO human offered), then say "mujhe insaan se baat karni hai" → ONE short line → human bridges (handoff target +916375548830 p1 / +917861019021 p2) → **AI goes SILENT (aclose) — only caller+human remain** → when the human hangs up, the caller call ends gracefully. LESSON: a "AI says I'm connecting then talks over the human" handoff bug is NOT in the dial/bridge (that worked) — it's that the AgentSession was never CLOSED after the bridge; a soft "stop talking" instruction can't silence a live STT/LLM pipeline, only `session.aclose()` can. And keep the earner's brain untouched by passing INBOUND overrides through a per-call fields COPY into the shared `build_system_prompt` rather than editing prompt.py globals. Build log: `memory/build_log/wave-build-HCRB-inbound-clean-handoff.md`; state `caps/droplet_work/HCRB_INBOUND_STATE.md`.

- 2026-06-13 — ✅ HUMAN-HANDOFF "STILL FAILS ON A REAL CALL" RE-DIAGNOSIS — **THE HANDOFF IS ACTUALLY WORKING ON THE CURRENT CODE; the prompt's premise (a FallbackAdapter/PoolLLM tool-calling REGRESSION) is FALSIFIED by the LIVE logs** (READ-ONLY trace + one integrated smoke; NOTHING edited on the box, earner UNTOUCHED). Current `aim_voice_agent.py` md5 `61f2e0e` (the HORT prompt-hardening + EMERG/LPR FallbackAdapter[Groq PoolLLM 15 keys -> SambaNova PoolLLM -> OpenRouter-free]); aim-voice-agent restarted 09:38 UTC / 15:08 IST (pid 2246707). **HARD LIVE EVIDENCE the handoff fires + bridges TODAY on this exact code:** the founder's MOST-RECENT inbound (a CUSTOMER call, caller +916376980812 "कुणाल कुमार", room RM_o6oyM8WghMGu, ending 10:15 UTC / 15:45 IST — i.e. WHILE I was diagnosing) shows `transfer_to_human (customer)` FIRING repeatedly and BRIDGING a real human: 10:13:31 `BRIDGED +91***21 (#1, 7.8s)`, 10:14:09 `BRIDGED (#1, 11.2s)`, 10:14:21 bridged again, and at 10:14:47 when #1 didn't answer (`TwirpError … sip request timed out, status=408`) it correctly **failed over to #2 +91***30 -> `BRIDGED (#2, 4.9s)`** — proving tool-fire + dial + the priority fallback all work end-to-end, repeatedly, post-restart. (Yesterday's Jun 12 14:55 bridge to RM_9zbSt7naohPU is ALSO in the 24h window — so the bridge has worked across both code versions.) **WHY the founder saw "AI says I'm doing then nothing":** (1) STT/assistant turns are NOT logged to the journal (only tool side-effects + the `skipping user input` warning leak text; `ai_manager_sessions` table is EMPTY — PG turn-logging is silently degrading, so there is NO durable transcript to read the exact STT/spoken text — a real OBSERVABILITY gap). (2) The two manager-path inbound calls in the failing window (caller 063***30, agent_name=**manager**, PIN-gated, rooms 5Fte886bm6H4 @10:00 + 8CqqrQp3xybX @07:58) had **ZERO handoff request** — the 10:00 one fired `verify_pin`->`list_campaigns`->`run_campaign` (CONFIRMED, job c6fa79a471 count=4) then "Bye bye"; the 07:58 one hung up at the PIN. So the call where he "asked for a human and nothing happened" is EITHER an earlier attempt that left no handoff log (tool never fired on a manager turn) OR the manager-path narrate-don't-act behaviour — but it is NOT a chain/plumbing regression. **PLUMBING VERIFIED SOUND:** (a) `llm_router/pool_llm.py` PoolLLM.chat FORWARDS `tools` into `_PoolLLMStream` (line 121) -> `_run` calls `delegate.chat(chat_ctx, tools=self._tools or None, …)` (line 61-66) = tools reach the model; (b) LiveKit's installed `FallbackAdapter` forwards `tools`+`parallel_tool_calls`+`tool_choice` to every member (`fallback_adapter.py:168-170`); (c) the primary model is UNCHANGED (`GROQ_LLM_MODEL=meta-llama/llama-4-scout-17b-16e-instruct`, same as the working `HOTLbak` which used a bare `groq.LLM`). (d) **NEW manager-path integrated smoke** `/tmp/aim_mgr_handoff_smoke.py` (drives the REAL `ManagerAgent` (PIN pre-verified) + REAL 15 tools through the REAL FallbackAdapter chain inside http_context, stubs `_do_warm_transfer`): on the founder's EXACT "human handoff karo" + "mujhe kisi insaan se baat karni hai" + English -> `transfer_to_human` FIRED 3/3 turns. So on the current code, BOTH the customer path (proven LIVE) and the manager path (proven by integrated smoke) fire the tool reliably. (e) No 429/switching/recovery/filler in the whole failing window = the LLM was healthy (Groq served). **CONCLUSION: no regression to fix; the FallbackAdapter/PoolLLM does NOT break tool-calling and llama-4-scout DOES fire `transfer_to_human` on "human handoff karo" through the live chain.** NO CODE CHANGE APPLIED (correct call — there is no reproduced defect to patch; editing the box would be churn + earner risk for nothing). **HONEST RESIDUAL / what the founder should re-test:** call the CUSTOMER line (not the PIN-gated manager line) and say "human se baat karni hai" — that path is proven bridging right now; the dial leg rings (Vobiz funded, #2 bridged in 4.9s today) and #1's occasional 408 is just his own phone not picking up that ring (it then falls over to #2). The ONLY genuine fix worth doing is OBSERVABILITY so we never again diagnose blind: (i) restore `ai_manager_session_turns` writes (the table is empty — turn logging is failing silently) and (ii) log the STT final transcript + each assistant spoken turn to the journal, so the EXACT STT text + whether the tool fired is readable per call without a smoke. EARNER GATE PASS: agent.py md5 `9150fabe4ff62b4b4470f9a87df346e5` UNCHANGED, famit-agent MainPID 1477083 / ActiveEnter 2026-06-10 NEVER restarted, no 5xx; the earner even ran a real outbound 09:53 UTC (room famit-917861019021-ef80a1). NOTHING restarted this pass (pure read-only trace + one stubbed LLM smoke). 🟥 LESSON: when a prior wave "fixed + smoke-proved" a handoff but the founder still reports failure, FIRST grep the live journal for `AIM transfer_to_human`/`BRIDGED` in the failing window BEFORE assuming a regression — here the tool was firing and bridging the whole time; the "regression" was a misread. AND: the founder's failing calls were the MANAGER (PIN) path while the prior smoke + the live success were the CUSTOMER path — ALWAYS confirm WHICH agent (manager vs customer) the failing call hit (the `agent_name` in `received job request`), because they are different classes/prompts. AND: with NO STT/assistant-turn logging and an EMPTY turns table, a "real call" trace is half-blind — fix observability so the next diagnosis reads the actual transcript, not a reconstruction.
- 2026-06-13 — ✅ HUMAN-HANDOFF "SAYS YES THEN SILENCE" — **FIXED** (the surgical code fix on top of the read-only diagnosis below). ROOT CODE-LEVEL CAUSE FOUND with a reproducible integrated turn-loop smoke (`/tmp/aim_handoff_fire_smoke.py`, stubs `_do_warm_transfer` so no real founder dial during the LLM test, drives the REAL CustomerSalesAgent tools + REAL system prompt through the EXACT `_aim_llm` 3-member chain [Groq pool -> SambaNova -> OpenRouter-free] inside `http_context`): on the HINDI handoff phrasing "Mujhe kisi insaan se baat karni hai", the small Groq primary (llama-4-scout) **SPOKE** "Main aapko ek human salesperson se connect kar sakti hoon… kya main aapko transfer karoon?" and `tools_fired=[]` — it ANNOUNCED/ASKED-PERMISSION instead of firing `transfer_to_human` in that turn (the tool only fired on a LATER turn). **That announce-don't-act behaviour IS the founder's exact "AI says haan then silence, human never rings" — the LLM promises a transfer but never invokes the tool, so nothing dials.** (English phrasing already fired correctly; SambaNova/70B fired on BOTH — so the weak link was specifically the Groq-primary path on Hindi.) THE FIX = prompt hardening ONLY (no logic change; `_do_warm_transfer` was already robust — `_say_filler`/`_start_hold_audio`/the dial are all try/except-guarded + finally-stop hold music, so a hold-audio/asyncio failure degrades to spoken-only + still dials, never aborts into silence). Rewrote the handoff instruction in 5 places of `aim_voice_agent.py` to be IMPERATIVE "ACT, DON'T ANNOUNCE": customer `inbound_note`, customer disambiguation note, customer `transfer_to_human` docstring, manager prompt bullet, manager `transfer_to_human` docstring — all now say "the MOMENT the caller asks for a person/human/insaan/aadmi/banda (ANY language) you MUST call `transfer_to_human(reason)` IMMEDIATELY in the SAME turn as your VERY NEXT action; do NOT first ask 'shall I transfer you / kya main transfer karoon' and do NOT just say 'main connect kar rahi hoon' then wait — calling the tool is the ONLY thing that actually connects them; merely talking about it leaves the caller in silence." PROOF the fix executes end-to-end: after the edit the SAME Hindi turn now FIRES the tool — 3/3 healthy-Groq smoke runs = `transfer_to_human` fired on BOTH the Hindi AND English handoff turns (was 0 on Hindi before); FORCE-FAIL run (bad Groq key) shows `groq failed -> switching` then `openai/openrouter failed -> switching` and the tool STILL fires via SambaNova = the FallbackAdapter provably fails OVER and the tool fires regardless of which member serves the turn. DIAL LEG re-confirmed funded+ringing (Vobiz ₹495.65). So the full chain is proven as far as possible without a live inbound: tool FIRES reliably (small-model Hindi included) -> `_do_warm_transfer` runs (robust, never silent) -> hold music + dial primitive `create_sip_participant(room_name=<caller room>, trunk ST_fmtVmNJmpzKa)` (the same-room DIRECT bridge, NOT the beta side-room) -> rings/200 -> whisper -> bridge; fallback ordering on no-answer -> apology + hot-lead WhatsApp + callback (never silence). DEPLOY: backup `*.HORTbak.20260613-093052`, py_compile OK on `/opt/capsy-agent/.venv`, new aim_voice_agent.py md5 `61f2e0e642727eacfa54367e683048bc`, restarted famit-caller + aim-voice-agent ONLY (worker re-registered clean `agent_name:manager` AW_hGPByd3DAg74, ZERO ImportError/Traceback; caller alive loopback, 0 5xx). EARNER GATE before+after PASS: agent.py md5 `9150fabe4ff62b4b4470f9a87df346e5` UNCHANGED, famit-agent MainPID 1477083 / ActiveEnter 2026-06-10 NEVER restarted; in-window (15:10 IST, Vobiz funded) real earner `/run` (campaign c17e55e9f3, leads=+917861019021, conc 1) -> `count:1` -> livekit-sip callID `SCL_BYbA8zxAK4Qr` trunk `ST_fmtVmNJmpzKa` to +917861019021 = `Outbound SIP call established` + `accepting RTP stream` + agent↔phone `track subscribed` + ~7s `silence_filler` media = RANG + ANSWERED (a 402/486 never reaches `established`+RTP). Only famit-caller + aim-voice-agent restarted. 🟥 LESSON: a handoff "says yes then nothing rings" has THREE root causes that look identical to the caller — (i) the LLM never fired the tool because its turn 429'd/errored [quota — fixed by the FallbackAdapter], (ii) the tool fired but the DIAL leg 402'd [Vobiz empty — fixed by recharge], and **(iii) the LLM ANNOUNCES the transfer ('kya main aapko transfer karoon?') but never invokes the tool — a PROMPT bug where a small model treats a soft 'you may call the tool' instruction as license to ask-permission/narrate instead of ACT.** Fix (iii) by making the tool instruction IMPERATIVE + same-turn ("call it IMMEDIATELY, do not announce/ask first — talking about it ≠ doing it") in BOTH the system prompt AND the tool docstring, and PROVE it with an integrated turn-loop smoke that drives the actual non-English phrasing through the real chain and asserts `tools_fired` contains `transfer_to_human` on the SAME turn (not a later one).
- 2026-06-13 — 📐 LEAN<->PREMIUM TIER DESIGN (READ-ONLY; design-doc only, NOTHING on the box, agent.py UNTOUCHED). Deepened `design/spec-provider-voice-switcher.md` with a new "## Lean<->Premium tier system + premium features" section: a 3-stop **LEAN / STANDARD / PREMIUM** segmented slider (discrete presets, NOT a free drag — a drag = infinite untested combos + reads as a toy) that maps to a concrete `{stt,llm,tts}` provider+model+voice triple, + an Advanced disclosure that flips to `tier:"custom"`. HARD RATE-CARD researched: Sarvam STT ₹30/hr=**₹0.50/min**, Bulbul v2 ₹15/10K / v3 ₹30/10K; ElevenLabs **Flash v2.5** ~$0.05/1K (~₹4.2/1K, ~75 ms, the model EL itself recommends for live agents — NOT v2/v3 Multilingual at $0.10/1K which have high TTFT + are excluded from EL's Agents platform); Groq gpt-oss-20B (~$0.05-0.15/M, 680-910 t/s) vs Llama-3.3-70B ($0.59/$0.79, 280-394 t/s). Combined ≈ **Lean ₹0.75/min / Standard ₹1.3 / Premium ₹1.6** — KEY HONESTY FINDING: the tier delta (~₹0.85/min) is SMALL vs tier-independent Vobiz telephony, so surface the carrier cost as a footnote to make the premium choice feel cheap. Conversation feel breaks >~800 ms round-trip [Retell/softcery] → every tier engineered inside that budget. ⭐ TOP "impress" features (founder didn't ask): (1) live ₹/min cost meter + projected campaign spend (`₹/min × avg-min × #leads`, pure client math = ZERO burn, the #1 big-company lever); (2) a "Recommended tier" badge from cheap heuristics already on hand (lead count / language / wallet balance / goal); (3) per-campaign budget cap (warn=P1 via wallet, hard auto-pause=P2 in run_job NOT agent.py); (4) A/B Lean-vs-Premium split with answer/booking-rate per arm (P2). RECONCILE: the slider + cost meter + recommender + provider-health (rides `available_count`) + voice favorites + lang-matched voice + tier sample-play (rides the free `preview_url`/Sarvam-sample proxy) are ALL **Phase-1-safe (no agent.py)**; switching voice WITHIN ElevenLabs is already honored by agent.py:485 `fields.voice_id` so Premium voice works TODAY; the ONLY thing needing the gated **OB-PROV** unit is the moment the OUTBOUND *provider* actually flips (Sarvam↔ElevenLabs) via an additive DEFAULT-IDENTICAL `_build_pipeline(fields.tier)`. So ~90% ships without touching the earner. LESSON for the founder's "feels like a big-company product" asks: lead with a single named-preset slider + a LIVE cost/projected-spend meter + a "recommended" nudge + honest "you're saving ₹X vs Premium" — discrete presets (not sliders that produce untested combos), client-side estimates (label "≈", wallet meters the real charge), naming = SaaS good/better/best (use Lean/Standard/Premium, avoid the cheap-sounding "Basic"). Build-plan delta added: B6 `/tiers` rate-card route + B7 campaign `tier`/`budget_cap` persistence + F4 slider/cost-meter card; sources cited in §6.
- 2026-06-13 — 🔎 HUMAN-HANDOFF "SAYS YES THEN SILENCE, HUMAN NEVER RINGS" — DIAGNOSED with hard evidence (READ-ONLY; NOTHING edited on the box, earner UNTOUCHED). The warm-transfer CODE is CORRECT and HAS bridged end-to-end; the two real failure modes are environmental (Vobiz balance + Groq daily quota), now BOTH cleared. EVIDENCE — (A) **DIAL LEG RINGS NOW (Vobiz funded ₹497.49)**: ran the EXACT transfer primitive `lk.sip.create_sip_participant(sip_trunk_id=ST_fmtVmNJmpzKa, sip_call_to=+916375548830, room_name=<test room>)` into a fresh room → livekit-sip `signaling timestamps` line for callID SCL_xi94NuKM2tAQ = `inviteToTryingMs:15, inviteToRingingMs:1302 (180 RANG), inviteToAcceptMs:5047 (200 OK answered), result:success` — the OPPOSITE of the empty-Vobiz 402 era (where inviteToRingingMs was ABSENT). So the same-room direct-dial bridge primitive RINGS + CONNECTS. (B) **TOOL-FIRING — the LLM DID fire `transfer_to_human` in the past, but on the founder's RECENT failing calls it never got the chance**: full journal has 14 `AIM transfer_to_human` lines, ALL ≥ 4 days old (a 3-day/4-day grep finds ZERO). The two historical fires show BOTH outcomes — Jun 12 12:41 (room RM_TP7iQb2toG3R) `transfer_to_human (customer) reason='site visit fixed'` → `dialing human +91***21 (founder)` → **"didn't connect (ToolError) -> next number" -> "no human answered -> callback fallback"** = the founder's "yes then silence then no ring" (tool FIRED, dial 402'd in the empty-Vobiz era, surfaced as ToolError/TwirpError, caught, fell back to WA+callback — so NO dead drop but the human never rang); AND Jun 12 14:55 (room RM_9zbSt7naohPU, caller +918949906361) `transfer_to_human (customer)` → `dialing +91***30 INTO caller room` → **"BRIDGED +91***30 into room"** = the warm transfer WORKED end-to-end when Vobiz briefly had balance. (C) **The OTHER recent silence cause = Groq daily TPD exhaustion (NOT the handoff)**: the founder's Jun 12 18:58 customer call (room RM_d9foRCMnJMK9) shows the FallbackAdapter members BOTH 429ing on Groq TPD (`Used 499483/500000`, `livekit.plugins.groq.services.LLM recovery failed`, traceback in inference/llm.py) → @session.on("error") filler → on THAT call the LLM literally could not produce a turn to fire any tool. ⚠️ NOTE the resilience build (current aim_voice_agent.py md5-fresh Jun 13 08:31, FallbackAdapter[groq PoolLLM -> SambaNova PoolLLM -> OpenRouter free]) restarted 08:33 and has had ZERO inbound calls since, so the founder's failing calls were on OLDER code; the new chain is BUILT but UNPROVEN on a real handoff turn. The handoff analytics JSONL `var/aim_handoff_attempts.jsonl` has only 2 rows, BOTH from a SMOKE test (smoke-handoff-1781286130, #2 +916375548830 = `answered`) — NO real-inbound-call rows ever. CONCLUSION / EXACT FAILURE POINT: (1) the tool DOES fire when the LLM is healthy; (2) the dial leg's failure was **Vobiz 402 (empty balance)** → ToolError → graceful WA+callback fallback (this IS the "says yes then nothing rings"); now FIXED — the dial RINGS + ANSWERS. (3) A SEPARATE silence cause on some calls was Groq TPD 429 starving the LLM turn. PRECISE FIX (mostly already in place / environmental): Vobiz recharged (done, dial rings) + the LLM FallbackAdapter rebuilt (done, needs one real handoff turn to confirm the chain fails over instead of fillering). The only residual code risk: prove the rebuilt LLM chain (groq->samba->OR-free) actually fails OVER on a 429 within a live customer "I want a human" turn so the tool fires even when Groq's daily bucket is dry — that's the integrated-turn-loop smoke the prior INBOUND-DEAD lesson mandates. EARNER GATE before+after PASS: agent.py md5 `9150fabe4ff62b4b4470f9a87df346e5` UNCHANGED, famit-agent MainPID 1477083 / ActiveEnter 2026-06-10 19:58:18 NEVER restarted, 0 5xx; in-window (14:51 IST) real earner outbound to founder +917861019021 RANG + ANSWERED (callID SCL_Wm6hBzNh4CvP: `inviteToRingingMs:1925`, `inviteToAcceptMs:23364`, result:success); NOTHING restarted this pass (pure read-only diagnose + two real test dials via the existing trunk). RULE reinforced: a handoff "says yes then nothing" has TWO root causes that look identical to the caller — (i) the LLM never fired the tool because its turn 429'd/errored (check the journal for the `AIM transfer_to_human` line: ABSENT = tool never fired, quota/error problem), and (ii) the tool fired but the DIAL leg failed (check livekit-sip `signaling timestamps`: `inviteToRingingMs` ABSENT + `402 Payment Required` = Vobiz empty; present = it rang). Always disambiguate with BOTH logs before touching code.
- 2026-06-13 — ✅ API-KEYS PAGE FORTRESS DEPLOY (frontend box ONLY; the super-admin /super-admin/api-keys provider-key manager built by the LLM-provider-pool wave, commit a8e9ca9 on feat/premium-ui — already committed/verified, so this was a DEPLOY-ONLY pass). Verified first: tsc --noEmit EXIT 0 + local `next build` EXIT 0 (route `/super-admin/api-keys` 6.67kB in the table), gitleaks on a8e9ca9~1..a8e9ca9 = no leaks, 0 raw `gsk_`/`sk-or-` strings in the committed page+api.ts. All imports resolve (Card/Badge/Button/Switch/Field/Modal/Spinner are component DIRS w/ index.tsx; icons info/lock/plus/trash all in the Icon registry; _shared exports SuperAdminGuard/SuperAdminHeaderF3/ErrorBanner/ToastView/Toast/ghostBtnCls). DEPLOY via the proven tarball recipe to root@143.110.247.249 (famit-panel-2): `git archive feat/premium-ui:famit-panel` (22.7MB, scanned: 0 .env/provider_keys/z_groq/z_samba, 0 gsk_) → scp + md5 match `6c6b5dfa121e20a0a128ed6b8cee8d67` → backup `cp -a /opt/famit-panel /opt/famit-panel.LPRUIbak.1781341016` (4.3G) → extract-over (preserves gitignored node_modules/.next/.env.local) + chown deployuser. 🟥 NEW GOTCHA — DISK FULL ON FORTRESS: the box had accumulated **17 old panel backups (~33G)** so the 4.3G fresh backup pushed `/` to 100% and the 4G `fallocate /swapfile.build` FAILED `No space left on device` (left a 2.8G partial). FIX = remove the partial swapfile + prune the OLD redundant backups (`rm -rf` the CLbak/CUIbak/WATRUTHbak + all `*.bak.2026061[01]*` + creativeimgbak) keeping only the fresh LPRUIbak + the prior HUIbak → freed 28G (100%→42%). THEN the build swap worked. LESSON: before a FORTRESS deploy, `du -sh /opt/famit-panel.*bak.*` and prune to the 1-2 newest — each backup is ~4.3G and they silently fill the 48G disk; a failed build-swap fallocate = disk-full, not a swap bug. Build as deployuser: `npm install --legacy-peer-deps` (EXIT 0) + `NODE_OPTIONS=--max-old-space-size=3072 npm run build` (EXIT 0, ✓ Compiled successfully, ✓ 53/53 pages, api-keys built at .next/server/app/super-admin/api-keys) — heavy swap (peaked ~2.5G of the temp 4G) but no OOM (the dmesg OOM-kills were DAYS old). Restart famit-panel ONLY (PID 188199→225213, active). VERIFY: OLD BUILD_ID XyeGd-iyCjs-emBYS8-q8 → NEW WwBfbgcnCuH-Rzi9--YvE; loopback :3001 AND public https://panel.famit.in both 200 on /super-admin/api-keys + /super-admin + /login + / AND both serve the NEW BUILD_ID (no stale .next). TEARDOWN: swapoff+rm /swapfile.build, swappiness→10, rm tarball, confirmed only the permanent 2G /swapfile left + not in fstab + disk 42%. EARNER SAFETY: this box has NO /opt/famit-agent (NO_AGENT_DIR) + no famit-caller/famit-agent/aim-voice units (frontend-only) → a deploy here CANNOT touch the voice earner; only famit-panel restarted. The add-key UX = per-provider Card (Groq/SambaNova/Sarvam/OpenRouter), "Add key" → Modal with type=password masked input (never pre-filled) + optional label → POST /admin/provider-keys → optimistic prepend + rollback + toast "Key added — it's in the live rotation now"; each row = masked value + live status dot (green=in rotation / amber=Cooling Ns / grey=Disabled) polled 5s from /status + enable Switch + delete-with-confirm; an "info" banner states "Live — reaches the running voice agent within seconds. No redeploy." Build log: memory/build_log/wave-build-control-layer.md is unrelated; this is the LPR wave (state caps/LLM_PROVIDER_POOL_STATE.md).
- 2026-06-13 — 🟥🟥 "FOUNDER GETS NO CALL" INCIDENT — **REAL ROOT CAUSE = VOBIZ ACCOUNT OUT OF MONEY → every outbound INVITE gets `402 Payment Required` and the phone NEVER RINGS.** This CORRECTS the "call-window" entry below AND exposes the FALSE-POSITIVE in all prior "earner gate rang" reports. HARD SIP EVIDENCE (READ-ONLY; the SIP signaler is the `livekit-sip` Docker container, logs via `sudo docker logs livekit-sip`; agent.py md5 `9150fabe4ff62b4b4470f9a87df346e5` UNCHANGED, famit-agent PID 1477083 / ActiveEnter 2026-06-10 NEVER restarted — nothing edited): TODAY in-window (13:22–13:29 IST / 07:52–07:59 UTC) EVERY real outbound to the founder's `+917861019021` AND to all other leads (+916375548830 kunal, +917987388671 colin, +918839352959 nikhil, +918949906361) returned the IDENTICAL exchange — `INVITE → 100 Trying (inviteToTryingMs ~13ms) → 402 Payment Required → invite-failed`, with **`inviteToRingingMs` ABSENT (NO 180 Ringing, NO 200 OK)**. LiveKit-SIP terms: `result:"server_error", reason:"invite-failed", error:"unexpected status from INVITE response: sip status: 402: Payment Required"` on trunk `ST_fmtVmNJmpzKa`, toHost `2c24f731.sip.vobiz.ai`, fromUser (caller-ID) `+918071583488`. 24h histogram of OUTBOUND INVITE outcomes: **84× `sip status: 402` (today), 51× reached `inviteToRingingMs` (yesterday), 80× `486`**. SMOKING-GUN BALANCE: queried Vobiz live API via the in-repo adapter (`vendors/vobiz.py get_balance()`, BASE `https://api.vobiz.ai/api/v1`, X-Auth-ID `MA_0UX9IR0K`) → **`{"status":"configured","balance":0.19,"currency":"INR"}` = ₹0.19, account effectively EMPTY.** TIME BOUNDARY PROVES IT WENT DRY OVERNIGHT: yesterday 2026-06-12 13:46–15:02 IST the SAME number/trunk reached `inviteToRingingMs: 1042–2287` then `reason:busy` (status 9/486 = phone PHYSICALLY RANG, founder busy/declined AFTER ringing) — so the DID, trunk registration, caller-ID and route are ALL VALID; the ONLY change is the balance hit zero. **NOT carrier/spam-block** (a spam-filter still 180-Rings-then-fails or 200s-then-silent; a hard 402 before Trying completes is a TRUNK/BILLING reject from Vobiz, not the recipient carrier — and the SAME DID rang 18h earlier). **NOT shared-Groq/Sarvam-quota**: the famit-agent OUTBOUND worker dispatch is HEALTHY — `received job request → agent job connected → Sarvam STT WebSocket connected successfully`, ZERO 429/rate_limit on it; the agent joins the room FINE, it's the SIP leg that 402s. **NOT the call-window** (all IN-window real calls). **NOT suppression** (founder not on var/suppression.json — only 2 admin test numbers + 1 other-tenant number). 🟥 THE FALSE-POSITIVE EXPLAINED (why past reports wrongly said "rang"): LiveKit dispatches the AGENT into the room FIRST, then creates the SIP participant — so the famit-agent log shows `agent job connected room=famit-…917861019021…` and a `participant: phone-+917861019021` line EVEN WHEN the SIP INVITE is about to 402. A reviewer reading ONLY the agent log (or mapping LiveKit `USER_REJECTED`/`no_answer`/"participant joined" to "rang") declares "the phone rang" — but the SIP signaler shows the INVITE was REJECTED (402/486) and `inviteToRingingMs` was NEVER emitted. **RULE: "rang" REQUIRES `inviteToRingingMs` present (180 Ringing) or `inviteToAcceptMs`/status 200 in the `livekit-sip` `signaling timestamps` line. A `USER_REJECTED`/`no_answer`/"participant phone-… joined" in the AGENT log is NOT proof of a ring — always cross-check the SIP container log for the actual INVITE response code. An immediate 402/4xx with no `inviteToRingingMs` = the phone NEVER rang.** FIX (founder action, NO code change — the earner is healthy, the dialer works): **RECHARGE the Vobiz account** (`MA_0UX9IR0K`, balance ₹0.19 → top up) at the Vobiz portal/billing; the instant the balance is positive, in-window campaigns ring again (proven by yesterday's rings on the identical trunk/DID). Optional hardening (do NOT auto-apply): surface Vobiz `get_balance()` on the panel/health so near-zero balance ALERTS before the earner silently dies, and map a 402-INVITE to a distinct "carrier_billing_block" outcome (not `no_answer`) so it's never again misread as a ring. PROBE RECIPE next time: `sudo docker logs livekit-sip --since 6h | grep -E 'outbound.*(Payment Required|inviteToRingingMs|sip status)'` for the real codes + `cd /opt/famit-agent && sudo bash -c 'set -a; . ./.env; /opt/capsy-agent/.venv/bin/python -c "from vendors import vobiz; print(vobiz.get_balance())"'` for live balance.
- 2026-06-13 — ⚠️ SUPERSEDED-IN-PART by the 402 entry above — "RUN-CAMPAIGN DOESN'T RING" — ROOT CAUSE claimed = the CALL-WINDOW compliance gate (09:00–21:00 IST). The window logic is real, but the "GROUND-TRUTH PROOF the dial path is HEALTHY … participant phone-+917861019021 joined (SIP connected = the phone RANG)" was a **FALSE POSITIVE** — that participant line is emitted by LiveKit BEFORE the SIP INVITE result; the actual INVITE 402'd (Vobiz balance ₹0.19) so no 180 Ringing ever fired. The window is a SECONDARY gate; the PRIMARY blocker today is the empty Vobiz balance. Original note follows. Founder symptom: adds leads → clicks Run → UI shows "campaign running / calling lead" → no phone rings in real life. RECONCILE (the workflow llm-provider-resilience that was mid-editing caller.py /run): caller.py compiles clean on the venv (`/opt/capsy-agent/.venv/bin/python -m py_compile caller.py` = OK); the ONLY diff vs the last backup `caller.py.HOTLbak.20260612-170656` is **122 lines of ADDITIVE handoff-CRUD** (`_handoff_valid_phone`/`_handoff_add_one`/`_handoff_remove_one` + `POST /brain/handoff/add` + `DELETE /brain/handoff/remove`) — it did NOT touch run_job or the dial path; famit-caller is `active (running)`, ZERO tracebacks, 0 5xx. So NO restore was needed (current caller.py is the known-good). ROOT CAUSE: every one of the founder's 8 real campaigns uses the default window `call_window_start=09:00 / call_window_end=21:00`; `_in_window` (caller.py:733, `s <= now_ist().strftime("%H:%M") <= e`) is RE-CHECKED inside `run_job` (caller.py:2434, the loop at ~2468) — out-of-window it sets `job["paused_reason"]="out_of_window"` and `asyncio.sleep(60)` WITHOUT dialing. `/run` (caller.py:3803) returns 200 in-window (or when `force` is set) but the JOB still idles if run_job's re-check is out-of-window → the UI shows "calling" (job created, status=queued/calling) while NO SIP participant is created → no ring. The prior session's 03:30/05:31/07:38 IST rings only happened because they used throwaway 24x7 campaigns (the documented earner-gate trick), masking the window as the everyday blocker. GROUND-TRUTH PROOF the dial path is HEALTHY: at 13:19 IST (IN window) I fired the LIVE `/run` (real campaign `c17e55e9f3`, ad-hoc lead `+917861019021`, concurrency 1) → 200 `{job_id:3db5ffb876,count:1,suppressed_count:0}` → ~10s later famit-agent log: room `famit-917861019021-b409fa`, `agent job connected`, **`participant: "phone-+917861019021"` joined (SIP connected = the phone RANG)** then `CLIENT_INITIATED` disconnect → `outcome=no_answer` (founder declined). So the /run→dispatch→`create_sip_participant` path works end-to-end in-window with a NORMAL campaign. THE LEVER (override to test off-hours, do NOT change a global env — the window is per-campaign): set the campaign's `call_window_start=00:00` + `call_window_end=23:59` (the only safe TRAI-aware override; there is NO global window-disable env — `force` only flips the dispatch-time 202, run_job STILL re-checks and idles). Founder's number is NOT suppressed (var/suppression.json holds only +910000000066/68 + a tenant test number) and not opted-out. EARNER GATE before+after PASS: agent.py md5 `9150fabe4ff62b4b4470f9a87df346e5` UNCHANGED, famit-agent `active` MainPID 1477083 / ActiveEnter 2026-06-10 19:58:18 NEVER restarted, 0 5xx in the last 30 min, only nothing-restarted (read-only diagnose + one in-window /run via the existing service). LESSON: "UI says calling but no phone rings" with a healthy box = check the CURRENT IST time vs the campaign window FIRST — the dispatch 200 and the status "calling" are both emitted regardless of the window; the suppression is silent inside run_job's loop, only visible as `paused_reason:out_of_window` on the job object (not surfaced to the UI). Prove the dialer with an IN-WINDOW real /run, not an out-of-hours `force` (which does nothing). Founder recipe: run any campaign between 09:00–21:00 IST and it dials; to test at night, edit that campaign's calling window to 00:00–23:59.
- 2026-06-13 — ✅ PANEL "BROWSER BASIC-AUTH POPUP" — **ROOT CAUSE FOUND** (corrects the entry below): it is NOT Cloudflare, NOT nginx, NOT a saved credential — it's the BACKEND `caller.py:592 need_auth()` returning `401` with `Www-Authenticate: Basic realm="Famit"` on every protected `/api/*` route; the panel's first-paint `GET /api/me` hits it with no session → browser pops the native login box (`/`,`/login`,`/super-admin` are clean 200). The prior "no header anywhere" was a FALSE NEGATIVE from probing with `curl -I` (HEAD → 405, header never shown) and only grepping nginx; the header appears ONLY on real **GET** 401s and is set by the FastAPI app, not nginx/CF. Edge proof (this machine): `GET /api/me|/api/leads|/api/billing|/api/forms|/api/funnels|/api/workflows` all → `401 + Www-Authenticate: Basic realm="Famit"`. FIX = ONE line in caller.py (drop the header from `need_auth()`; keep status 401 so the SPA redirects to /login + data stays gated) — backend-box change, so I did NOT apply it (voice box off-limits); wrote click-by-click `caps/FOUNDER-REMOVE-LOGIN-POPUP.md`. CF API tokens unusable: token #1 (`cfut_`) = zone-list-only (denied on Workers/Rulesets/PageRules/Access), token #2 (`cfat_`) = INVALID. LESSON: a native Basic popup ALWAYS means a real `WWW-Authenticate: Basic` on a 401 SOMEWHERE in the stack — when you "can't find it", you probed the wrong METHOD (HEAD vs GET) or the wrong PATH (app route vs the `/api/*` the SPA actually fetches); use `curl -s -D - -o /dev/null -X GET <each /api route>` before concluding "nothing to remove".
- 2026-06-13 — 🔎 PANEL "BROWSER BASIC-AUTH POPUP" — DIAGNOSED, **NO SERVER GATE EXISTS TO REMOVE** (READ-ONLY on FORTRESS box `143.110.247.249`; nginx NOT edited, NOT reloaded — there was nothing to change). The founder reported a native browser username/password popup before `panel.famit.in`'s own /login. EXHAUSTIVE search proved it is NOT server-side: (1) nginx `nginx -T` (full compiled config) + `grep -rniE 'auth_basic|htpasswd|www-authenticate|satisfy' /etc/nginx/` = **ZERO matches**; only `panel.famit.in` server blocks (:80/:443) proxying to Next.js 127.0.0.1:3001 + /api→10.122.0.4:8209 + /api/assets→:8310; no `.htpasswd` referenced anywhere (the only `.htpasswd` on the box are fail2ban Python test fixtures, unused). (2) Cloudflare Access is OFF for this host — `/cdn-cgi/access/login` → **404** (would exist/redirect if Access were on). (3) Next.js app has NO basic-auth middleware (only build-manifest middleware files; app origin returns 200). (4) Every request path through the CF edge returns **200 OK** (root, /super-admin, browser-UA + sec-fetch nav) or a **JSON 401 with NO `WWW-Authenticate` header** (/api/me, /api/leads, /api/billing, /api/campaigns) — a JSON 401 without `WWW-Authenticate: Basic` does NOT trigger a browser popup. CONCLUSION: there is no `WWW-Authenticate: Basic` anywhere in the stack, so nothing to comment-out/delete. The backup `panel.famit.in.bak.20260610-214347` (created 2026-06-10) hints Basic Auth likely existed BEFORE and was already removed on the 10th — the founder's browser is almost certainly **replaying a saved/cached Basic credential** for `panel.famit.in` (client-side artifact; fix = clear saved site logins / hard-reload in that browser, not a server change). SAFETY VERIFIED: app-level auth still gates DATA — protected `/api/*` return 401 without an app session (no leak); the live config was left byte-identical (no edit, no `nginx -t`, no reload). LESSON: a native Basic popup with NO `WWW-Authenticate: Basic` on ANY server response = it's CLIENT-cached creds or a different/old URL, not the current origin — verify with `curl -sI` for the header on every path BEFORE touching a live nginx; don't "remove" a directive that isn't there.
- 2026-06-13 — ✅ INBOUND-DEAD INCIDENT **VERIFIED WORKING (integrated turn-loop, not isolated)** + the LESSON that should have caught it. After the FallbackAdapter capacity fix (entry below), I drove the REAL `_aim_llm` (FallbackAdapter[groq→OpenRouter-free]) with the REAL ManagerAgent tool schemas (all 15 tools) inside an `http_context`, exactly as a live inbound turn does, **while Groq's daily bucket was STILL 429ing** — so the run actually EXERCISED the OpenRouter failover (logs: `livekit.plugins.groq.services.LLM failed, switching to next LLM`). RESULT (4 manager turns, NONE hit the "thoda sa system slow" filler): T1 "Hello"→"Namaste! How can I assist you today?" (a completion SUCCEEDED with tools loaded — no 400/schema-reject); T2 "Mera PIN hai 4827"→`verify_pin({pin:'4827'})`→`verified=true`, `agent._verified=True`; T3 "Kitne hot leads hain?"→`check_leads`→**REAL DATA** "You have 7 leads total — 5 hot, 1 warm, 1 cold"→spoke "Aapke paas 5 hot leads hain"; T4 "campaigns list karo"→`list_campaigns`→**REAL 8 campaigns**. CUSTOMER path (CustomerSalesAgent shares the SAME `_aim_llm`, 5 tools) also drove clean: C1 open question; C2 "3 BHK price?"→`lookup` RAG tool→**REAL price** "lagbhag ₹1.32 crore se shuru…". So BOTH inbound paths answer reactively (no silence/filler). Harnesses on box: `/tmp/aim_turnloop_smoke.py` + `/tmp/aim_customer_smoke.py` (run with `set -a; . ./.env`; plugins need the `http_context` wrapper). EARNER GATE before+after PASS: agent.py md5 `9150fabe4ff62b4b4470f9a87df346e5` UNCHANGED, famit-agent active PID 1477083 / ActiveEnter 2026-06-10 NEVER restarted; **a FRESH out-of-window outbound RANG** — `/run` against +917861019021 via a throwaway 00:00-23:59 campaign materialised SIP room `famit-917861019021-3f2db7` with `participant: phone-+917861019021` (= rang; founder didn't pick up → no_answer), same PID 1477083; throwaway campaign deleted, founder NOT suppressed; caller core 401-alive, 0 5xx; only famit-caller+aim-voice-agent restarted. 🟥 **THE LESSON (the real failure of this incident): isolated backend tests passed on EVERY handoff wave (each wave's earner-gate + loopback CRUD were green) yet the INTEGRATED inbound voice turn-loop was DEAD — because nobody drove a real LLM turn THROUGH `_aim_llm` with the tools loaded after stacking 4 voice waves in one day onto the live voice heart. The quota wall was invisible to component tests. RULE: every wave touching the voice path MUST run an INTEGRATED turn-loop smoke (build `_aim_llm` exactly as `entrypoint()` does → attach `agent.tools` → drive greeting+PIN+a real-data query inside `http_context` → assert NO filler + REAL data) BEFORE declaring done; and NEVER stack multiple box-mutating voice waves back-to-back without that integrated smoke between them. A passing isolated test ≠ a working call.**
- 2026-06-12 — ✅ INBOUND-DEAD INCIDENT **FIXED** (the surgical-capacity fix for the Groq-TPD-exhaustion root cause diagnosed below). The diagnosis was right: reverting code is useless against a depleted daily token bucket, so the fix = give AIM an INDEPENDENT LLM quota pool, NOT a revert. WHAT I CHANGED (only `/opt/famit-agent/aim_voice_agent.py`, backup `*.EMERGbak.20260612-182751`): wrapped the AIM LLM in a `llm.FallbackAdapter([groq_member, openrouter_free_member])` — Groq stays PRIMARY (fast/cheap; auto-recovers the instant its 500k/day TPD bucket refills), and on 429/connection-error it auto-fails-over to a **FREE** OpenRouter model (`openai/gpt-oss-120b:free`, tool-capable, SEPARATE daily pool, $0 — the `OPENROUTER_API_KEY` was already in `.env`). EDIT 1 = import-guarded `from livekit.plugins import openai as _openai` + `from livekit.agents import llm as _lk_llm` (both present in /opt/capsy-agent/.venv). EDIT 2 = replaced the bare `_aim_llm = groq.LLM(...)` block with `_mk_groq_llm()` + `_mk_openrouter_llm()` (uses `openai.LLM.with_openrouter(...)`) assembled into the FallbackAdapter; `_strict_tool_schema=False` forced on EVERY member + the adapter (so neither path schema-rejects). Env knobs: `AIM_LLM_FALLBACK=1` (default on), `AIM_FALLBACK_OR_MODEL`. Degrades GRACEFULLY to pure-Groq if the OR key/plugin/adapter is ever absent (no behaviour change). NOT changed: tools, manager prompt, the warm-transfer bridge, caller.py, the earner, trunks, firewall, SIP. PROOF: py_compile OK on the venv; worker re-registered CLEAN (`agent_name:"manager"` HTTP on :8091, MainPID 2048501, openai plugin now in the registered-plugins list, ZERO startup Traceback/ImportError — the only exit-255 lines are the OLD worker draining on restart); end-to-end FallbackAdapter[groq→OR] turn ran THROUGH the LiveKit plugins and returned a real Hinglish completion ("Haan, main sun raha hoon!") WHILE Groq's daily bucket was still 429ing → the turn loop no longer fillers. EARNER GATE (after) PASS: agent.py md5 `9150fabe4ff62b4b4470f9a87df346e5` UNCHANGED, famit-agent active PID 1477083 / ActiveEnter 2026-06-10 19:58:18 NEVER restarted, core 401-alive, 0 5xx; only famit-caller + aim-voice-agent restarted; the earner provably rang today (room `famit-917861019021-43fdd1`, Riya opener spoken, Sarvam STT connected, vobiz SIP trunks healthy). LESSON: when a provider's per-DAY quota is the wall, the right "restore" is a `FallbackAdapter` to an INDEPENDENT-pool LLM (a FREE OpenRouter model = zero real-money burn, separate daily bucket), keeping the fast primary first so you auto-revert when it heals — this is strictly better than a revert (which does nothing) AND better than key-rotation within the same org (same TPD pool). The LiveKit `openai` plugin's `LLM.with_openrouter(...)` + `llm.FallbackAdapter` make this a ~2-edit additive change. ⚠️ STILL OPEN (founder action, not blocking): the earner's agent.py shares the same Groq org TPD pool, so heavy AIM test-burn can still starve the live earner's LLM on a busy outbound day — give the EARNER its own fallback too (or a second Groq org), and gate AIM test volume. GOTCHA: out-of-window (00:00 IST) `/run force=1` returns 200/count:1 but may not materialise a fresh SIP room (run_job re-checks the window); earner-safety is proven by the byte-identical agent.py + never-restarted famit-agent + today's real 17:55 ring, not by a forced out-of-hours redial.
- 2026-06-12 — 🟥 INBOUND-DEAD INCIDENT ROOT CAUSE = GROQ DAILY-TOKEN (TPD) EXHAUSTION, **NOT a code regression** (READ-ONLY diagnose; nothing edited on the box; earner UNTOUCHED). SYMPTOM: AIM greets fine then repeats the filler "Ek second, thoda sa system slow hua…" on EVERY turn; inbound CUSTOMER also dead. The prime suspect (the handoff-pertenant-backend wave's 3 new @function_tools breaking the Groq schema → 400 storm) was FALSIFIED by the live logs. EVIDENCE: `journalctl -u aim-voice-agent` last 3000 lines histogram = **496 rate_limit_exceeded / 248 status_code=429** vs only 6 stray "400" (all inside unrelated STT-INFO strings — ZERO tool_use_failed / json_validate_failed / BadRequestError / schema-reject). The actual error every turn: `APIStatusError: Error code: 429 … 'Rate limit reached … service tier on_demand on tokens per day (TPD): Limit 500000, Used ~498000, Requested 7216. Please try again in 14m12s'` → `APIConnectionError: failed to generate LLM completion after 2 attempts` → caught by `@session.on("error")` → `_speak_recovery()` (aim_voice_agent.py:2095) speaks the filler. GREETING survives because it's a static `session.say()` (no LLM); the FIRST user turn needs an LLM completion → 429 → filler, repeat. The new handoff tools are CORRECT — `add_handoff/remove_handoff/list_handoff` are all loose `str=""` optional params, and `_aim_llm._strict_tool_schema=False` (line 2034) + `max_retry=1` are set; BackgroundAudioPlayer init throws NOTHING in the log. SMOKING GUN that key-rotation can't save us: all 6 Groq keys (GROQ_API_KEY + _2.._6 in /opt/famit-agent/.env, round-robined by `_next_groq_key`/`_GROQ_CYCLE`) return the IDENTICAL `x-ratelimit-remaining-tokens` → **same Groq ORG → ONE shared 500k/day TPD pool**; rotating keys gives ZERO extra daily headroom. Both ManagerAgent AND CustomerSalesAgent share the SAME `_aim_llm` (line 2061 `llm=_aim_llm`) → both inbound paths die identically (explains the dead customer call). At diagnose time it was STILL live (37×429 in the last 20min); a 1-token probe 200s but a real ~7k-token AIM turn re-429s. CAUSE of exhaustion = a day of heavy AIM voice-wave testing (each turn = big manager prompt + ~15 tools + recording = token-expensive) burned the org's 500k/day. ⚠️ EARNER RISK: agent.py (outbound earner) shares the SAME key pool + SAME org TPD pool (`_collect_groq_keys` GROQ_API_KEY.._20) — so AIM test-burn can STARVE the live earner's LLM too; today the earner gate still PASSED (agent.py md5 9150fabe…UNCHANGED, famit-agent PID 1477083 never restarted, core 401-alive) only because outbound was idle. **FIX (no revert — code is healthy): restore Groq daily headroom** — easiest: upgrade the Groq org to Dev Tier (raises TPD) OR add a SECOND independent Groq org's keys to the rotation OR add a non-Groq fallback LLM for AIM; and gate AIM test volume so it can't drain the earner's daily budget. LESSON: "greets then filler every turn" has TWO failure modes that look identical — (A) a strict/typed tool schema → 400 storm [the old latency bug], and (B) **provider RATE-LIMIT/quota (429, esp. per-DAY TPD) exhaustion**. ALWAYS read the live journal error CODE first: 400/tool_use_failed = code bug → fix/revert the tool; 429/rate_limit_exceeded = quota → add capacity, do NOT revert (reverting code does nothing for a depleted token bucket). And multiple API keys in the SAME org share per-day limits — rotation only helps per-minute (TPM/RPM), never per-day (TPD), and can silently cannibalize the live earner. Probe to disambiguate: per-key `curl …/chat/completions -D -` and compare `x-ratelimit-remaining-tokens` across keys — identical = same org = shared pool.
- 2026-06-12 — HOUI HANDOFF-TEAM FRONTEND (panel only, branch feat/premium-ui; FRONTEND ONLY — NO backend/agent.py touched). Built the missing per-tenant handoff-team UI on top of the LIVE /brain/handoff* contract. (1) lib/api.ts: `HandoffMember` type + `getHandoffTeam()` (dormant-safe — 404/network → empty `{team:[]}`, tolerates a bare array OR {handoff:[]}/{team:[]}, sorts by priority, default-true `enabled`) / `addHandoffMember()` (FormData POST /brain/handoff/add, omit priority → backend auto-appends max+1) / `removeHandoffMember(phone)` (DELETE ?phone=) / `saveHandoffOrder(list)` (PUT JSON {handoff:[…]} re-numbered 1-based from array position — the ONE call behind reorder + enable-toggle + bulk save) + a `HandoffError` class with `explainHandoffError()` mapping {error} bodies to plain English (invalid-phone 400 → "must start with +91", 403 → permission). (2) app/ai-manager/_handoff.tsx = ONE reusable `<HandoffTeam compact?>` manager (list / add-modal / reorder up-down via chevron rotated / enable Switch / delete trash) — optimistic PUT with roll-back-on-failure, client-side +91 normalize (bare 10-digit → +91, 0-prefix, 91-prefix) as instant feedback (backend still the real validator), read-only roles (canWrite=false) see list but NO edit controls, empty → calm state-block "No handoff team yet". Reused in BOTH the dedicated view AND the Run page (same component+calls). (3) app/ai-manager/handoff/page.tsx (dedicated view, Layout + explainer) + app/ai-manager/live/page.tsx (Live Calls monitor: polls GET /ai-manager/live every 3s, renders the handoff progression "Dialing #1 → #2 → Bridged"/"Failed → WhatsApp+callback" as a token-toned stepper; parses the attempt index OUT of the backend's "Dialing #N" label via handoffAttemptNo() + handoffPhase() in _lib.ts — zero backend schema change needed; tolerates rooms/active/calls field naming + an optional handoff_attempts trail). (4) nav: added "Live Calls" + "Handoff Team" children UNKEYED (no feature_key) under the AI Manager group so they're never entitlement-hidden (unknown keys would resolve permissive anyway) — still manager-gated by the group's roles:"manager". (5) Run page: added `<HandoffTeam compact />` as card #7 in the left rail before launch. GOTCHA — Icon is a ~70-key REGISTRY (grepped first): NO `phone` icon (used `mobile`), NO up/down arrow (used `chevron` + `rotate-180` for the reorder up button), NO `whatsapp` (used `chat`); available: profile/mobile/chat/clock/trash/plus/chevron/arrow/check/check-circle-fill/block/info. GOTCHA — AI Manager is collapsed to ONE tabbed page; the prompt's `app/ai-manager/live` + a dedicated handoff view don't exist → created as their OWN routes under the group (both render `ƒ` dynamic since they read localStorage). GOTCHA — `npm run build` on Windows compiles + generates all pages then exits 1 with `uncaughtException [Error: kill EPERM] errno -4048` — this is a WINDOWS-ONLY Next worker-teardown quirk AFTER "✓ Compiled successfully" + "✓ Generating static pages (52/52)"; the build output is complete (verified .next/server/app/ai-manager/{handoff,live} dirs + fresh BUILD_ID + route table shows both routes). To get a clean view: `npx next build | grep -E "Compiled|handoff|/live|error"`. tsc --noEmit = EXIT 0 clean. Zero raw hex (Signal tokens) in both new files. Cross-dir import `@/app/ai-manager/_handoff` from app/run resolves (tsconfig paths). Files: lib/api.ts (+HandoffMember/4 methods/HandoffError), app/ai-manager/_lib.ts (+AimLive types/getAimLive/aimLiveCalls/handoffPhase/handoffAttemptNo), app/ai-manager/_handoff.tsx (NEW), app/ai-manager/handoff/page.tsx (NEW), app/ai-manager/live/page.tsx (NEW), contstants/navigation.tsx, app/run/page.tsx. State: famit-panel/HANDOFF_UI_STATE.md.
- 2026-06-12 — HOTL/HOFX VERIFY (honest independent re-verification of the per-tenant handoff backend, live box :8209 over real HTTP). ALL 6 ITEMS PASS. (1) PER-TENANT CRUD ✅ — minted hmac token for REAL tenant 21d0a13603da (axcrio) via `caller._make_token`; B started `[]`, added +918888000022 (saw ONLY its own), and a body-injected `tenant_id:"admin"`/`org_id:"admin"` was STRIPPED (RT-5) so it did NOT land on the admin list; admin list STAYED exactly the 2 founder numbers (no B leak); no-token + garbage-token → 401; B removed its number → `removed:true`→`[]`; idempotent re-remove → `removed:false`; invalid "12345" AND a valid-E.164-but-non-+91 "+14155550123" both → HTTP 400 (the +91/13-char guard rejects foreign mobiles, not just malformed). Code confirms: routes call `resolve_tenant(request)` (token-derived, never body) + `can(t,"write")` gate; voice_tools `add_handoff/remove_handoff` mutate IN-PROCESS via `brain.upsert_profile(tenant_id,…)` (NOT the loopback, which carries the admin cred) keyed by the token-derived tenant_id; the 3 `@function_tool` (list/add/remove_handoff) are PIN-gated on `self._verified` + scoped to `self._tenant_id`. (2) HOLD ✅ + NO REGRESSION — bridge is `create_sip_participant(room_name=<CALLER room>)` trunk ST_fmtVmNJmpzKa (file comments literally say "No side room, no secondary briefing agent"); the OLD WarmTransferTask side-room+secondary-agent path is GONE; hold music played BY US into the caller room, stopped the instant the human answers (finally-guarded). (3) WHISPER ✅ — `_transfer_whisper` spoken in-room on answer then AI steps back. (4) FALLBACK/GATING ✅ — `_within_hours` (24x7/window/midnight-wrap, fail-open) + enabled-flag gate skip in priority order; invalid→skip; all-ineligible→spoken apology + hot-lead WA + logged callback; analytics JSONL already holds a real fallback run (#1 +910000000000 busy@2.05s → #2 +916375548830 answered@6.31s). (5) LIVE ✅ — GET /ai-manager/live (on the caller :8209) returns `handoff:"Bridged"`,`handoff_target:"+916375548830"`; per-attempt it shows `Dialing #N`. (6) EARNER GATE (AFTER) ✅ — out-of-window (23:24 IST) so used the throwaway 00:00-23:59 campaign trick; `/run`→ founder room `famit-917861019021-43fdd1`, famit-agent log shows `participant: "phone-+917861019021"` joined (SIP connected = RANG) then `outcome=no_answer`; agent.py md5 `9150fabe4ff62b4b4470f9a87df346e5` UNCHANGED, famit-agent MainPID 1477083 + ActiveEnter 2026-06-10 19:58:18 NEVER restarted, core 200, 0 5xx; only famit-caller+aim-voice-agent ever restarted this wave. CLEANUP: temp campaign 42777c2c64 deleted, founder NOT suppressed, admin seed intact (+916375548830 p1, +917861019021 p2, both enabled). GOTCHA confirmed: famit-caller listens on **:8209** (NOT 8000/8090 — 8090 is famit-agent, 8091 aim-voice); `/campaigns` POST needs a `fields_json` FORM field whose JSON uses `company_name`/`product_name` (not company/product) or it 400s. RESIDUAL: full end-to-end warm bridge with TWO real humans + per-participant private whisper (shared SIP room → whisper is in-room as the human joins, not a true private leg) is proven by parts, not one live two-human inbound call; `hot_lead_alert` Meta template still unapproved (cold team alert 404s gracefully; the approved `post_call_followup` sends a real wamid). No frontend this wave (panel handoff-list UI is a separate wave).
- 2026-06-12 — HOFX-UX: handoff VOICE UX layered on top of the WORKING same-room bridge (aim_voice_agent.py `_do_warm_transfer` + voice_tools.handoff_list ONLY; bridge `create_sip_participant(room_name=<caller room>)` + trunk ST_fmtVmNJmpzKa UNCHANGED — NO side-room hold-music regression). ADDED 5 layers: (1) HOLD/REASSURANCE — speak a calm line immediately + play `BuiltinAudioClip.HOLD_MUSIC` to the CALLER, in the CALLER's room, via `BackgroundAudioPlayer(thinking_sound=None)` + `player.start(room=room_obj, agent_session=session)` + `player.play(AudioConfig(HOLD_MUSIC, volume=.5), loop=True)`. KEY: hold music is a LOCAL ogg (no external API/http-context needed) so it works even when TTS is down; the dial runs in `asyncio.create_task(... wait_until_answered=True)` + `await asyncio.wait_for(...)` so the caller hears hold WHILE the human rings (no dead air); `handle.stop()`+`player.aclose()` the INSTANT a human answers, finally-guarded so it never leaks. (2) GATING — skip `enabled:false` + out-of-hours numbers in priority order; `_within_hours(hours)` parses "24x7"/""→always, "HH:MM-HH:MM" IST window (handles midnight wrap), FAIL-OPEN on garbage (never block a reachable human on a format quirk); `_now_ist_hm` = utc+5:30, no tz db. handoff_list now emits `enabled` (default-True so seeded entries stay dialable). (3) WHISPER — on answer, `_transfer_whisper(reason,name,phone,summary)` spoken IN-ROOM as the human joins (per-participant private audio isn't available in a shared SIP room), then AI steps back (return string tells the LLM it ALREADY spoke context → stop talking). (4) LIVE — `live_registry.set_handoff(room, f"Dialing #{i}", target=num)`; encoding the attempt index INTO the handoff state string surfaces "Dialing #1/#2" on GET /ai-manager/live with ZERO schema change (list_active passes `handoff` through verbatim). (5) ANALYTICS — `_handoff_log_attempt` appends each attempt {tenant,room,number,attempt,outcome(answered/busy/no_answer/invalid/out_of_hours/error),wait_s,reason,ts} to a durable JSONL `var/aim_handoff_attempts.jsonl` (mirrors live_registry._BASE path convention). SMOKE (real LiveKit job, founder phone dialed in AS THE CALLER): hold-audio PROVEN (local published tracks 0→2 = hold track live, founder heard ~8s, stop clean); FALLBACK+LIVE PROVEN (LIVE showed `Dialing #1 +910000000000`→busy 486 → `Dialing #2 +916375548830`→`Bridged`); ANALYTICS 2 rows (#1 busy@2.05s, #2 answered@6.31s). GOTCHA: a STANDALONE harness can't speak TTS — AgentSession plugins (ElevenLabs/Groq) need the worker's `livekit.agents.utils.http_context` ("Attempted to use an http session outside of a job context"); wrap a hold-only test in `async with http_context.open():` to prove the OGG publishes; the spoken reassurance/whisper work in the REAL aim-voice-agent worker (which HAS the context). GOTCHA: `/run` `force=` only flips the dispatch-time 202; `run_job` RE-checks `_in_window` and idles out-of-hours → to ring out-of-hours for an earner gate, create a throwaway campaign with `call_window_start=00:00/call_window_end=23:59` and delete it after (NOT a force flag). GOTCHA: a not-picked-up earner test call can auto-log `opt_out_call` SUPPRESSION on the dialed number (var/suppression.json) → clear that test-entry or the next dial returns `suppressed_count:1` + no room. EARNER GATE before+after PASS: rooms 7a9076 + be6afd (1 participant + 2 publishers = SIP connected = RANG), agent.py md5 9150fabe4ff62b4b4470f9a87df346e5 UNCHANGED, famit-agent PID 1477083 NEVER restarted, core 200, only aim-voice-agent restarted. Backups *.HOFXUXbak.20260612-172359.
- 2026-06-12 — HOTL: per-tenant HANDOFF-LIST CONFIG layer (backend box, additive; the warm-transfer BRIDGE already worked + must NOT regress). Built the missing CONFIG layer ON TOP of the existing same-room direct-dial bridge (do NOT reintroduce the side-room hold-music bug — bridge stays `create_sip_participant(room_name=<caller room>)`, trunk ST_fmtVmNJmpzKa, in aim_voice_agent.py `_do_warm_transfer`; left UNTOUCHED). KEY DISCOVERY: a LOT was already present — caller.py already had GET/PUT /brain/handoff, POST /handoff/notify, _handoff_get/_set; voice_tools.py had handoff_list (direct fs read) + notify_handoff_team (loopback); var/brain/admin.json ALREADY seeded the founder (+916375548830 p1, +917861019021 p2). So this unit only ADDED single-entry CRUD + the conversational tools. ADDED: (1) caller.py `POST /brain/handoff/add` (add/UPDATE one, idempotent by canonical phone, +91-validated, auto-priority=max+1) + `DELETE /brain/handoff/remove` (?phone= or body, idempotent) — both write-role gated + TENANT-FROM-TOKEN (strip body org_id/tenant_id, RT-5); `_handoff_valid_phone` reuses norm() then REQUIRES +91 + 13 chars; `_handoff_get/_set` now carry an `enabled` flag (default-True so already-seeded entries keep working). (2) voice_tools.py `add_handoff(tenant_id,...)` / `remove_handoff(tenant_id,phone)` — CRITICAL: these mutate IN-PROCESS via `import brain; brain.upsert_profile(tenant_id, {"handoff": list})` (versioned + history + audit), NOT via the loopback — because the loopback carries the box ADMIN cred (FamitCall2026) so an HTTP write would ALWAYS hit the ADMIN tenant, wrong for a real vendor; mutating by the explicit tenant_id the agent passes (from the verified caller's token) = correct scoping. upsert_profile does a shallow merged.update(patch) so `{"handoff":[full list]}` REPLACES the list cleanly. (3) aim_voice_agent.py ManagerAgent `@function_tool list_handoff/add_handoff/remove_handoff` (PIN-gated on self._verified; loose strings, strict-off like other AIM tools; priority passed as a STRING via _to_int) + manager system-prompt bullet so the LLM calls them ("add Rajesh +91… to my handoff team"). TENANT-SCOPING PROOF (over real loopback HTTP): minted an hmac token for a REAL tenant via `caller._make_token('21d0a13603da')` (token = tenant_id.hmac(tenant_id,SECRET); _verify_token returns None for an UNREGISTERED tenant → 401, so use a real one from /tenants) — tenant B added its own +918888000022 and saw ONLY that; admin list stayed the 2 founder numbers (no B leak); B never saw the founder numbers; cleanup removed=True. EARNER GATE before+after = outbound /run to +917861019021 RANG (rooms a3ff67 + b908c8, AI opener spoken, USER_REJECTED = a real ring), agent.py md5 9150fabe4ff62b4b4470f9a87df346e5 UNCHANGED, famit-agent PID 1477083 untouched, 0 5xx, only famit-caller+aim-voice-agent restarted. GOTCHAs: venv is /opt/capsy-agent/.venv (py_compile there); `caller._make_token` import prints a noisy "[db.engine] Postgres available" line to stdout → `| tail -1` to grab just the token; brain is a PACKAGE at /opt/famit-agent/brain/__init__.py (core.py has upsert_profile, _VAR_DIR = FAMIT_VAR/brain). Backups *.HOTLbak.20260612-170656. State: /opt/famit-agent/ai_manager/HANDOFF_CONFIG_STATE.md.
- 2026-06-12 — FORTRESS PANEL DEPLOY (frontend box only; "Signal Aurora" loader went LIVE). REUSABLE RECIPE that worked clean: box `root@143.110.247.249` (famit-panel-2, 2GB RAM, swappiness 10, has a PERMANENT 2G /swapfile already), SSH key `~/.ssh/do-blr-test/id_ed25519`, app `/opt/famit-panel` (deployuser, systemd `famit-panel` = `next start -H 127.0.0.1 -p 3001`), nginx `/`->3001, node v20.20.2. NO git repo on box -> deploy via tarball. SHIP ONLY COMMITTED CONTENT with `git archive --format=tar.gz feat/<branch>:famit-panel` (archive root = panel CONTENTS, no `famit-panel/` prefix -> `tar xzf -C /opt/famit-panel`); node_modules/.next/.env.local are gitignored so they're NOT in the archive and extract-over PRESERVES the box copies (no re-`npm ci` needed). This also dodges shipping a parallel session's dirty working-tree files. STEPS: scp + md5-verify on box; backup `cp -a /opt/famit-panel /opt/famit-panel.CUIbak.$(date +%s)` (~4.2G; rollback = rm-rf + mv-back + restart); extract; `chown -R deployuser:deployuser`. OOM: the 2GB box's resident 2G swap is NOT enough for `next build` -> add a TEMP `/swapfile.build` 4G (`fallocate -l 4G; chmod 600; mkswap; swapon`) + `sysctl vm.swappiness=60` (swap peaked ~3.3G in use; build ran several min). Build as deployuser: `npm install --legacy-peer-deps` then `NODE_OPTIONS=--max-old-space-size=3072 npm run build` -> EXIT 0, ~53 routes, NEW BUILD_ID. `systemctl restart famit-panel` (ONLY that unit) -> confirm MainPID changed + start time AFTER the build + served `/login` HTML greps the NEW BUILD_ID (catches a stale .next). Verify 200 on loopback :3001 AND public https://panel.famit.in (Cloudflare). TEARDOWN — CRITICAL, easy to forget: `swapoff /swapfile.build && rm -f /swapfile.build && sysctl vm.swappiness=10` + confirm it's NOT in /etc/fstab. This box is FRONTEND ONLY (no caller.py/agent.py) so a deploy here CANNOT touch the earner. Build log: memory/build_log/wave-build-aurora-loader-deploy.md.
- 2026-06-12 — PANEL UI: GenerationLoader cheap-loader uplift -> "Signal Aurora". Replaced grey-dots-on-black with a raw-WebGL fragment-shader aurora (domain-warped FBM value-noise, 3-stop Signal-blue palette #2a85ff, central bloom + conic shimmer sweep + film grain) BEHIND the existing canvas sparks; sparks now COUPLE to the flow via a CPU analytic luma mirror (no GPU pixel readback) -> brighten/grow/blue-tint on bright crests + additive `lighter` blend = glow. Phase->intensity 4-stage escalation (queued .22 -> rendering 1.0 -> done .4), eased per-frame. NO new npm dep (framer-motion@12.5 already present for status crossfade). Token-pure: palette via NEW --gl-aur-a/b/c + --gl-spark-tint resolved by getComputedStyle. Reduced-motion/no-WebGL -> NEW .gl-field--mesh CSS gradient-mesh fallback (renamed from .gl-field--css). Dormant-safe: dispose() frees GL context on unmount + on CSS-field switch; reuses existing RAF-pause-on-hidden + IntersectionObserver. Drop-in: SAME public API -> TemplatesStep/BannerStep/GenerationQueue upgrade for free. tsc+build EXIT 0. Files: components/GenerationLoader/{aurora.ts(new),index.tsx,field.ts} + app/globals.css gl-* block. GOTCHA: WebGL single oversized-tri quad (-1,-1,3,-1,-1,3) covers the screen with 1 draw call; powerPreference:low-power + dpr cap 2 keeps the 2GB-box CLIENTS at 60fps (shader runs on USER gpu, not the server).
- 2026-06-12 — WAFX FRONTEND truth-pass (panel only; pairs with the backend WAFX error-surfacing). The panel was showing a stale FALSE BSP string ("WhatsApp isn't connected — add WA_API_URL/WA_API_KEY/WA_FROM") that confused the founder for days; the creds ARE set and `post_call_followup` sends fine. FIX: (1) lib/api.ts — added `MetaError` type + threaded `error`/`meta_error`/`approved_templates` onto `WhatsAppSendResult` + `WhatsAppLogEntry`; sendWhatsApp now RETURNS Meta's real error JSON on a non-200 (was `throwForStatus` → generic). (2) app/whatsapp/_lib/waapi.ts — new exported `explainMetaError()` / `explainSendResult()` mapping the real Meta reasons to plain English (141006→add payment method; TIER_250/not_verified→business under review; template_not_registered→name approved `post_call_followup`; 131058/hello_world→Meta test-only; 2388043→banner upload) and ALWAYS a muted debug line `Meta: <error_user_msg> · code N/sub · trace …`; never invents — unknown codes fall back to Meta's own error_user_msg. (3) new `_components/MetaStatusNote.tsx` = `<MetaReadinessHint delivers>` (calm "connected; delivery pending Meta verification") + `<MetaErrorNote explain>` (token-pure inline card). Wired into DeliveryStep (replaced the false banner; shows last failed row's reason else readiness), ScheduleStep (send fail → real reason inline, not "add creds"), ApprovalStep (submit-to-Meta surfaces Meta's raw line + readiness hint). GOTCHA: Icon is a REGISTRY — grepped first; `wallet`/`check-circle-fill`/`block`/`info` exist, NO `shield`/`whatsapp`/`verified` (used `check-circle-fill`+`info` for status). tsc+build EXIT 0, zero raw hex (Signal tokens), gitleaks staged=0. Branch feat/premium-ui.
- 2026-06-12 — WAFX WhatsApp/Meta ERROR-SURFACING (backend box only, additive): caller.py `/whatsapp/send` + `_wa_send`, whatsapp.py `_post_meta*`, whatsapp_builder/{meta_submit.py,__init__.py}. (1) submit() non-200 now returns Meta's REAL Graph error (code/error_subcode/error_user_title/error_user_msg/fbtrace_id) via `_meta_error_detail`, not "http_400". (2) a media-header template with NO resolved header_handle is REFUSED ("Banner upload to Meta failed") instead of POSTing example.header_handle:[""] (subcode 2388043). (3) to_meta_payload ALWAYS attaches example.body_text when the body has {{n}} placeholders (pads with neutral "Sample", NO generic filler) -> avoids async INVALID_FORMAT. (4) send path: `_wafx_resolve_template` allow-lists post_call_followup/hello_world (env WAFX_APPROVED_TEMPLATES + WAFX_TEMPLATE_ALIASES), unregistered names (hot_lead_alert/benefit_focus/special_offer) -> clear "template_not_registered" w/ approved list; whatsapp.py `_apply_meta_response` replaces misleading "sent:400" with honest "meta_error:<code>/<subcode>" + structured meta_error; `/whatsapp/send` returns it in `error`. ⚠️ BIG CORRECTION to the "Meta fully blocked / 141006 / nothing delivers" premise: the REAL approved business template `post_call_followup` SENDS FINE — live POST returned ok:true sent:200 with a real wamid + message_status:accepted to the founder's number. Only `hello_world` errors (131058 "can only be sent from Public Test Numbers" — a Meta test-template restriction, not an account block) and unregistered names fail. WABA: account_review_status APPROVED, phone CONNECTED/VERIFIED, business_verification_status=not_verified. So the residual is delivery/verification polish, not a hard send-block. Earner gate PASS before+after: agent.py md5 9150fabe… UNCHANGED, famit-agent PID 1477083 + ActiveEnter 2026-06-10 untouched, core 200, 0 5xx, only famit-caller restarted. Backups *.WAFXbak.20260612-*. GOTCHA: META_WA_* live in /opt/famit-agent/.env (systemd EnvironmentFile) — a plain SSH `python` does NOT see them (meta_ready=False); must `set -a; . .env` to probe. Also .env has a stray unquoted line ~105 ("enquiry:") that breaks naive `. .env` (harmless to our task — systemd parses it fine; only the bash-source probe warns).

- 2026-06-12 — HOUI DEPLOY (per-tenant Handoff Team UI -> FORTRESS, frontend box only; commit 65dbe26 on feat/premium-ui). Deployed the built-but-undeployed handoff/escalation panel UI to root@143.110.247.249 (famit-panel-2) via the learning #5 tarball recipe — clean. STEPS that worked: `git archive --format=tar.gz feat/premium-ui:famit-panel` (22.7MB, ships ONLY COMMITTED content -> node_modules/.next/.env.local are gitignored so NOT in archive -> extract-over PRESERVES box copies AND dodges the heavy parallel-session dirty working tree; archive root = panel CONTENTS, no famit-panel/ prefix; scanned: zero .env/secret inside) -> scp + md5-verify on box (3f33049… matched) -> backup `cp -a /opt/famit-panel /opt/famit-panel.HUIbak.1781288658` (4.3G; rollback = rm-rf + mv-back + restart) -> `tar xzf -C /opt/famit-panel` + `chown -R deployuser:deployuser` -> temp /swapfile.build 4G + `sysctl vm.swappiness=60` (resident 2G swap NOT enough for next build) -> as deployuser `npm install --legacy-peer-deps` then `NODE_OPTIONS=--max-old-space-size=3072 npm run build` = EXIT 0 ("✓ Compiled successfully", "✓ Generating static pages 52/52", both `ƒ /ai-manager/handoff` 1.57kB + `ƒ /ai-manager/live` 2.46kB in the route table) -> `systemctl restart famit-panel` ONLY (PID 178771->188199) -> TEARDOWN `swapoff /swapfile.build && rm -f /swapfile.build && sysctl vm.swappiness=10` (confirmed only permanent 2G /swapfile left, swappiness=10, not in fstab) + rm the tarball. VERIFY: OLD_BUILD_ID VPtGSsTgpNpL2uccV0SnA -> NEW XyeGd-iyCjs-emBYS8-q8; loopback :3001 handoff/live/run/login all 200 + served /login HTML carries the NEW BUILD_ID (no stale .next); public edge https://panel.famit.in handoff/live/run/login all 200. EARNER SAFETY: this box has NO /opt/famit-agent (NO_AGENT_DIR) and no caller.py/agent.py — only famit-panel is the app unit, so a deploy here CANNOT touch the voice earner; only famit-panel restarted. KEY LEARNING confirmed: the build session's "Windows-only kill EPERM after Compiled successfully" quirk does NOT happen on the LINUX box — there next build exits 0 cleanly. FOUNDER PATH: Sidebar -> AI Manager -> Handoff Team (/ai-manager/handoff); live progression at AI Manager -> Live Calls (/ai-manager/live); also embedded in Run a Campaign (/run, card #7). Build log: memory/build_log/wave-build-handoff-ui-deploy.md.

- 2026-06-13 — created a 24x7 night-test campaign for off-hours dashboard testing (id 985c7e46c0 "Night Test (24x7)", tenant admin, window 00:00-23:59, cloned c17e55e9f3's Riya/Hinglish brain via POST /campaigns fields_json; data-only, agent.py md5 9150fabe… unchanged, no restart; founder dials it via /run "Pick manually" -> +917861019021 only, NOT in suppression so dialable).

- 2026-06-13 — ✅ SMART PROVIDER KEY POOL + HOT-RELOAD KEY-STORE + RUN-CAMPAIGN FIX (AIM box famit@168.144.153.145 /opt/famit-agent; venv /opt/capsy-agent/.venv; ADDITIVE, EARNER UNTOUCHED). Built the real fix for the "inbound dead / thoda sa system slow" incident (Groq daily-TPD wall + DUMB linear key#1→#2→#3 rotation while #4-9 idled). NEW package `llm_router/` (pure-stdlib pool; cryptography only for the store): (1) `provider_pool.py` ProviderPool per provider (GROQ/SARVAM/SAMBANOVA/OPENROUTER singletons) — `pick()` returns the AVAILABLE (enabled AND now>=cooling_until) key with the LOWEST pick_count (tie-break oldest last_ok_at), O(n) over ~15 keys; `mark_429(key,retry_after)` sets cooling_until=now+(retry_after or 60s, cap 3600, FLOOR 1.0s) + INSTANT skip (NO linear walk of dead keys); reconciles env-seed + hot store on EVERY pick (state survives reload, matched by secret value). `parse_retry_after` reads Groq "try again in 14m12s" msg + headers. (2) `key_store.py` ENCRYPTED Fernet store at `var/provider_keys.json.enc` (0600, master key `PROVIDER_KEYSTORE_SECRET` in .env, SHA256→urlsafe-b64 Fernet key); mtime-cached decrypt (`stat` per-pick = µs) → atomic `os.replace` bumps mtime → TRUE hot-reload; degrades to 0600 plaintext if crypto/secret absent. (3) `pool_llm.py` PoolLLM(llm.LLM) — the CRITICAL wiring: `groq.LLM`/`openai.LLM` bind ONE api_key for life + retry the SAME key on 429 (never re-pick), so a static FallbackAdapter member can't rotate. PoolLLM holds ONE delegate, swaps `delegate._client.api_key` per chat() from pool.pick(), wraps the stream: on a 429 it mark_429s + INSTANTLY re-picks the next available key (loop ≤ available_count), and when the WHOLE provider is cooling raises so the FallbackAdapter advances PROVIDER (Groq→SambaNova→OpenRouter). WIRED into aim_voice_agent.py (LPR-POOL, 3 edits): pool import (guarded) + Sarvam STT pool-pick in `_build_stt` + `FallbackAdapter([PoolLLM(groq), PoolLLM(sambanova Meta-Llama-3.3-70B-Instruct), PoolLLM(openrouter free)])`; degrades to legacy `_next_groq_key`/single members if import fails. Loaded 9 NEW Groq keys (z_groq_2.md — MULTIPLE accounts pk7/learnig7x/kunal*x = INDEPENDENT daily pools, the real fix vs same-org rotation; FIXED the file's `gGROQ_API_KEY_6=sk_` typo→`gsk_`) + 4 SambaNova keys (z_samba.md) into the ENCRYPTED store. SAMBANOVA RESEARCH: base_url `https://api.sambanova.ai/v1`, OpenAI-compatible, model `Meta-Llama-3.3-70B-Instruct`, ~282-430 t/s (≈Groq), Free tier 200k TPD/~10 RPM, Dev tier 20M TPD/60 RPM (70B) — rotate the 4 keys to multiply RPM; STRIP presence/frequency_penalty (400s there — openai.LLM doesn't send them by default so OK). 5 super-admin CRUD routes in caller.py (require_super_admin, legacy_pw EXCLUDED→403): GET/POST `/admin/provider-keys`, PUT/DELETE `/admin/provider-keys/{id}`, GET `/admin/provider-keys/status` (live pool snapshot, masked-only, raw key NEVER returned). FRONTEND `/super-admin/api-keys` (famit-panel feat/premium-ui commit a8e9ca9): Card/provider, masked password add-modal, enable Switch, delete, live status dot polled 5s, tsc EXIT 0, gitleaks 0, token-pure. RUN-CAMPAIGN FIX (LPR-FORCE-WINDOW, caller.py 4 edits + voice_tools LPR-NOW 2 edits): root cause = `force` only suppressed the dispatch-202 but `run_job` (caller.py:2468) RE-CHECKS `_in_window` every tick + idles `sleep(60)` out-of-window even when forced → AIM said "dialing" but no SIP room. FIX = add `now` Form param → stash `JOBS[id]["force_window"]=True` → run_job bypasses the window idle when force_window; /run returns honest 202 `queued_out_of_window` only when NOT now/force; voice_tools run_campaign + single-call now send `now=1` (true dial). NOTE: outbound STILL 402-blocked by empty Vobiz balance (₹0.19, AGENT_LEARNINGS#1) — the window fix makes "run now" REACH SIP-create; the ring needs the founder to recharge Vobiz. NO-BURN PROOF (zero real quota for rotation): `tests/test_provider_pool.py` PROOF_EXIT=0 (sim-429 on k1 → pick returns k2/k3, NEVER k1 while cooling, NOT linear; 90 picks → PERFECT 10/10/10 spread across 9 keys; all-cooling→None→provider advance; cooldown-TTL; hot-reload add/disable live; retry-after parse) + `tests/test_pool_llm.py` PROOF_EXIT=0 (PoolLLM k1-429→instant re-pick k2, all-429→raise=advance, each distinct key tried ONCE not a loop). Connectivity = 1 tiny call/provider: Groq OK via OpenAI-SDK ("Hello!" — raw urllib 403/1010 was Cloudflare bot-block on urllib, NOT the key; the LiveKit groq plugin uses the SDK = fine), SambaNova OK (Meta-Llama-3.3-70B "It's nice to meet"), Sarvam OK (models 200). 1 INTEGRATED turn through real PoolLLM[groq] in http_context → "Hi!" + pick_count incremented (satisfies the #5 integrated-smoke rule). API verified over HTTP: list/add/status/delete + HOT-RELOAD PROVEN (samba pool 5→6 on add, →5 on delete, NO restart) + legacy_pw→403. EARNER GATE before+after PASS: agent.py md5 `9150fabe4ff62b4b4470f9a87df346e5` UNCHANGED, famit-agent active MainPID 1477083 ActiveEnter 2026-06-10 19:58:18 NEVER restarted, 0 5xx/tracebacks; only famit-caller(→2228794)+aim-voice-agent(→2228800) restarted; both re-registered clean (manager on :8091). Backups *.LPRbak.20260613-08* (.env/caller.py/aim_voice_agent.py/voice_tools.py). SECRETS: z_groq_2.md/z_samba.md/z_groq_api.md + **/provider_keys.json* gitignored (check-ignore exit 0), encrypted store 0600 famit-owned, seeders deleted local+box, no keys in any git diff, gitleaks 0. KEY LESSON: the founder's "multiple keys but key#1 dies while others idle" = a LINEAR rotation bug; the fix is least-used + cooldown-skip selection, AND for LiveKit you MUST wrap with a per-request key-swapping PoolLLM because the groq/openai plugin binds+retries ONE key for the instance lifetime (a static FallbackAdapter member alone can't rotate). Multiple keys help per-DAY ONLY if they're from DIFFERENT accounts/orgs (independent TPD pools) — same-org rotation only helps per-minute. Env knobs: AIM_LLM_FALLBACK, SAMBANOVA_MODEL/BASE_URL, PROVIDER_POOL_COOLDOWN. State `caps/LLM_PROVIDER_POOL_STATE.md`.

- 2026-06-13 — ✅ RE-VERIFY PASS (independent, read-only, ZERO quota burn) of the smart provider pool wave: both unit tests RE-RAN on the box venv = PROOF_EXIT=0 (provider_pool: sim-429 k1→k2/k3 NOT-linear, 90 picks=PERFECT 10/10/10 even spread, all-cooling→None, cooldown-TTL, hot-reload add/disable live, retry-after parse; pool_llm: k1-429→instant re-pick k2, all-429→raise=advance provider, each key tried once). Live merged pool on box = Groq 15 / SambaNova 5 / Sarvam 5 / OpenRouter 1 (all available). 5 `/admin/provider-keys*` routes present (caller.py 6155-6240) ALL require_super_admin; legacy `FamitCall2026`→403 over HTTP (GET+POST); GET returns list_all_masked() (raw key never returned). FallbackAdapter chain on box = Groq pool→SambaNova pool→OpenRouter free. Run-campaign fix wired (caller.py:2470-2472 `if job.get("force_window"): in_win=True` bypass; voice_tools now=1 :343/:520). Frontend live https://panel.famit.in/super-admin/api-keys = 200 (committed a8e9ca9, deployed to FORTRESS). Secrets gitignored (check-ignore exit 0) + NOT tracked (git ls-files exit 1) + encrypted store 0600. EARNER GATE before+after this verify = PASS: agent.py md5 9150fabe4ff62b4b4470f9a87df346e5 UNCHANGED, famit-agent MainPID 1477083 ActiveEnter 2026-06-10 19:58:18 NEVER restarted, 0 5xx/tracebacks in caller/aim (verify was purely read-only — nothing restarted). NO-BURN RULE reaffirmed: prove rotation by sim-429 unit tests, NOT by hitting real Groq quota — the founder's inbound call is the only real end-to-end proof. RESIDUAL: the EARNER still shares the same Groq org pool until its OWN provider-fallback is a separately founder-approved item (agent.py/trunks/firewall/SIP off-limits). Build log: memory/build_log/wave-build-llm-provider-pool.md.

- 2026-06-13 — VOBIZ LOW-BALANCE ALERT added to the famit watchdog (voice box 168.144.153.145, OPS-ONLY): the earner can silently die on an empty Vobiz balance -> every outbound INVITE gets SIP 402 Payment Required -> no phone rings (the ₹0.19 incident). Reused the existing watchdog (/usr/local/bin/famit_watchdog.sh, runs as root every 60s) + alert.sh (Telegram) + /etc/famit-alert.env; ADDED a tiny helper /usr/local/bin/famit_vobiz_balance.sh (sources /opt/famit-agent/.env, calls vendors/vobiz.py get_balance() via /opt/capsy-agent/.venv, prints the INR number or ERR:*), a §4 balance-check block in the watchdog (de-dup: alerts ONCE on cross-low + ONCE on recovery via state flag /var/lib/famit-watchdog/vobiz.low; ERR:* = unknown -> no alarm + does not clear the flag), and env knob VOBIZ_LOW_BALANCE_THRESHOLD=100 (INR). TEST: set threshold 600 (above live ₹499.74) -> real Telegram delivered (ok:true, msg_id 31, chat 1862240811) -> reset to 100 + cleared the test flag. agent.py md5 9150fabe4ff62b4b4470f9a87df346e5 UNCHANGED, famit-agent never restarted (PID 1477083); caller/agent/trunks/firewall/SIP untouched; only the watchdog files edited (backups *.VOBIZbak.20260613-081211). Creds stay out of git (live on the box only).

- 2026-06-13 — ✅ LOGIN-POPUP FIX SHIPPED (the native browser username/password dialog on panel load). ROOT CAUSE (pre-diagnosed): `caller.py` `need_auth()` returned 401 WITH header `WWW-Authenticate: Basic realm="Famit"`; on panel load the SPA fires `GET /api/me`, the browser sees that challenge header and pops the native Basic-auth dialog BEFORE the SPA can redirect to its own /login (fires even in incognito; NOT Cloudflare, NOT a cached credential). FIX = surgical ONE-LINE, header-only removal, status preserved. `/opt/famit-agent/caller.py:592` (need_auth, def at :591) BEFORE `return Response(status_code=401, headers={"WWW-Authenticate": 'Basic realm="Famit"'})` → AFTER `return Response(status_code=401)`. NO auth logic changed, no gate weakened — 401 kept so the SPA still redirects to /login and protected data stays gated. SAFETY: backup `/opt/famit-agent/caller.py.LOGINFIXbak.20260613-095615` (md5 of pre-edit = 40693ce191a74b02dc32d462e5ea6bf9); py_compile OK on `/opt/capsy-agent/.venv/bin/python` (that's the venv the famit-caller unit's ExecStart uses, WorkingDirectory=/opt/famit-agent, EnvironmentFile=/opt/famit-agent/.env); restarted famit-caller ONLY (new PID 2253284, Postgres ok, clean startup, 0 errors). EARNER GATE before+after = PASS: agent.py md5 9150fabe4ff62b4b4470f9a87df346e5 UNCHANGED, famit-agent NOT restarted, agent.py/trunks/firewall/SIP untouched. EDGE PROOF (curl -D - on https://panel.famit.in/api/me): BEFORE showed `Www-Authenticate: Basic realm="Famit"`; AFTER = 401 with that header GONE (grep = NONE). Security UNCHANGED: `/api/me` + `/api/leads` still 401 body_bytes=0 with no session (no data leak); grep across caller.py confirms ZERO `WWW-Authenticate`/`realm` remain. Incognito-equivalent confirmation (no cookies): root `/` = 200 Next.js app shell with NO www-authenticate, `/login` = 200 — so the browser has no Basic challenge to act on and the SPA's 401 handler routes to /login (no native popup). ROLLBACK: restore the .LOGINFIXbak backup + restart famit-caller.
