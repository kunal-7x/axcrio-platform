# EXPLORE-1 — Existing Inbound Voice State (READ-ONLY map)

_Authored 2026-06-12 from the LIVE box famit@168.144.153.145 (SSH key do-blr-test). Evidence-grounded, file:line cited. No changes made. This is the substrate the two-mode inbound brain (CUSTOMER sales + MANAGER command) will be built ON TOP OF — additive, never touching the outbound earner._

> ⚠️ **#1 CONSTRAINT — NEVER BREAK OUTBOUND.** The outbound Riya earner = `agent.py` / `agent_name="capsy"` / `famit-agent.service` / port 8090 / outbound trunks. It was just restored after an infra mistake. The inbound work lives in a **separate worker** (`aim_voice_agent.py` / `agent_name="manager"` / `aim-voice-agent.service` / port 8091) that **shares the venv `/opt/capsy-agent/.venv` and EnvironmentFile `/opt/famit-agent/.env`** with outbound but imports none of agent.py's call path. Any plan must keep that isolation: additive files, separate dispatch rule + DID, no edits to agent.py / the outbound trunk / the shared env keys the earner reads.

---

## A. WHAT IS LIVE RIGHT NOW (verified, not from old notes)

- **The inbound worker IS deployed and running** — `aim-voice-agent.service` = `active (running)`, PID 1738870 up since Jun 11 20:26, exec `python /opt/famit-agent/aim_voice_agent.py start` under the capsy venv. So inbound is NO LONGER "parked/dormant" — it is registered with LiveKit as `agent_name=manager` and accepts jobs.
- **But it has never completed a successful voice session.** The last job (Jun 11 21:22, `job_id AJ_eZTVE38Yt58H`) shows: greeting authored → **STT WebSocket connect failed** (`APIConnectionError: Failed to connect to STT WebSocket`, root = transient `aiohttp _resolve_host → CancelledError → TimeoutError` on `api.sarvam.ai`) → the bare single-STT pump **fataled the job** → then `outcome=reject:unregistered actions=0` → `process exiting`. Two distinct failures stacked: a transient STT crash AND the test caller wasn't in the registry.
- **No live SIP wiring yet** — `lk sip inbound list` and `lk sip dispatch list` both return EMPTY. There is no inbound trunk binding a DID, no dispatch rule routing a DID → `agent_name=manager`. So even though the worker runs, real PSTN calls have no path to it yet; the wiring recipe (`ai_manager/wiring/sip_dispatch.md`) is UN-APPLIED.

## B. THE TWO INBOUND MODES — current coverage

- **MANAGER inbound (vision B): ~70% built, voice-rail broken.** The whole brain exists: `ai_manager/state_machine.py` `CommandMachine` runs the deterministic S0–S9 loop (S0 connect → S1 identify via `registry.lookup(caller_id)` → S2 anti-spoof PIN BEFORE data → S3 context → S4 capture intent → S5 permission → S6 fresh scoped step-up PIN for risky → S7 spoken confirm w/ amount read-back → S8 delegate→runner → S9 report). `aim_voice_agent.py` drives it via a sync↔async `VoiceTransport` (speak/listen/collect_secret; DTMF-preferred PIN with spoken-digit fallback). This is the conversational-command path the founder wants — it ALREADY asks clarifying questions (S4 NLU → `kind:"clarify"` on low confidence) and confirms before executing.
- **CUSTOMER inbound (vision A): 0% built — entirely greenfield.** The inbound agent explicitly has **"NO sales-pitch LLM persona"** (`aim_voice_agent.py:28`, `:525`); the LLM only fills slots for the command machine. There is **no branch** for "a lead calls back" or "a new banner caller." The state machine has ONE entry (`run(caller_id)`) that goes straight to manager identify→PIN and `reject:unregistered` for anyone not a registered manager number — a lead would simply be rejected today. Nothing pulls prior call history/transcripts, nothing disambiguates campaigns, nothing runs the sales script on inbound.

## C. WHAT IS REUSABLE FOR THE NEW WORK (the seams)

- **Sales-context reuse anchor for customer-inbound lives in the OUTBOUND code (read-only reference, do NOT edit):** `agent.py` already loads a per-call campaign brain — `caller.py:142 _load_campaign(campaign_id) → {fields, system_prompt}`, dispatch metadata `{"campaign_id","lead_name"}` (agent.py:351-375), and **cross-call memory** ("recover the lead's phone from the room name, load prior call" agent.py:391-396; `TRANSCRIPT_DIR` caller.py:43). This is EXACTLY the machinery customer-inbound needs (pull a lead's prior transcript + continue; load a campaign's script/knowledge for a banner caller) — to be **re-implemented in the inbound worker**, reading the same campaign/transcript stores, never importing agent.py.
- **Latency moat is inherited, not re-derived** — the inbound `AgentSession` is built from the SAME proven kwargs (Sarvam `saarika:v2.5 language="unknown"` code-mix, Groq llama-4-scout round-robin keys, ElevenLabs flash_v2_5, preemptive_generation, semantic turn-detection, `max_completion_tokens` not `max_tokens`). The customer-inbound sales agent should reuse this same session builder.
- **Registry = the per-number identity map (multi-vendor scaffolding), already wired as the S1 gate.** `ai_manager/registry.py` + `var/aim_numbers.jsonl` (6849 B, seeded Jun 11 21:00): each number row = `tenant_id · role · verify_mode · grants[] · status`; `lookup(phone, tenant_id?)` returns the active+verified row (effective permission = role AND grants, computed in `identity.permits`). It is **file-backed JSONL today** (PG migration noted as future, RLS like crm/core). This is what maps "designated number → AI Manager identity" — and the same registry concept extends to "this DID → which campaign" for customer-inbound (a campaign-DID map, not yet present).
- **Safety + execution spine is shared and proven:** `firewall.py` (PIN 4827 salted-hash + HS256 step-up, sub-bound), `workforce/runner.py` + `workforce/tools/catalog.py` (25 command tools → real caller.py routes; re-enforces caps/kill-switch/wallet independently of voice), `identity.classify_risk` (deterministic risk; LLM self-label ignored). `ai_manager/intent/driver.py` NLU (closed-enum, CONF_MIN=0.55→clarify, Hinglish). Persistence: `ai_manager/store.py` create_session/command/action_run + `_persist_turn` → PG `ai_manager_*` tables (sessions/commands/action_runs/audit_logs/profiles/authorized_users/idempotency exist), with `schema.sql` (21 KB) live.

## D. WHAT IS HALF-BUILT / BROKEN / MISSING (gaps to close)

- **P0 — STT has no resilience (the silence bug, STILL live as of Jun 11 21:22).** `_build_stt` (aim_voice_agent.py:644) is a **single Sarvam STT, no retry, no FallbackAdapter** — one transient WS/DNS blip propagates out of `_stt_pump` and kills the whole job before speech. Fix = wrap in `livekit.agents.stt.FallbackAdapter([sarvam, sarvam_backup_key])` + connect-retry. (NOTE: the never-silent pieces ARE done — greeting speaks on join before the gate at `:536-542`, and the entrypoint has a try/except that always speaks an apology at `:381`. Only STT resilience remains for P0.)
- **ai_manager FastAPI router NOT mounted in caller.py** — `wiring/caller_endpoints.diff` is an un-applied one-line `include_router`. So the panel's `/ai-manager/sessions` + `/numbers` calls have no live backend yet; the worker writes turns best-effort but the read API is dark.
- **Recording is effectively a no-op for replay** — `recorder.py` exists and the machine pauses/resumes it around PIN spans, but there is **no LiveKit Egress started, no Spaces/S3 creds, no upload, no `recording_url` surfaced** end-to-end. The audit/replay product (transcript + recording in panel) is incomplete.
- **Multi-vendor not fully through the voice path** — registry IS the S1 gate now (good), but there's still a hard `AIM_AUTHORIZED_CALLER` / `AIM_EXTRA_AUTHORIZED` allowlist layered on top (aim_voice_agent.py:124-133, soft by default), per-vendor PIN is the single box firewall PIN (not per-tenant), and there is **no DID→tenant / DID→campaign dispatch** (the recipe in `wiring/sip_dispatch.md` uses one DID → one `manager` agent).
- **No customer-inbound disambiguation/routing at all** — missing: (1) a branch that classifies the inbound caller [registered-manager vs known-lead vs unknown-new], (2) lead lookup by caller-ID into the CRM/lead store with prior-transcript load, (3) campaign disambiguation ("which property?" OR infer from a campaign-specific DID), (4) a sales `AgentSession` persona path that runs the campaign script like outbound. These are the founder's vision (A) and do not exist.
- **No panel sessions LIST page** (only the `[id]` detail exists); **no automated outbound regression-gate** before/after inbound changes; registry/persistence are JSONL/PG-mixed and need consolidation onto PG+RLS for true multi-vendor isolation.

---

## 12-LINE MAP OF EXISTING INBOUND PIECES + STATE

1. `aim-voice-agent.service` — **LIVE/active** (PID 1738870, port 8091, `agent_name=manager`), separate from outbound; never completed a clean session.
2. `aim_voice_agent.py` — inbound worker: reuses proven session kwargs, never-silent greet+apology DONE; **STT still single-provider (P0 crash live)**; drives CommandMachine.
3. `ai_manager/state_machine.py` `CommandMachine` — **manager S0–S9 command loop WORKS offline** (identify→PIN→intent→permission→step-up→confirm→delegate→report); no customer/sales branch.
4. `ai_manager/registry.py` + `var/aim_numbers.jsonl` — per-number `tenant/role/verify_mode/grants/status`; `lookup()` is the live S1 gate; **file-backed (PG migration pending)**; seeded.
5. `ai_manager/intent/driver.py` — closed-enum Hinglish NLU, CONF_MIN→clarify; **reusable for both modes**.
6. `workforce/runner.py` + `tools/catalog.py` — 25 command tools → real caller.py routes, independent caps/kill-switch; **the manager execution engine, solid**.
7. `firewall.py` + `firewall_bridge.py` — PIN 4827 salted-hash + scoped HS256 step-up; **shared, proven**; per-vendor PIN not yet split.
8. `ai_manager/store.py` + `schema.sql` + PG `ai_manager_*` tables — session/command/action_run/audit persistence **exists**; voice write-path partially wired (`_persist_turn` best-effort).
9. `recorder.py` — pause/resume around PIN **exists**, but **NO Egress/upload/`recording_url`** → replay incomplete.
10. `ai_manager/endpoints.py` (router) + `wiring/caller_endpoints.diff` — read API **DEFINED, NOT MOUNTED** in caller.py → panel backend dark.
11. SIP inbound — `wiring/sip_dispatch.md` recipe **UN-APPLIED**; `lk sip inbound/dispatch list` = **EMPTY** (no DID bound, no dispatch rule).
12. **CUSTOMER-INBOUND (sales callback + new banner caller, vision A) — DOES NOT EXIST**; the only reuse seam is the outbound `caller.py:_load_campaign` + agent.py cross-call/prior-transcript memory (read-only reference to re-implement in the inbound worker).
