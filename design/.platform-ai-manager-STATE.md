# platform-ai-manager (VOICE COMMAND CENTER) — STATE

TASK: write execution-ready spec to design/platform-ai-manager.md, return summary + creds. PLANNING only.

KEY DISTINCTION (locked): this is the INBOUND VOICE FRONT-DOOR (vendor calls a number, speaks commands,
PIN-gated, delegates). It is NOT the orchestration brain — that already exists in
design/automation-aimanager.md (autonomous tick-loop). The voice layer COMPOSES over:
  - LiveKit voice spine (agent.py: AgentSession, WorkerOptions(agent_name)) -> 2nd worker agent_name="manager"
  - auth.py (JWT issue_pair/resolve_token/roles) -> mints real tenant token for verified caller
  - firewall.py PIN/OTP step-up (design/credit-ledger-firewall.md §6) -> THE risk gate, reused verbatim
  - automation/aimanager/ orchestrator+tools+caps+ad adapters -> the delegation target
  - audit.py (append-only), ratelimit.py+redis (PIN lockout)

DECISIONS (locked):
1. State machine VERIFY->CONTEXT->INTENT->PERMIT->PIN(step-up)->CONFIRM->DELEGATE->REPORT. Every transition
   deterministic; LLM only fills slots (closed-enum IntentMatch), never authorizes.
2. Login-auth (S2 PIN, mints access token) DISTINCT from per-action step-up (S6, fresh scoped 300s token).
3. classify_risk = deterministic allow-list table; model's risk label ignored. Engine RE-enforces caps (defense in depth).
4. Dormant-until-creds: intent LLM (groq default), OTP sender (twilio/msg91/whatsapp), inbound SIP.
5. Offline test drives full state machine w/ fake transport + StubDelegate/StubFirewall, zero keys/network.
6. NO spine edits (agent.py/caller.py/auth.py untouched); endpoints + sip dispatch ship as un-applied diffs.

VERIFIED ON DISK 2026-06-09: agent.py WorkerOptions(agent_name=AGENT_NAME) L865-867, dispatch-meta JSON;
auth.py issue_pair/resolve_token/roles; firewall §6 step-up (var/pins.json salted sha256, var/secret, amr).

STATUS: DONE + 3 ADVISOR FIXES FOLDED.
ADVISOR FIXES (2026-06-09):
1. CROSS-PLANE TRANSPORT (was the live-path blocker): voice box != API box. Added §3.2 — registry lookup,
   PIN/step-up, and delegation are AUTHENTICATED HTTP calls to AIM_VOICE_API_BASE (not localhost/local file)
   with AIM_VOICE_SERVICE_TOKEN. PIN verified ONLY on API box (/firewall/step-up). Updated §10.A2 + §11.
2. §11 FACTUAL SLIP fixed: PINs are var/pins.json (file, API box), NOT Postgres; only wallet is PG.
3. AUDIO-CHANNEL PIN LEAK: spoken PIN transits Sarvam STT + recording. §6.5 now requires recorder pause around
   digit capture + prefer OTP/DTMF; test 8b added. §10.A2 env added.
4. "AI-workforce" maps to engine tool registry (no *workforce* doc exists) — stated in §1.
SECOND ADVISOR PASS — swept cross-plane fix through:
5. §5.1 registry = API-box-owned, voice-side is READ CLIENT (was "own local JSONL" — contradicted §3.2).
6. §5.2 sessions = voice worker POSTs to API box on call_end (not local authoritative).
7. §5.3 lockout = firewall on API box is AUTHORITATIVE; voice keeps only fail-fast in-session counter.
8. §7 added GET /ai-manager/numbers/lookup (caller-ID hop) + POST /ai-manager/sessions (service-token).
Advisor confirmed: no further rounds needed. Doc: design/platform-ai-manager.md. Returning summary+creds.

ADVERSARIAL RED-TEAM (2026-06-09, third pass) — added "## RED-TEAM FIXES (folded)" to the doc. Verdict
GO-with-folds. CORRECTS prior STATE fix #4: it was WRONG — design/platform-ai-workforce.md DOES exist on
disk (a complete AgentRunner/RoleSpec/ToolRegistry spec). Three folds:
  A. Killed the false "no workforce doc" claim in §1.
  B. Re-pointed delegate.py target: automation/aimanager/ -> aiwf/manager/delegate.py -> AgentRunner.run(role=…)
     (the prototype aimanager engine = the ops_manager role on the shared runner; one guardrails.check gate).
     Contained re-point; state machine + gates + offline test UNCHANGED.
  C. §0 GROUND-TRUTH honesty marker: firewall.py + automation/aimanager/ + aiwf/ are DESIGNS not code
     (Glob = no .py). Spec-on-spec build; units 1-3 (safety spine on stubs) unblocked, units 5-6 blocked.
OWNERSHIP CONFIRMED: this doc IS the settled owner of the voice front-door (platform-ai-workforce.md RT-1
L29-45 says "DO NOT re-implement; DELEGATE to platform-ai-manager.md"; its §9 body is stale/superseded).
Guardrail design (login-vs-stepup, fresh scoped stepup, recorder-pause PIN-leak, cross-plane §3.2) sound,
preserved. Residual: spec-on-spec stack, sibling doc internal inconsistency, cross-plane transport
integration-test-only, §6.3 should converge to runner risk_class.
