# AUDIT-1 lifecycle — STATE (scratch)

Goal: write design/audit-lifecycle.md mapping ONE pipeline, 7 stages + panel, rate each BUILT/PARTIAL/NOT-BUILT/NOT-TESTED/DORMANT. Return 16-line matrix.

## Sources read
- INBOUND-PIPELINE-MASTER-PLAN.md (v1) — DONE: gap table, phases, evidence index (file:line)
- INBOUND-PIPELINE-MASTER-PLAN-V2.md — DONE: handoff/hot-WA/RAG/modular, feature inventory §8, gaps §10

## Key ground-truth (from V2, all verified on box 168.144.153.145)
- OUTBOUND earner LIVE: agent.py / capsy / famit-agent.service / :8090. FROZEN.
- INBOUND voice rail BROKEN (P0 silence: sarvam stt max_retry=0). 0 clean inbound sessions ever.
- SIP inbound NOT wired (lk sip inbound/dispatch EMPTY; SIP container UDP-only, needs TCP-5060).
- Memory per-person var/memory/{digits}.json LIVE (outbound writes). load/build_recap/save exist. Inbound NOT wired to it.
- post-call summarize+interest LIVE (agent.py:155). hot=score>=70 caller.py:1297.
- WA send LIVE code (whatsapp.py:248 template / :233 text) but DORMANT (Meta creds pending). post-call WA hooks fire caller.py:1417/1600.
- WA LLM multi-step conversation: separate wave mid-flight (wa-llm-conversation.md). check state.
- inbound returning-caller / new-caller campaign-ask: DESIGNED, NOT built (no sales-in worker).
- handoff/warm-transfer: primitive present (transfer_sip_participant) but ZERO code calls it, no handoff list. NOT built.
- hot-lead->WA-to-team: reuse exists, notify_handoff_team NOT built, needs hot_lead_alert template (GAP-C1).
- RAG/pgvector: BUILT (kb/core.py hybrid, schema, HNSW/GIN, FORCE-RLS) but corpus 0 rows, embedder not_configured, voice never imports kb. PARTIAL/DORMANT.
- booking/payments/crm engines mounted but voice un-wired.
- recording _NullRecorder no-op. ai_manager endpoints NOT mounted. No panel sessions list.

## VERIFIED ON BOX (2026-06-12, read-only)
- famit-agent + aim-voice-agent BOTH active. famit-caller/bridge/aiasset/llm-router active.
- WA: Meta creds PRESENT (META_WA_* all set), FEATURE_WHATSAPP=1, BUILDER=1. NOT dormant anymore (V2 plan stale on this).
- KB corpus = 0/0/0 (kb_sources/documents/chunks) -> RAG built, empty. No EMBED_ keys in .env -> dense leg dormant.
- handoff grep across *.py = 0 hits -> warm-transfer/notify_team NOT built. transfer_sip_participant present in venv (1).
- var/memory = 7 files (outbound writes). var/wa_threads = 0 (auto-followup never fired; all gates OFF).
- lk sip lists: CLI creds not loadable from .env in non-interactive; V2 plan already documents inbound/dispatch EMPTY + outbound trunks frozen.

## PANEL (famit-panel) — backed vs front-end
- BASE = NEXT_PUBLIC_API_BASE -> live backend. fetch(`${BASE}/calls?limit=200`) + /calls/{id} = REAL.
- calls/page.tsx: backed; shows transcript + interest + outcome badges. CRM [id]: 360 timeline (13 refs) + WhatsApp + Calls.
- leads (getLeads hot/sort), callbacks (getCallbacks/add/cancel) backed. whatsapp: /whatsapp/send + /whatsapp/log backed (NO threads/inbound-convo viewer).
- ai-manager/sessions page exists. NO handoff settings card (settings/page.tsx 0 hits). NO inbound_dids super-admin surface.

## DONE -> writing audit-lifecycle.md now
