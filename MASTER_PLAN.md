# 🧭 MASTER_PLAN — Famit/Axcrio — READ THIS FIRST after ANY compaction / new session

> **This is the single richest source of truth.** If the conversation compacted or you're a fresh session: read in this ORDER →
> 1. **THIS FILE** (the full vision + complete build plan + rules + facts).
> 2. `ORCHESTRATOR.md` (live wave status: what's RUNNING / DONE / blocked, resume run-IDs).
> 3. `NEXT-BIG-BUILDS.md` (the sequenced build queue, P0→P2).
> 4. `AGENT_LEARNINGS.md` (hard-won lessons / don't-repeat — the carrier-DID block, no-test-call rule, etc.).
> 5. `CONTINUE_HERE.md` (newest-on-top resume notes) + `design/*.md` (per-feature execution specs) + `memory/build_log/*.md`.
> Then CONTINUE the night-build autonomously (see §RULES). Never re-ask the founder; never re-discover what's below.

---

## 1. WHAT WE ARE BUILDING (the product + the goal)
**Famit / Axcrio** = an **AI Revenue Workforce SaaS** (live at `panel.famit.in`) that replaces a telecaller team + a marketing agency. It owns the WHOLE revenue loop: **Ad → Lead → AI voice call (multilingual) → WhatsApp follow-up → appointment booked → sale → conversion-signal back to Meta/Google.** Multi-tenant, paise-metered, immutable audit, super-admin control plane. **GOAL = a billion-dollar, production-grade, SELLABLE, differentiated product.** The founder gives ROUGH sketches; we build the WHOLE thing + add the features he didn't name.

**Three code surfaces:** `famit-panel/` (Next.js App-Router admin panel — the bulk of UI), `droplet_work/` (Python backend — `caller.py` FastAPI :8209, `agent.py` the LIVE outbound voice EARNER [SACRED], `aim_voice_agent.py` inbound AI-Manager worker :8091, `wallet.py`, `firewall.py`, `ai_manager/`, `llm_router/`, `kb/`, `crm/`, `ai_asset/` :8310), `growth-os/` (next-gen ads monorepo, scaffold-only).

---

## 2. 🟥 STANDING RULES (the founder's operating mandate — NON-NEGOTIABLE)
1. **FULL AUTONOMY, day & night.** Founder gives a rough idea → build the ENTIRE production-grade thing end-to-end + **autonomously ADD the out-of-box / sellable / differentiating features, security, UI he didn't name.** NEVER ask permission to add what the product needs. Reserve questions for genuine forks only.
2. **Pipeline every task:** know issue → EXPLORE the real codebase for the true root cause → deep production-grade RESEARCH (web-search; security/multi-tenant-isolation/scale/low-latency/cost) → DESIGN → EXECUTE (backend all-green → frontend control-UI → DB → end-to-end test → deploy → VERIFY on the founder's REAL flow) → fill every missing layer. Think billion-dollar system, not prototype.
3. **Every backend capability ships with a FRONTEND control UI** (full CRUD + configure + test/preview, real-time, reuse the Core_2/Capsy kit + frontend-design skill, Inter Display, zero raw hex). Never backend-only.
4. **A green report ≠ a working product** — only the founder's REAL integrated use is truth (a real call rings = SIP `inviteToRingingMs>0`/180/200, NOT the agent-join log; a message arrives; he sees it work). Verify the real flow.
5. **🟥 NEVER touch the EARNER:** `agent.py` (md5 `9150fabe4ff62b4b4470f9a87df346e5`), outbound trunks `ST_fmtVmNJmpzKa`/`ST_LH8ighJJtHSi`, the firewall, the SIP container. Additive + isolated + earner-gated. **ONE box-mutating wave at a time** (shared caller.py + panel + FORTRESS deploy; and the Anthropic server rate-limits heavy parallel load — pace it; on "Server temporarily limiting requests", wait + resume; granular waves resume from cache).
6. **🟥 NO outbound TEST CALLS** — our per-wave "earner test call" methodology spam-flagged the DID with the carrier (broke the live earner). Verify "earner untouched" via **agent.py md5 UNCHANGED + famit-agent process (MainPID 1477083) NOT restarted + /health 200 + 0 5xx ONLY.** A real call is the founder's job, never an automated gate.
7. **Never burn the founder's PAID credits** (free providers, e.g. Pollinations / ElevenLabs free preview_url / ModelScope-free; 1 test max).
8. **Compaction-proof + crash-safe:** each wave commits per verified unit, updates `ORCHESTRATOR.md` + `AGENT_LEARNINGS.md` + `NEXT-BIG-BUILDS.md`. Make waves GRANULAR (per-unit agents) so a throttle/drop costs ≤1 unit + resumes. **Never end a turn with ZERO waves running while queue remains** — the wave-completion notification re-fires the loop; always launch the next before yielding.
9. **Autonomy honesty:** Claude Code runs only while the laptop is ON + the session/tab alive — it can't run if the machine sleeps or the tab closes. On interruption, a fresh session reads this file-set and continues. (Proven mid-session.)

---

## 3. ✅ ALREADY BUILT + LIVE (don't rebuild — verify on box if unsure)
**Run-page 4-step redesign** (4-step horizontal stepper + sticky Launch-summary rail, progressive-disclosure audience Tabs, new `app/run/_stepper.tsx`, Core_2 composed, mobile pill+dots; pure presentational re-housing, all state/handlers verbatim; BUILD_ID `jcDEy4iclWbxS_zvVpvk0` LIVE 2026-06-14). · **Voice-preview `t=` fix** (`GET /voice-preview` accepts `?t=<jwt>` so `<audio src>` plays without headers; same `_extract_cred`+`resolve_tenant` path, scoped to this route, no PII/spend; commit `e275ebc` LIVE). · Handoff bridge (direct same-room `create_sip_participant`) + per-tenant handoff list + Handoff Team UI + hold-music/whisper/fallback + the "says-yes-then-silence" prompt fix (imperative same-turn tool-fire) + `session.aclose()` AI-exit after bridge + `participant_disconnected` graceful hangup + the **named** transfer line ("Ek second, main aapko <Name> se connect kar rahi hoon") + Live-Calls monitor. · #9 hardening pt1 (RLS isolation 42/0, Restart=always, hardened /health, Telegram watchdog + Vobiz low-balance alert). · Smart Groq key-pool (`llm_router/provider_pool.py`, cooldown-aware, 9 new keys, hot-reload) + SambaNova fallback + Sarvam rotation + **API Keys page** (`/super-admin/api-keys`) + run_campaign EXECUTION fix (typed-schema bug). · Groq→OpenRouter-free FallbackAdapter (quota emergency). · "Signal Aurora" fluid generation UI. · **Recordings** for EVERY call (inbound AIM + outbound auto-egress) + presigned streamable+downloadable API + CRM lead-profile Recordings player + 90d Spaces retention + AIM finalize-on-read fix. · **Image preview fix** (asset-detail presigned) + **CRM transcript chat-view** (customer-right/AI-left). · **Provider/Voice switcher Phase-1** (Lean/Standard/Premium slider + ₹/min cost-meter + FREE voice preview + custom-provider CRUD, on the Run page) — OB-PROV (outbound provider-swap) is Phase-2 gated. · Login-popup fix (`need_auth` dropped the Basic header). · Campaign-run "Start anyway" off-hours force-window fix. · WhatsApp backend truthful Meta-error surfacing + panel truth-UI.

---

## 4. 📋 THE COMPLETE BUILD QUEUE — build TOP-DOWN, one wave at a time (full detail in `NEXT-BIG-BUILDS.md`)
**P0 (now):** ✅ 🚀 **PERF OVERHAUL DONE** (6/6 integrated-verified, all units LIVE). · ✅ **Multilingual adaptive voice DONE (2026-06-14, MLV 2-pass)** — the LLM now MIRRORS the caller every turn (Hindi↔English, mid-call switch), the greeting is language-NEUTRAL (`"Namaste, this is {agent} from {company}. … how can I help you today?"` — no "Hello/Haan" stutter, no Hindi-priming), STT confirmed auto/multilingual (`SARVAM_STT_LANG` unset → Sarvam auto-detect). INBOUND `aim_voice_agent.py` only, earner UNTOUCHED (agent.py md5 `9150fabe…` unchanged, famit-agent never restarted). Live-shape integrated smoke PASS via real Groq llama-4-scout; final acceptance = founder's real inbound call. Founder recipe `FOUNDER-MULTILINGUAL-CALL-TEST.md`. · Two-party inbound handoff = founder real-call acceptance.
**P1 (sequential):** AIM Access+PIN (`.wf/aim-access-and-pin.js` staged — repoint Setup tab → live `/ai-manager/numbers` CRUD + `POST /firewall/pin/change` old→new + audit/grants/lockout). · 🔒 Funnels/Media MOUNT-BLOCKER security fix (read tenant from BODY = cross-tenant hole → token-deriving build_router BEFORE serving). · Workflow/Funnel execution (human-language nodes + Trigger→leads→campaign→Run actually runs). · **VIDEO STUDIO** (prompt→video→preview→assets, "add your API key", manual upload, library Images↔Videos toggle, attach video+banner to WhatsApp+Ads). · **VAULT** (PIN-gated per-vendor encrypted key store; super-admin always, vendor hidden-by-default+toggle; future self-host drop-in). · RAG populate+wire (kb built but empty; load campaign knowledge + ground the agents). · Per-person memory inbound + WA-reply (keystone gap — only outbound recaps use it). · #9 hardening pt2 (onboarding, billing/metering at scale). · ai_asset go-wide (`/assets/raw` stream local_path map + per-tenant gate + events leg + FE nginx /api/assets proxy [needs FE-box root]).
**P2 (forgotten/spec'd):** the **6 un-built Creative sub-products** (Brochure/Catalog, Creative-Batch all-sizes, Ads-Engine, Landing-Page Builder, 3D-Model, A/B Testing-Lab — `design/creative-*.md`). · Control-Layer C10 AI-Copilot entitlement gate. · AIM step-up→runner approval-row wiring. · WhatsApp residuals (reply-brain deep context, hot_lead_alert template, banner-in-builder). · Switcher P2 (A/B voice tier, budget auto-pause, per-call ₹ in log). · growth-os (`@growth-os/events` codegen build FAILS — fix first). · Logto OIDC wiring. · AIM dedicated-service 39-unit items (Argon2id PIN, strict-JSON NLU, PolicyEngine, CostGuard, compliance window/DLT gate, 8 dashboard pages — superseded by in-process; extract only if droplet limit raised). · media_gen monolith retirement.
**⏸️ ON-HOLD (build ONLY when founder asks):** Credit/Billing/Plans (balance icon→Credits page, top-up ₹1=1 credit, spend breakdown across image/video/recordings/calls, Razorpay-ready, plan tiers).

## 5. 🚫 GATED / FOUNDER-BLOCKED (skip + flag; do NOT ask, do NOT build until cleared)
- 🟥 **DID `+918071583488` CARRIER-SPAM-BLOCKED** (since 2026-06-13 ~12:51 UTC) → outbound campaigns get immediate 486/480/603, NO ring. NOT our code. **Founder must: rest the DID + Vobiz support (check throttle/DLT-spam/suspension, rotate to a clean DID).** Until cleared: no outbound dialing works; build everything else; place NO test calls.
- **OB-PROV** (outbound provider-swap) + **Earner own LLM fallback** — both need an `agent.py` edit → founder sign-off + a real ring-gate (so deferred until DID un-rested). Earner-LLM-fallback is a real SAFETY item (earner shares the Groq pool).
- **ModelScope image** → founder must BIND Alibaba Cloud (key 401s). `FOUNDER-MODELSCOPE-BIND.md`.
- **WhatsApp delivery** → founder must fix Meta WABA (payment 141006 + business verification + subscribe `messages` webhook). `FOUNDER-META-WHATSAPP-FIX.md`. (Backend+panel ready; Meta accepts but won't deliver.)
- **Razorpay** (Credits, on-hold) · **Ads OAuth** (Meta/Google) · **SambaNova** Developer tier · **FE-box root** for the nginx /api/assets proxy.

## 6. 🔑 KEY FACTS / ARCHITECTURE (so nothing is re-discovered)
- **Backend box:** `famit@168.144.153.145` (priv 10.122.0.4), key `C:\Users\kunal\.ssh\do-blr-test\id_ed25519` (VERIFIED working), `/opt/famit-agent`, caller API **:8209**, X-Auth header = `FamitCall2026` (legacy admin bearer — must be 403'd on /admin/* + /ai-manager/* + /firewall/*; tenant ALWAYS from `resolve_tenant`/token, never body). caller venv `/opt/capsy-agent/.venv`; AI-Asset svc `:8310` venv `/opt/famit-aiasset`. Services: famit-agent (earner), famit-caller, famit-bridge, aim-voice-agent (manager :8091), famit-aiasset, llm-router. Restart ONLY famit-caller/aim-voice-agent (never famit-agent).
- **FRONTEND box (FORTRESS):** `root@143.110.247.249`, `/opt/famit-panel`, Next on 127.0.0.1:3001, nginx + Cloudflare. Deploy: build LOCALLY (2GB box OOMs on-box builds) → backup-first → **md5-gate the scp** → atomic swap → `chown deployuser` → restart famit-panel only → verify 200 + new BUILD_ID on loopback + the panel.famit.in edge.
- **Voice stack:** Vobiz SIP → LiveKit → Sarvam STT → Groq/SambaNova LLM (FallbackAdapter) → ElevenLabs/Sarvam TTS. Inbound DID **+918071583488**, PIN **4827** (per-tenant hashed in firewall.py, NOT fixed). Founder phone **+917861019021**. Handoff targets +916375548830 / +917861019021. Call window 09:00–21:00 IST (TRAI; "Start anyway" force-bypasses). A REAL ring = `inviteToRingingMs>0` / 180 / 200 in the **livekit-sip** container log (NOT the agent-join line; a 486 with only `inviteToTryingMs` = carrier rejected pre-ring, never rang).
- **DO Spaces** `capsy-recordings` (sgp1) PRIVATE → serve PRESIGNED GET urls (boto3, ~1h) for ALL external fetch (images, recordings, video). Raw private url → 403. (This 403 bug recurs across images/recordings/video — always presign.)
- Secrets gitignored (`.env`, `.env.local`, `final_env.md`, `z_groq_2.md`, `z_samba.md`, `lead/ALL_CREDENTIALS.md`, `fortress/*`); gitleaks before push.

## 7. 🌙 CURRENT STATE (2026-06-14, autonomous night-build)
RUNNING: **perf-overhaul [wx16gfkps]** (resume `wf_2029b033-94e`). NEXT on its completion: multilingual voice → AIM-access → funnels-security+workflow → video studio → vault → RAG/memory → hardening pt2 → ai_asset go-wide → 6 creative sub-products. A deep "gold-mine sweep" agent is appending any STILL-forgotten items to this file + NEXT-BIG-BUILDS. **Keep building, one wave at a time, never stop, no test-calls, earner-safe.**

---

## 🪙 GOLD-MINE SWEEP (net-new forgotten items) — 2026-06-14
> Deep read-only sweep of EVERY `*.md` (root + `design/` + `memory/` + `growth-os/` + sales) + a code grep for unfinished markers + LIVE-box verification (`famit@168.144.153.145`, prod `.env` flags + route probes). Items below are **NET-NEW** — de-duped against §4 / NEXT-BIG-BUILDS / the a7b7853 audit. Status verified on the live box where noted. **EARNER-SAFE: all additive; NONE touch agent.py.** Priority key: 🅿0 quick/high-value · 🅿1 big-but-clear · 🅿2 spec'd/lower-urgency · ⛔ gated.

### A. BUILT-BUT-DORMANT backend routers (flag-OFF in prod — confirmed 404 on box; FE page ALREADY exists → cheapest wins)
> Live `.env` has FORMS/SUPPORT/WORKFLOWS/BOOKING/FUNNELS/AI_MANAGER/WHATSAPP/WHATSAPP_BUILDER **=1 (mounted, 200)**. These three are absent = **OFF/dormant**, yet each has a full FE page shipped:
1. **ADS ENGINE** (autonomous ad campaigns: propose/approve/optimize, step-up PIN) · `droplet_work/ads_engine/endpoints.py`, mount `caller.py:6409` `FEATURE_ADS`, FE `famit-panel/app/ads/page.tsx` ("coming soon" copy) · status **flag-OFF, `/ads/*`→404 on box** · needs: set `FEATURE_ADS=1` + verify cross-tenant safety + wire FE off "coming soon" + ads OAuth for real spend · ⛔ gated on Meta/Google Ads OAuth for live spend, but the propose/approve flow is 🅿1 SAFE.
2. **MEDIA-GEN router** (provider-agnostic video/image gen — distinct from ai_asset image-banner) · `droplet_work/media_gen/router.py`, mount `caller.py:6442` `FEATURE_MEDIA`, FE under creative · status **flag-OFF, `/media/*`→404** · needs: body-tenant→token-deriving `build_router` (same mount-blocker as funnels), then flag-on · 🅿2 (superseded-by-ai_asset per §4 item 25, but the VIDEO path is the live gap → fold into Video Studio).
3. **PAYMENTS** (Razorpay/Stripe collections + invoice/receipt issuing) · `droplet_work/payments/router.py` (needs `wire()`), mount `caller.py:6528` `FEATURE_PAYMENTS`, FE `famit-panel/app/payments/page.tsx` (page shipped) · status **flag-OFF, `/payments/*`→404** · needs: Razorpay keys + `wire()` init + flag-on · ⛔ gated on Razorpay (same blocker as Credits/Billing on-hold).

### B. INBOUND production-grade gaps (from `design/PRODUCTION-ROADMAP.md` + `INBOUND-PIPELINE-MASTER-PLAN-V2.md` + `inbound-gap-analysis.md` — NOT in §4)
4. **Never-silent apology guard** — try/except in the inbound entrypoint so any exception speaks a graceful line instead of dead air · `inbound-gap-analysis.md` P0.2 · planned · 🅿0 SAFE (inbound `aim_voice_agent.py` only).
5. **Inbound recording Egress** — start/pause-around-PIN/finalize inbound audio to DO Spaces + store presigned `recording_url` (outbound already auto-egresses; inbound does NOT) · `inbound-gap-analysis.md` P1.3 · designed · 🅿1 SAFE (Spaces creds exist).
6. **Inbound spend metering** — meter inbound minutes + handoff legs + template-gen + RAG embeds into the wallet (today only outbound is metered) · `PRODUCTION-ROADMAP.md` P2.4 · designed · 🅿1 SAFE.
7. **Compliant recording posture** — spoken consent line + PIN-span pause + Indian-region storage + retention automation · `PRODUCTION-ROADMAP.md` P2.5 · spec-only · ⛔ light policy sign-off; the consent line is 🅿1 SAFE.
8. **DPDP data-handling + delete-my-data endpoint** — per-person purge path + purpose-limit docs (legal exposure for a live SaaS) · `INBOUND-...V2.md` E4 / `PRODUCTION-ROADMAP.md` P2.9 · spec-only · 🅿1 SAFE (additive endpoint).
9. **Inbound business analytics dashboard** — containment / booking / transfer / hot / sentiment / language-mix per call record · `PRODUCTION-ROADMAP.md` P3.1 · spec-only · 🅿1 SAFE (FE + read queries).
10. **Mid-call `lead_is_hot` LLM tool** — buy-intent phrase detect mid-call reusing `_CLOSE_*` banks → mark hot in real time · `INBOUND-...V2.md` GAP-B1 · designed · 🅿1 SAFE (inbound).
11. **Warm-transfer no-answer fallback ladder** — ring next target by `ring_strategy`, voicemail-detect, logged callback if all miss · `INBOUND-...V2.md` 3.4 · designed · 🅿1 SAFE.
12. **Post-call workflow event emission** — emit `call.completed` into the workflow DSL so a completed call can trigger automation · `INBOUND-...V2.md` F1 · designed · 🅿1 SAFE (pairs with Workflow execution wave).
13. **Customer-mode "sales-in" inbound worker (Mode A)** — inbound SALES brain over `voice_core` (`sales_flow.py`), distinct from the AI-Manager command brain · `design/CUSTOMER-MODE-BUILD-STATE.md` · designed · 🅿1 SAFE (new worker, isolated).
14. **Knowledge-gap + objection learning loop** — mine transcripts → draft KB chunks → curator UI → re-ingest (self-improving brain) · `INBOUND-...V2.md` P7 D2/D3 · spec-only · 🅿2 (depends on RAG populate first).
15. **External CRM sync adapters** — Salesforce / HubSpot / Zoho native push/webhook adapters · `INBOUND-...V2.md` F2 · spec-only · ⛔ gated on founder API keys.
16. **Ads flywheel: promote-winner-to-ad** — top creative-test template → auto-create Meta/Google ad (closes the Revenue-Truth loop) · `PRODUCTION-ROADMAP.md` P3.2 · spec-only · ⛔ gated on ads creds (pairs with item 1).

### C. VOICE-QUALITY ladder (from `VOICE_ARCHITECTURE_RESEARCH.md` — research done, build NOT queued)
17. **LiveKit semantic turn-detector** — add the `turn-detector` plugin alongside Silero VAD (Hindi ~99.4% endpoint accuracy; cuts false barge-in) · Phase-1 · 🅿1 SAFE (CPU-only, same framework; do on INBOUND first).
18. **Structured per-stage flow layer** — refactor the monolith prompt into greet/pitch/qualify/close LiveKit agents with handoffs · Phase-2 · 🅿2 SAFE (inbound first; outbound gated).
19. **Eval/replay harness + per-call QA score** — 20-50 persona scenarios + LLM-judge rubric to gate every prompt/flow change, and score each real call for coaching · `design/eval-harness.md` (full spec) + `PRODUCTION-ROADMAP.md` P2.7 · spec-only · 🅿1 SAFE (offline, read-only on transcripts — never in the call hot-path). **High leverage: makes every future voice change provable.**
20. **In-memory grounding bank** — per-campaign objection bank + fact sheet pre-embedded into in-memory FAISS / injected into prompt (lighter than the full RAG vector DB) · Phase-4 · 🅿2 SAFE (overlaps RAG wave; cheaper interim).
21. **LoRA/QLoRA fine-tune on real telecaller transcripts** · Phase-5 · ⛔ DEFERRED (only after eval harness + a self-host-vs-Groq latency/cost decision).

### D. LATENCY-ARCHITECTURE inbound (from `design/LATENCY-ARCHITECTURE.md` — distinct from the shipped app-perf wave)
22. **Inbound voice warm-cache + pooled HTTP client** — warm caches on session start, Redis hot-cache, reuse pooled HTTP clients to STT/LLM/TTS (cuts first-token latency on inbound) · P1/P2 · 🅿1 SAFE (inbound).
23. **Inbound endpointing/turn tuning** — VAD-vs-semantic + barge-in-preserving config tune · P4 · 🅿2 SAFE (config, pairs with item 17).

### E. GROWTH-OS net-new (contracts exist, code missing — beyond the "fix events codegen" item already tracked)
24. **Integration Hub** — full connector lifecycle (12 ops: list/get/disconnect/test/oauthStart/oauthCallback + Origin bridge push/webhook/getCampaign/dailyReport/leadScore/signalHealth); `connections` table exists (`0001_init.sql:270`) but **zero module mounted** · `growth-os/contracts/openapi/integration-hub.yaml` · schema-no-endpoint · ⛔ Phase-0 contracts-first (build after the monorepo boots).
25. **Billing money-path** — only `GET /billing/consumption` stub exists; `credit_consumption` table (`0001_init.sql:297`) has no write endpoint; ledger debit + reconciliation deferred · `growth-os/.../billing.controller.ts` · defined-not-mounted · ⛔ "no money movement Phase 0".
26. **Gateway OIDC + Kafka SSE feed** — dev JWT mint is a stub disabled in prod (Logto OIDC = Phase 3); SSE feed is in-memory-bus only (Kafka = Phase 1) · `growth-os/.../gateway.controller.ts` · stub · ⛔ Phase 1/3 infra.

### F. WhatsApp builder residuals (from `PRODUCTION-ROADMAP.md` §3 — finer than the tracked "WhatsApp residuals")
27. **Template-builder `status=partial` fallback cards** (WB-2 FE) · 🅿0 SAFE. **Banner submit via DO Spaces `header_url`** (WB-3, avoids resumable upload) · 🅿1 SAFE. **Submit-to-Meta button + status-webhook field subscription** (WB-4) · ⛔ gated on Meta webhook. · `PRODUCTION-ROADMAP.md` §3.

**SWEEP TOTALS:** 27 net-new items grouped A–F. Cheapest immediate wins: A1/A3 (flip a flag on already-built routers w/ FE pages — pending only their external creds), B4 (apology guard), F27a (WB-2 fallback). Highest leverage: C19 (eval harness — makes all voice work provable). Biggest gated cluster: growth-os (E) + ads/payments creds. **All EARNER-SAFE; agent.py untouched.**
