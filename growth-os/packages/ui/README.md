# @growth-os/ui

Shared UI kit (React + shadcn-style primitives, §4). Phase-0 = `cn()` util + app-chrome design
tokens. Real components (ApprovalCard, SignalHealthBadge, the AI-CMO brief card, the Action
Ledger explanation viewer) land with `apps/dashboard`. Per-tenant brand kit is separate (§15.1).
