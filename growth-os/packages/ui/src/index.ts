/**
 * @growth-os/ui — shared UI kit barrel (§4 React + shadcn). Phase-0: tokens + cn() util only;
 * components (Button, Card, ApprovalCard, SignalHealthBadge, ...) land with apps/dashboard.
 */
export { cn } from './cn.js';
export { tokens, type Tokens } from './tokens.js';
