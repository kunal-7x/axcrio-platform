# dashboard (app)

> Build phase: **P1** · Runtime: Next.js · §20 deployable.

## Purpose
Next.js + shadcn operator console: connect -> wizard -> live feed (SSE) -> approvals -> report. Uses @growth-os/sdk against the public API (§17, the dashboard uses the API).

## Status
**Phase-0 placeholder.** Consumes `@growth-os/sdk` + `@growth-os/ui`. Built in phase **P1** (§21).
